<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>字节番茄小说后端一面面经</title>
      <link href="/2025/06/27/%E9%9D%A2%E7%BB%8F/bytedance/%E5%AD%97%E8%8A%82%E7%95%AA%E8%8C%84%E5%B0%8F%E8%AF%B4%E5%90%8E%E7%AB%AF%E4%B8%80%E9%9D%A2%E9%9D%A2%E7%BB%8F/"/>
      <url>/2025/06/27/%E9%9D%A2%E7%BB%8F/bytedance/%E5%AD%97%E8%8A%82%E7%95%AA%E8%8C%84%E5%B0%8F%E8%AF%B4%E5%90%8E%E7%AB%AF%E4%B8%80%E9%9D%A2%E9%9D%A2%E7%BB%8F/</url>
      
        <content type="html"><![CDATA[<p>鼠鼠第一次投大厂面试,写面经攒人品:</p><ol><li>个人介绍(2min)</li><li>挖项目(我跟后端相关的项目似乎只有抖音商城(字节跳动青训营),他一直挖我这个项目)</li><li>服务是怎么被发现的?(微服务)</li><li>假如你这个服务要更新,要更平滑,不让用户感到延迟,你会怎么做?</li></ol><ul><li>维护两套环境（蓝：当前生产环境；绿：新版本环境）。</li><li>新版本在绿环境测试通过后，切换流量到绿环境，蓝环境作为回滚备用。</li><li>优点：零停机，用户无感知切换。</li></ul><ol start="5"><li><p>你输入一个url的处理过程</p></li><li><p>看你系统比较好,讲一讲linux io复用</p></li><li><p>然后就开始写题了,面试官直接口述,输入一个数字,输出下一个最小的比这个数大的数字(重新排列)leetcode类似的题目是:<a href="https://leetcode.cn/problems/next-greater-element-iii/">556. 下一个更大元素 III</a>这道题手撕还是相当紧张的,给我撕出来了</p></li><li><p>数据库事务是什么</p></li><li><p>mvcc是什么,怎么实现的?</p></li><li><p>场景题,给你一个番茄小说的书,有十万订阅,如何快而准确的通知到所有订阅的人更新了(这里要求你去用具体的实现)</p></li></ol><p>我这里寄了,后面复盘的时候,应该是使用feed流,这个是用ai写的答案:</p><ul><li><p>推模式 (Fanout-On-Write&#x2F;Writes)：</p><ul><li><strong>操作时机：</strong> 当关键事件（如新章节发布）发生时<strong>立即</strong>执行。</li><li><strong>目标用户：</strong> <strong>核心活跃粉丝（数量相对较小）</strong>。</li><li><strong>动作：</strong> 将事件<strong>直接写入</strong>这些目标粉丝的个人 <strong>收件箱（Inbox Feed）</strong>（一个按时间排序的数据存储）。用户访问自己的 Feed 流时，直接从这个收件箱拉取即可，延迟极低。</li></ul></li><li><p>拉模式 (Fanout-On-Read)：</p><ul><li><strong>操作时机：</strong> 当用户主动请求访问 Feed 流时执行。</li><li><strong>目标用户：</strong> <strong>非核心粉丝（长尾粉丝，数量大）</strong> 或 触发推模式的粉丝，在访问 Feed 时可能需要拉取更长时间范围内的数据。</li><li><strong>动作：</strong> 后端服务在用户请求时，<strong>实时聚合</strong>用户所关注对象（收藏的书籍）的 <strong>发件箱（Outbox Feed）</strong> 数据（包含所有发布事件），按时间排序后返回给用户。这需要访问多个发件箱（每个收藏的书一本）并聚合。</li></ul></li><li><p><strong>「推拉结合」的关键：</strong> <strong>合理区分「核心粉丝」与「长尾粉丝」</strong>，只对核心粉丝进行实时写入。</p><p><strong>关键组件与流程详解：</strong></p><ol><li><strong>事件源 (Event Source)：</strong><ul><li><strong>新章节发布：</strong> 最核心的事件源。携带 <code>bookId</code>, <code>chapterId</code>, <code>publishTimestamp</code>。</li><li><strong>粉丝关系变更：</strong> 用户收藏 (<code>favor</code>) 或取消收藏 (<code>unfavor</code>) 一本书。携带 <code>userId</code>, <code>bookId</code>, <code>action</code>, <code>timestamp</code>。</li></ul></li><li><strong>事件总线 (Event Bus)：</strong><ul><li>使用高吞吐、可靠的消息队列如 Kafka&#x2F;Pulsar。接收上述事件并进行持久化，供下游消费者订阅。</li></ul></li></ol><p>后面是就是针对十万用户的进行<strong>精准界定“核心活跃粉丝” (<code>HotFanCache</code>)：</strong>,查询优化,<strong>高性能存储与分片：</strong></p></li></ul><ol start="10"><li>反问,问了业务是什么,技术栈是什么,然后和面试官聊的蛮开心的,面试官夸了基础好,知道稳了</li></ol><p>10min之后,hr通知2面.1面成功.</p>]]></content>
      
      
      <categories>
          
          <category> 面经 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>字节番茄小说后端二面面经</title>
      <link href="/2025/06/27/%E9%9D%A2%E7%BB%8F/bytedance/%E5%AD%97%E8%8A%82%E7%95%AA%E8%8C%84%E5%B0%8F%E8%AF%B4%E5%90%8E%E7%AB%AF%E4%BA%8C%E9%9D%A2%E9%9D%A2%E7%BB%8F/"/>
      <url>/2025/06/27/%E9%9D%A2%E7%BB%8F/bytedance/%E5%AD%97%E8%8A%82%E7%95%AA%E8%8C%84%E5%B0%8F%E8%AF%B4%E5%90%8E%E7%AB%AF%E4%BA%8C%E9%9D%A2%E9%9D%A2%E7%BB%8F/</url>
      
        <content type="html"><![CDATA[<p>鼠鼠第一次投大厂面试,写面经攒人品:</p><ol><li><p>个人介绍(2min)</p></li><li><p>对于个人简历上写的linux内核mm模块的patch的问题</p><ul><li>问linux上读了哪些部分的源码</li><li>问下我理解中的linuxmm部分源码</li><li>问我从中学到了什么</li></ul></li><li><p>问既然我对linux源码这么了解,请问下虚拟内存(?这个问题跟上面难度好像不成正比吧)</p></li><li><p>接下来我提到了我项目中的dpdk,他具体问了我dpdk是什么?怎么实现的?</p></li><li><p>dpdk有哪些实际的业务?(这个我只在量化上面用过,其他不知道)</p></li><li><p>然后看到我项目中做了cmu的数据库,然后问了我</p></li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">select * from test  limit 100,100;</span><br><span class="line">select * from test  limit 10000000,100;</span><br></pre></td></tr></table></figure><p>哪个快?(看到过类似的题目,只记得答案是无论如何第一个快)</p><ol start="7"><li><p>手撕代码,就是区间合并问题,输入[1,3],[7,9],[2,6],输出[1,6],[7,9]</p><p>这个比较常见,直接就开撕了吧,5min做出来了</p></li><li><p>然后继续八股,问我了解http协议吗</p></li><li><p>问我http请求发送后,是怎么接收到处理的全过程(跟前面的url类似的)</p></li><li><p>他们都说tcp是一个面向字节流的协议,你说说是怎么回事</p></li><li><p>tcp连接过多会发生什么</p></li><li><p>你刚刚说了粘包,你能说说他吗?</p></li><li><p>场景题,设计贪吃蛇(要求)</p><ol><li>每五步增加长度1</li><li>不能碰到墙</li><li>只可以上下左右移动</li><li>不能碰到身体</li></ol></li><li><p>反问环节,问昨天为什么我回答的rss订阅为什么不可以,我在这一个小时中的表现,我需要去做哪一些改变.</p></li></ol><p>后续 4min收到通过通知,准备三面</p>]]></content>
      
      
      <categories>
          
          <category> 面经 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Linux中断时发生的情况</title>
      <link href="/2024/11/25/Linux/interrupt/%E4%B8%AD%E6%96%AD%E6%97%B6%E5%8F%91%E7%94%9F%E7%9A%84%E6%83%85%E5%86%B5/"/>
      <url>/2024/11/25/Linux/interrupt/%E4%B8%AD%E6%96%AD%E6%97%B6%E5%8F%91%E7%94%9F%E7%9A%84%E6%83%85%E5%86%B5/</url>
      
        <content type="html"><![CDATA[<p>我们具体研究一次典型中断发生时的运行流程:</p><h2 id="禁止调度和抢占"><a href="#禁止调度和抢占" class="headerlink" title="禁止调度和抢占"></a>禁止调度和抢占</h2><p>首先我们需要了解，当系统处于中断上下文时，是禁止发生调度和抢占的。进程的<a href="computer_system/%E8%BF%9B%E7%A8%8B/thread_info.md">thread_info</a>中有个preempt_count成员变量，其作为一个变量，包含了3个计数器和一个标志位，如下：</p><table><thead><tr><th>位</th><th>描述</th><th>解释</th></tr></thead><tbody><tr><td>0~7</td><td>抢占计数器</td><td>也可以说是锁占有数</td></tr><tr><td>8~15</td><td>软中断计数器</td><td>记录软中断被禁用次数，0表示可以进行软中断</td></tr><tr><td>16~27</td><td>硬中断计数器</td><td>表示中断处理嵌套次数，irq_enter()增加它，irq_exit()减少它</td></tr><tr><td>28</td><td>PREEMPT_ACTIVE标志</td><td>表明正在进行内核抢占，设置此标志也禁止了抢占</td></tr></tbody></table><p>当进入到中断时，中断处理程序会调用irq_enter()函数禁止抢占和调度。当中断退出时，会通过irq_exit()减少其硬件计数器。我们需要清楚的就是，无论系统处于硬中断还是软中断，调度和抢占都是被禁止的。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">irq_enter</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> cpu = smp_processor_id();</span><br><span class="line"></span><br><span class="line">rcu_irq_enter();</span><br><span class="line"><span class="keyword">if</span> (idle_cpu(cpu) &amp;&amp; !in_interrupt()) &#123;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Prevent raise_softirq from needlessly waking up ksoftirqd</span></span><br><span class="line"><span class="comment"> * here, as softirq will be serviced on return from interrupt.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">local_bh_disable();</span><br><span class="line">tick_check_idle(cpu);</span><br><span class="line">_local_bh_enable();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__irq_enter();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * It is safe to do non-atomic ops on -&gt;hardirq_context,</span></span><br><span class="line"><span class="comment"> * because NMI handlers may not preempt and the ops are</span></span><br><span class="line"><span class="comment"> * always balanced, so the interrupted value of -&gt;hardirq_context</span></span><br><span class="line"><span class="comment"> * will always be restored.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __irq_enter()\</span></span><br><span class="line"><span class="meta">do &#123;\</span></span><br><span class="line"><span class="meta">account_system_vtime(current);\</span></span><br><span class="line"><span class="meta">add_preempt_count(HARDIRQ_OFFSET);\</span></span><br><span class="line"><span class="meta">trace_hardirq_enter();\</span></span><br><span class="line"><span class="meta">&#125; while (0)</span></span><br></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">irq_exit</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">account_system_vtime(current);</span><br><span class="line">trace_hardirq_exit();</span><br><span class="line">sub_preempt_count(IRQ_EXIT_OFFSET);</span><br><span class="line"><span class="keyword">if</span> (!in_interrupt() &amp;&amp; local_softirq_pending())</span><br><span class="line">invoke_softirq();</span><br><span class="line"></span><br><span class="line">rcu_irq_exit();</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_NO_HZ</span></span><br><span class="line"><span class="comment">/* Make sure that timer wheel updates are propagated */</span></span><br><span class="line"><span class="keyword">if</span> (idle_cpu(smp_processor_id()) &amp;&amp; !in_interrupt() &amp;&amp; !need_resched())</span><br><span class="line">tick_nohz_stop_sched_tick(<span class="number">0</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">preempt_enable_no_resched();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Exit irq context without processing softirqs:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __irq_exit()\</span></span><br><span class="line"><span class="meta">do &#123;\</span></span><br><span class="line"><span class="meta">trace_hardirq_exit();\</span></span><br><span class="line"><span class="meta">account_system_vtime(current);\</span></span><br><span class="line"><span class="meta">sub_preempt_count(HARDIRQ_OFFSET);\</span></span><br><span class="line"><span class="meta">&#125; while (0)</span></span><br></pre></td></tr></table></figure><h2 id="中断产生"><a href="#中断产生" class="headerlink" title="中断产生"></a>中断产生</h2><p>我们需要先明确一下，中断控制器与CPU相连的三种线:<strong>INTR、数据线、INTA</strong>。</p><p><a href="%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8.md#%E4%B8%AD%E6%96%AD%E6%8E%A7%E5%88%B6%E5%99%A8">中断和异常</a></p><h2 id="SMP系统"><a href="#SMP系统" class="headerlink" title="SMP系统"></a>SMP系统</h2><p>在SMP系统，也就是多核情况下，外部的中断控制器有可能会于多个CPU相连，这时候当一个中断产生时，中断控制器有两种方式将此中断送到CPU上，分别是<strong>静态分发</strong>和<strong>动态分发</strong>。区别就是静态分发设置了指定中断送往指定的一个或多个CPU上。动态分发则是由中断控制器控制中断应该发往哪个CPU或CPU组。</p><p>CPU已经接收到了中断信号以及中断向量号。此时CPU会自动跳转到中断描述符表地址，以中断向量号作为一个偏移量，直接访问中断向量号对应的门描述符。在门描述符中，有个特权级(DPL)，系统会先检查这个位，然后清除EFLAGS的IF标志位(这也说明了发发生中断时实际上CPU是禁止其他可屏蔽中断的)，之后转到描述符中的中断处理程序中。在上一篇文章我们知道，所有的中断门描述符的中断处理程序都被初始化成了interrupt[i]，它是一段汇编代码。<br><a href="%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8.md#%E5%88%9D%E5%A7%8B%E5%8C%96%EF%BC%9A">初始化</a></p><h2 id="中断和异常发生时CPU自动完成的工作"><a href="#中断和异常发生时CPU自动完成的工作" class="headerlink" title="中断和异常发生时CPU自动完成的工作"></a>中断和异常发生时CPU自动完成的工作</h2><p>我们先注意看一下中断描述符表(<a href="computer_system/%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8/%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8.md#Gate_Descriptor">Gate_Descriptor</a>)，里面的每个中断描述符都有一个段选择符和一个偏移量以及一个DPL(特权级)，而偏移量其实就是中断处理程序的入口地址，当中断或异常发生时：</p><ul><li>CPU首先会确定是中断或异常的向量号，然后根据这个向量号作为偏移量，通过读取idtr中保存的中断向量表(IDT)的基地址获取相应的门描述符。并从门描述符中拿出其中的段选择符</li><li>根据段选择符从GDT中获取这个段的段描述符(为什么只从GDT中获取？因为初始化所有中段描述符时使用的段选择符几乎都是__USER_CS,__KERNEL_CS,TSS，这几个段选择符对应的段描述符都保存在GDT中)。而这几个段描述符中的基地址都是0x00000000，所以偏移量就是中断处理程序入口地址。</li><li>这时还没有进入到中断处理程序，CPU会先使用CS寄存器的当前特权级(CPL)与中断向量描述符中对应的段描述符的DPL进行比较，如果DPL的值 &lt;&#x3D; CPL的值，则通过检查，而DPL的值 &gt; CPL的值时，会产生一个”通用保护”异常。这种情况发生的可能性很小，因为在上一篇初始化的文章中也可以看出来，中断初始化所用的段选择符都是__KERNEL_CS，而异常的段选择符几乎也都是__KERNEL_CS，只除了极特殊的几个除外。也就是大多数中断和异常的段选择符DPL都是0，CPL无论是在内核态(CPL &#x3D; 0)或者是用户态(CPL &#x3D; 3)，都可以执行这些中断和异常。这里的检查可以理解为检查是否需要切换段。</li><li>如果是用户程序的异常(非CPU内部产生的异常),只有系统门和系统中断门的DPL是3，其他的异常门的DPL都为0。这样做的好处是避免了用户程序访问陷阱门、中断门和任务门。 (这里可以理解为进入此”门”的权限, 所以只有系统门和系统中断门是程序能够主动进入的, 也就是我们做系统调用走的门)</li><li>如果以上检查都通过，并且CS寄存器的特权级发生变化(用户态陷入内核)，则CPU会访问此CPU的TSS段(通过tr寄存器)，在TSS段中读取当前进程的SS和ESP到SS和ESP寄存器中，这样就完成了用户态栈到内核态栈的切换。之后把之前的SS寄存器和ESP寄存器的值保存到当前内核栈中。</li><li>将eflags、CS、EIP、ESP、SS寄存器的值压入内核栈中。这样就保存了返回后需要执行的上下文。</li><li>最后将刚才获取到的段选择符加载到CS寄存器(CS段切换)，段描述符加载到CS对应的非编程寄存器中，也就是CS寄存器保存的是__KERNEL_CS，CS寄存器的非编程寄存器中保存的是对应的段描述符。而根据段描述符中的段基址+段内偏移量(保存在门描述符中)，则得到了处理程序入口地址，实际上我们知道段基址是0x00000000，段内偏移量实际上就是处理程序入口地址，这个门描述符的段内偏移量会被放到IP寄存器中。</li></ul><h2 id="interrupt-i"><a href="#interrupt-i" class="headerlink" title="interrupt[i]"></a>interrupt[i]</h2><p>interrupt[i]的每个元素都相同，执行相同的汇编代码，这段汇编代码实际上很简单，它主要工作就是将<strong>中断向量号</strong>和<strong>被中断上下文</strong>(进程上下文或者中断上下文)保存到栈中，最后调用do_IRQ函数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"># 代码地址:arch/x86/kernel/entry_32.S</span><br><span class="line"></span><br><span class="line"># 开始</span><br><span class="line"><span class="number">1</span>:    pushl_cfi $(~<span class="built_in">vector</span>+<span class="number">0x80</span>)    <span class="comment">/* Note: always in signed byte range */</span> # 先会执行这一句，将中断向量号取反然后加上<span class="number">0x80</span>压入栈中</span><br><span class="line">      .<span class="keyword">if</span> ((<span class="built_in">vector</span>-FIRST_EXTERNAL_VECTOR)%<span class="number">7</span>) &lt;&gt; <span class="number">6</span></span><br><span class="line">    jmp <span class="number">2f</span>                                                     # 数字定义的标号为临时标号，可以任意重复定义，例如：<span class="string">&quot;2f&quot;</span>代表正向第一次出现的标号<span class="string">&quot;2:&quot;</span>，<span class="number">3b</span>代表反向第一次出现的标号<span class="string">&quot;3:&quot;</span></span><br><span class="line">      .endif</span><br><span class="line">      .previous                                             # .previous使汇编器返回到该自定义段之前的段进行汇编，则回到上面的数据段</span><br><span class="line">    .<span class="type">long</span> <span class="number">1b</span>                                                 # 在数据段中执行标号<span class="number">1</span>的操作</span><br><span class="line">      .section .entry.text, <span class="string">&quot;ax&quot;</span>                             # 回到代码段</span><br><span class="line"><span class="built_in">vector</span>=<span class="built_in">vector</span>+<span class="number">1</span> </span><br><span class="line">    .endif </span><br><span class="line">  .endr</span><br><span class="line"><span class="number">2</span>:    jmp common_interrupt</span><br><span class="line"></span><br><span class="line">common_interrupt:</span><br><span class="line">    ASM_CLAC</span><br><span class="line">    addl $<span class="number">-0x80</span>,(%esp) # 此时栈顶是(~<span class="built_in">vector</span> + <span class="number">0x80</span>)，这里再加上<span class="number">-0x80</span>，实际就是中断向量号取反，用于区别系统调用，系统调用是正数，中断向量是负数</span><br><span class="line">    SAVE_ALL                 # 保存现场，将寄存器值压入栈中</span><br><span class="line">    TRACE_IRQS_OFF           # 关闭中断跟踪</span><br><span class="line">    movl %esp,%eax           # 将栈指针保存到eax寄存器，供do_IRQ使用</span><br><span class="line">    call do_IRQ              # 调用do_IRQ</span><br><span class="line">    jmp ret_from_intr        # 跳转到ret_from_intr，进行中断返回的一些处理</span><br><span class="line">ENDPROC(common_interrupt)</span><br><span class="line">    CFI_ENDPROC</span><br></pre></td></tr></table></figure><p>需要注意这里面有一个SAVE_ALL是用于保存用户态寄存器值的，在上面的文章中有提到CPU会自动保存原来特权级的段和栈到内核栈中，但是通用寄存器并不会保存，所以这里会有个SAVE_ALL来保存通用寄存器的值。所以当SAVE_ALL执行完后，所有的上下文都已经保存完毕，最后结果如下:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">.macro SAVE_ALL</span><br><span class="line">cld</span><br><span class="line">PUSH_GS</span><br><span class="line">pushl_cfi %fs</span><br><span class="line">/*CFI_REL_OFFSET fs, 0;*/</span><br><span class="line">pushl_cfi %es</span><br><span class="line">/*CFI_REL_OFFSET es, 0;*/</span><br><span class="line">pushl_cfi %ds</span><br><span class="line">/*CFI_REL_OFFSET ds, 0;*/</span><br><span class="line">pushl_cfi %eax</span><br><span class="line">CFI_REL_OFFSET eax, 0</span><br><span class="line">pushl_cfi %ebp</span><br><span class="line">CFI_REL_OFFSET ebp, 0</span><br><span class="line">pushl_cfi %edi</span><br><span class="line">CFI_REL_OFFSET edi, 0</span><br><span class="line">pushl_cfi %esi</span><br><span class="line">CFI_REL_OFFSET esi, 0</span><br><span class="line">pushl_cfi %edx</span><br><span class="line">CFI_REL_OFFSET edx, 0</span><br><span class="line">pushl_cfi %ecx</span><br><span class="line">CFI_REL_OFFSET ecx, 0</span><br><span class="line">pushl_cfi %ebx</span><br><span class="line">CFI_REL_OFFSET ebx, 0</span><br><span class="line">movl $(__USER_DS), %edx</span><br><span class="line">movl %edx, %ds</span><br><span class="line">movl %edx, %es</span><br><span class="line">movl $(__KERNEL_PERCPU), %edx</span><br><span class="line">movl %edx, %fs</span><br><span class="line">SET_KERNEL_GS %edx</span><br><span class="line">.endm</span><br></pre></td></tr></table></figure><h2 id="do-IRQ"><a href="#do-IRQ" class="headerlink" title="do_IRQ"></a>do_IRQ</h2><p>这是中断处理的核心函数，来到这里时，系统已经做了两件事:</p><ul><li>系统屏蔽了所有可屏蔽中断(清除了CPU的IF标志位，由CPU自动完成)</li><li>将中断向量号和所有寄存器值保存到内核栈中</li></ul><p>在do_IRQ中，首先会添加硬中断计数器，此行为导致了中断期间禁止调度发送，此后会根据中断向量号从vector_irq[]数组中获取对应的中断号，并调用handle_irq()函数出来该中断号对应的中断出来例程。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line">__visible <span class="type">unsigned</span> <span class="type">int</span> __irq_entry <span class="title function_">do_IRQ</span><span class="params">(<span class="keyword">struct</span> pt_regs *regs)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* 将栈顶地址保存到全局变量__irq_regs中，old_regs用于保存现在的__irq_regs值，这一行代码很重要，实现了嵌套中断情况下的现场保存与还原 */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">pt_regs</span> *<span class="title">old_regs</span> =</span> set_irq_regs(regs);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 获取中断向量号，因为中断向量号是以取反方式保存的，这里再次取反 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="built_in">vector</span> = ~regs-&gt;orig_ax;</span><br><span class="line">    <span class="comment">/* 中断向量号 */</span></span><br><span class="line">    <span class="type">unsigned</span> irq;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 硬中断计数器增加，硬中断计数器保存在preempt_count */</span></span><br><span class="line">    irq_enter();</span><br><span class="line">    <span class="comment">/* 这里开始禁止调度，因为preempt_count不为0 */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 退出idle进程(如果当前进程是idle进程的情况下) */</span></span><br><span class="line">    exit_idle();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 根据中断向量号获取中断号 */</span></span><br><span class="line">    irq = __this_cpu_read(vector_irq[<span class="built_in">vector</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 主要函数是handle_irq，进行中断服务例程的处理 */</span></span><br><span class="line">    <span class="keyword">if</span> (!handle_irq(irq, regs)) &#123;</span><br><span class="line">        <span class="comment">/* EIO模式的应答 */</span></span><br><span class="line">        ack_APIC_irq();</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 该中断号并没有发生过多次触发 */</span></span><br><span class="line">        <span class="keyword">if</span> (irq != VECTOR_RETRIGGERED) &#123;</span><br><span class="line">            pr_emerg_ratelimited(<span class="string">&quot;%s: %d.%d No irq handler for vector (irq %d)\n&quot;</span>,</span><br><span class="line">                         __func__, smp_processor_id(),</span><br><span class="line">                         <span class="built_in">vector</span>, irq);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">/* 将此中断向量号对应的vector_irq设置为未定义 */</span></span><br><span class="line">            __this_cpu_write(vector_irq[<span class="built_in">vector</span>], VECTOR_UNDEFINED);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 硬中断计数器减少 */</span></span><br><span class="line">    irq_exit();</span><br><span class="line">    <span class="comment">/* 这里开始允许调度 */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 恢复原来的__irq_regs值 */</span></span><br><span class="line">    set_irq_regs(old_regs);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="handle-irq"><a href="#handle-irq" class="headerlink" title="handle_irq()"></a>handle_irq()</h3><p>do_IRQ()函数中最重要的就是handle_irq()处理了，我们看看:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">handle_irq</span><span class="params">(<span class="type">unsigned</span> irq, <span class="keyword">struct</span> pt_regs *regs)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irq_desc</span> *<span class="title">desc</span>;</span></span><br><span class="line">    <span class="type">int</span> overflow;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 检查栈是否溢出 */</span></span><br><span class="line">    overflow = check_stack_overflow();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 获取中断描述符 */</span></span><br><span class="line">    desc = irq_to_desc(irq);</span><br><span class="line">    <span class="comment">/* 检查是否获取到中断描述符 */</span></span><br><span class="line">    <span class="keyword">if</span> (unlikely(!desc))</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 检查使用的栈，有两种情况，如果进程的内核栈配置为8K，则使用进程的内核栈，如果为4K，系统会专门为所有中断分配一个4K的栈专门用于硬中断处理栈，一个4K专门用于软中断处理栈，还有一个4K专门用于异常处理栈 */</span></span><br><span class="line">    <span class="keyword">if</span> (user_mode_vm(regs) || !execute_on_irq_stack(overflow, desc, irq)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (unlikely(overflow))</span><br><span class="line">            print_stack_overflow();</span><br><span class="line">        <span class="comment">/* 执行handle_irq */</span></span><br><span class="line">        desc-&gt;handle_irq(irq, desc);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="irq-desc"><a href="#irq-desc" class="headerlink" title="irq_desc"></a>irq_desc</h4><p><a href="%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8.md#%E4%B8%AD%E6%96%AD%E6%8F%8F%E8%BF%B0%E7%AC%A6">中断描述符</a></p><p>好的，最后执行中断描述符中的handle_irq指针所指函数，我们回忆一下，在初始化阶段，所有的中断描述符的handle_irq指针指向了handle_level_irq()函数，文章开头我们也说过，中断产生方式有两种：一种电平触发、一种是边沿触发。handle_level_irq()函数就是用于处理电平触发的情况，系统内建了一些handle_irq函数，具体定义在include&#x2F;linux&#x2F;irq.h文件中，上面的引用有所以及。</p><h3 id="handle-level-irq"><a href="#handle-level-irq" class="headerlink" title="handle_level_irq()"></a>handle_level_irq()</h3><p>我们主要看看handle_level_irq()函数函数，因为触发方式不同，通知中断控制器、CPU屏蔽、中断状态设置的时机都不同，它们的代码都在kernel&#x2F;irq&#x2F;chip.c中。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span></span><br><span class="line"><span class="title function_">handle_level_irq</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> irq, <span class="keyword">struct</span> irq_desc *desc)</span></span><br><span class="line">&#123;</span><br><span class="line">    raw_spin_lock(&amp;desc-&gt;lock);</span><br><span class="line">    <span class="comment">/* 通知中断控制器屏蔽该中断线，并设置中断描述符屏蔽该中断 */</span></span><br><span class="line">    mask_ack_irq(desc);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 检查此irq是否处于运行状态，也就是检查IRQD_IRQ_INPROGRESS标志和IRQD_WAKEUP_ARMED标志。大家可以看看，还会检查poll */</span></span><br><span class="line">    <span class="keyword">if</span> (!irq_may_run(desc))</span><br><span class="line">        <span class="keyword">goto</span> out_unlock;</span><br><span class="line"></span><br><span class="line">    desc-&gt;istate &amp;= ~(IRQS_REPLAY | IRQS_WAITING);</span><br><span class="line">    <span class="comment">/* 增加此中断号所在proc中的中断次数 */</span></span><br><span class="line">    kstat_incr_irqs_this_cpu(irq, desc);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * If its disabled or no action available</span></span><br><span class="line"><span class="comment">     * keep it masked and get out of here</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">/* 判断IRQ是否有中断服务例程(irqaction)和是否被系统禁用 */</span></span><br><span class="line">    <span class="keyword">if</span> (unlikely(!desc-&gt;action || irqd_irq_disabled(&amp;desc-&gt;irq_data))) &#123;</span><br><span class="line">        desc-&gt;istate |= IRQS_PENDING;</span><br><span class="line">        <span class="keyword">goto</span> out_unlock;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 在里面执行中断服务例程 */</span></span><br><span class="line">    handle_irq_event(desc);</span><br><span class="line">    <span class="comment">/* 通知中断控制器恢复此中断线 */</span></span><br><span class="line">    cond_unmask_irq(desc);</span><br><span class="line"></span><br><span class="line">out_unlock:</span><br><span class="line">    raw_spin_unlock(&amp;desc-&gt;lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="handle-irq-event"><a href="#handle-irq-event" class="headerlink" title="handle_irq_event()"></a>handle_irq_event()</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">irqreturn_t</span> <span class="title function_">handle_irq_event</span><span class="params">(<span class="keyword">struct</span> irq_desc *desc)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irqaction</span> *<span class="title">action</span> =</span> desc-&gt;action;</span><br><span class="line">    <span class="type">irqreturn_t</span> ret;</span><br><span class="line"></span><br><span class="line">    desc-&gt;istate &amp;= ~IRQS_PENDING;</span><br><span class="line">    <span class="comment">/* 设置该中断处理正在执行，设置此中断号的状态为IRQD_IRQ_INPROGRESS */</span></span><br><span class="line">    irqd_set(&amp;desc-&gt;irq_data, IRQD_IRQ_INPROGRESS);</span><br><span class="line">    raw_spin_unlock(&amp;desc-&gt;lock);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 主要，具体看 */</span></span><br><span class="line">    ret = handle_irq_event_percpu(desc, action);</span><br><span class="line"></span><br><span class="line">    raw_spin_lock(&amp;desc-&gt;lock);</span><br><span class="line">    <span class="comment">/* 取消此中断号的IRQD_IRQ_INPROGRESS状态 */</span></span><br><span class="line">    irqd_clear(&amp;desc-&gt;irq_data, IRQD_IRQ_INPROGRESS);</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="handle-irq-event-percpu"><a href="#handle-irq-event-percpu" class="headerlink" title="handle_irq_event_percpu()"></a>handle_irq_event_percpu()</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">irqreturn_t</span></span><br><span class="line"><span class="title function_">handle_irq_event_percpu</span><span class="params">(<span class="keyword">struct</span> irq_desc *desc, <span class="keyword">struct</span> irqaction *action)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">irqreturn_t</span> retval = IRQ_NONE;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> flags = <span class="number">0</span>, irq = desc-&gt;irq_data.irq;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* desc中的action是一个链表，每个节点包含一个处理函数，这个循环是遍历一次action链表，分别执行一次它们的处理函数 */</span></span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        <span class="type">irqreturn_t</span> res;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 用于中断跟踪 */</span></span><br><span class="line">        trace_irq_handler_entry(irq, action);</span><br><span class="line">        <span class="comment">/* 执行处理，在驱动中定义的中断处理最后就是被赋值到中断服务例程action的handler指针上，这里就执行了驱动中定义的中断处理 */</span></span><br><span class="line">        res = action-&gt;handler(irq, action-&gt;dev_id);</span><br><span class="line">        trace_irq_handler_exit(irq, action, res);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (WARN_ONCE(!irqs_disabled(),<span class="string">&quot;irq %u handler %pF enabled interrupts\n&quot;</span>,</span><br><span class="line">                  irq, action-&gt;handler))</span><br><span class="line">            local_irq_disable();</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 中断返回值处理 */</span></span><br><span class="line">        <span class="keyword">switch</span> (res) &#123;</span><br><span class="line">        <span class="comment">/* 需要唤醒该中断处理例程的中断线程 */</span></span><br><span class="line">        <span class="keyword">case</span> IRQ_WAKE_THREAD:</span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">             * Catch drivers which return WAKE_THREAD but</span></span><br><span class="line"><span class="comment">             * did not set up a thread function</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="comment">/* 该中断服务例程没有中断线程 */</span></span><br><span class="line">            <span class="keyword">if</span> (unlikely(!action-&gt;thread_fn)) &#123;</span><br><span class="line">                warn_no_thread(irq, action);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">/* 唤醒线程 */</span></span><br><span class="line">            __irq_wake_thread(desc, action);</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* Fall through to add to randomness */</span></span><br><span class="line">        <span class="keyword">case</span> IRQ_HANDLED:</span><br><span class="line">            flags |= action-&gt;flags;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        retval |= res;</span><br><span class="line">        <span class="comment">/* 下一个中断服务例程 */</span></span><br><span class="line">        action = action-&gt;next;</span><br><span class="line">    &#125; <span class="keyword">while</span> (action);</span><br><span class="line"></span><br><span class="line">    add_interrupt_randomness(irq, flags);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* 中断调试会使用 */</span></span><br><span class="line">    <span class="keyword">if</span> (!noirqdebug)</span><br><span class="line">        note_interrupt(irq, desc, retval);</span><br><span class="line">    <span class="keyword">return</span> retval;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其实代码上很简单，我们需要注意几个屏蔽中断的方式：<strong>清除EFLAGS的IF标志、通知中断控制器屏蔽指定中断、设置中断描述符的状态为IRQD_IRQ_INPROGRESS</strong>。在上述代码中这三种状态都使用到了，我们具体解释一下：</p><ul><li>**清除EFLAGS的IF标志：CPU禁止中断，当CPU进入到中断处理时自动会清除EFLAGS的IF标志，也就是进入中断处理时会自动禁止中断。在SMP系统中，就是单个CPU禁止中断。</li><li>**通知中断控制器屏蔽指定中断：在中断控制器处就屏蔽中断，这样该中断产生后并不会发到CPU上。在SMP系统中，效果相当于所有CPU屏蔽了此中断。系统在执行此中断的中断处理函数才会要求中断控制器屏蔽该中断，所以没必要在此中断的处理过程中中断控制器再发一次中断信号给CPU。</li><li>**设置中断描述符的状态为IRQD_IRQ_INPROGRESS：在SMP系统中，同一个中断信号有可能发往多个CPU，但是中断处理只应该处理一次，所以设置状态为IRQD_IRQ_INPROGRESS，其他CPU执行此中断时都会先检查此状态(可看handle_level_irq()函数)。</li></ul><p>所以在SMP系统下，对于handle_level_irq而言，一次典型的情况是:中断控制器接收到中断信号，发送给一个或多个CPU，收到的CPU会自动禁止中断，并执行中断处理函数，在中断处理函数中CPU会通知中断控制器屏蔽该中断，之后当执行中断服务例程时会设置该中断描述符的状态为IRQD_IRQ_INPROGRESS，表明其他CPU如果执行该中断就直接退出，因为本CPU已经在处理了。</p><h2 id="软中断"><a href="#软中断" class="headerlink" title="软中断"></a>软中断</h2><blockquote><p>我们看到中断实际分为了两个部分，俗称就是一部分是<strong>硬中断</strong>，一部分是<strong>软中断</strong>。软中断是专门用于处理中断过程中费时费力的操作，而为什么系统要分硬中断和软中断呢？问得明白点就是为什么需要软中断。我们可以试着想想，如果只有硬中断的情况下，我们需要在中断处理过程中执行一些耗时的操作，比如浮点数运算，复杂算法的运算时，其他的外部中断就不能得到及时的响应，因为在硬中断过程中中断是关闭着的，甚至一些很紧急的中断会得不到响应，系统稳定性和及时性都受到很大影响。所以linux为了解决上述这种情况，将中断处理分为了两个部分，硬中断和软中断。首先一个外部中断得到响应时，会先关中断，并进入到硬中断完成较为紧急的操作，然后开中断，并在软中断执行那些非紧急、可延时执行的操作；在这种情况下，紧急操作可以立即执行，而其他的外部中断也可以获得一个较为快速的响应。这也是软中断存在的必要性。在软中断过程中是不可以被抢占也不能被阻塞的，也不能在一个给定的CPU上交错执行。</p></blockquote><p>软中断是在中断框架中专门用于处理非紧急操作的，在SMP系统中，软中断可以并发地运行在多个CPU上，但在一些路径在需要使用自旋锁进行保护。在系统中，很多东西都分优先级，软中断也不例外，有些软中断要求更快速的响应运行，在内核中软中断一共分为10个，同时也代表着10种不同的优先级，系统用一个枚举变量表示：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    HI_SOFTIRQ=<span class="number">0</span>,                     <span class="comment">/* 高优先级tasklet */</span>                              <span class="comment">/* 优先级最高 */</span></span><br><span class="line">    TIMER_SOFTIRQ,                    <span class="comment">/* 时钟相关的软中断 */</span></span><br><span class="line">    NET_TX_SOFTIRQ,                   <span class="comment">/* 将数据包传送到网卡 */</span></span><br><span class="line">    NET_RX_SOFTIRQ,                   <span class="comment">/* 从网卡接收数据包 */</span></span><br><span class="line">    BLOCK_SOFTIRQ,                    <span class="comment">/* 块设备的软中断 */</span></span><br><span class="line">    BLOCK_IOPOLL_SOFTIRQ,             <span class="comment">/* 支持IO轮询的块设备软中断 */</span></span><br><span class="line">    TASKLET_SOFTIRQ,                  <span class="comment">/* 常规tasklet */</span></span><br><span class="line">    SCHED_SOFTIRQ,                    <span class="comment">/* 调度程序软中断 */</span></span><br><span class="line">    HRTIMER_SOFTIRQ,                  <span class="comment">/* 高精度计时器软中断 */</span></span><br><span class="line">    RCU_SOFTIRQ,                      <span class="comment">/* RCU锁软中断，该软中断总是最后一个软中断 */</span> 　　　　  <span class="comment">/* 优先级最低 */</span></span><br><span class="line"></span><br><span class="line">    NR_SOFTIRQS                       <span class="comment">/* 软中断数，为10 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>注释中的tasklet我们之后会说明，这里先无视它。每一个优先级的软中断都使用一个struct softirq_action结构来表示，在这个结构中，只有一个成员变量，就是action函数指针，因为不同的软中断它的处理方式可能不同，从优先级表中就可以看出来，有块设备的，也有网卡处理的。系统将这10个软中断用softirq_vec[10]的数组进行保存。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 用于描述一个软中断 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">softirq_action</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* 此软中断的处理函数 */</span>        </span><br><span class="line">    <span class="type">void</span>    (*action)(<span class="keyword">struct</span> softirq_action *);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 10个软中断描述符都保存在此数组 */</span></span><br><span class="line"><span class="type">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">softirq_action</span> <span class="title">softirq_vec</span>[<span class="title">NR_SOFTIRQS</span>] __<span class="title">cacheline_aligned_in_smp</span>;</span></span><br></pre></td></tr></table></figure><p>系统一般使用open_softirq()函数进行软中断描述符的初始化，主要就是将action函数指针指向该软中断应该执行的函数。在start_kernel()进行系统初始化中，就调用了softirq_init()函数对HI_SOFTIRQ和TASKLET_SOFTIRQ两个软中断进行了初始化:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> __init <span class="title function_">softirq_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> cpu;</span><br><span class="line"></span><br><span class="line">    for_each_possible_cpu(cpu) &#123;</span><br><span class="line">        per_cpu(tasklet_vec, cpu).tail =</span><br><span class="line">            &amp;per_cpu(tasklet_vec, cpu).head;</span><br><span class="line">        per_cpu(tasklet_hi_vec, cpu).tail =</span><br><span class="line">            &amp;per_cpu(tasklet_hi_vec, cpu).head;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 开启常规tasklet */</span></span><br><span class="line">    open_softirq(TASKLET_SOFTIRQ, tasklet_action);</span><br><span class="line">    <span class="comment">/* 开启高优先级tasklet */</span></span><br><span class="line">    open_softirq(HI_SOFTIRQ, tasklet_hi_action);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 开启软中断 */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">open_softirq</span><span class="params">(<span class="type">int</span> nr, <span class="type">void</span> (*action)(<span class="keyword">struct</span> softirq_action *))</span></span><br><span class="line">&#123;</span><br><span class="line">    softirq_vec[nr].action = action;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以看到，TASKLET_SOFTIRQ的action操作使用了tasklet_action()函数，HI_SOFTIRQ的action操作使用了tasklet_hi_action()函数，这两个函数我们需要结合tasklet进行说明。我们也可以看看其他的软中断使用了什么函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">open_softirq(TIMER_SOFTIRQ, run_timer_softirq);</span><br><span class="line">    open_softirq(NET_TX_SOFTIRQ, net_tx_action);</span><br><span class="line">    open_softirq(NET_RX_SOFTIRQ, net_rx_action);</span><br><span class="line">    open_softirq(BLOCK_SOFTIRQ, blk_done_softirq);</span><br><span class="line">    open_softirq(BLOCK_IOPOLL_SOFTIRQ, blk_iopoll_softirq);</span><br><span class="line">    open_softirq(SCHED_SOFTIRQ, run_rebalance_domains);</span><br><span class="line">    open_softirq(HRTIMER_SOFTIRQ, run_hrtimer_softirq);</span><br><span class="line">    open_softirq(RCU_SOFTIRQ, rcu_process_callbacks);</span><br></pre></td></tr></table></figure><p>其实很明显可以看出，除了TASKLET_SOFTIRQ和HI_SOFTIRQ，其他的软中断更多地是用于特定的设备和环境，对于我们普通的IO驱动和设备而已，使用的软中断几乎都是TASKLET_SOFTIRQ和HI_SOFTIRQ，而系统为了对这些不同IO设备进行统一的处理，就在TASKLET_SOFTIRQ和HI_SOFTIRQ的action函数中使用到了tasklet。</p><p>　　对于每个CPU，都有一个irq_cpustat_t的数据结构，里面有一个__softirq_pending变量，这个变量很重要，用于表示该CPU的哪个软中断处于挂起状态，在软中断处理时可以根据此值跳过不需要处理的软中断，直接处理需要处理的软中断。内核使用local_softirq_pending()获取此CPU的__softirq_pending的值。</p><p>　　当使用open_softirq设置好某个软中断的action指针后，该软中断就会开始可以使用了，其实更明了地说，从中断初始化完成开始，即使所有的软中断都没有使用open_softirq()进行初始化，软中断都已经开始使用了，只是所有软中断的action都为空，系统每次执行到软中断都没有软中断需要执行罢了。</p><p>　　在每个CPU上一次软中断处理的一个典型流程是：</p><ol><li>硬中断执行完毕，开中断。</li><li>检查该CPU是否处于嵌套中断的情况，如果处于嵌套中，则不执行软中断，也就是在最外层中断才执行软中断。</li><li>执行软中断，设置一个软中断执行最多使用时间和循环次数(10次)。</li><li>进入循环，获取CPU的__softirq_pending的副本。</li><li>执行此__softirq_pending副本中所有需要执行的软中断。</li><li>如果软中断执行完毕，退出中断上下文。</li><li>如果还有软中断需要执行(在软中断期间又发发生了中断，产生了新的软中断，新的软中断记录在CPU的__softirq_pending上，而我们的__softirq_pending只是个副本)。</li><li>检查此次软中断总共使用的时间和循环次数，条件允许继续执行软中断，循环次数减一，并跳转到第4步。</li></ol><p>我们具体看一下代码，首先在irq_exit()中会检查是否需要进行软中断处理：</p><h3 id="irq-exit"><a href="#irq-exit" class="headerlink" title="irq_exit()"></a>irq_exit()</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">irq_exit</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> __ARCH_IRQ_EXIT_IRQS_DISABLED</span></span><br><span class="line">    local_irq_disable();</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    WARN_ON_ONCE(!irqs_disabled());</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    account_irq_exit_time(current);</span><br><span class="line">    <span class="comment">/* 减少preempt_count的硬中断计数器 */</span></span><br><span class="line">    preempt_count_sub(HARDIRQ_OFFSET);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* in_interrupt()会检查preempt_count上的软中断计数器和硬中断计数器来判断是否处于中断嵌套中 */</span></span><br><span class="line">    <span class="comment">/* local_softirq_pending()则会检查该CPU的__softirq_pending变量，是否有软中断挂起 */</span></span><br><span class="line">    <span class="keyword">if</span> (!in_interrupt() &amp;&amp; local_softirq_pending())</span><br><span class="line">        invoke_softirq();</span><br><span class="line"></span><br><span class="line">    tick_irq_exit();</span><br><span class="line">    rcu_irq_exit();</span><br><span class="line">    trace_hardirq_exit(); <span class="comment">/* must be last! */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们再进入到invoke_softirq():</p><h3 id="invoke-softirq"><a href="#invoke-softirq" class="headerlink" title="invoke_softirq()"></a>invoke_softirq()</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">invoke_softirq</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!force_irqthreads) &#123;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_HAVE_IRQ_EXIT_ON_IRQ_STACK</span></span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * We can safely execute softirq on the current stack if</span></span><br><span class="line"><span class="comment">         * it is the irq stack, because it should be near empty</span></span><br><span class="line"><span class="comment">         * at this stage.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="comment">/* 软中断处理函数 */</span></span><br><span class="line">        __do_softirq();</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * Otherwise, irq_exit() is called on the task stack that can</span></span><br><span class="line"><span class="comment">         * be potentially deep already. So call softirq in its own stack</span></span><br><span class="line"><span class="comment">         * to prevent from any overrun.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        do_softirq_own_stack();</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">/* 如果强制使用软中断线程进行软中断处理，会通知调度器唤醒软中断线程ksoftirqd */</span></span><br><span class="line">        wakeup_softirqd();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>重头戏就在__do_softirq()中:</p><h3 id="do-softirq"><a href="#do-softirq" class="headerlink" title="__do_softirq()"></a>__do_softirq()</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br></pre></td><td class="code"><pre><span class="line">asmlinkage __visible <span class="type">void</span> __do_softirq(<span class="type">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* 为了防止软中断执行时间太长，设置了一个软中断结束时间 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> end = jiffies + MAX_SOFTIRQ_TIME;</span><br><span class="line">    <span class="comment">/* 保存当前进程的标志 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> old_flags = current-&gt;flags;</span><br><span class="line">    <span class="comment">/* 软中断循环执行次数: 10次 */</span></span><br><span class="line">    <span class="type">int</span> max_restart = MAX_SOFTIRQ_RESTART;</span><br><span class="line">    <span class="comment">/* 软中断的action指针 */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">softirq_action</span> *<span class="title">h</span>;</span></span><br><span class="line">    <span class="type">bool</span> in_hardirq;</span><br><span class="line">    __u32 pending;</span><br><span class="line">    <span class="type">int</span> softirq_bit;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Mask out PF_MEMALLOC s current task context is borrowed for the</span></span><br><span class="line"><span class="comment">     * softirq. A softirq handled such as network RX might set PF_MEMALLOC</span></span><br><span class="line"><span class="comment">     * again if the socket is related to swap</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    current-&gt;flags &amp;= ~PF_MEMALLOC;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 获取此CPU的__softirq_pengding变量值 */</span></span><br><span class="line">    pending = local_softirq_pending();</span><br><span class="line">    <span class="comment">/* 用于统计进程被软中断使用时间 */</span></span><br><span class="line">    account_irq_enter_time(current);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 增加preempt_count软中断计数器，也表明禁止了调度 */</span></span><br><span class="line">    __local_bh_disable_ip(_RET_IP_, SOFTIRQ_OFFSET);</span><br><span class="line">    in_hardirq = lockdep_softirq_start();</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 循环10次的入口，每次循环都会把所有挂起需要执行的软中断执行一遍 */</span></span><br><span class="line">restart:</span><br><span class="line">    <span class="comment">/* 该CPU的__softirq_pending清零，当前的__softirq_pending保存在pending变量中 */</span></span><br><span class="line">    <span class="comment">/* 这样做就保证了新的软中断会在下次循环中执行 */</span></span><br><span class="line">    set_softirq_pending(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 开中断 */</span></span><br><span class="line">    local_irq_enable();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* h指向软中断数组头 */</span></span><br><span class="line">    h = softirq_vec;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 每次获取最高优先级的已挂起软中断 */</span></span><br><span class="line">    <span class="keyword">while</span> ((softirq_bit = ffs(pending))) &#123;</span><br><span class="line">        <span class="type">unsigned</span> <span class="type">int</span> vec_nr;</span><br><span class="line">        <span class="type">int</span> prev_count;</span><br><span class="line">        <span class="comment">/* 获取此软中断描述符地址 */</span></span><br><span class="line">        h += softirq_bit - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 减去软中断描述符数组首地址，获得软中断号 */</span></span><br><span class="line">        vec_nr = h - softirq_vec;</span><br><span class="line">        <span class="comment">/* 获取preempt_count的值 */</span></span><br><span class="line">        prev_count = preempt_count();</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 增加统计中该软中断发生次数 */</span></span><br><span class="line">        kstat_incr_softirqs_this_cpu(vec_nr);</span><br><span class="line"></span><br><span class="line">        trace_softirq_entry(vec_nr);</span><br><span class="line">        <span class="comment">/* 执行该软中断的action操作 */</span></span><br><span class="line">        h-&gt;action(h);</span><br><span class="line">        trace_softirq_exit(vec_nr);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 之前保存的preempt_count并不等于当前的preempt_count的情况处理，也是简单的把之前的复制到当前的preempt_count上，这样做是防止最后软中断计数不为0导致系统不能够执行调度 */</span></span><br><span class="line">        <span class="keyword">if</span> (unlikely(prev_count != preempt_count())) &#123;</span><br><span class="line">            pr_err(<span class="string">&quot;huh, entered softirq %u %s %p with preempt_count %08x, exited with %08x?\n&quot;</span>,</span><br><span class="line">                   vec_nr, softirq_to_name[vec_nr], h-&gt;action,</span><br><span class="line">                   prev_count, preempt_count());</span><br><span class="line">            preempt_count_set(prev_count);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* h指向下一个软中断，但下个软中断并不一定需要执行，这里只是配合softirq_bit做到一个处理 */</span></span><br><span class="line">        h++;</span><br><span class="line">        pending &gt;&gt;= softirq_bit;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    rcu_bh_qs();</span><br><span class="line">    <span class="comment">/* 关中断 */</span></span><br><span class="line">    local_irq_disable();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 循环结束后再次获取CPU的__softirq_pending变量，为了检查是否还有软中断未执行 */</span></span><br><span class="line">    pending = local_softirq_pending();</span><br><span class="line">    <span class="comment">/* 还有软中断需要执行 */</span></span><br><span class="line">    <span class="keyword">if</span> (pending) &#123;</span><br><span class="line">        <span class="comment">/* 在还有软中断需要执行的情况下，如果时间片没有执行完，并且循环次数也没到10次，继续执行软中断 */</span></span><br><span class="line">        <span class="keyword">if</span> (time_before(jiffies, end) &amp;&amp; !need_resched() &amp;&amp;</span><br><span class="line">            --max_restart)</span><br><span class="line">            <span class="keyword">goto</span> restart;</span><br><span class="line">        <span class="comment">/* 这里是有软中断挂起，但是软中断时间和循环次数已经用完，通知调度器唤醒软中断线程去执行挂起的软中断，软中断线程是ksoftirqd，这里只起到一个通知作用，因为在中断上下文中是禁止调度的 */</span></span><br><span class="line">        wakeup_softirqd();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    lockdep_softirq_end(in_hardirq);</span><br><span class="line">    <span class="comment">/* 用于统计进程被软中断使用时间 */</span></span><br><span class="line">    account_irq_exit_time(current);</span><br><span class="line">    <span class="comment">/* 减少preempt_count中的软中断计数器 */</span></span><br><span class="line">    __local_bh_enable(SOFTIRQ_OFFSET);</span><br><span class="line">    WARN_ON_ONCE(in_interrupt());</span><br><span class="line">    <span class="comment">/* 还原进程标志 */</span></span><br><span class="line">    tsk_restore_flags(current, old_flags, PF_MEMALLOC);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在目录 &#x2F;kernel&#x2F;softirq.c</p><h3 id="tasklet"><a href="#tasklet" class="headerlink" title="tasklet"></a>tasklet</h3><p>软中断有多种，部分种类有自己特殊的处理，如从NET_TX_SOFTIRQ和NET_RT_SOFTIRQ、BLOCK_SOFTIRQ等，而如HI_SOFTIRQ和TASKLET_SOFTIRQ则是专门使用tasklet。它是在I&#x2F;O驱动程序中实现可延迟函数的首选方法，如上一句所说，它建立在HI_SOFTIRQ和TASKLET_SOFTIRQ这两种软中断之上，多个tasklet可以与同一个软中断相关联，系统会使用一个链表组织他们，而每个tasklet执行自己的函数处理。而HI_SOFTIRQ和TASKLET_SOFTIRQ这两个软中断并没有什么区别，他们只是优先级上的不同而已，系统会先执行HI_SOFTIRQ的tasklet，再执行TASKLET_SOFTIRQ的tasklet。同一个tasklet不能同时在几个CPU上执行，一个tasklet在一个时间上只能在一个CPU的软中断链上，不能同时在多个CPU的软中断链上，并且当这个tasklet正在执行时，其他CPU不能够执行这个tasklet。也就是说，tasklet不必要编写成可重入的函数。</p><p>系统会为每个CPU维护两个链表，用于保存HI_SOFTIRQ的tasklet和TASKLET_SOFTIRQ的tasklet，这两个链表是tasklet_vec和tasklet_hi_vec，它们都是双向链表，如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tasklet_head</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tasklet_struct</span> *<span class="title">head</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tasklet_struct</span> **<span class="title">tail</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="title function_">DEFINE_PER_CPU</span><span class="params">(<span class="keyword">struct</span> tasklet_head, tasklet_vec)</span>;</span><br><span class="line"><span class="type">static</span> <span class="title function_">DEFINE_PER_CPU</span><span class="params">(<span class="keyword">struct</span> tasklet_head, tasklet_hi_vec)</span>;</span><br></pre></td></tr></table></figure><p>在softirq_init()函数中，会将每个CPU的tasklet_vec链表和tasklet_hi_vec链表进行初始化，将他们的头尾相连，实现为一个空链表。由于tasklet_vec和tasklet_hi_vec处理方式几乎一样，只是软中断的优先级别不同，我们只需要理解系统如何对tasklet_vec进行处理即可。需要注意的是，tasklet_vec链表都是以顺序方式执行，并不会出现后一个先执行，再到前一个先执行(在软中断期间被中断的情况)，之后的代码我们详细说明。</p><p>介绍完tasklet_vec和tasklet_hi_vec链表，我们来看看tasklet，tasklet简单来说，就是一个处理函数的封装，类似于硬中断中的irqaction结构。一般来说，在一个驱动中如果需要使用tasklet进行软中断的处理，只需要一个中断对应初始化一个tasklet，它可以在每次中断产生时重复使用。系统使用tasklet_struct结构进行描述一个tasklet，而且对于同一个tasklet_struct你可以选择放在tasklet_hi_vec链表或者tasklet_vec链表上。我们来看看：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tasklet_struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tasklet_struct</span> *<span class="title">next</span>;</span>      <span class="comment">/* 指向链表下一个tasklet */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> state;              <span class="comment">/* tasklet状态 */</span></span><br><span class="line">    <span class="type">atomic_t</span> count;                   <span class="comment">/* 禁止计数器，调用tasklet_disable()会增加此数，tasklet_enable()减少此数 */</span></span><br><span class="line">    <span class="type">void</span> (*func)(<span class="type">unsigned</span> <span class="type">long</span>);      <span class="comment">/* 处理函数 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> data;               <span class="comment">/* 处理函数使用的数据 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>tasklet状态主要分为以下两种：</p><ul><li>**TASKLET_STATE_SCHED:这种状态表示此tasklet处于某个tasklet链表之上(可能是tasklet_vec也可能是tasklet_hi_vec)。</li><li>**TASKLET_STATE_RUN:表示此tasklet正在运行中。</li></ul><p>这两个状态主要就是用于防止tasklet同时在几个CPU上运行和在同一个CPU上交错执行。</p><p>而func指针就是指向相应的处理函数。在编写驱动时，我们可以使用tasklet_init()函数或者DECLARE_TASKLET宏进行一个task_struct结构的初始化，之后可以使用tasklet_schedule()或者tasklet_hi_schedule()将其放到相应链表上等待CPU运行。我们使用一张图描述一下软中断和tasklet结合运行的情况：</p><p>我们知道，每个软中断都有自己的action函数，在HI_SOFTIRQ和TASKLET_SOFTIRQ的action函数中，就用到了它们对应的TASKLET_HI_VEC链表和TASKLET_VEC链表，并依次顺序执行链表中的每个tasklet结点。</p><p>在SMP系统中，我们会遇到一个问题：两个CPU都需要执行同一个tasklet的情况，虽然一个tasklet只能放在一个CPU的tasklet_vec链表或者tasklet_hi_vec链表上，但是这种情况是有可能发生的，我们设想一下，中断在CPU1上得到了响应，并且它的tasklet放到了CPU1的tasklet_vec上进行执行，而当中断的tasklet上正在执行时，此中断再次发生，并在CPU2上进行了响应，此时CPU2将此中断的tasklet放到CPU2的tasklet_vec上，并执行到此中断的tasklet。</p><p>实际上，为了处理这种情况，在HI_SOFTIRQ和TASKLET_SOFTIRQ的action函数中，会先将对应的tasklet链表取出来，并把对应的tasklet链表的head和tail清空，如果在执行过程中，某个tasklet的state为TASKLET_STATE_RUN状态，说明其他CPU正在处理这个tasklet，这时候当前CPU则会把此tasklet加入到当前CPU已清空的tasklet链表的末尾，然后设置__softirq_pending变量，这样，在下次循环软中断的过程中，会再次检查这个tasklet。也就是如果其他CPU的这个tasklet一直不退出，当前CPU就会不停的置位tasklet的pending，然后不停地循环检查。</p><p>我们可以看看TASKLET_SOFTIRQ的action处理：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">tasklet_action</span><span class="params">(<span class="keyword">struct</span> softirq_action *a)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">tasklet_struct</span> *<span class="title">list</span>;</span></span><br><span class="line"></span><br><span class="line">    local_irq_disable();</span><br><span class="line">    <span class="comment">/* 将tasklet链表从该CPU中拿出来 */</span></span><br><span class="line">    <span class="built_in">list</span> = __this_cpu_read(tasklet_vec.head);</span><br><span class="line">    <span class="comment">/* 将该CPU的此软中断的tasklet链表清空 */</span></span><br><span class="line">    __this_cpu_write(tasklet_vec.head, <span class="literal">NULL</span>);</span><br><span class="line">    __this_cpu_write(tasklet_vec.tail, this_cpu_ptr(&amp;tasklet_vec.head));</span><br><span class="line">    local_irq_enable();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 链表已经处于list中，并且该CPU的tasklet_vec链表为空 */</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">list</span>) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">tasklet_struct</span> *<span class="title">t</span> =</span> <span class="built_in">list</span>;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">list</span> = <span class="built_in">list</span>-&gt;next;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 检查并设置该tasklet为TASKLET_STATE_RUN状态 */</span></span><br><span class="line">        <span class="keyword">if</span> (tasklet_trylock(t)) &#123;</span><br><span class="line">            <span class="comment">/* 检查是否被禁止 */</span></span><br><span class="line">            <span class="keyword">if</span> (!<span class="type">atomic_read</span>(&amp;t-&gt;count)) &#123;</span><br><span class="line">                <span class="comment">/* 清除其TASKLET_STATE_SCHED状态 */</span></span><br><span class="line">                <span class="keyword">if</span> (!test_and_clear_bit(TASKLET_STATE_SCHED,</span><br><span class="line">                            &amp;t-&gt;state))</span><br><span class="line">                    BUG();</span><br><span class="line">                <span class="comment">/* 执行该tasklet的func处理函数 */</span></span><br><span class="line">                t-&gt;func(t-&gt;data);</span><br><span class="line">                <span class="comment">/* 清除该tasklet的TASKLET_STATE_RUN状态 */</span></span><br><span class="line">                tasklet_unlock(t);</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            tasklet_unlock(t);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 以下为tasklet为TASKLET_STATE_RUN状态下的处理 */</span></span><br><span class="line">        <span class="comment">/* 禁止中断 */</span></span><br><span class="line">        local_irq_disable();</span><br><span class="line">        <span class="comment">/* 将此tasklet添加的该CPU的tasklet_vec链表尾部 */</span></span><br><span class="line">        t-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">        *__this_cpu_read(tasklet_vec.tail) = t;</span><br><span class="line">        __this_cpu_write(tasklet_vec.tail, &amp;(t-&gt;next));</span><br><span class="line">        <span class="comment">/* 设置该CPU的此软中断处于挂起状态，设置irq_cpustat_t的__sofirq_pending变量，这样在软中断的下次执行中会再次执行此tasklet */</span></span><br><span class="line">        __raise_softirq_irqoff(TASKLET_SOFTIRQ);</span><br><span class="line">        <span class="comment">/* 开启中断 */</span></span><br><span class="line">        local_irq_enable();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="软中断处理线程"><a href="#软中断处理线程" class="headerlink" title="软中断处理线程"></a>软中断处理线程</h3><p>当有过多软中断需要处理时，为了保证进程能够得到一个满意的响应时间，设计时给定软中断一个时间片和循环次数，当时间片和循环次数到达但软中断又没有处理完时，就会把剩下的软中断交给软中断处理线程进行处理，这个线程是一个内核线程，其作为一个普通进程，优先级是120。其核心处理函数是run_ksoftirqd()，其实此线程的处理也很简单，就是调用了上面的__do_softirq()函数，我们可以具体看看：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 在smpboot_thread_fun的一个死循环中被调用 */</span></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">run_ksoftirqd</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> cpu)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* 禁止中断，在__do_softirq()中会开启 */</span></span><br><span class="line">    local_irq_disable();</span><br><span class="line">    <span class="comment">/* 检查该CPU的__softirq_pending是否有软中断被挂起 */</span></span><br><span class="line">    <span class="keyword">if</span> (local_softirq_pending()) &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * We can safely run softirq on inline stack, as we are not deep</span></span><br><span class="line"><span class="comment">         * in the task stack here.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="comment">/* 执行软中断 */</span></span><br><span class="line">        __do_softirq();</span><br><span class="line">        rcu_note_context_switch(cpu);</span><br><span class="line">        <span class="comment">/* 开中断 */</span></span><br><span class="line">        local_irq_enable();</span><br><span class="line">        <span class="comment">/* 检查是否需要调度 */</span></span><br><span class="line">        cond_resched();</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 开中断 */</span></span><br><span class="line">    local_irq_enable();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
          <category> interrupt </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Linux中断和异常</title>
      <link href="/2024/11/15/Linux/interrupt/%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8/"/>
      <url>/2024/11/15/Linux/interrupt/%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8/</url>
      
        <content type="html"><![CDATA[<h2 id="什么是中断？"><a href="#什么是中断？" class="headerlink" title="什么是中断？"></a>什么是中断？</h2><p>我们将会从下面的主题开始：</p><ul><li><p>什么是 <code>中断（interrupts）</code> ？</p></li><li><p>什么是 <code>中断处理（interrupt handlers）</code> ？</p><p>  中断就是当软件或者硬件需要使用 CPU 时引发的 <code>事件（event）</code>。比如，当我们在键盘上按下一个键的时候，我们下一步期望做什么？操作系统和电脑应该怎么做？做一个简单的假设，每一个物理硬件都有一根连接 CPU 的中断线，设备可以通过它对 CPU 发起中断信号。但是中断信号并不是直接发送给 CPU。在老机器上中断信号发送给 <a href="http://en.wikipedia.org/wiki/Programmable_Interrupt_Controller">PIC</a> ，它是一个顺序处理各种设备的各种中断请求的芯片。在新机器上，则是<a href="https://en.wikipedia.org/wiki/Advanced_Programmable_Interrupt_Controller">高级程序中断控制器（Advanced Programmable Interrupt Controller）</a>做这件事情，即我们熟知的 <code>APIC</code>。一个 APIC 包括两个独立的设备：</p><ul><li><code>Local APIC</code></li><li><code>I/O APIC</code><br>  第一个设备 - <code>Local APIC</code> 存在于每个CPU核心中，Local APIC 负责处理特定于 CPU 的中断配置。Local APIC 常被用于管理来自 APIC 时钟（APIC-timer）、热敏元件和其他与 I&#x2F;O 设备连接的设备的中断。</li></ul><p>  第二个设备 - <code>I/O APIC</code> 提供了多核处理器的中断管理。它被用来在所有的 CPU 核心中分发外部中断。更多关于 local 和 I&#x2F;O APIC 的内容将会在这一节的下面讲到。就如你所知道的，中断可以在任何时间发生。当一个中断发生时，操作系统必须立刻处理它。但是 <code>处理一个中断</code> 是什么意思呢？当一个中断发生时，操作系统必须确保下面的步骤顺序：</p><ul><li>内核必须暂停执行当前进程(取代当前的任务)；</li><li>内核必须搜索中断处理程序并且转交控制权(执行中断处理程序)；</li><li>中断处理程序结束之后，被中断的进程能够恢复执行。</li></ul></li></ul><h2 id="可编程中断控制器-PIC、APIC"><a href="#可编程中断控制器-PIC、APIC" class="headerlink" title="可编程中断控制器(PIC、APIC)"></a>可编程中断控制器(PIC、APIC)</h2><h3 id="中断控制器"><a href="#中断控制器" class="headerlink" title="中断控制器"></a>中断控制器</h3><p>中断控制器是作为中断(IRQ)和CPU核之间的一个桥梁而存在的，每个CPU内部都有一个自己的中断控制器，中断线并不是直接与CPU核相连，而是与CPU内部或外部的中断控制器相连。而为什么叫做可编程中断控制器，是因为其本身有一定的寄存器，CPU可以通过操作设置中断控制器屏蔽某个中断引脚的信号，实现硬件上的中断屏蔽。中断控制器也可以级联提供更多的中断线，具体如下：</p><p><img src="/computer_system/%E5%9B%BE%E7%89%87/%E5%8F%AF%E7%BC%96%E7%A8%8B%E4%B8%AD%E6%96%AD%E6%8E%A7%E5%88%B6%E5%99%A8.jpg"><br>如上图，CPU的INTR与中断控制器的INT相连，INTA与ACK相连，当一个外部中断发生时(比如键盘中断IRQ1)，中断控制器与CPU交互操作如下： </p><ol><li>IRQ1发生中断，主中断控制器接收到中断信号，检查中断屏蔽寄存器IRQ1是否被屏蔽，如果屏蔽则忽略此中断信号。</li><li>将中断控制器中的中断请求寄存器对应的IRQ1位置位，表示收到IRQ1中断。</li><li>中断控制器拉高INT引脚电平，告知CPU有中断发生。</li><li>CPU每执行完一条指令时，都会检查INTR引脚是否被拉高，这里已被拉高。</li><li>CPU检查EFLAGS寄存器的中断运行标志位IF是否为1，若为1，表明允许中断，通过INTA向中断控制器发出应答。</li><li>中断控制器接收到应答信号，将IRQ1的中断向量号发到数据总线上，此时CPU会通过数据总线读取IRQ1的中断向量号。</li><li>最后，如果中断控制器需要EOI(End of Interrupt)信号，CPU则会发送，否则中断控制器自动将INT拉低，并清除IRQ1对应的中断请求寄存器位。</li></ol><h3 id="irq-chip结构"><a href="#irq-chip结构" class="headerlink" title="irq_chip结构"></a>irq_chip结构</h3><p>在linux内核中，用struct irq_chip结构体描述一个可编程中断控制器，它的整个结构和调度器中的调度类类似，里面定义了中断控制器的一些操作，如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">irq_chip</span> &#123;</span></span><br><span class="line">    <span class="comment">/* 中断控制器的名字 */</span></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>    *name;</span><br><span class="line">    <span class="comment">/* 控制器初始化函数 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="title function_">int</span>    <span class="params">(*irq_startup)</span><span class="params">(<span class="keyword">struct</span> irq_data *data)</span>;</span><br><span class="line">    <span class="comment">/* 控制器关闭函数 */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_shutdown)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 使能irq操作，通常是直接调用irq_unmask()，通过data参数指明irq */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_enable)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 禁止irq操作，通常是直接调用irq_mask，严格意义上，他俩其实代表不同的意义，disable表示中断控制器根本就不响应该irq，而mask时，中断控制器可能响应该irq，只是不通知CPU */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_disable)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 用于CPU对该irq的回应，通常表示cpu希望要清除该irq的pending状态，准备接受下一个irq请求 */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_ack)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 屏蔽irq操作，通过data参数表明指定irq */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_mask)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 相当于irq_mask() + irq_ack() */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_mask_ack)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 取消屏蔽指定irq操作 */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_unmask)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 某些中断控制器需要在cpu处理完该irq后发出eoi信号 */</span></span><br><span class="line">    <span class="type">void</span>        (*irq_eoi)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/*  用于设置该irq和cpu之间的亲和力，就是通知中断控制器，该irq发生时，那些cpu有权响应该irq */</span></span><br><span class="line">    <span class="type">int</span>        (*irq_set_affinity)(<span class="keyword">struct</span> irq_data *data, <span class="type">const</span> <span class="keyword">struct</span> cpumask *dest, <span class="type">bool</span> force);</span><br><span class="line">    <span class="type">int</span>        (*irq_retrigger)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="comment">/* 设置irq的电气触发条件，例如 IRQ_TYPE_LEVEL_HIGH(电平触发) 或 IRQ_TYPE_EDGE_RISING(边缘触发) */</span></span><br><span class="line">    <span class="type">int</span>        (*irq_set_type)(<span class="keyword">struct</span> irq_data *data, <span class="type">unsigned</span> <span class="type">int</span> flow_type);</span><br><span class="line">    <span class="comment">/* 通知电源管理子系统，该irq是否可以用作系统的唤醒源 */</span></span><br><span class="line">    <span class="type">int</span>        (*irq_set_wake)(<span class="keyword">struct</span> irq_data *data, <span class="type">unsigned</span> <span class="type">int</span> on);</span><br><span class="line"></span><br><span class="line">    <span class="type">void</span>        (*irq_bus_lock)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="type">void</span>        (*irq_bus_sync_unlock)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line"></span><br><span class="line">    <span class="type">void</span>        (*irq_cpu_online)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="type">void</span>        (*irq_cpu_offline)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line"></span><br><span class="line">    <span class="type">void</span>        (*irq_suspend)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="type">void</span>        (*irq_resume)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="type">void</span>        (*irq_pm_shutdown)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line"></span><br><span class="line">    <span class="type">void</span>        (*irq_calc_mask)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line"></span><br><span class="line">    <span class="type">void</span>        (*irq_print_chip)(<span class="keyword">struct</span> irq_data *data, <span class="keyword">struct</span> seq_file *p);</span><br><span class="line">    <span class="type">int</span>        (*irq_request_resources)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line">    <span class="type">void</span>        (*irq_release_resources)(<span class="keyword">struct</span> irq_data *data);</span><br><span class="line"></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span>    flags;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="中断向量表-IDT"><a href="#中断向量表-IDT" class="headerlink" title="中断向量表(IDT)"></a>中断向量表(IDT)</h2><p>每个中断处理程序的&#x3D;&#x3D;地址&#x3D;&#x3D;都保存在一个特殊的位置，这个位置被称为 <code>中断描述符表（Interrupt Descriptor Table）</code> 或者 <code>IDT</code>。处理器使用一个唯一的数字来识别中断和异常的类型，这个数字被称为 <code>中断标识码（vector number）</code>。</p><p>中断可以分为 ：</p><ul><li><code>可屏蔽的（maskable）</code> </li><li><code>不可屏蔽的（non-maskable）</code></li></ul><p>异常可以被分为：</p><ul><li><code>故障（Faults）</code></li><li><code>陷入（Traps）</code></li><li><code>终止（Aborts）</code></li></ul><p><code>故障</code> 是在执行一个“不完善的”指令（可以在之后被修正）之前被报告的异常。如果发生了，它允许被中断的程序继续执行。</p><p>接下来的 <code>陷入</code> 是一个在执行了 <code>陷入</code> 指令后立刻被报告的异常。陷入同样允许被中断的程序继续执行，就像 <code>故障</code> 一样。</p><p>最后的 <code>终止</code> 是一个从不报告引起异常的精确指令的异常，并且不允许被中断的程序继续执行。</p><p>可屏蔽的中断可以被阻塞，使用 <code>x86_64</code> 的指令 - <code>sti</code> 和 <code>cli</code>。我们可以在 Linux 内核代码中找到他们：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">native_irq_disable</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">        <span class="keyword">asm</span> <span class="title function_">volatile</span><span class="params">(<span class="string">&quot;cli&quot;</span>: : :<span class="string">&quot;memory&quot;</span>)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>和：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">native_irq_enable</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">        <span class="keyword">asm</span> <span class="title function_">volatile</span><span class="params">(<span class="string">&quot;sti&quot;</span>: : :<span class="string">&quot;memory&quot;</span>)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这两个指令修改了在中断寄存器中的 <code>IF</code> 标识位。 <code>sti</code> 指令设置 <code>IF</code> 标识，<code>cli</code> 指令清除这个标识。不可屏蔽的中断总是被报告。通常，任何硬件上的失败都映射为不可屏蔽中断。</p><p>如果多个异常或者中断同时发生，处理器以事先设定好的中断优先级处理他们。我们可以定义下面表中的从最低到最高的优先级：</p><table><thead><tr><th>优先级 (Priority)</th><th>描述 (Description)</th></tr></thead><tbody><tr><td>1</td><td>硬件复位和机器检查 (Hardware Reset and Machine Checks)</td></tr><tr><td></td><td>- RESET</td></tr><tr><td></td><td>- Machine Check</td></tr><tr><td></td><td>任务切换时的陷阱 (Trap on Task Switch)</td></tr><tr><td>2</td><td>- TSS 中的 T 标志被设置 (T flag in TSS is set)</td></tr><tr><td></td><td></td></tr><tr><td></td><td>外部硬件干预 (External Hardware Interventions)</td></tr><tr><td></td><td>- FLUSH</td></tr><tr><td>3</td><td>- STOPCLK</td></tr><tr><td></td><td>- SMI</td></tr><tr><td></td><td>- INIT</td></tr><tr><td></td><td>上一条指令的陷阱 (Traps on the Previous Instruction)</td></tr><tr><td>4</td><td>- Breakpoints</td></tr><tr><td></td><td>- Debug Trap Exceptions</td></tr><tr><td>5</td><td>不可屏蔽中断 (Nonmaskable Interrupts)</td></tr><tr><td>6</td><td>可屏蔽的硬件中断 (Maskable Hardware Interrupts)</td></tr><tr><td>7</td><td>代码断点错误 (Code Breakpoint Fault)</td></tr><tr><td>8</td><td>从下一条指令获取时的错误 (Faults from Fetching Next Instruction)</td></tr><tr><td></td><td>- 代码段限制违规 (Code-Segment Limit Violation)</td></tr><tr><td></td><td>- 代码页错误 (Code Page Fault)</td></tr><tr><td></td><td>从下一条指令解码时的错误 (Faults from Decoding the Next Instruction)</td></tr><tr><td></td><td>- 指令长度超过 15 字节 (Instruction length &gt; 15 bytes)</td></tr><tr><td>9</td><td>- 无效操作码 (Invalid Opcode)</td></tr><tr><td></td><td>- 协处理器不可用 (Coprocessor Not Available)</td></tr><tr><td></td><td></td></tr><tr><td>10</td><td>执行指令时的错误 (Faults on Executing an Instruction)</td></tr><tr><td></td><td>- 溢出 (Overflow)</td></tr><tr><td></td><td>- 边界错误 (Bound error)</td></tr><tr><td></td><td>- 无效 TSS (Invalid TSS)</td></tr><tr><td></td><td>- 段不存在 (Segment Not Present)</td></tr><tr><td></td><td>- 栈错误 (Stack fault)</td></tr><tr><td></td><td>- 通用保护错误 (General Protection)</td></tr><tr><td></td><td>- 数据页错误 (Data Page Fault)</td></tr><tr><td></td><td>- 对齐检查 (Alignment Check)</td></tr><tr><td></td><td>- x87 FPU 浮点异常 (x87 FPU Floating-point exception)</td></tr><tr><td></td><td>- SIMD 浮点异常 (SIMD floating-point exception)</td></tr><tr><td></td><td>- 虚拟化异常 (Virtualization exception)</td></tr></tbody></table><p>现在我们了解了一些关于各种类型的中断和异常的内容，是时候转到更实用的部分了。我们从 <code>中断描述符表（IDT）</code> 开始。就如之前所提到的，<code>IDT</code> 保存了中断和异常处理程序的入口指针。<code>IDT</code> 是一个类似于 <code>全局描述符表（Global Descriptor Table）</code>的结构.但是他们确实有一些不同，<code>IDT</code> 的表项被称为 <code>门（gates）</code>，而不是 <code>描述符（descriptors）</code>。它可以包含下面的一种：</p><ul><li>中断门（Interrupt gates）</li><li>任务门（Task gates）</li><li>陷阱门（Trap gates）</li></ul><p>CPU为了防止恶意程序访问中断，限制了中断门的权限，而在某些时候，用户程序又必须使用中断，所以Linux把中断描述符的中断向量类型改为了5种：<strong>中断门，系统门，系统中断门，陷阱门，任务门</strong>。这个中断向量表的基地址保存在idtr寄存器中。</p><p>这个是idt的一般形式：</p><table><thead><tr><th>Name</th><th>Vector nr.</th><th>Type</th><th>Mnemonic</th><th>Error code?</th></tr></thead><tbody><tr><td><a href="https://wiki.osdev.org/Exceptions#Division_Error">Division Error</a></td><td>0 (0x0)</td><td>Fault</td><td>#DE</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Debug">Debug</a></td><td>1 (0x1)</td><td>Fault&#x2F;Trap</td><td>#DB</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Non_Maskable_Interrupt" title="Non Maskable Interrupt">Non-maskable Interrupt</a></td><td>2 (0x2)</td><td>Interrupt</td><td>-</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Breakpoint">Breakpoint</a></td><td>3 (0x3)</td><td>Trap</td><td>#BP</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Overflow">Overflow</a></td><td>4 (0x4)</td><td>Trap</td><td>#OF</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Bound_Range_Exceeded">Bound Range Exceeded</a></td><td>5 (0x5)</td><td>Fault</td><td>#BR</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Invalid_Opcode">Invalid Opcode</a></td><td>6 (0x6)</td><td>Fault</td><td>#UD</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Device_Not_Available">Device Not Available</a></td><td>7 (0x7)</td><td>Fault</td><td>#NM</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Double_Fault">Double Fault</a></td><td>8 (0x8)</td><td>Abort</td><td>#DF</td><td>Yes (Zero)</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Coprocessor_Segment_Overrun">Coprocessor Segment Overrun</a></td><td>9 (0x9)</td><td>Fault</td><td>-</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Invalid_TSS">Invalid TSS</a></td><td>10 (0xA)</td><td>Fault</td><td>#TS</td><td>Yes</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Segment_Not_Present">Segment Not Present</a></td><td>11 (0xB)</td><td>Fault</td><td>#NP</td><td>Yes</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Stack-Segment_Fault">Stack-Segment Fault</a></td><td>12 (0xC)</td><td>Fault</td><td>#SS</td><td>Yes</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#General_Protection_Fault">General Protection Fault</a></td><td>13 (0xD)</td><td>Fault</td><td>#GP</td><td>Yes</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Page_Fault">Page Fault</a></td><td>14 (0xE)</td><td>Fault</td><td>#PF</td><td>Yes</td></tr><tr><td>Reserved</td><td>15 (0xF)</td><td>-</td><td>-</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#x87_Floating-Point_Exception">x87 Floating-Point Exception</a></td><td>16 (0x10)</td><td>Fault</td><td>#MF</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Alignment_Check">Alignment Check</a></td><td>17 (0x11)</td><td>Fault</td><td>#AC</td><td>Yes</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Machine_Check">Machine Check</a></td><td>18 (0x12)</td><td>Abort</td><td>#MC</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#SIMD_Floating-Point_Exception">SIMD Floating-Point Exception</a></td><td>19 (0x13)</td><td>Fault</td><td>#XM&#x2F;#XF</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Virtualization_Exception">Virtualization Exception</a></td><td>20 (0x14)</td><td>Fault</td><td>#VE</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Control_Protection_Exception">Control Protection Exception</a></td><td>21 (0x15)</td><td>Fault</td><td>#CP</td><td>Yes</td></tr><tr><td>Reserved</td><td>22-27 (0x16-0x1B)</td><td>-</td><td>-</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Hypervisor_Injection_Exception">Hypervisor Injection Exception</a></td><td>28 (0x1C)</td><td>Fault</td><td>#HV</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#VMM_Communication_Exception">VMM Communication Exception</a></td><td>29 (0x1D)</td><td>Fault</td><td>#VC</td><td>Yes</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Security_Exception">Security Exception</a></td><td>30 (0x1E)</td><td>Fault</td><td>#SX</td><td>Yes</td></tr><tr><td>Reserved</td><td>31 (0x1F)</td><td>-</td><td>-</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#Triple_Fault">Triple Fault</a></td><td>-</td><td>-</td><td>-</td><td>No</td></tr><tr><td><a href="https://wiki.osdev.org/Exceptions#FPU_Error_Interrupt">FPU Error Interrupt</a></td><td>IRQ 13</td><td>Interrupt</td><td>#FERR</td><td>No</td></tr><tr><td>我们可以照着这个格式进行初始化：</td><td></td><td></td><td></td><td></td></tr></tbody></table><p>在 <code>x86</code> 架构中，只有 <a href="http://en.wikipedia.org/wiki/Long_mode">long mode</a> 中断门和陷阱门可以在 <code>x86_64</code> 中引用。就像 <code>全局描述符表</code>，<code>中断描述符表</code> 在 <code>x86</code> 上是一个 8 字节数组门，而在 <code>x86_64</code> 上是一个 16 字节数组门。与 <code>全局描述符表</code> 不一样的是，<code>中断描述符表</code> 的第一个元素可以是一个门。它并不是强制要求的。</p><p>在 <a href="https://github.com/torvalds/linux/blob/master/arch/x86/boot/pm.c">arch&#x2F;x86&#x2F;boot&#x2F;pm.c</a>中。<code>中断描述符表</code> 可以在线性地址空间和基址的任何地方被加载，只要在 <code>x86</code> 上以 8 字节对齐，在 <code>x86_64</code> 上以 16 字节对齐。<code>IDT</code> 的基址存储在一个特殊的寄存器 - IDTR。在 <code>x86</code> 上有两个指令 - 协调工作来修改 <code>IDTR</code> 寄存器：</p><ul><li><code>LIDT</code>  ：用来加载 <code>IDT</code> 的基址，即在 <code>IDTR</code> 的指定操作数。</li><li><code>SIDT</code>  ：用来在指定操作数中读取和存储 <code>IDTR</code> 的内容。</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">+-----------------------------------+----------------------+</span><br><span class="line">|                                   |                      |</span><br><span class="line">|     Base address of the IDT       |   Limit of the IDT   |</span><br><span class="line">|                                   |                      |</span><br><span class="line">+-----------------------------------+----------------------+</span><br><span class="line">47                                16 15                    0</span><br></pre></td></tr></table></figure><p>让我们看看 <code>setup_idt</code> 的实现，我们准备了一个 <code>null_idt</code>，并且使用 <code>lidt</code> 指令把它加载到 <code>IDTR</code> 寄存器。注意，<code>null_idt</code> 是 <code>gdt_ptr</code> 类型，后者定义如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">gdt_ptr</span> &#123;</span></span><br><span class="line">        u16 len;</span><br><span class="line">        u32 ptr;</span><br><span class="line">&#125; __attribute__((packed));</span><br></pre></td></tr></table></figure><h2 id="Gate-Descriptor"><a href="#Gate-Descriptor" class="headerlink" title="Gate_Descriptor"></a>Gate_Descriptor</h2><p>现在，让我们看看 <code>IDT</code> 入口结构体，它是一个在 <code>x86</code> 中被称为门的 16 字节数组。它拥有下面的结构,Gate Descriptor (64-bit):</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">127                                                                             96</span><br><span class="line">+-------------------------------------------------------------------------------+</span><br><span class="line">|                                                                               |</span><br><span class="line">|                                Reserved                                       |</span><br><span class="line">|                                                                               |</span><br><span class="line">+--------------------------------------------------------------------------------</span><br><span class="line">95                                                                              64</span><br><span class="line">+-------------------------------------------------------------------------------+</span><br><span class="line">|                                                                               |</span><br><span class="line">|                               Offset 63..32                                   |</span><br><span class="line">|                                                                               |</span><br><span class="line">+-------------------------------------------------------------------------------+</span><br><span class="line">63                               48 47      46  44   42    39             34    32</span><br><span class="line">+-------------------------------------------------------------------------------+</span><br><span class="line">|                                  |       |  D  |   |     |      |   |   |     |</span><br><span class="line">|       Offset 31..16              |   P   |  P  | 0 |Type |0 0 0 | 0 | 0 | IST |</span><br><span class="line">|                                  |       |  L  |   |     |      |   |   |     |</span><br><span class="line"> -------------------------------------------------------------------------------+</span><br><span class="line">31                                   16 15                                      0</span><br><span class="line">+-------------------------------------------------------------------------------+</span><br><span class="line">|                                      |                                        |</span><br><span class="line">|          Segment Selector            |                 Offset 15..0           |</span><br><span class="line">|                                      |                                        |</span><br><span class="line">+-------------------------------------------------------------------------------+</span><br></pre></td></tr></table></figure><p>为了把索引格式化成 IDT 的格式，处理器把异常和中断向量分为 16 个级别。处理器处理异常和中断的发生就像它看到 <code>call</code> 指令时处理一个程序调用一样。处理器使用中断或异常的唯一的数字或 <code>中断标识码</code> 作为索引来寻找对应的 <code>中断描述符表</code> 的条目。现在让我们更近距离地看看 <code>IDT</code> 条目。</p><p>就像我们所看到的一样，在表中的 <code>IDT</code> 条目由下面的域组成：</p><ul><li><code>0-15</code> bits - 段选择器偏移，处理器用它作为中断处理程序的入口指针基址；</li><li><strong>Offset:</strong> A 64-bit value, split in three parts. It represents the address of the entry point of the <strong><a href="https://wiki.osdev.org/Interrupt_Service_Routines" title="Interrupt Service Routines">Interrupt Service Routine</a></strong>.</li><li><code>16-31</code> bits - 段选择器基址，包含中断处理程序入口指针；</li><li><strong>Selector:</strong> A <strong><a href="https://wiki.osdev.org/Segment_Selector" title="Segment Selector">Segment Selector</a></strong> with multiple fields which must point to a valid code segment in your <strong><a href="https://wiki.osdev.org/GDT" title="GDT">GDT</a></strong>.</li><li><code>IST</code> - 在 <code>x86_64</code> 上的一个新的机制，下面我们会介绍它；</li><li><strong>IST:</strong> A 3-bit value which is an offset into the <strong>Interrupt Stack Table</strong>, which is stored in the <strong><a href="https://wiki.osdev.org/Task_State_Segment" title="Task State Segment">Task State Segment</a></strong>. If the bits are all set to zero, the <strong>Interrupt Stack Table</strong> is not used.</li><li><code>DPL</code> - 描述符特权级；</li><li><strong>DPL:</strong> A 2-bit value which defines the <strong><a href="https://wiki.osdev.org/Security#Rings" title="Security">CPU Privilege Levels</a></strong> which are allowed to access this interrupt via the <strong>INT</strong> instruction. Hardware interrupts ignore this mechanism.</li><li><code>P</code> - 段存在标志；</li><li><strong>P:</strong> Present bit. Must be set (<strong>1</strong>) for the descriptor to be valid.</li><li><code>48-63</code> bits - 中断处理程序基址的第二部分；</li><li><code>64-95</code> bits - 中断处理程序基址的第三部分；</li><li><code>96-127</code> bits - CPU 保留位.</li><li><code>Type</code> 域描述了 <code>IDT</code> 条目的类型。有三种不同的中断处理程序：<ul><li>中断门（Interrupt gate）<br>  <strong>0b1110</strong> 或 <strong>0xE</strong>：64 位中断门</li><li>陷入门（Trap gate）<br>  <strong>0b1111</strong> 或 <strong>0xF</strong>：64 位陷阱门</li><li>任务门（Task gate）</li></ul></li></ul><p><code>IST</code> 或者说是 <code>Interrupt Stack Table</code> 是 <code>x86_64</code> 中的新机制，它用来代替传统的栈切换机制。之前的 <code>x86</code> 架构提供的机制可以在响应中断时自动切换栈帧。<code>IST</code> 是 <code>x86</code> 栈切换模式的一个修改版，在它使能之后可以无条件地切换栈，并且可以被任何与确定中断（我们将在下面介绍它）关联的 <code>IDT</code> 条目中的中断使能。从这里可以看出，<code>IST</code> 并不是所有的中断必须的，一些中断可以继续使用传统的栈切换模式。<code>IST</code> 机制在<a href="http://en.wikipedia.org/wiki/Task_state_segment">任务状态段（Task State Segment）</a>或者 <code>TSS</code> 中提供了 7 个 <code>IST</code> 指针。<code>TSS</code> 是一个包含进程信息的特殊结构，用来在执行中断或者处理 Linux 内核异常的时候做栈切换。每一个指针都被 <code>IDT</code> 中的中断门引用。</p><p><code>中断描述符表(idt)</code> 使用 <code>gate_desc</code> 的数组描述：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">extern</span> gate_desc idt_table[];</span><br></pre></td></tr></table></figure><p><code>gate_desc</code> 定义如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">gate_struct64</span> <span class="title">gate_desc</span>;</span></span><br></pre></td></tr></table></figure><p><code>gate_struct64</code> 定义如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">gate_struct64</span> &#123;</span></span><br><span class="line">        u16 offset_low;</span><br><span class="line">        u16 segment;</span><br><span class="line">        <span class="type">unsigned</span> ist : <span class="number">3</span>, zero0 : <span class="number">5</span>, type : <span class="number">5</span>, dpl : <span class="number">2</span>, p : <span class="number">1</span>;</span><br><span class="line">        u16 offset_middle;</span><br><span class="line">        u32 offset_high;</span><br><span class="line">        u32 zero1;</span><br><span class="line">&#125; __attribute__((packed));</span><br></pre></td></tr></table></figure><h2 id="中断处理"><a href="#中断处理" class="headerlink" title="中断处理"></a>中断处理</h2><p>当我们发生异常或中断时，系统首先会判断权限字段(安全处理)，权限通过则进入指定的处理函数，而所有的中断门的中断处理函数都是同一个，它首先是一段汇编代码，汇编代码操作如下：</p><ul><li>执行SAVE_ALL宏，保存<strong>中断向量号</strong>和<strong>寄存器上下文</strong>至当前运行进程的内核栈或者硬中断请求栈(当内核栈大小为8K时保存在内核栈，若为4K，则保存在硬中断请求栈)。</li><li>调用**do_IRQ()**函数。</li><li>跳转到ret_from_intr，这是一段汇编代码，主要用于判断是否需要进行调度。</li></ul><p>每个能够产生中断的设备或者模块都会在内核中注册一个<strong>中断服务例程(ISR)</strong>，当产生中断时，中断处理程序会被执行，在中断处理程序中，首先会保存中断向量号和上下文，之后执行中断线对应的中断服务例程。对于CPU来说，中断线是非常宝贵的资源，而由于计算机的发展，外部设备数量和种类越来越多，导致了中断线资源不足的情况，linux为了应对这种情况，实现了两种中断线分配方式，分别是：<strong>共享中断线，中断线动态分配</strong>。</p><ul><li>共享中断线</li></ul><p>　　多个设备共用一条中断线，当此条中断线发生中断时，因为不可能预先知道哪个特定的设备产生了中断，因此，这条中断线上的每个中断服务例程都会被执行，以验证是哪个设备产生的中断(一般的，设备产生中断时，会标记自己的状态寄存器，中断服务例程通过检查每个设备的状态寄存器来查找产生中断的设备)。</p><ul><li>中断线动态分配</li></ul><p>　　一条中断线在可能使用的时刻才与一个设备驱动程序关联起来，这样一来，即使几个硬件设备并不共享中断线，同一个中断向量也可以由这几个设备在不同时刻运行。</p><p>　共享中断线的分配方式是比较常见的，一次典型的基于共享中断线的中断处理流程如下：</p><p>由于中断处于中断上下文中，所以在中断处理过程中，会有以下几个特性：</p><ul><li>中断处理程序正在运行时，CPU会通知中断控制器屏蔽产生此中断的中断线。此中断线发出的信号被暂时忽略，当中断处理程序结束时恢复此中断线。</li><li>在中断服务例程的设计中，原则上是立即处理紧急的操作，将非紧急的操作延后处理(交给软中断进行处理)。</li><li>中断处理程序是运行在中断上下文，但是其是代表进程运行的，因此它所代表的进行必须处于TASK_RUNNING状态，否则可能出现僵死情况，因此在中断处理程序中不能执行任何阻塞过程。</li></ul><h2 id="中断描述符"><a href="#中断描述符" class="headerlink" title="中断描述符"></a>中断描述符</h2><p>中断描述符用于描述IRQ号的属性与状态，每个IRQ号都有它自己的中断描述符，这些中断描述符用一个数组保存， 这个数组就是<strong>中断描述符表</strong>，整个中断描述符表长度为NR_IRQS（通常为224）项。而当系统中的中断号允许超过224项时， 会用一个radix_tree来组织这些中断描述符， 每次新分配一个IRQ号时， 就会生成对应的中断描述符， 放入radix_tree中。当产生一个中断或者异常时，首先会从中断向量表中获取到一个中断向量号时(此中断向量号有可能表示中断，也可能表示的是一个异常)，如果是一个中断导致的，会执行do_IRQ()函数，而在do_IRQ()函数中，会根据中断向量号，从中断描述符表中获取对应的中断描述符，如下图：</p><p>整个中断描述符结构如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">irq_desc</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irq_data</span>        <span class="title">irq_data</span>;</span></span><br><span class="line">    <span class="comment">/* irq的统计信息，在proc中可查到 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> __percpu    *kstat_irqs;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* 回调函数，当此中断产生中断时，会调用handle_irq，在handle_irq中进行遍历irqaction链表</span></span><br><span class="line"><span class="comment">     * handle_simple_irq  用于简单处理；</span></span><br><span class="line"><span class="comment">     * handle_level_irq  用于电平触发中断的流控处理；</span></span><br><span class="line"><span class="comment">     * handle_edge_irq  用于边沿触发中断的流控处理；</span></span><br><span class="line"><span class="comment">     * handle_fasteoi_irq  用于需要响应eoi的中断控制器；</span></span><br><span class="line"><span class="comment">     * handle_percpu_irq  用于只在单一cpu响应的中断；</span></span><br><span class="line"><span class="comment">     * handle_nested_irq  用于处理使用线程的嵌套中断；</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">irq_flow_handler_t</span>    handle_irq;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_IRQ_PREFLOW_FASTEOI</span></span><br><span class="line">    <span class="type">irq_preflow_handler_t</span>    preflow_handler;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="comment">/* 中断服务例程链表 */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irqaction</span>    *<span class="title">action</span>;</span>    <span class="comment">/* IRQ action list */</span></span><br><span class="line">    <span class="comment">/* 状态 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        status_use_accessors;</span><br><span class="line">    <span class="comment">/* 函数调用中使用，另一个名称为istate */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        core_internal_state__do_not_mess_with_it;</span><br><span class="line">    <span class="comment">/* 嵌套深度，中断线被激活显示0，如果为正数，表示被禁止次数 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        depth;        <span class="comment">/* nested irq disables */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        wake_depth;    <span class="comment">/* nested wake enables */</span></span><br><span class="line">    <span class="comment">/* 此中断线上发生的中断次数 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        irq_count;    <span class="comment">/* For detecting broken IRQs */</span></span><br><span class="line">    <span class="comment">/* 上次发生未处理中断时的jiffies值 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span>        last_unhandled;    <span class="comment">/* Aging timer for unhandled count */</span></span><br><span class="line">    <span class="comment">/* 中断线上无法处理的中断次数，如果当第100000次中断发生时，有超过99900次是意外中断，系统会禁止这条中断线 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        irqs_unhandled;</span><br><span class="line">    <span class="type">atomic_t</span>        threads_handled;</span><br><span class="line">    <span class="type">int</span>            threads_handled_last;</span><br><span class="line">    <span class="comment">/* 锁 */</span></span><br><span class="line">    <span class="type">raw_spinlock_t</span>        lock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">cpumask</span>        *<span class="title">percpu_enabled</span>;</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_SMP</span></span><br><span class="line">    <span class="comment">/* CPU亲和力关系，其实就是每个CPU是占一个bit长度，某CPU上置为1表明该CPU可以进行这个中断的处理 */</span></span><br><span class="line">    <span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">cpumask</span>    *<span class="title">affinity_hint</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irq_affinity_notify</span> *<span class="title">affinity_notify</span>;</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_GENERIC_PENDING_IRQ</span></span><br><span class="line">    <span class="comment">/* 用于调整irq在各个cpu之间的平衡 */</span></span><br><span class="line">    <span class="type">cpumask_var_t</span>        pending_mask;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span>        threads_oneshot;</span><br><span class="line">    <span class="type">atomic_t</span>        threads_active;</span><br><span class="line">    <span class="comment">/* 用于synchronize_irq()，等待该irq所有线程完成 */</span></span><br><span class="line">    <span class="type">wait_queue_head_t</span>       wait_for_threads;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_PM_SLEEP</span></span><br><span class="line">    <span class="comment">/* irqaction数量 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        nr_actions;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        no_suspend_depth;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        force_resume_depth;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_PROC_FS</span></span><br><span class="line">    <span class="comment">/* 指向与IRQn相关的/proc/irq/n目录的描述符 */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">proc_dir_entry</span>    *<span class="title">dir</span>;</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="type">int</span>            parent_irq;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">module</span>        *<span class="title">owner</span>;</span></span><br><span class="line">    <span class="comment">/* 在/proc/interrupts所显示名称 */</span></span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>        *name;</span><br><span class="line">&#125; ____cacheline_internodealigned_in_smp;</span><br></pre></td></tr></table></figure><p><code>core_internal_state__do_not_mess_with_it</code>成员是用于记录此中断线状态的，中断线状态有如下几种形式：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">IRQS_AUTODETECT    　　　　<span class="comment">/* 该IRQ线用来进行硬件设备探测 */</span></span><br><span class="line">    IRQS_SPURIOUS_DISABLED    <span class="comment">/* 该IRQ线被禁止，是由于产生了欺骗性中断 */</span></span><br><span class="line">    IRQS_POLL_INPROGRESS      <span class="comment">/* 该IRQ进行轮询检查是否发生中断 */</span></span><br><span class="line">    IRQS_ONESHOT　　　　　　　　<span class="comment">/* 此IRQ没有在主处理函数中进行unmasked处理 */</span></span><br><span class="line">    IRQS_REPLAY　　　　　　　　 <span class="comment">/* IRQ线已被禁止，但前一个出现的中断还没有被应答 */</span></span><br><span class="line">    IRQS_WAITING　　　　　　　　<span class="comment">/* 进行硬件设备探测时，会将所有没有挂载中断服务程序的IRQ线状态设置为IRQS_WAITING，如果该IRQ上有中断产生，就清除这个状态，可以推断哪些引脚产生过中断 */</span></span><br><span class="line">    IRQS_PENDING　　　　　　　　<span class="comment">/* IRQ已经被应答(挂起)，但是内核还没有进行处理 */</span></span><br><span class="line">    IRQS_SUSPENDED　　　　　　 <span class="comment">/* 此IRQ被延迟 */</span></span><br></pre></td></tr></table></figure><h2 id="中断服务例程-ISR"><a href="#中断服务例程-ISR" class="headerlink" title="中断服务例程(ISR)"></a>中断服务例程(ISR)</h2><p>　中断服务例程用于描述一个设备的中断处理(区别与中断处理函数)，每个申请了中断的外部设备都会有一个中断服务例程，其作用就是执行对应设备的中断处理。当多个设备共享IRQ线时，内核会将此IRQ线上所有设备的中断服务例程组织成一个链表并保存在中断描述符中，当此IRQ线产生中断时，中断处理函数会依次执行此IRQ线上的中断服务例程。内核使用struct irqaction描述一个中断服务例程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">irqaction</span> &#123;</span></span><br><span class="line">    <span class="comment">/* 此中断服务例程的中断处理函数 */</span></span><br><span class="line">    <span class="type">irq_handler_t</span>        handler;</span><br><span class="line">    <span class="comment">/* 设备ID，一般用于指向中断处理时需要的数据结构传入handler */</span></span><br><span class="line">    <span class="type">void</span>            *dev_id;</span><br><span class="line">    <span class="comment">/* 此中断服务例程在CPU上所对应的设备ID */</span></span><br><span class="line">    <span class="type">void</span> __percpu        *percpu_dev_id;</span><br><span class="line">    <span class="comment">/* 链表中下一个中断服务例程 */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irqaction</span>    *<span class="title">next</span>;</span></span><br><span class="line">    <span class="comment">/* 进行中断处理的内核线程执行函数 */</span></span><br><span class="line">    <span class="type">irq_handler_t</span>        thread_fn;</span><br><span class="line">    <span class="comment">/* 一个内核线程，用于执行中断处理 */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span>    *<span class="title">thread</span>;</span></span><br><span class="line">    <span class="comment">/* IRQ线，IRQ号 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        irq;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span>        flags;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span>        thread_flags;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span>        thread_mask;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>        *name;</span><br><span class="line">    <span class="comment">/* 指向/proc/irq/n目录的描述符 */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">proc_dir_entry</span>    *<span class="title">dir</span>;</span></span><br><span class="line">&#125; ____cacheline_internodealigned_in_smp;</span><br></pre></td></tr></table></figure><h2 id="irq-stat数组"><a href="#irq-stat数组" class="headerlink" title="irq_stat数组"></a>irq_stat数组</h2><p>此数组包含NR_CPUS个元素，系统中每个CPU对应数组中的一个元素。每个元素的类型为irq_cpustat_t，其包含几个计数器和内核记录CPU正在做什么的标志。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> __softirq_pending;        <span class="comment">/* 表示挂起的软中断，每一位表示一个软中断，为1表示挂起 */</span></span><br><span class="line">    <span class="type">long</span> idle_timestamp;                    <span class="comment">/* CPU变为空闲的时间点 */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 硬中断统计. */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> irq_timer_count;            <span class="comment">/* 定时器中断统计 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> irq_syscall_count;            <span class="comment">/* 系统调用中断统计 */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> irq_resched_count;        </span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> irq_hv_flush_count;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> irq_call_count;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> irq_hv_msg_count;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> irq_dev_intr_count;</span><br><span class="line"></span><br><span class="line">&#125; ____cacheline_aligned <span class="type">irq_cpustat_t</span>;</span><br></pre></td></tr></table></figure><h2 id="关系"><a href="#关系" class="headerlink" title="关系"></a>关系</h2><p><strong>中断描述符(struct irq_desc)，中断控制器描述符(struct irq_chip)，中断服务例程(struct irqaction)</strong>。它们的组织形式如下：</p><h2 id="深入Linux内核中的中断和异常处理"><a href="#深入Linux内核中的中断和异常处理" class="headerlink" title="深入Linux内核中的中断和异常处理"></a>深入Linux内核中的中断和异常处理</h2><p>在 <code>x86_64</code> 架构中，每一个活动的线程在 Linux 内核中都有一个很大的栈。这个栈的大小由 <code>THREAD_SIZE</code> 定义，而且与下面的定义相等：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#define PAGE_SHIFT      12</span><br><span class="line">#define PAGE_SIZE       (_AC(1,UL) &lt;&lt; PAGE_SHIFT)</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">#define THREAD_SIZE_ORDER       (2 + KASAN_STACK_ORDER)</span><br><span class="line">#define THREAD_SIZE  (PAGE_SIZE &lt;&lt; THREAD_SIZE_ORDER)</span><br></pre></td></tr></table></figure><p><code>PAGE_SIZE</code> 是 <code>4096</code> 字节，<code>THREAD_SIZE_ORDER</code> 的值依赖于 <code>KASAN_STACK_ORDER</code>。就像我们看到的，<code>KASAN_STACK</code> 依赖于 <code>CONFIG_KASAN</code> 内核配置参数，它定义如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#ifdef CONFIG_KASAN</span><br><span class="line">    #define KASAN_STACK_ORDER 1</span><br><span class="line">#else</span><br><span class="line">    #define KASAN_STACK_ORDER 0</span><br><span class="line">#endif</span><br></pre></td></tr></table></figure><p><code>KASan</code> 是一个运行时内存<a href="http://lwn.net/Articles/618180/">调试器</a>。所以，如果 <code>CONFIG_KASAN</code> 被禁用，<code>THREAD_SIZE</code> 是 <code>16384</code> ；如果内核配置选项打开，<code>THREAD_SIZE</code> 的值是 <code>32768</code>。这块栈空间保存着有用的数据，只要线程是活动状态或者僵尸状态。但是当线程在用户空间的时候，这个内核栈是空的，除非 <code>thread_info</code> 结构在这个栈空间的底部。活动的或者僵尸线程并不是在他们栈中的唯一的线程，与每一个 CPU 关联的特殊栈也存在于这个空间。当内核在这个 CPU 上执行代码的时候，这些栈处于活动状态；当在这个 CPU 上执行用户空间代码时，这些栈不包含任何有用的信息。每一个 CPU 也有一个特殊的 per-cpu 栈。首先是给外部中断使用的 <code>中断栈（interrupt stack）</code>。它的大小定义如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#define IRQ_STACK_ORDER (2 + KASAN_STACK_ORDER)</span><br><span class="line">#define IRQ_STACK_SIZE (PAGE_SIZE &lt;&lt; IRQ_STACK_ORDER)</span><br></pre></td></tr></table></figure><p>或者是 <code>16384</code> 字节。Per-cpu 的中断栈在 <code>x86_64</code> 架构中使用 <code>irq_stack_union</code> 联合描述:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">union</span> <span class="title">irq_stack_union</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> irq_stack[IRQ_STACK_SIZE];</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">        <span class="type">char</span> gs_base[<span class="number">40</span>];</span><br><span class="line">        <span class="type">unsigned</span> <span class="type">long</span> stack_canary;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>第一个 <code>irq_stack</code> 域是一个 16KB 的数组。然后你可以看到 <code>irq_stack_union</code> 联合包含了一个结构体，这个结构体有两个域：</p><ul><li><code>gs_base</code> - 总是指向 <code>irqstack</code> 联合底部的 <code>gs</code> 寄存器。在 <code>x86_64</code> 中， per-cpu 和 stack canary 共享 <code>gs</code> 寄存器。所有的 per-cpu 标志初始值为零，并且 <code>gs</code> 指向 per-cpu 区域的开始。你已经知道<a href="http://en.wikipedia.org/wiki/Memory_segmentation">段内存模式</a>已经废除很长时间了，但是我们可以使用<a href="http://en.wikipedia.org/wiki/Model-specific_register">特殊模块寄存器（Model specific registers）</a>给这两个段寄存器 - <code>fs</code> 和 <code>gs</code> 设置基址，并且这些寄存器仍然可以被用作地址寄存器。如果你记得 Linux 内核初始程序的第一<a href="http://xinqiu.gitbooks.io/linux-insides-cn/content/Initialization/linux-initialization-1.html">部分</a>，你会记起我们设置了 <code>gs</code> 寄存器：</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">movl    $MSR_GS_BASE,%ecx</span><br><span class="line">movl    initial_gs(%rip),%eax</span><br><span class="line">movl    initial_gs+4(%rip),%edx</span><br><span class="line">wrmsr</span><br></pre></td></tr></table></figure><p><code>initial_gs</code> 指向 <code>irq_stack_union</code>:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">GLOBAL(initial_gs)</span><br><span class="line">.quad    INIT_PER_CPU_VAR(irq_stack_union)</span><br></pre></td></tr></table></figure><ul><li><code>stack_canary</code> - <a href="http://en.wikipedia.org/wiki/Stack_buffer_overflow#Stack_canaries">Stack canary</a> 对于中断栈来说是一个用来验证栈是否已经被修改的 <code>栈保护者（stack protector）</code>。<code>gs_base</code> 是一个 40 字节的数组，<code>GCC</code> 要求 stack canary 在被修正过的偏移量上，并且 <code>gs</code> 的值在 <code>x86_64</code> 架构上必须是 <code>40</code>，在 <code>x86</code> 架构上必须是 <code>20</code>。</li></ul><p><code>irq_stack_union</code> 是 <code>percpu</code> 的第一个数据, 我们可以在 <code>System.map</code>中看到它：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0000000000000000 D __per_cpu_start</span><br><span class="line">0000000000000000 D irq_stack_union</span><br><span class="line">0000000000004000 d exception_stacks</span><br><span class="line">0000000000009000 D gdt_page</span><br></pre></td></tr></table></figure><p>我们可以看到它在代码中的定义:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">DECLARE_PER_CPU_FIRST(union irq_stack_union, irq_stack_union) __visible;</span><br></pre></td></tr></table></figure><p>现在，是时候来看 <code>irq_stack_union</code> 的初始化过程了。除了 <code>irq_stack_union</code> 的定义，我们可以在<a href="https://github.com/torvalds/linux/blob/master/arch/x86/include/asm/processor.h">arch&#x2F;x86&#x2F;include&#x2F;asm&#x2F;processor.h</a>中查看下面的 per-cpu 变量</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">DECLARE_PER_CPU(char *, irq_stack_ptr);</span><br><span class="line">DECLARE_PER_CPU(unsigned int, irq_count);</span><br></pre></td></tr></table></figure><p>第一个就是 <code>irq_stack_ptr</code>。从这个变量的名字中可以知道，它显然是一个指向这个栈顶的指针。第二个 <code>irq_count</code> 用来检查 CPU 是否已经在中断栈。<code>irq_stack_ptr</code> 的初始化在<a href="https://github.com/torvalds/linux/blob/master/arch/x86/kernel/setup_percpu.c">arch&#x2F;x86&#x2F;kernel&#x2F;setup_percpu.c</a>的 <code>setup_per_cpu_areas</code> 函数中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> __init <span class="title function_">setup_per_cpu_areas</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_64</span></span><br><span class="line">for_each_possible_cpu(cpu) &#123;</span><br><span class="line">    ...</span><br><span class="line">    ...</span><br><span class="line">    ...</span><br><span class="line">    per_cpu(irq_stack_ptr, cpu) =</span><br><span class="line">            per_cpu(irq_stack_union.irq_stack, cpu) +</span><br><span class="line">            IRQ_STACK_SIZE - <span class="number">64</span>;</span><br><span class="line">    ...</span><br><span class="line">    ...</span><br><span class="line">    ...</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在，我们一个一个查看所有 CPU，并且设置 <code>irq_stack_ptr</code>。事实证明它等于中断栈的顶减去 <code>64</code>。为什么是 <code>64</code>？TODO [<a href="https://github.com/torvalds/linux/blob/master/arch/x86/kernel/cpu/common.c">arch&#x2F;x86&#x2F;kernel&#x2F;cpu&#x2F;common.c</a>] 代码如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">void load_percpu_segment(int cpu)</span><br><span class="line">&#123;</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">        loadsegment(gs, 0);</span><br><span class="line">        wrmsrl(MSR_GS_BASE, (unsigned long)per_cpu(irq_stack_union.gs_base, cpu));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>就像我们所知道的一样，<code>gs</code> 寄存器指向中断栈的栈底：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">movl    $MSR_GS_BASE,%ecx</span><br><span class="line">    movl    initial_gs(%rip),%eax</span><br><span class="line">    movl    initial_gs+4(%rip),%edx</span><br><span class="line">    wrmsr</span><br><span class="line"></span><br><span class="line">    GLOBAL(initial_gs)</span><br><span class="line">    .quad    INIT_PER_CPU_VAR(irq_stack_union)</span><br></pre></td></tr></table></figure><p>现在我们可以看到 <code>wrmsr</code> 指令，这个指令从 <code>edx:eax</code> 加载数据到 被 <code>ecx</code> 指向的<a href="https://xinqiu.gitbooks.io/linux-insides-cn/content/Interrupts/(http:/en.wikipedia.org/wiki/Model-specific_register)">MSR寄存器</a>。在这里MSR寄存器是 <code>MSR_GS_BASE</code>，它保存了被 <code>gs</code> 寄存器指向的内存段的基址。<code>edx:eax</code> 指向 <code>initial_gs</code> 的地址，它就是 <code>irq_stack_union</code> 的基址。</p><p>我们还知道，<code>x86_64</code> 有一个叫 <code>中断栈表（Interrupt Stack Table）</code> 或者 <code>IST</code> 的组件，当发生不可屏蔽中断、双重错误等等的时候，这个组件提供了切换到新栈的功能。这可以到达7个 <code>IST</code> per-cpu 入口。其中一些如下; There can be up to seven <code>IST</code> entries per-cpu. Some of them are:</p><ul><li><code>DOUBLEFAULT_STACK</code></li><li><code>NMI_STACK</code></li><li><code>DEBUG_STACK</code></li><li><code>MCE_STACK</code></li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#define DOUBLEFAULT_STACK 1</span><br><span class="line">#define NMI_STACK 2</span><br><span class="line">#define DEBUG_STACK 3</span><br><span class="line">#define MCE_STACK 4</span><br></pre></td></tr></table></figure><p>所有被 <code>IST</code> 切换到新栈的中断门描述符都由 <code>set_intr_gate_ist</code> 函数初始化。例如:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">set_intr_gate_ist(X86_TRAP_NMI, &amp;nmi, NMI_STACK);</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">set_intr_gate_ist(X86_TRAP_DF, &amp;double_fault, DOUBLEFAULT_STACK);</span><br></pre></td></tr></table></figure><p>其中 <code>&amp;nmi</code> 和 <code>&amp;double_fault</code> 是中断函数的入口地址：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">asmlinkage void nmi(void);</span><br><span class="line">asmlinkage void double_fault(void);</span><br></pre></td></tr></table></figure><p>定义在 <a href="https://github.com/torvalds/linux/blob/master/arch/x86/kernel/entry_64.S">arch&#x2F;x86&#x2F;kernel&#x2F;entry_64.S</a>中</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">idtentry double_fault do_double_fault has_error_code=1 paranoid=2</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">ENTRY(nmi)</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">END(nmi)</span><br></pre></td></tr></table></figure><p>当一个中断或者异常发生时，新的 <code>ss</code> 选择器被强制置为 <code>NULL</code>，并且 <code>ss</code> 选择器的 <code>rpl</code> 域被设置为新的 <code>cpl</code>。旧的 <code>ss</code>、<code>rsp</code>、寄存器标志、<code>cs</code>、<code>rip</code> 被压入新栈。在 64 位模型下，中断栈帧大小固定为 8 字节，所以我们可以得到下面的栈:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">+---------------+</span><br><span class="line">|               |</span><br><span class="line">|      SS       | 40</span><br><span class="line">|      RSP      | 32</span><br><span class="line">|     RFLAGS    | 24</span><br><span class="line">|      CS       | 16</span><br><span class="line">|      RIP      | 8</span><br><span class="line">|   Error code  | 0</span><br><span class="line">|               |</span><br><span class="line">+---------------+</span><br></pre></td></tr></table></figure><p>如果在中断门中 <code>IST</code> 域不是 <code>0</code>，我们把 <code>IST</code> 读到 <code>rsp</code> 中。如果它关联了一个中断向量错误码，我们再把这个错误码压入栈。如果中断向量没有错误码，就继续并且把虚拟错误码压入栈。我们必须做以上的步骤以确保栈一致性。接下来我们从门描述符中加载段选择器域到 CS 寄存器中，并且通过验证第 <code>21</code> 位的值来验证目标代码是一个 64 位代码段，例如 <code>L</code> 位在 <code>全局描述符表（Global Descriptor Table）</code>。最后我们从门描述符中加载偏移域到 <code>rip</code> 中，<code>rip</code> 是中断处理函数的入口指针。然后中断函数开始执行，在中断函数执行结束后，它必须通过 <code>iret</code> 指令把控制权交还给被中断进程。<code>iret</code> 指令无条件地弹出栈指针（<code>ss:rsp</code>）来恢复被中断的进程，并且不会依赖于 <code>cpl</code> 改变。</p><p>Linux内核中关于 <code>x86_64</code>架构的代码中与中断相关的最早期代码出现在 <a href="https://github.com/torvalds/linux/blob/16f73eb02d7e1765ccab3d2018e0bd98eb93d973/arch/x86/boot/pm.c">arch&#x2F;x86&#x2F;boot&#x2F;pm.c</a>文件中，该文件首次配置了 <a href="http://en.wikipedia.org/wiki/Interrupt_descriptor_table">中断描述符表</a>(IDT)。对IDT的配置在<code>go_to_protected_mode</code>函数中完成，该函数首先调用了 <code>setup_idt</code>函数配置了IDT，然后将处理器的工作模式切换为 <a href="http://en.wikipedia.org/wiki/Protected_mode">保护模式</a>:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">go_to_protected_mode</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">        ...</span><br><span class="line">        setup_idt();</span><br><span class="line">        ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>setup_idt</code>函数在同一文件中定义，它仅仅是用 <code>NULL</code>填充了中断描述符表:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">static void setup_idt(void)</span><br><span class="line">&#123;</span><br><span class="line">        static const struct gdt_ptr null_idt = &#123;0, 0&#125;;</span><br><span class="line">        asm volatile(&quot;lidtl %0&quot; : : &quot;m&quot; (null_idt));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>gdt_ptr</code>表示了一个48-bit的特殊功能寄存器 <code>GDTR</code>，其包含了全局描述符表 <code>Global Descriptor</code>的基地址:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">gdt_ptr</span> &#123;</span></span><br><span class="line">        u16 len;</span><br><span class="line">        u32 ptr;</span><br><span class="line">&#125; __attribute__((packed));</span><br></pre></td></tr></table></figure><p>显然，在此处的 <code>gdt_prt</code>不是代表 <code>GDTR</code>寄存器而是代表 <code>IDTR</code>寄存器，因为我们将其设置到了中断描述符表中。之所以在Linux内核代码中没有<code>idt_ptr</code>结构体，是因为其与<code>gdt_prt</code>具有相同的结构而仅仅是名字不同，因此没必要定义两个重复的数据结构。可以看到，内核在此处并没有填充<code>Interrupt Descriptor Table</code>，这是因为此刻处理任何中断或异常还为时尚早，因此我们仅仅以<code>NULL</code>来填充<code>IDT</code>。</p><p>在设置完 <a href="http://en.wikipedia.org/wiki/Interrupt_descriptor_table">Interrupt descriptor table</a>, <a href="http://en.wikipedia.org/wiki/GDT">Global Descriptor Table</a>和其他一些东西以后，内核开始进入保护模式，这部分代码在 <a href="https://github.com/torvalds/linux/blob/16f73eb02d7e1765ccab3d2018e0bd98eb93d973/arch/x86/boot/pmjump.S">arch&#x2F;x86&#x2F;boot&#x2F;pmjump.S</a>中实现.</p><p>进入保护模式的代码位于 <code>boot_params.hdr.code32_start</code>，你可以在 <a href="https://github.com/torvalds/linux/blob/16f73eb02d7e1765ccab3d2018e0bd98eb93d973/arch/x86/boot/pm.c">arch&#x2F;x86&#x2F;boot&#x2F;pm.c</a>的末尾看到内核将入口函数指针和启动参数 <code>boot_params</code>传递给了 <code>protected_mode_jump</code>函数:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">protected_mode_jump(boot_params.hdr.code32_start,</span><br><span class="line">                            (u32)&amp;boot_params + (ds() &lt;&lt; <span class="number">4</span>));</span><br></pre></td></tr></table></figure><p><code>protected_mode_jump</code>通过一种<a href="http://en.wikipedia.org/wiki/Intel_8086">8086</a>的调用 <a href="http://en.wikipedia.org/wiki/X86_calling_conventions#List_of_x86_calling_conventions">约定</a>，通过 <code>ax</code>和 <code>dx</code>两个寄存器来获取参数:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">GLOBAL(protected_mode_jump)</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">        .byte   <span class="number">0x66</span>, <span class="number">0xea</span>              <span class="meta"># ljmpl opcode</span></span><br><span class="line"><span class="number">2</span>:      .<span class="type">long</span>   in_pm32                 <span class="meta"># offset</span></span><br><span class="line">        .word   __BOOT_CS               <span class="meta"># segment</span></span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">...</span><br><span class="line">ENDPROC(protected_mode_jump)</span><br></pre></td></tr></table></figure><p>其中 <code>in_pm32</code>包含了对32-bit入口的跳转语句:<br>&#96;</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">GLOBAL(in_pm32)</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">        jmpl    *%eax <span class="comment">// %eax contains address of the `startup_32`</span></span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">ENDPROC(in_pm32)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="初始化："><a href="#初始化：" class="headerlink" title="初始化："></a>初始化：</h2><p>而整个初始化的过程与中断处理过程相应，首先先初始化中断向量表，再初始化中断描述符表和中断描述符。中断控制器描述符是系统预定编写好的静态变量，如i8259A中断控制器对应的变量就是i8259A_chip。这时一个中断已经初始化完毕，之后驱动需要使用此中断时系统会将驱动中的中断处理加入到该中断的中断服务例程链表中。如下图</p><h3 id="中断api"><a href="#中断api" class="headerlink" title="中断api"></a>中断api</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 设置一个中断门</span></span><br><span class="line"><span class="comment">  * n：中断号 </span></span><br><span class="line"><span class="comment">  * addr:中断处理程序入口地址</span></span><br><span class="line"><span class="comment">  */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> set_intr_gate(n, addr)                        \</span></span><br><span class="line"><span class="meta">    do &#123;                                \</span></span><br><span class="line"><span class="meta">        BUG_ON((unsigned)n &gt; 0xFF);                \</span></span><br><span class="line"><span class="meta">        _set_gate(n, GATE_INTERRUPT, (void *)addr, 0, 0,    \</span></span><br><span class="line"><span class="meta">              __KERNEL_CS);                    \</span></span><br><span class="line"><span class="meta">        _trace_set_gate(n, GATE_INTERRUPT, (void *)trace_##addr,\</span></span><br><span class="line"><span class="meta">                0, 0, __KERNEL_CS);            \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 设置一个系统中断门 */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">set_system_intr_gate</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> n, <span class="type">void</span> *addr)</span></span><br><span class="line">&#123;</span><br><span class="line">    BUG_ON((<span class="type">unsigned</span>)n &gt; <span class="number">0xFF</span>);</span><br><span class="line">    _set_gate(n, GATE_INTERRUPT, addr, <span class="number">0x3</span>, <span class="number">0</span>, __KERNEL_CS);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 设置一个系统门 */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">set_system_trap_gate</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> n, <span class="type">void</span> *addr)</span></span><br><span class="line">&#123;</span><br><span class="line">    BUG_ON((<span class="type">unsigned</span>)n &gt; <span class="number">0xFF</span>);</span><br><span class="line">    _set_gate(n, GATE_TRAP, addr, <span class="number">0x3</span>, <span class="number">0</span>, __KERNEL_CS);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 设置一个陷阱门 */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">set_trap_gate</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> n, <span class="type">void</span> *addr)</span></span><br><span class="line">&#123;</span><br><span class="line">    BUG_ON((<span class="type">unsigned</span>)n &gt; <span class="number">0xFF</span>);</span><br><span class="line">    _set_gate(n, GATE_TRAP, addr, <span class="number">0</span>, <span class="number">0</span>, __KERNEL_CS);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 设置一个任务门 */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">set_task_gate</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> n, <span class="type">unsigned</span> <span class="type">int</span> gdt_entry)</span></span><br><span class="line">&#123;</span><br><span class="line">    BUG_ON((<span class="type">unsigned</span>)n &gt; <span class="number">0xFF</span>);</span><br><span class="line">    _set_gate(n, GATE_TASK, (<span class="type">void</span> *)<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, (gdt_entry&lt;&lt;<span class="number">3</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这几个函数用于设置不同门的API函数，他们的参数n都为中断号，而他们都会调用_set_gate()函数，只是参数不同，_set_gate()函数如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 设置一个门描述符，并写入中断向量表</span></span><br><span class="line"><span class="comment"> * gate: 中断号</span></span><br><span class="line"><span class="comment"> * type: 门类型</span></span><br><span class="line"><span class="comment"> * addr: 中断处理程序入口</span></span><br><span class="line"><span class="comment"> * dpl:  权限位</span></span><br><span class="line"><span class="comment"> * ist:  64位系统才使用</span></span><br><span class="line"><span class="comment"> * seg:  段选择符</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> _set_gate(<span class="type">int</span> gate, <span class="type">unsigned</span> type, <span class="type">void</span> *addr,</span><br><span class="line">                 <span class="type">unsigned</span> dpl, <span class="type">unsigned</span> ist, <span class="type">unsigned</span> seg)</span><br><span class="line">&#123;</span><br><span class="line">    gate_desc s;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 生成一个门描述符 */</span></span><br><span class="line">    pack_gate(&amp;s, type, (<span class="type">unsigned</span> <span class="type">long</span>)addr, dpl, ist, seg);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * does not need to be atomic because it is only done once at</span></span><br><span class="line"><span class="comment">     * setup time</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">/* 将新的门描述符写入中断向量表中的gate项，使用memcpy进行写入 */</span></span><br><span class="line">    write_idt_entry(idt_table, gate, &amp;s);</span><br><span class="line">    <span class="comment">/* 用于跟踪? 暂时还不清楚这个 trace_idt_table 的用途 */</span></span><br><span class="line">    write_trace_idt_entry(gate, &amp;s);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="初始化中断向量"><a href="#初始化中断向量" class="headerlink" title="初始化中断向量"></a>初始化中断向量</h3><p>虽然称之为中断向量表，其实对于CPU来说只是一个起始地址，此地址开始每向上8个字节为一个中断向量。我们的CPU上有一个idtr寄存器，它专门用于保存中断向量表地址，当产生一个中断时，CPU会自动从idtr寄存器保存的中断向量表地址处获取相应的中断向量，然后判断权限并跳转至中断处理函数。当计算机刚启动时，首先会启动引导程序(BIOS)，在BIOS中会把中断向量表存放在内存开始位置(0x00000000)。BIOS会有自己的一些默认中断处理函数，而当BIOS处理完后，会将计算机控制器转交给linux，而linux会在使用BIOS的中断向量表的同时重新设置新的中断向量表(新的地址保存在配置中的CONFIG_VECTORS_BASE)，之后会完全使用新的中断向量表。</p><p>一般的，我们也把中断向量表中的中断向量称为<strong>门描述符</strong>，其大小为64位，其主要保存了<strong>段选择符、权限位和中断处理程序入口地址</strong>。CPU主要将门分为三种：<strong>任务门，中断门，陷阱门</strong>。虽然CPU把门描述符分为了三种，但是linux为了处理更多种情况，把门描述符分为了五种，分别为<strong>中断门，系统门，系统中断门，陷阱门，任务门</strong>；但其存储结构与CPU定义的门不变。结构如下：<img src="/2024/11/15/Linux/interrupt/%E4%B8%AD%E6%96%AD%E5%92%8C%E5%BC%82%E5%B8%B8/#Gate_Descriptor"><br>门描述符的初始化主要分为两部分，我们知道，中断向量表中保存的是中断和异常，所以整个中断描述符的初始化需要分为中断初始化和异常初始化。而中断向量表的初始化情况是，第一部分是经过一段汇编代码对整个中断向量表进行初始化，第二部分是在系统进入start_kernel()函数后分别对异常和中断进行初始化。在linux中，中断向量表用idt_table[NR_VECTORS]数组进行描述，中断向量(门描述符)在系统中用struct desc_struct结构表示，具体我们可以往下看。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">__INIT</span><br><span class="line">setup_once:</span><br><span class="line"></span><br><span class="line">    movl $idt_table,%edi                           # idt_table就是中断向量表，地址保存到edi中</span><br><span class="line">    movl $early_idt_handlers,%eax                  # early_idt_handlers地址保存到eax中，early_idt_handlers是二维数组，每行9个字符</span><br><span class="line">    movl $NUM_EXCEPTION_VECTORS,%ecx               # NUM_EXCEPTION_VECTORS地址保存到ecx中，ecx用于循环，NUM_EXCEPTION_VECTORS为32</span><br><span class="line">1:</span><br><span class="line">    movl %eax,(%edi)                               # 将eax的值保存到edi保存的地址中</span><br><span class="line">    movl %eax,4(%edi)                              # 将eax的值保存到edi保存的地址+4中</span><br><span class="line">    /* interrupt gate, dpl=0, present */</span><br><span class="line">    movl $(0x8E000000 + __KERNEL_CS),2(%edi)       # 将(0x8E000000 + __KERNEL_CS)一共4个字节保存到edi保存的地址+2的位置中</span><br><span class="line">    addl $9,%eax                                   # eax += 9，指向early_idt_handlers数组下一列</span><br><span class="line">    addl $8,%edi                                   # edi += 8，就是下一个门描述符地址</span><br><span class="line">    loop 1b                                        # 根据ecx是否为0进行循环  </span><br><span class="line">    # 前32个中断向量初始化结果：</span><br><span class="line">    #  |63                         48|47                   32|</span><br><span class="line">#   31                         16|15                    0|</span><br><span class="line">    #  |early_idt_handlers[i](高16位)|        0x8E00         |     __KERNEL_CS      |early_idt_handlers[i](低16位)|                           </span><br><span class="line"></span><br><span class="line">    movl $256 - NUM_EXCEPTION_VECTORS,%ecx         # 256 - 32 保存到ecx，进行新一轮的循环</span><br><span class="line">    movl $ignore_int,%edx                          # ignore_int保存到edx</span><br><span class="line">    movl $(__KERNEL_CS &lt;&lt; 16),%eax                 # (__KERNEL_CS &lt;&lt; 16)保存到eax</span><br><span class="line">    movw %dx,%ax       </span><br><span class="line">    movw $0x8E00,%dx 2:</span><br><span class="line">    movl %eax,(%edi)</span><br><span class="line">    movl %edx,4(%edi)</span><br><span class="line">    addl $8,%edi                                   # edi += 8，就是下一个门描述符地址</span><br><span class="line">    loop 2b  </span><br><span class="line">    # 其他中断向量初始化结果：</span><br><span class="line">    #  |63                      48|47                   32|31                  16|15                       0|</span><br><span class="line">    #  |    ignore_int(高16位)    |        0x8E00         |     __KERNEL_CS      |    ignore_int(低16位)    |</span><br></pre></td></tr></table></figure><p>如果CPU是X86，之后会通过 lidt  idt_descr 命令将中断向量表(idt_descr)地址放入idtr寄存器；如果不是，则暂时不会将idt_descr放入idtr寄存器(在trap_init()函数再执行这步操作)。idtr寄存器一共是48位，低16位保存的是中断向量表长度，高32位保存的是中断向量表基地址。我们可以看看idt_descr的形式，如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">idt_descr:</span><br><span class="line">    .word IDT_ENTRIES*8-1        # 这里放入的是表长度， 256 * 8 - 1</span><br><span class="line">    .long idt_table                # idt_table地址放在这，idt_table定义在/arch/x86/kernel/trap.h中</span><br><span class="line"></span><br><span class="line">/* 我们再看看 idt_table 是怎么定义的，idt_table代表的就是中断向量表 */</span><br><span class="line">/* 代码地址：arch/x86/kernel/Traps.c */</span><br><span class="line">gate_desc idt_table[NR_VECTORS] __page_aligned_bss;</span><br><span class="line"></span><br><span class="line">/* 继续，看看 gate_desc ，用于描述一个中断向量 */</span><br><span class="line">#ifdef CONFIG_X86_64</span><br><span class="line">typedef struct gate_struct64 gate_desc;</span><br><span class="line">#else</span><br><span class="line">typedef struct desc_struct gate_desc;</span><br><span class="line">#endif</span><br><span class="line"></span><br><span class="line">/* 我们看看32位下的 struct desc_struct，此结构就是一个中断向量(门描述符)  */</span><br><span class="line">struct desc_struct &#123;</span><br><span class="line">    union &#123;</span><br><span class="line">        struct &#123;</span><br><span class="line">            unsigned int a;</span><br><span class="line">            unsigned int b;</span><br><span class="line">        &#125;;</span><br><span class="line">        struct &#123;</span><br><span class="line">            u16 limit0;</span><br><span class="line">            u16 base0;</span><br><span class="line">            unsigned base1: 8, type: 4, s: 1, dpl: 2, p: 1;</span><br><span class="line">            unsigned limit: 4, avl: 1, l: 1, d: 1, g: 1, base2: 8;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125; __attribute__((packed));</span><br></pre></td></tr></table></figure><p>可以看出，在汇编代码初始化部分，所有的门描述符的DPL权限位都设置为0(用户态不可访问)，段选择符设置为__KERNEL_CS内核代码段。而对于中断处理函数设置则不同，前32个门描述符的中断处理函数为early_idt_handlers，之后的门描述符的中断处理函数为ignore_int。而在linux中，0<del>19的中断向量是用于异常和陷阱。20</del>31的中断向量是intel保留使用的。</p><h3 id="初始化异常向量"><a href="#初始化异常向量" class="headerlink" title="初始化异常向量"></a>初始化异常向量</h3><p>异常向量作为在中断向量表中的前20个向量(0~19)，在汇编代码中已经将其的处理函数设置为early_idt_handlers，而进入start_kernel()函数后，系统会在trap_init()函数中重新设置它们的处理函数，由于异常和陷阱的特殊性，它们并没有像中断这样复杂的数据结构，单纯的，每个异常和陷阱有它们自己的中断处理函数，系统只是简单地把中断处理函数放入异常和陷阱的门描述符中。在了解trap_init()函数之前，我们需要先了解如下几个函数:<a href="#%E4%B8%AD%E6%96%ADapi">中断api</a></p><p>　　了解了以上的设置门描述符的函数，我们再看看trap_init()函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> __init <span class="title function_">trap_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 使用了EISA总线 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_EISA</span></span><br><span class="line">    <span class="type">void</span> __iomem *p = early_ioremap(<span class="number">0x0FFFD9</span>, <span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (readl(p) == <span class="string">&#x27;E&#x27;</span> + (<span class="string">&#x27;I&#x27;</span>&lt;&lt;<span class="number">8</span>) + (<span class="string">&#x27;S&#x27;</span>&lt;&lt;<span class="number">16</span>) + (<span class="string">&#x27;A&#x27;</span>&lt;&lt;<span class="number">24</span>))</span><br><span class="line">        EISA_bus = <span class="number">1</span>;</span><br><span class="line">    early_iounmap(p, <span class="number">4</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Interrupts/Exceptions */</span></span><br><span class="line"><span class="comment">//enum &#123;</span></span><br><span class="line"><span class="comment">//    X86_TRAP_DE = 0,    /*  0, 除0操作 Divide-by-zero */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_DB,        /*  1, 调试使用 Debug */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_NMI,        /*  2, 非屏蔽中断 Non-maskable Interrupt */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_BP,        /*  3, 断点 Breakpoint */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_OF,        /*  4, 溢出 Overflow */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_BR,        /*  5, 越界异常 Bound Range Exceeded */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_UD,        /*  6, 无效操作码 Invalid Opcode */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_NM,        /*  7, 无效设备 Device Not Available */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_DF,        /*  8, 双重故障 Double Fault */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_OLD_MF,    /*  9, 协处理器段超限 Coprocessor Segment Overrun */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_TS,        /* 10, 无效任务状态段(TSS) Invalid TSS */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_NP,        /* 11, 段不存在 Segment Not Present */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_SS,        /* 12, 栈段错误 Stack Segment Fault */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_GP,        /* 13, 保护错误 General Protection Fault */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_PF,        /* 14, 页错误 Page Fault */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_SPURIOUS,    /* 15, 欺骗性中断 Spurious Interrupt */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_MF,        /* 16, X87 浮点数异常 Floating-Point Exception */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_AC,        /* 17, 对齐检查 Alignment Check */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_MC,        /* 18, 设备检查 Machine Check */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_XF,        /* 19, SIMD 浮点数异常 Floating-Point Exception */</span></span><br><span class="line"><span class="comment">//    X86_TRAP_IRET = 32,    /* 32, 汇编指令异常 IRET Exception */</span></span><br><span class="line"><span class="comment">//&#125;;</span></span><br><span class="line"></span><br><span class="line">    set_intr_gate(X86_TRAP_DE, divide_error);</span><br><span class="line">    <span class="comment">/* 在32位系统上其效果等同于 set_intr_gate  */</span></span><br><span class="line">    set_intr_gate_ist(X86_TRAP_NMI, &amp;nmi, NMI_STACK);</span><br><span class="line">    <span class="comment">/* int4 can be called from all */</span></span><br><span class="line">    set_system_intr_gate(X86_TRAP_OF, &amp;overflow);</span><br><span class="line">    set_intr_gate(X86_TRAP_BR, bounds);</span><br><span class="line">    set_intr_gate(X86_TRAP_UD, invalid_op);</span><br><span class="line">    set_intr_gate(X86_TRAP_NM, device_not_available);</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_32</span></span><br><span class="line">    set_task_gate(X86_TRAP_DF, GDT_ENTRY_DOUBLEFAULT_TSS);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    set_intr_gate_ist(X86_TRAP_DF, &amp;double_fault, DOUBLEFAULT_STACK);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    set_intr_gate(X86_TRAP_OLD_MF, coprocessor_segment_overrun);</span><br><span class="line">    set_intr_gate(X86_TRAP_TS, invalid_TSS);</span><br><span class="line">    set_intr_gate(X86_TRAP_NP, segment_not_present);</span><br><span class="line">    set_intr_gate(X86_TRAP_SS, stack_segment);</span><br><span class="line">    set_intr_gate(X86_TRAP_GP, general_protection);</span><br><span class="line">    set_intr_gate(X86_TRAP_SPURIOUS, spurious_interrupt_bug);</span><br><span class="line">    set_intr_gate(X86_TRAP_MF, coprocessor_error);</span><br><span class="line">    set_intr_gate(X86_TRAP_AC, alignment_check);</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_MCE</span></span><br><span class="line">    set_intr_gate_ist(X86_TRAP_MC, &amp;machine_check, MCE_STACK);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    set_intr_gate(X86_TRAP_XF, simd_coprocessor_error);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 将前32个中断号都设置为已使用状态 */</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; FIRST_EXTERNAL_VECTOR; i++)</span><br><span class="line">        set_bit(i, used_vectors);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_IA32_EMULATION</span></span><br><span class="line">    <span class="comment">/* 设置0x80系统调用的系统中断门 */</span></span><br><span class="line">    set_system_intr_gate(IA32_SYSCALL_VECTOR, ia32_syscall);</span><br><span class="line">    set_bit(IA32_SYSCALL_VECTOR, used_vectors);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_32</span></span><br><span class="line">    <span class="comment">/* 设置0x80系统调用的系统门 */</span></span><br><span class="line">    set_system_trap_gate(SYSCALL_VECTOR, &amp;system_call);</span><br><span class="line">    set_bit(SYSCALL_VECTOR, used_vectors);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Set the IDT descriptor to a fixed read-only location, so that the</span></span><br><span class="line"><span class="comment">     * &quot;sidt&quot; instruction will not leak the location of the kernel, and</span></span><br><span class="line"><span class="comment">     * to defend the IDT against arbitrary memory write vulnerabilities.</span></span><br><span class="line"><span class="comment">     * It will be reloaded in cpu_init() */</span></span><br><span class="line">    <span class="comment">/* 将中断向量表设置在一个固定的只读的位置,以便“sidt”指令不会泄漏内核的位置,和保护中断向量表可以处于任意内存写的漏洞。它将会在 cpu_init() 中被加载到idtr寄存器 */</span></span><br><span class="line">    __set_fixmap(FIX_RO_IDT, __pa_symbol(idt_table), PAGE_KERNEL_RO);</span><br><span class="line">    idt_descr.address = fix_to_virt(FIX_RO_IDT);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 执行CPU的初始化，对于中断而言，在 cpu_init() 中主要是将 idt_descr 放入idtr寄存器中 */</span></span><br><span class="line">    cpu_init();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* x86_init是一个定义了很多x86体系上的初始化操作，这里执行的另一个trap_init()函数为空函数，什么都不做 */</span></span><br><span class="line">    x86_init.irqs.trap_init();</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_64</span></span><br><span class="line">    <span class="comment">/* 64位操作 */</span></span><br><span class="line">    <span class="comment">/* 将 idt_table 复制到 debug_idt_table 中 */</span></span><br><span class="line">    <span class="built_in">memcpy</span>(&amp;debug_idt_table, &amp;idt_table, IDT_ENTRIES * <span class="number">16</span>);</span><br><span class="line">    set_nmi_gate(X86_TRAP_DB, &amp;debug);</span><br><span class="line">    set_nmi_gate(X86_TRAP_BP, &amp;int3);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在代码中，used_vectors变量是一个bitmap，它用于记录中断向量表中哪些中断已经被系统注册和使用，哪些未被注册使用。trap_init()已经完成了异常和陷阱的初始化。对于linux而言，中断号0<del>19是专门用于陷阱和故障使用的，以上代码也表明了这一点，而20</del>31一般是intel用于保留的。而我们的外部IRQ线使用的中断为32<del>255(代码中32号中断被用作汇编指令异常中断)。所以，在trap_init()代码中，专门对0</del>19号中断的门描述符进行了初始化，最后将新的中断向量表起始地址放入idtr寄存器中。在trap_init()中我们看到每个异常和陷阱都有他们自己的处理函数，不过它们的处理函数的处理方式都大同小异，如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">#代码地址：arch/x86/kernel/entry_32.S</span><br><span class="line"></span><br><span class="line"># <span class="number">11</span>号异常处理函数入口</span><br><span class="line">ENTRY(segment_not_present)</span><br><span class="line">    RING0_EC_FRAME</span><br><span class="line">    ASM_CLAC</span><br><span class="line">    pushl_cfi $do_segment_not_present</span><br><span class="line">    jmp error_code</span><br><span class="line">    CFI_ENDPROC</span><br><span class="line"><span class="title function_">END</span><span class="params">(segment_not_present)</span></span><br><span class="line"></span><br><span class="line"># 12号异常处理函数入口</span><br><span class="line"><span class="title function_">ENTRY</span><span class="params">(stack_segment)</span></span><br><span class="line">    RING0_EC_FRAME</span><br><span class="line">    ASM_CLAC</span><br><span class="line">    pushl_cfi $do_stack_segment</span><br><span class="line">    jmp error_code</span><br><span class="line">    CFI_ENDPROC</span><br><span class="line"><span class="title function_">END</span><span class="params">(stack_segment)</span></span><br><span class="line"></span><br><span class="line"># 17号异常处理函数入口</span><br><span class="line"><span class="title function_">ENTRY</span><span class="params">(alignment_check)</span></span><br><span class="line">    RING0_EC_FRAME</span><br><span class="line">    ASM_CLAC</span><br><span class="line">    pushl_cfi $do_alignment_check</span><br><span class="line">    jmp error_code</span><br><span class="line">    CFI_ENDPROC</span><br><span class="line"><span class="title function_">END</span><span class="params">(alignment_check)</span></span><br><span class="line"></span><br><span class="line"># 0号异常处理函数入口</span><br><span class="line"><span class="title function_">ENTRY</span><span class="params">(divide_error)</span></span><br><span class="line">    RING0_INT_FRAME</span><br><span class="line">    ASM_CLAC</span><br><span class="line">    pushl_cfi $0            <span class="meta"># no <span class="keyword">error</span> code</span></span><br><span class="line">    pushl_cfi $do_divide_error</span><br><span class="line">    jmp error_code</span><br><span class="line">    CFI_ENDPROC</span><br><span class="line"><span class="title function_">END</span><span class="params">(divide_error)</span></span><br></pre></td></tr></table></figure><p>在trap_init()函数中调用了cpu_init()函数，在此函数中会将新的中断向量表地址放入idtr寄存器中，而具体内核是如何实现的呢，之前已经说明，idtr寄存器的低16位保存的是中断向量表长度，高32位保存的是中断向量表基地址，相对于的，内核定义了一个struct desc_ptr结构专门用于保存idtr寄存器内容，其如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">/* 代码地址：arch/x86/include/asm/Desc_defs.h */</span><br><span class="line">struct desc_ptr &#123;</span><br><span class="line">    unsigned short size;</span><br><span class="line">    unsigned long address;</span><br><span class="line">&#125; __attribute__((packed)) ;</span><br><span class="line"></span><br><span class="line">/* 代码地址：arch/x86/kernel/cpu/Common.c */</span><br><span class="line">/* 专门用于保存需要写入idtr寄存器值的变量，这里可以看出，中断向量表长度为256 * 16 - 1，地址为idt_table */</span><br><span class="line">struct desc_ptr idt_descr = &#123; NR_VECTORS * 16 - 1, (unsigned long) idt_table &#125;;</span><br></pre></td></tr></table></figure><p>在cpu_init()中，会调用load_current_idt()函数进行写入，如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">load_current_idt</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (is_debug_idt_enabled())</span><br><span class="line">        <span class="comment">/* 开启了中断调试，用的是 debug_idt_descr 和 debug_idt_table */</span></span><br><span class="line">        load_debug_idt();</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (is_trace_idt_enabled())</span><br><span class="line">        <span class="comment">/* 开启了中断跟踪，用的是 trace_idt_descr 和 trace_idt_table */</span></span><br><span class="line">        load_trace_idt();</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="comment">/* 普通情况，用的是 idt_descr 和 idt_table */</span></span><br><span class="line">        load_idt((<span class="type">const</span> <span class="keyword">struct</span> desc_ptr *)&amp;idt_descr);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* load_idt()的定义 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> load_idt(dtr)                native_load_idt(dtr)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* native_load_idt()的定义 */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">void</span> <span class="title function_">native_load_idt</span><span class="params">(<span class="type">const</span> <span class="keyword">struct</span> desc_ptr *dtr)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">asm</span> <span class="title function_">volatile</span><span class="params">(<span class="string">&quot;lidt %0&quot;</span>::<span class="string">&quot;m&quot;</span> (*dtr))</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="初始化中断"><a href="#初始化中断" class="headerlink" title="初始化中断"></a>初始化中断</h3><p>内核是在异常和陷阱初始化完成的情况下才会进行中断的初始化，中断的初始化也是处于start_kernel()函数中，分为两个部分，分别是early_irq_init()和init_IRQ()。early_irq_init()是第一步的初始化，其工作主要是跟硬件无关的一些初始化，比如一些变量的初始化，分配必要的内存等。init_IRQ()是第二步，其主要就是关于硬件部分的初始化了。</p><p>　　首先我们先看看中断描述符表irq_desc[NR_IRQS]：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 中断描述符表 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">irq_desc</span> <span class="title">irq_desc</span>[<span class="title">NR_IRQS</span>] __<span class="title">cacheline_aligned_in_smp</span> =</span> &#123;</span><br><span class="line">    [<span class="number">0</span> ... NR_IRQS<span class="number">-1</span>] = &#123;</span><br><span class="line">        .handle_irq    = handle_bad_irq,</span><br><span class="line">        .depth        = <span class="number">1</span>,</span><br><span class="line">        .lock        = __RAW_SPIN_LOCK_UNLOCKED(irq_desc-&gt;lock),</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>可以看到，irq_desc数组有NR_IRQS个元素，NR_IRQS并不是256-32，实际上，虽然中断向量表中一共有256项(前32项用作异常和intel保留)，但并不是所有中断向量都会使用到，所以中断描述符表也不一定是256-32项，CPU可以使用多少个中断是由中断控制器(PIC、APIC)或者内核配置决定的，我们看看NR_IRQS的定义：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">/* IOAPIC为外部中断控制器 */</span><br><span class="line">#ifdef CONFIG_X86_IO_APIC</span><br><span class="line">#define CPU_VECTOR_LIMIT        (64 * NR_CPUS)</span><br><span class="line">#define NR_IRQS                    \</span><br><span class="line">    (CPU_VECTOR_LIMIT &gt; IO_APIC_VECTOR_LIMIT ?    \</span><br><span class="line">        (NR_VECTORS + CPU_VECTOR_LIMIT)  :    \</span><br><span class="line">        (NR_VECTORS + IO_APIC_VECTOR_LIMIT))</span><br><span class="line">#else /* !CONFIG_X86_IO_APIC: NR_IRQS_LEGACY = 16 */</span><br><span class="line">#define NR_IRQS            NR_IRQS_LEGACY</span><br><span class="line">#endif</span><br></pre></td></tr></table></figure><p>这时我们可以先看看early_irq_init()函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> __init <span class="title function_">early_irq_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> count, i, node = first_online_node;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irq_desc</span> *<span class="title">desc</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 初始化irq_default_affinity变量，此变量用于设置中断默认的CPU亲和力 */</span></span><br><span class="line">    init_irq_default_affinity();</span><br><span class="line"></span><br><span class="line">    printk(KERN_INFO <span class="string">&quot;NR_IRQS:%d\n&quot;</span>, NR_IRQS);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 指向中断描述符表irq_desc */</span></span><br><span class="line">    desc = irq_desc;</span><br><span class="line">    <span class="comment">/* 获取中断描述符表长度 */</span></span><br><span class="line">    count = ARRAY_SIZE(irq_desc);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">        <span class="comment">/* 为kstat_irqs分配内存，每个CPU有自己独有的kstat_irqs数据，此数据用于统计 */</span></span><br><span class="line">        desc[i].kstat_irqs = alloc_percpu(<span class="type">unsigned</span> <span class="type">int</span>);</span><br><span class="line">        <span class="comment">/* 为 desc-&gt;irq_data.affinity 和 desc-&gt;pending_mask 分配内存 */</span></span><br><span class="line">        alloc_masks(&amp;desc[i], GFP_KERNEL, node);</span><br><span class="line">        <span class="comment">/* 初始化中断描述符的锁 */</span></span><br><span class="line">        raw_spin_lock_init(&amp;desc[i].lock);</span><br><span class="line">        <span class="comment">/* 设置中断描述符的锁所属的类，此类用于防止死锁 */</span></span><br><span class="line">        lockdep_set_class(&amp;desc[i].lock, &amp;irq_desc_lock_class);</span><br><span class="line">        <span class="comment">/* 一些变量的初始化 */</span></span><br><span class="line">        desc_set_defaults(i, &amp;desc[i], node, <span class="literal">NULL</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> arch_early_irq_init();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>更多的初始化在desc_set_defaults()函数中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">desc_set_defaults</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> irq, <span class="keyword">struct</span> irq_desc *desc, <span class="type">int</span> node,</span></span><br><span class="line"><span class="params">        <span class="keyword">struct</span> module *owner)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> cpu;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 中断号 */</span></span><br><span class="line">    desc-&gt;irq_data.irq = irq;</span><br><span class="line">    <span class="comment">/* 中断描述符的中断控制器芯片为 no_irq_chip  */</span></span><br><span class="line">    desc-&gt;irq_data.chip = &amp;no_irq_chip;</span><br><span class="line">    <span class="comment">/* 中断控制器的私有数据为空 */</span></span><br><span class="line">    desc-&gt;irq_data.chip_data = <span class="literal">NULL</span>;</span><br><span class="line">    desc-&gt;irq_data.handler_data = <span class="literal">NULL</span>;</span><br><span class="line">    desc-&gt;irq_data.msi_desc = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">/* 设置中断状态 desc-&gt;status_use_accessors 为初始化状态_IRQ_DEFAULT_INIT_FLAGS */</span></span><br><span class="line">    irq_settings_clr_and_set(desc, ~<span class="number">0</span>, _IRQ_DEFAULT_INIT_FLAGS);</span><br><span class="line">    <span class="comment">/* 中断默认被禁止，设置 desc-&gt;irq_data-&gt;state_use_accessors = IRQD_IRQ_DISABLED */</span></span><br><span class="line">    irqd_set(&amp;desc-&gt;irq_data, IRQD_IRQ_DISABLED);</span><br><span class="line">    <span class="comment">/* 设置中断处理回调函数为 handle_bad_irq，handle_bad_irq作为默认的回调函数，此函数中基本上不做什么处理，就是在屏幕上打印此中断信息，并且desc-&gt;kstat_irqs++ */</span></span><br><span class="line">    desc-&gt;handle_irq = handle_bad_irq;</span><br><span class="line">    <span class="comment">/* 嵌套深度为1，表示被禁止1次 */</span></span><br><span class="line">    desc-&gt;depth = <span class="number">1</span>;</span><br><span class="line">    <span class="comment">/* 初始化此中断发送次数为0 */</span></span><br><span class="line">    desc-&gt;irq_count = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">/* 无法处理的中断次数为0 */</span></span><br><span class="line">    desc-&gt;irqs_unhandled = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">/* 在/proc/interrupts所显名字为空 */</span></span><br><span class="line">    desc-&gt;name = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">/* owner为空 */</span></span><br><span class="line">    desc-&gt;owner = owner;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* 初始化kstat_irqs中每个CPU项都为0 */</span></span><br><span class="line">    for_each_possible_cpu(cpu)</span><br><span class="line">        *per_cpu_ptr(desc-&gt;kstat_irqs, cpu) = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* SMP系统才使用的初始化，设置</span></span><br><span class="line"><span class="comment">     * desc-&gt;irq_data.node = first_online_node </span></span><br><span class="line"><span class="comment">     * desc-&gt;irq_data.affinity = irq_default_affinity</span></span><br><span class="line"><span class="comment">     * 清除desc-&gt;pending_mask</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    desc_smp_init(desc, node);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>整个early_irq_init()在这里就初始化完毕了，相对来说比较简单，可以说early_irq_init()只是初始化了中断描述符表中的所有元素。</p><p>在看init_IRQ()前需要看看legacy_pic这个变量，它其实就是CPU内部的中断控制器i8259A，定义了与i8259A相关的一些处理函数和中断数量，如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">legacy_pic</span> <span class="title">default_legacy_pic</span> =</span> &#123;</span><br><span class="line">    .nr_legacy_irqs = NR_IRQS_LEGACY,</span><br><span class="line">    .chip  = &amp;i8259A_chip,</span><br><span class="line">    .mask = mask_8259A_irq,</span><br><span class="line">    .unmask = unmask_8259A_irq,</span><br><span class="line">    .mask_all = mask_8259A,</span><br><span class="line">    .restore_mask = unmask_8259A,</span><br><span class="line">    .init = init_8259A,</span><br><span class="line">    .irq_pending = i8259A_irq_pending,</span><br><span class="line">    .make_irq = make_8259A_irq,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">legacy_pic</span> *<span class="title">legacy_pic</span> =</span> &amp;default_legacy_pic;</span><br></pre></td></tr></table></figure><p>在X86体系下，CPU使用的内部中断控制器是i8259A，内核就定义了这个变量进行使用，在init_IRQ()中会将所有的中断描述符的中断控制器芯片指向i8259A，具体我们先看看init_IRQ()代码：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">void __init init_IRQ(void)</span><br><span class="line">&#123;</span><br><span class="line">    int i;</span><br><span class="line"></span><br><span class="line">    /*</span><br><span class="line">     * On cpu 0, Assign IRQ0_VECTOR..IRQ15_VECTOR&#x27;s to IRQ 0..15.</span><br><span class="line">     * If these IRQ&#x27;s are handled by legacy interrupt-controllers like PIC,</span><br><span class="line">     * then this configuration will likely be static after the boot. If</span><br><span class="line">     * these IRQ&#x27;s are handled by more mordern controllers like IO-APIC,</span><br><span class="line">     * then this vector space can be freed and re-used dynamically as the</span><br><span class="line">     * irq&#x27;s migrate etc.</span><br><span class="line">     */</span><br><span class="line">    /* nr_legacy_irqs() 返回 legacy_pic-&gt;nr_legacy_irqs，为16</span><br><span class="line">     * vector_irq是一个int型的数组，长度为中断向量表长，其保存的是中断向量对应的中断号(如果中断向量是异常则没有中断号)</span><br><span class="line">     * i8259A中断控制器使用IRQ0~IRQ15这16个中断号，这里将这16个中断号设置到CPU0的vector_irq数组的0x30~0x3f上。</span><br><span class="line">     */</span><br><span class="line">    for (i = 0; i &lt; nr_legacy_irqs(); i++)</span><br><span class="line">        per_cpu(vector_irq, 0)[IRQ0_VECTOR + i] = i;</span><br><span class="line"></span><br><span class="line">    /* x86_init是一个结构体，里面定义了一组X86体系下的初始化函数 */</span><br><span class="line">    x86_init.irqs.intr_init();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>x86_init.irqs.intr_init()是一个函数指针，其指向native_init_IRQ()，我们可以直接看看native_init_IRQ()：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> __init <span class="title function_">native_init_IRQ</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Execute any quirks before the call gates are initialised: */</span></span><br><span class="line">    <span class="comment">/* 这里又是执行x86_init结构中的初始化函数，pre_vector_init()指向 init_ISA_irqs  */</span></span><br><span class="line">    x86_init.irqs.pre_vector_init();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 初始化中断向量表中的中断控制器中默认的一些中断门初始化 */</span></span><br><span class="line">    apic_intr_init();</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Cover the whole vector space, no vector can escape</span></span><br><span class="line"><span class="comment">     * us. (some of these will be overridden and become</span></span><br><span class="line"><span class="comment">     * &#x27;special&#x27; SMP interrupts)</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">/* 第一个外部中断，默认是32 */</span></span><br><span class="line">    i = FIRST_EXTERNAL_VECTOR;</span><br><span class="line">    <span class="comment">/* 在used_vectors变量中找出所有没有置位的中断向量，我们知道，在trap_init()中对所有异常和陷阱和系统调用中断都置位了used_vectors，没有置位的都为中断</span></span><br><span class="line"><span class="comment">     * 这里就是对所有中断设置门描述符</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    for_each_clear_bit_from(i, used_vectors, NR_VECTORS) &#123;</span><br><span class="line">        <span class="comment">/* IA32_SYSCALL_VECTOR could be used in trap_init already. */</span></span><br><span class="line">        <span class="comment">/* interrupt[]数组保存的是外部中断的中断门信息</span></span><br><span class="line"><span class="comment">         * 这里将中断向量表中空闲的中断向量设置为中断门,interrupt是一个函数指针数组，其将31~255数组元素指向interrupt[i]函数</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        set_intr_gate(i, interrupt[i - FIRST_EXTERNAL_VECTOR]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 如果外部中断控制器需要，则安装一个中断处理例程irq2到中断IRQ2上 */</span></span><br><span class="line">    <span class="keyword">if</span> (!acpi_ioapic &amp;&amp; !of_ioapic &amp;&amp; nr_legacy_irqs())</span><br><span class="line">        setup_irq(<span class="number">2</span>, &amp;irq2);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_32</span></span><br><span class="line">    <span class="comment">/* 在x86_32模式下，会为当前CPU分配一个中断使用的栈空间 */</span></span><br><span class="line">    irq_ctx_init(smp_processor_id());</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在native_init_IRQ()中，又使用了x86_init变量中的pre_vector_init函数指针，其指向init_ISA_irqs()函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> __init <span class="title function_">init_ISA_irqs</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* CHIP默认是i8259A_chip */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">irq_chip</span> *<span class="title">chip</span> =</span> legacy_pic-&gt;chip;</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined(CONFIG_X86_64) || defined(CONFIG_X86_LOCAL_APIC)</span></span><br><span class="line">    <span class="comment">/* 使用了CPU本地中断控制器 */</span></span><br><span class="line">    <span class="comment">/* 开启virtual wire mode */</span></span><br><span class="line">    init_bsp_APIC();</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="comment">/* 其实就是调用init_8259A()，进行8259A硬件的初始化 */</span></span><br><span class="line">    legacy_pic-&gt;init(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nr_legacy_irqs(); i++)</span><br><span class="line">        <span class="comment">/* i为中断号，chip是irq_chip结构，最后是中断回调函数 </span></span><br><span class="line"><span class="comment">         * 设置了中断号i的中断描述符的irq_data.irq_chip = i8259A_chip</span></span><br><span class="line"><span class="comment">         * 设置了中断回调函数为handle_level_irq</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        irq_set_chip_and_handler(i, chip, handle_level_irq);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在init_ISA_irqs()函数中，最主要的就是将内核使用的外部中断的中断描述符的中断控制器设置为i8259A_chip，中断回调函数为handle_level_irq。</p><p>回到native_init_IRQ()函数，当执行完x86_init.irqs.pre_vector_init()之后，会执行apic_initr_init()函数，这个函数中会初始化一些中断控制器特定的中断函数(这些中断游离于之前描述的中断体系中，它们没有自己的中断描述符，中断向量中直接保存它们自己的中断处理函数，类似于异常与陷阱的调用情况)，具体我们看看：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> __init <span class="title function_">apic_intr_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    smp_intr_init();</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_THERMAL_VECTOR</span></span><br><span class="line">    <span class="comment">/* 中断号为: 0xfa，处理函数为: thermal_interrupt */</span></span><br><span class="line">    alloc_intr_gate(THERMAL_APIC_VECTOR, thermal_interrupt);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_MCE_THRESHOLD</span></span><br><span class="line">    alloc_intr_gate(THRESHOLD_APIC_VECTOR, threshold_interrupt);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined(CONFIG_X86_64) || defined(CONFIG_X86_LOCAL_APIC)</span></span><br><span class="line">    <span class="comment">/* self generated IPI for local APIC timer */</span></span><br><span class="line">    alloc_intr_gate(LOCAL_TIMER_VECTOR, apic_timer_interrupt);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* IPI for X86 platform specific use */</span></span><br><span class="line">    alloc_intr_gate(X86_PLATFORM_IPI_VECTOR, x86_platform_ipi);</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_HAVE_KVM</span></span><br><span class="line">    <span class="comment">/* IPI for KVM to deliver posted interrupt */</span></span><br><span class="line">    alloc_intr_gate(POSTED_INTR_VECTOR, kvm_posted_intr_ipi);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* IPI vectors for APIC spurious and error interrupts */</span></span><br><span class="line">    alloc_intr_gate(SPURIOUS_APIC_VECTOR, spurious_interrupt);</span><br><span class="line">    alloc_intr_gate(ERROR_APIC_VECTOR, error_interrupt);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* IRQ work interrupts: */</span></span><br><span class="line"><span class="meta"># <span class="keyword">ifdef</span> CONFIG_IRQ_WORK</span></span><br><span class="line">    alloc_intr_gate(IRQ_WORK_VECTOR, irq_work_interrupt);</span><br><span class="line"><span class="meta"># <span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在apic_intr_init()函数中，使用了alloc_intr_gate()函数进行处理，这个函数的处理也很简单，置位该中断号所处used_vectors位置，调用set_intr_gate()设置一个中断门描述符。<br>到这里整个中断及异常都已经初始化完成了。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul><li>在linux系统中，中断一共有256个，0<del>19主要用于异常与陷阱，20</del>31是intel保留，未使用。32~255作为外部中断进行使用。特别的，0x80中断用于系统调用。</li><li>机器上电时，BIOS会初始化一个中断向量表，当交接给linux内核后，内核会自己新建立一个中断向量表，之后完全使用自己的中断向量表，舍弃BIOS的中断向量表。</li><li>在x86上系统默认使用的中断控制器为i8259A。</li><li>中断描述符的初始化过程中，内核会将中断描述符的默认中断控制器设置为i8259A，中断处理回调函数为handle_level_irq()。</li><li>外部中断的门描述的中断处理函数都为interrupt[i]。</li></ul><p>中断的初始化大体上分为两个部分，第一个部分为汇编代码的中断向量表的初次初始化，第二部分为C语言代码，其又分为异常与陷阱的初始化和中断的初始化。如图：</p><ul><li>在汇编的中断向量表初始化过中，其主要对整个中断向量表进行了初始化，其主要工作是：<ul><li>所有的门描述符的权限位为0;</li><li>所有的门描述符的段选择符为__KERNEL_CS;</li><li>0~31的门描述符的中断处理程序为early_idt_handlers[i](0 &lt;&#x3D; i &lt;&#x3D; 31);</li><li>其他的门描述符的中断处理程序为ignore_int</li></ul></li></ul><ul><li>而trap_init()所做的异常与陷阱初始化，就是修改中断向量表的前19项(异常和中断)，主要修改他们的中断处理函数入口和权限位，特殊的如任务门还会设置它们的段选择符。在trap_init()中就已经把所有的异常和陷阱都初始化完成了，并会把新的中断向量表地址放入idtr寄存器，开始使用新的中断向量表。</li><li>在early_irq_init()中，主要工作是初始化整个中断描述符表，将数组中的每个中断描述符中的必要变量进行初始化。</li><li>最后在init_IRQ()中，主要工作是初始化中断向量表中的所有中断门描述符，对于一般的中断，内核将它们的中断处理函数入口设置为interrupt[i]，而一些特殊的中断会在apic_intr_init()中进行设置。之后，init_IRQ()会初始化内部和外部的中断控制器，最后将一般的中断使用的中断控制器设置为i8259A，中断处理函数为handle_level_irq(电平触发)。</li></ul>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
          <category> interrupt </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Linux进程详解</title>
      <link href="/2024/10/20/Linux/process/%E8%BF%9B%E7%A8%8B/"/>
      <url>/2024/10/20/Linux/process/%E8%BF%9B%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<h2 id="进程概述"><a href="#进程概述" class="headerlink" title="进程概述"></a><strong>进程概述</strong></h2><ol><li><p>进程与线程的区别<br> <strong>进程</strong>是系统进行资源分配和调度的一个独立单位. <strong>线程</strong>是<strong>进程</strong>的一个实体,是CPU调度和分派的基本单位。</p></li><li><p>父子进程<br> 共享含有程序代码的页，但是各自拥有独立的数据拷贝，因此子进程对一个内存单元的修改对父进程是不可见的（反之亦然）。<br> 写时复制技术允许读相同的物理页，只有两者之中的一个试图写一个物理页，内核就把这个页的内容拷贝到一个新的物理页，并把这个新的物理页分配给正在写的进程。</p></li><li><p>Linux下有3个特殊的进程，idle进程(PID &#x3D; 0), init进程(PID &#x3D; 1)和kthreadd(PID &#x3D; 2)</p><ul><li>idle进程由系统自动创建, 运行在内核态.idle进程其pid&#x3D;0，其前身是系统创建的第一个进程，也是唯一一个没有通过fork或者kernel_thread产生的进程。完成加载系统后，演变为进程调度、交换。</li><li>init进程由idle通过kernel_thread创建，在内核空间完成初始化后, 加载init程序, 并最终用户空间由0进程创建，完成系统的初始化. 是系统中所有其它用户进程的祖先进程 .Linux中的所有进程都是有init进程创建并运行的。首先Linux内核启动，然后在用户空间中启动init进程，再启动其他系统进程。在系统启动完成完成后，init将变为守护进程监视系统其他进程。</li><li>kthread进程由idle通过kernel_thread创建，并始终运行在内核空间, 负责所有内核线程的调度和管理.它的任务就是管理和调度其他内核线程kernel_thread, 会循环执行一个kthread的函数，该函数的作用就是运行kthread_create_list全局链表中维护的kthread, 当我们调用kernel_thread创建的内核线程会被加入到此链表中，因此所有的内核线程都是直接或者间接的以kthreadd为父进程 。</li></ul></li></ol><h2 id="进程描述符"><a href="#进程描述符" class="headerlink" title="进程描述符"></a><strong>进程描述符</strong></h2><p>进程描述符(process descriptor)(struct task_struct),它的字段包含了与一个进程相关的所有信息。因为进程描述符中存放了那么多信息，所以它是相当复杂的。它不仅包含了很多进程属性的字段，而且一些字段还包括了指向其他数据结构的指针。</p><img src="/2024/10/20/Linux/process/%E8%BF%9B%E7%A8%8B/Linux%E8%BF%9B%E7%A8%8B%E6%8F%8F%E8%BF%B0%E7%AC%A6.png" class><h3 id="具体代码："><a href="#具体代码：" class="headerlink" title="具体代码："></a>具体代码：</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br><span class="line">214</span><br><span class="line">215</span><br><span class="line">216</span><br><span class="line">217</span><br><span class="line">218</span><br><span class="line">219</span><br><span class="line">220</span><br><span class="line">221</span><br><span class="line">222</span><br><span class="line">223</span><br><span class="line">224</span><br><span class="line">225</span><br><span class="line">226</span><br><span class="line">227</span><br><span class="line">228</span><br><span class="line">229</span><br><span class="line">230</span><br><span class="line">231</span><br><span class="line">232</span><br><span class="line">233</span><br><span class="line">234</span><br><span class="line">235</span><br><span class="line">236</span><br><span class="line">237</span><br><span class="line">238</span><br><span class="line">239</span><br><span class="line">240</span><br><span class="line">241</span><br><span class="line">242</span><br><span class="line">243</span><br><span class="line">244</span><br><span class="line">245</span><br><span class="line">246</span><br><span class="line">247</span><br><span class="line">248</span><br><span class="line">249</span><br><span class="line">250</span><br><span class="line">251</span><br><span class="line">252</span><br><span class="line">253</span><br><span class="line">254</span><br><span class="line">255</span><br><span class="line">256</span><br><span class="line">257</span><br><span class="line">258</span><br><span class="line">259</span><br><span class="line">260</span><br><span class="line">261</span><br><span class="line">262</span><br><span class="line">263</span><br><span class="line">264</span><br><span class="line">265</span><br><span class="line">266</span><br><span class="line">267</span><br><span class="line">268</span><br><span class="line">269</span><br><span class="line">270</span><br><span class="line">271</span><br><span class="line">272</span><br><span class="line">273</span><br><span class="line">274</span><br><span class="line">275</span><br><span class="line">276</span><br><span class="line">277</span><br><span class="line">278</span><br><span class="line">279</span><br><span class="line">280</span><br><span class="line">281</span><br><span class="line">282</span><br><span class="line">283</span><br><span class="line">284</span><br><span class="line">285</span><br><span class="line">286</span><br><span class="line">287</span><br><span class="line">288</span><br><span class="line">289</span><br><span class="line">290</span><br><span class="line">291</span><br><span class="line">292</span><br><span class="line">293</span><br><span class="line">294</span><br><span class="line">295</span><br><span class="line">296</span><br><span class="line">297</span><br><span class="line">298</span><br><span class="line">299</span><br><span class="line">300</span><br><span class="line">301</span><br><span class="line">302</span><br><span class="line">303</span><br><span class="line">304</span><br><span class="line">305</span><br><span class="line">306</span><br><span class="line">307</span><br><span class="line">308</span><br><span class="line">309</span><br><span class="line">310</span><br><span class="line">311</span><br><span class="line">312</span><br><span class="line">313</span><br><span class="line">314</span><br><span class="line">315</span><br><span class="line">316</span><br><span class="line">317</span><br><span class="line">318</span><br><span class="line">319</span><br><span class="line">320</span><br><span class="line">321</span><br><span class="line">322</span><br><span class="line">323</span><br><span class="line">324</span><br><span class="line">325</span><br><span class="line">326</span><br><span class="line">327</span><br><span class="line">328</span><br><span class="line">329</span><br><span class="line">330</span><br><span class="line">331</span><br><span class="line">332</span><br><span class="line">333</span><br><span class="line">334</span><br><span class="line">335</span><br><span class="line">336</span><br><span class="line">337</span><br><span class="line">338</span><br><span class="line">339</span><br><span class="line">340</span><br><span class="line">341</span><br><span class="line">342</span><br><span class="line">343</span><br><span class="line">344</span><br><span class="line">345</span><br><span class="line">346</span><br><span class="line">347</span><br><span class="line">348</span><br><span class="line">349</span><br><span class="line">350</span><br><span class="line">351</span><br><span class="line">352</span><br><span class="line">353</span><br><span class="line">354</span><br><span class="line">355</span><br><span class="line">356</span><br><span class="line">357</span><br><span class="line">358</span><br><span class="line">359</span><br><span class="line">360</span><br><span class="line">361</span><br><span class="line">362</span><br><span class="line">363</span><br><span class="line">364</span><br><span class="line">365</span><br><span class="line">366</span><br><span class="line">367</span><br><span class="line">368</span><br><span class="line">369</span><br><span class="line">370</span><br><span class="line">371</span><br><span class="line">372</span><br><span class="line">373</span><br><span class="line">374</span><br><span class="line">375</span><br><span class="line">376</span><br><span class="line">377</span><br><span class="line">378</span><br><span class="line">379</span><br><span class="line">380</span><br><span class="line">381</span><br><span class="line">382</span><br><span class="line">383</span><br><span class="line">384</span><br><span class="line">385</span><br><span class="line">386</span><br><span class="line">387</span><br><span class="line">388</span><br><span class="line">389</span><br><span class="line">390</span><br><span class="line">391</span><br><span class="line">392</span><br><span class="line">393</span><br><span class="line">394</span><br><span class="line">395</span><br><span class="line">396</span><br><span class="line">397</span><br><span class="line">398</span><br><span class="line">399</span><br><span class="line">400</span><br><span class="line">401</span><br><span class="line">402</span><br><span class="line">403</span><br><span class="line">404</span><br><span class="line">405</span><br><span class="line">406</span><br><span class="line">407</span><br><span class="line">408</span><br><span class="line">409</span><br><span class="line">410</span><br><span class="line">411</span><br><span class="line">412</span><br><span class="line">413</span><br><span class="line">414</span><br><span class="line">415</span><br><span class="line">416</span><br><span class="line">417</span><br><span class="line">418</span><br><span class="line">419</span><br><span class="line">420</span><br><span class="line">421</span><br><span class="line">422</span><br><span class="line">423</span><br><span class="line">424</span><br><span class="line">425</span><br><span class="line">426</span><br><span class="line">427</span><br><span class="line">428</span><br><span class="line">429</span><br><span class="line">430</span><br><span class="line">431</span><br><span class="line">432</span><br><span class="line">433</span><br><span class="line">434</span><br><span class="line">435</span><br><span class="line">436</span><br><span class="line">437</span><br><span class="line">438</span><br><span class="line">439</span><br><span class="line">440</span><br><span class="line">441</span><br><span class="line">442</span><br><span class="line">443</span><br><span class="line">444</span><br><span class="line">445</span><br><span class="line">446</span><br><span class="line">447</span><br><span class="line">448</span><br><span class="line">449</span><br><span class="line">450</span><br><span class="line">451</span><br><span class="line">452</span><br><span class="line">453</span><br><span class="line">454</span><br><span class="line">455</span><br><span class="line">456</span><br><span class="line">457</span><br><span class="line">458</span><br><span class="line">459</span><br><span class="line">460</span><br><span class="line">461</span><br><span class="line">462</span><br><span class="line">463</span><br><span class="line">464</span><br><span class="line">465</span><br><span class="line">466</span><br><span class="line">467</span><br><span class="line">468</span><br><span class="line">469</span><br><span class="line">470</span><br><span class="line">471</span><br><span class="line">472</span><br><span class="line">473</span><br><span class="line">474</span><br><span class="line">475</span><br><span class="line">476</span><br><span class="line">477</span><br><span class="line">478</span><br><span class="line">479</span><br><span class="line">480</span><br><span class="line">481</span><br><span class="line">482</span><br><span class="line">483</span><br><span class="line">484</span><br><span class="line">485</span><br><span class="line">486</span><br><span class="line">487</span><br><span class="line">488</span><br><span class="line">489</span><br><span class="line">490</span><br><span class="line">491</span><br><span class="line">492</span><br><span class="line">493</span><br><span class="line">494</span><br><span class="line">495</span><br><span class="line">496</span><br><span class="line">497</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> &#123;</span></span><br><span class="line"><span class="keyword">volatile</span> <span class="type">long</span> state;<span class="comment">/* -1 unrunnable, 0 runnable, &gt;0 stopped */</span></span><br><span class="line">                            <span class="comment">/* -1 表示不可运行，0 表示可运行，&gt;0 表示已停止 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="built_in">stack</span>;            <span class="comment">/* Pointer to the stack */</span></span><br><span class="line">                            <span class="comment">/* 指向栈的指针 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">atomic_t</span> usage;         <span class="comment">/* Usage counter */</span></span><br><span class="line">                            <span class="comment">/* 使用计数器 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> flags;<span class="comment">/* Process flags */</span></span><br><span class="line">                            <span class="comment">/* 进程标志 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> ptrace;    <span class="comment">/* Tracing flags */</span></span><br><span class="line">                            <span class="comment">/* 跟踪标志 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> lock_depth;         <span class="comment">/* Lock depth for BKL */</span></span><br><span class="line">                            <span class="comment">/* 为BKL锁的深度 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_SMP</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> __ARCH_WANT_UNLOCKED_CTXSW</span></span><br><span class="line"><span class="type">int</span> oncpu;              <span class="comment">/* Which CPU is the task running on */</span></span><br><span class="line">                            <span class="comment">/* 任务正在哪个CPU上运行 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> prio, static_prio, normal_prio; <span class="comment">/* Dynamic, static, and normal priorities */</span></span><br><span class="line">                                        <span class="comment">/* 动态优先级，静态优先级和普通优先级 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> rt_priority;           <span class="comment">/* Real-time priority */</span></span><br><span class="line">                                        <span class="comment">/* 实时优先级 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">sched_class</span> *<span class="title">sched_class</span>;</span> <span class="comment">/* Scheduling class of the task */</span></span><br><span class="line">                                            <span class="comment">/* 任务的调度类 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sched_entity</span> <span class="title">se</span>;</span>                  <span class="comment">/* For CFS scheduling */</span></span><br><span class="line">                                            <span class="comment">/* 用于CFS调度 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sched_rt_entity</span> <span class="title">rt</span>;</span>               <span class="comment">/* For real-time scheduling */</span></span><br><span class="line">                                            <span class="comment">/* 用于实时调度 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_PREEMPT_NOTIFIERS</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">hlist_head</span> <span class="title">preempt_notifiers</span>;</span>     <span class="comment">/* List of preempt notifiers */</span></span><br><span class="line">                                             <span class="comment">/* 抢占通知器列表 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">char</span> fpu_counter;               <span class="comment">/* FPU usage counter */</span></span><br><span class="line">                                             <span class="comment">/* FPU使用计数器 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_BLK_DEV_IO_TRACE</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> btrace_seq;                 <span class="comment">/* Block trace sequence */</span></span><br><span class="line">                                             <span class="comment">/* 块跟踪序列 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> policy;                     <span class="comment">/* Scheduling policy */</span></span><br><span class="line">                                             <span class="comment">/* 调度策略 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">cpumask_t</span> cpus_allowed;                  <span class="comment">/* CPUs allowed to run this task */</span></span><br><span class="line">                                             <span class="comment">/* 允许运行此任务的CPU */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_PREEMPT_RCU</span></span><br><span class="line"><span class="type">int</span> rcu_read_lock_nesting;               <span class="comment">/* Nesting level of RCU read-side critical sections */</span></span><br><span class="line">                                             <span class="comment">/* RCU读侧临界区的嵌套级别 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">char</span> rcu_read_unlock_special;            <span class="comment">/* Special flags for RCU read-side */</span></span><br><span class="line">                                             <span class="comment">/* RCU读侧的特殊标志 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">rcu_node_entry</span>;</span>         <span class="comment">/* RCU node entry */</span></span><br><span class="line">                                             <span class="comment">/* RCU节点条目 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_TREE_PREEMPT_RCU</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rcu_node</span> *<span class="title">rcu_blocked_node</span>;</span>       <span class="comment">/* Pointer to blocked node in RCU */</span></span><br><span class="line">                                             <span class="comment">/* 指向RCU中被阻塞节点的指针 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_RCU_BOOST</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rt_mutex</span> *<span class="title">rcu_boost_mutex</span>;</span>        <span class="comment">/* RT mutex for RCU boosting */</span></span><br><span class="line">                                             <span class="comment">/* 用于RCU增强的实时互斥锁 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined(CONFIG_SCHEDSTATS) || defined(CONFIG_TASK_DELAY_ACCT)</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sched_info</span> <span class="title">sched_info</span>;</span>            <span class="comment">/* Scheduler statistics */</span></span><br><span class="line">                                             <span class="comment">/* 调度器统计信息 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">tasks</span>;</span>                  <span class="comment">/* List of tasks */</span></span><br><span class="line">                                             <span class="comment">/* 任务列表 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_SMP</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">plist_node</span> <span class="title">pushable_tasks</span>;</span>        <span class="comment">/* Tasks that are ready to run on any CPU */</span></span><br><span class="line">                                             <span class="comment">/* 准备在任何CPU上运行的任务 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mm_struct</span> *<span class="title">mm</span>, *<span class="title">active_mm</span>;</span>        <span class="comment">/* Memory descriptors */</span></span><br><span class="line">                                             <span class="comment">/* 内存描述符 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_COMPAT_BRK</span></span><br><span class="line"><span class="type">unsigned</span> brk_randomized:<span class="number">1</span>;               <span class="comment">/* Is the brk space randomized? */</span></span><br><span class="line">                                             <span class="comment">/* brk空间是否随机化？ */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined(SPLIT_RSS_COUNTING)</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_rss_stat</span><span class="title">rss_stat</span>;</span>         <span class="comment">/* RSS stats */</span></span><br><span class="line">                                             <span class="comment">/* RSS统计 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> exit_state;                          <span class="comment">/* Exit state of the task */</span></span><br><span class="line">                                             <span class="comment">/* 任务的退出状态 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> exit_code, exit_signal;              <span class="comment">/* Exit code and signal */</span></span><br><span class="line">                                             <span class="comment">/* 退出代码和信号 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> pdeath_signal;                       <span class="comment">/* Signal to send on parent death */</span></span><br><span class="line">                                             <span class="comment">/* 父进程死亡时发送的信号 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> personality;                <span class="comment">/* Personality of the process */</span></span><br><span class="line">                                             <span class="comment">/* 进程的性格 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> did_exec:<span class="number">1</span>;                     <span class="comment">/* Did an exec */</span></span><br><span class="line">                                             <span class="comment">/* 是否执行了exec */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> in_execve:<span class="number">1</span>;                    <span class="comment">/* Is in an execve syscall */</span></span><br><span class="line">                                             <span class="comment">/* 是否在execve系统调用中 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> in_iowait:<span class="number">1</span>;                    <span class="comment">/* Is waiting on IO */</span></span><br><span class="line">                                             <span class="comment">/* 是否在等待IO */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> sched_reset_on_fork:<span class="number">1</span>;          <span class="comment">/* Reset scheduler params on fork */</span></span><br><span class="line">                                             <span class="comment">/* 在fork时重置调度器参数 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">pid_t</span> pid;                               <span class="comment">/* Process ID */</span></span><br><span class="line">                                             <span class="comment">/* 进程ID */</span></span><br><span class="line"></span><br><span class="line"><span class="type">pid_t</span> tgid;                              <span class="comment">/* Thread group ID */</span></span><br><span class="line">                                             <span class="comment">/* 线程组ID */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_CC_STACKPROTECTOR</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> stack_canary;              <span class="comment">/* Canary value for stack protection */</span></span><br><span class="line">                                             <span class="comment">/* 栈保护的金丝雀值 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">real_parent</span>;</span>         <span class="comment">/* Real parent process */</span></span><br><span class="line">                                                <span class="comment">/* 实际父进程 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">parent</span>;</span>              <span class="comment">/* Parent process */</span></span><br><span class="line">                                                <span class="comment">/* 父进程 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">children</span>;</span>               <span class="comment">/* List of children */</span></span><br><span class="line">                                                <span class="comment">/* 子进程列表 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">sibling</span>;</span>                <span class="comment">/* Sibling link */</span></span><br><span class="line">                                                <span class="comment">/* 兄弟进程链接 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">group_leader</span>;</span>        <span class="comment">/* Leader of the process group */</span></span><br><span class="line">                                                <span class="comment">/* 进程组的领导进程 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">ptraced</span>;</span>                <span class="comment">/* List of tasks this task is ptracing */</span></span><br><span class="line">                                                <span class="comment">/* 该任务正在ptrace的任务列表 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">ptrace_entry</span>;</span>           <span class="comment">/* Entry for list of ptraced tasks */</span></span><br><span class="line">                                                <span class="comment">/* ptrace任务列表的入口 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pid_link</span> <span class="title">pids</span>[<span class="title">PIDTYPE_MAX</span>];</span>       <span class="comment">/* Links for PID hash table */</span></span><br><span class="line">                                                <span class="comment">/* PID哈希表的链接 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">thread_group</span>;</span>           <span class="comment">/* List of threads in the group */</span></span><br><span class="line">                                                <span class="comment">/* 组中线程的列表 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">completion</span> *<span class="title">vfork_done</span>;</span>           <span class="comment">/* Completion for vfork */</span></span><br><span class="line">                                                <span class="comment">/* vfork完成情况 */</span></span><br><span class="line"><span class="type">int</span> __user *set_child_tid;               <span class="comment">/* Set child TID */</span></span><br><span class="line">                                                <span class="comment">/* 设置子线程的TID */</span></span><br><span class="line"><span class="type">int</span> __user *clear_child_tid;             <span class="comment">/* Clear child TID */</span></span><br><span class="line">                                                <span class="comment">/* 清除子线程的TID */</span></span><br><span class="line"></span><br><span class="line"><span class="type">cputime_t</span> utime, stime;                  <span class="comment">/* User and system CPU time */</span></span><br><span class="line">                                                <span class="comment">/* 用户态和系统态CPU时间 */</span></span><br><span class="line"><span class="type">cputime_t</span> utimescaled, stimescaled;      <span class="comment">/* Scaled user and system CPU time */</span></span><br><span class="line">                                                <span class="comment">/* 缩放的用户态和系统态CPU时间 */</span></span><br><span class="line"><span class="type">cputime_t</span> gtime;                         <span class="comment">/* Guest time */</span></span><br><span class="line">                                                <span class="comment">/* 客户操作系统时间 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> nvcsw, nivcsw;             <span class="comment">/* Voluntary and involuntary context switches */</span></span><br><span class="line">                                                <span class="comment">/* 自愿和非自愿上下文切换 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">start_time</span>;</span>              <span class="comment">/* Start time in monotonic clock */</span></span><br><span class="line">                                                <span class="comment">/* 单调时钟的开始时间 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">real_start_time</span>;</span>         <span class="comment">/* Start time in wall clock */</span></span><br><span class="line">                                                <span class="comment">/* 墙钟的开始时间 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> min_flt, maj_flt;          <span class="comment">/* Minor and major faults */</span></span><br><span class="line">                                                <span class="comment">/* 次要和主要错误 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_cputime</span> <span class="title">cputime_expires</span>;</span>     <span class="comment">/* CPU time limits */</span></span><br><span class="line">                                                <span class="comment">/* CPU时间限制 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">cpu_timers</span>[3];</span>          <span class="comment">/* CPU timers */</span></span><br><span class="line">                                                <span class="comment">/* CPU计时器 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">cred</span> __<span class="title">rcu</span> *<span class="title">real_cred</span>;</span>      <span class="comment">/* Real credentials */</span></span><br><span class="line">                                                <span class="comment">/* 实际凭证 */</span></span><br><span class="line"><span class="type">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">cred</span> __<span class="title">rcu</span> *<span class="title">cred</span>;</span>           <span class="comment">/* Effective credentials */</span></span><br><span class="line">                                                <span class="comment">/* 有效凭证 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">cred</span> *<span class="title">replacement_session_keyring</span>;</span><span class="comment">/* Replacement session keyring */</span></span><br><span class="line">                                                <span class="comment">/* 替换会话密钥环 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">char</span> comm[TASK_COMM_LEN];                <span class="comment">/* Command name */</span></span><br><span class="line">                                                <span class="comment">/* 命令名称 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> link_count, total_link_count;        <span class="comment">/* Link counts for filesystems */</span></span><br><span class="line">                                                <span class="comment">/* 文件系统的链接计数 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_SYSVIPC</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sysv_sem</span> <span class="title">sysvsem</span>;</span>                 <span class="comment">/* System V IPC semaphore */</span></span><br><span class="line">                                                <span class="comment">/* System V IPC信号量 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_DETECT_HUNG_TASK</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> last_switch_count;         <span class="comment">/* Last scheduler tick */</span></span><br><span class="line">                                                <span class="comment">/* 上一次调度器时钟 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thread_struct</span> <span class="title">thread</span>;</span>             <span class="comment">/* CPU-specific state */</span></span><br><span class="line">                                                <span class="comment">/* CPU特定状态 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">fs_struct</span> *<span class="title">fs</span>;</span>                    <span class="comment">/* Filesystem information */</span></span><br><span class="line">                                                <span class="comment">/* 文件系统信息 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">files_struct</span> *<span class="title">files</span>;</span>              <span class="comment">/* Open files */</span></span><br><span class="line">                                                <span class="comment">/* 打开的文件 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">nsproxy</span> *<span class="title">nsproxy</span>;</span>                 <span class="comment">/* Namespace proxy */</span></span><br><span class="line">                                                <span class="comment">/* 命名空间代理 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">signal_struct</span> *<span class="title">signal</span>;</span>            <span class="comment">/* Signal state */</span></span><br><span class="line">                                                <span class="comment">/* 信号状态 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sighand_struct</span> *<span class="title">sighand</span>;</span>          <span class="comment">/* Signal handlers */</span></span><br><span class="line">                                                <span class="comment">/* 信号处理程序 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">sigset_t</span> blocked, real_blocked;          <span class="comment">/* Blocked signals */</span></span><br><span class="line">                                                <span class="comment">/* 阻塞信号 */</span></span><br><span class="line"><span class="type">sigset_t</span> saved_sigmask;                  <span class="comment">/* Saved signal mask */</span></span><br><span class="line">                                                <span class="comment">/* 保存的信号掩码 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sigpending</span> <span class="title">pending</span>;</span>               <span class="comment">/* Pending signals */</span></span><br><span class="line">                                                <span class="comment">/* 待处理的信号 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> sas_ss_sp;                 <span class="comment">/* Stack segment pointer for set_altstack */</span></span><br><span class="line">                                                <span class="comment">/* 用于set_altstack的堆栈段指针 */</span></span><br><span class="line"><span class="type">size_t</span> sas_ss_size;                      <span class="comment">/* Stack segment size for set_altstack */</span></span><br><span class="line">                                                <span class="comment">/* 用于set_altstack的堆栈段大小 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> (*notifier)(<span class="type">void</span> *priv);             <span class="comment">/* Task notifier */</span></span><br><span class="line">                                                <span class="comment">/* 任务通知程序 */</span></span><br><span class="line"><span class="type">void</span> *notifier_data;                     <span class="comment">/* Notifier data */</span></span><br><span class="line">                                                <span class="comment">/* 通知程序数据 */</span></span><br><span class="line"><span class="type">sigset_t</span> *notifier_mask;                 <span class="comment">/* Notifier signal mask */</span></span><br><span class="line">                                                <span class="comment">/* 通知程序信号掩码 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">audit_context</span> *<span class="title">audit_context</span>;</span>     <span class="comment">/* Audit context */</span></span><br><span class="line">                                                <span class="comment">/* 审计上下文 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_AUDITSYSCALL</span></span><br><span class="line"><span class="type">uid_t</span> loginuid;                          <span class="comment">/* Login UID */</span></span><br><span class="line">                                                <span class="comment">/* 登录UID */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> sessionid;                  <span class="comment">/* Session ID */</span></span><br><span class="line">                                                <span class="comment">/* 会话ID */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">seccomp_t</span> seccomp;                       <span class="comment">/* Secure computing state */</span></span><br><span class="line">                                                <span class="comment">/* 安全计算状态 */</span></span><br><span class="line"></span><br><span class="line">u32 parent_exec_id;                      <span class="comment">/* Parent execution ID */</span></span><br><span class="line">                                                <span class="comment">/* 父执行ID */</span></span><br><span class="line">u32 self_exec_id;                        <span class="comment">/* Self execution ID */</span></span><br><span class="line">                                                <span class="comment">/* 自身执行ID */</span></span><br><span class="line"></span><br><span class="line"><span class="type">spinlock_t</span> alloc_lock;                   <span class="comment">/* Lock for resource allocation */</span></span><br><span class="line">                                                <span class="comment">/* 资源分配锁 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_GENERIC_HARDIRQS</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">irqaction</span> *<span class="title">irqaction</span>;</span>             <span class="comment">/* IRQ actions */</span></span><br><span class="line">                                                <span class="comment">/* IRQ操作 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">raw_spinlock_t</span> pi_lock;                  <span class="comment">/* Lock for priority inheritance */</span></span><br><span class="line">                                                <span class="comment">/* 优先级继承锁 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_RT_MUTEXES</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">plist_head</span> <span class="title">pi_waiters</span>;</span>            <span class="comment">/* List of priority inheritance waiters */</span></span><br><span class="line">                                                <span class="comment">/* 优先级继承等待者列表 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rt_mutex_waiter</span> *<span class="title">pi_blocked_on</span>;</span>   <span class="comment">/* RT mutex this is blocked on */</span></span><br><span class="line">                                                <span class="comment">/* 阻塞于此的RT互斥体 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_DEBUG_MUTEXES</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mutex_waiter</span> *<span class="title">blocked_on</span>;</span>         <span class="comment">/* Mutex this task is blocked on */</span></span><br><span class="line">                                                <span class="comment">/* 此任务阻塞的互斥体 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_TRACE_IRQFLAGS</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> irq_events;                <span class="comment">/* Number of IRQ events */</span></span><br><span class="line">                                            <span class="comment">/* IRQ事件的数量 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> hardirq_enable_ip;        <span class="comment">/* IP where hard IRQs were enabled */</span></span><br><span class="line">                                            <span class="comment">/* 启用硬中断的位置（IP） */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> hardirq_disable_ip;       <span class="comment">/* IP where hard IRQs were disabled */</span></span><br><span class="line">                                            <span class="comment">/* 禁用硬中断的位置（IP） */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> hardirq_enable_event;      <span class="comment">/* Event count at hard IRQ enable */</span></span><br><span class="line">                                            <span class="comment">/* 启用硬中断时的事件计数 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> hardirq_disable_event;     <span class="comment">/* Event count at hard IRQ disable */</span></span><br><span class="line">                                            <span class="comment">/* 禁用硬中断时的事件计数 */</span></span><br><span class="line"><span class="type">int</span> hardirqs_enabled;                   <span class="comment">/* Flag indicating if hard IRQs are enabled */</span></span><br><span class="line">                                            <span class="comment">/* 表示硬中断是否启用的标志 */</span></span><br><span class="line"><span class="type">int</span> hardirq_context;                    <span class="comment">/* Flag indicating if currently in hard IRQ context */</span></span><br><span class="line">                                            <span class="comment">/* 表示当前是否在硬中断上下文中的标志 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> softirq_disable_ip;       <span class="comment">/* IP where soft IRQs were disabled */</span></span><br><span class="line">                                            <span class="comment">/* 禁用软中断的位置（IP） */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> softirq_enable_ip;        <span class="comment">/* IP where soft IRQs were enabled */</span></span><br><span class="line">                                            <span class="comment">/* 启用软中断的位置（IP） */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> softirq_disable_event;     <span class="comment">/* Event count at soft IRQ disable */</span></span><br><span class="line">                                            <span class="comment">/* 禁用软中断时的事件计数 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> softirq_enable_event;      <span class="comment">/* Event count at soft IRQ enable */</span></span><br><span class="line">                                            <span class="comment">/* 启用软中断时的事件计数 */</span></span><br><span class="line"><span class="type">int</span> softirqs_enabled;                   <span class="comment">/* Flag indicating if soft IRQs are enabled */</span></span><br><span class="line">                                            <span class="comment">/* 表示软中断是否启用的标志 */</span></span><br><span class="line"><span class="type">int</span> softirq_context;                    <span class="comment">/* Flag indicating if currently in soft IRQ context */</span></span><br><span class="line">                                            <span class="comment">/* 表示当前是否在软中断上下文中的标志 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_LOCKDEP</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAX_LOCK_DEPTH 48UL                 <span class="comment">/* Maximum lock depth for lock dependency tracking */</span></span></span><br><span class="line">                                            <span class="comment">/* 锁依赖跟踪的最大锁深度 */</span></span><br><span class="line">u64 curr_chain_key;                     <span class="comment">/* Current lock chain key */</span></span><br><span class="line">                                            <span class="comment">/* 当前锁链的键值 */</span></span><br><span class="line"><span class="type">int</span> lockdep_depth;                      <span class="comment">/* Current depth of lock dependency tracking */</span></span><br><span class="line">                                            <span class="comment">/* 当前锁依赖跟踪的深度 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> lockdep_recursion;         <span class="comment">/* Flag to detect lockdep recursion */</span></span><br><span class="line">                                            <span class="comment">/* 检测锁依赖递归的标志 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">held_lock</span> <span class="title">held_locks</span>[<span class="title">MAX_LOCK_DEPTH</span>];</span> <span class="comment">/* Array of held locks */</span></span><br><span class="line">                                            <span class="comment">/* 保存的锁的数组 */</span></span><br><span class="line"><span class="type">gfp_t</span> lockdep_reclaim_gfp;              <span class="comment">/* GFP flags for lockdep reclaim context */</span></span><br><span class="line">                                            <span class="comment">/* lockdep回收上下文的GFP标志 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *journal_info;                     <span class="comment">/* Pointer to journaling information */</span></span><br><span class="line">                                            <span class="comment">/* 指向日志信息的指针 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">bio_list</span> *<span class="title">bio_list</span>;</span>              <span class="comment">/* List of block I/O bio structures */</span></span><br><span class="line">                                            <span class="comment">/* 块I/O bio结构的列表 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_BLOCK</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">blk_plug</span> *<span class="title">plug</span>;</span>                  <span class="comment">/* Pointer to blk_plug for stacking block device I/O */</span></span><br><span class="line">                                            <span class="comment">/* 指向用于堆叠块设备I/O的blk_plug的指针 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">reclaim_state</span> *<span class="title">reclaim_state</span>;</span>    <span class="comment">/* Reclaim state for memory management */</span></span><br><span class="line">                                            <span class="comment">/* 内存管理的回收状态 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">backing_dev_info</span> *<span class="title">backing_dev_info</span>;</span> <span class="comment">/* Information about backing device */</span></span><br><span class="line">                                               <span class="comment">/* 后备设备的信息 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">io_context</span> *<span class="title">io_context</span>;</span>          <span class="comment">/* I/O context for asynchronous I/O */</span></span><br><span class="line">                                            <span class="comment">/* 异步I/O的I/O上下文 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> ptrace_message;           <span class="comment">/* Message passed to tracer */</span></span><br><span class="line">                                            <span class="comment">/* 传递给跟踪器的消息 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">siginfo_t</span> *last_siginfo;                <span class="comment">/* Last signal info, for ptrace */</span></span><br><span class="line">                                            <span class="comment">/* 最后的信号信息，用于ptrace */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_io_accounting</span> <span class="title">ioac</span>;</span>         <span class="comment">/* I/O accounting */</span></span><br><span class="line">                                            <span class="comment">/* I/O记账 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined(CONFIG_TASK_XACCT)</span></span><br><span class="line">u64 acct_rss_mem1;                      <span class="comment">/* Accumulated resident set size memory usage */</span></span><br><span class="line">                                            <span class="comment">/* 累计的常驻集大小内存使用 */</span></span><br><span class="line">u64 acct_vm_mem1;                       <span class="comment">/* Accumulated virtual memory usage */</span></span><br><span class="line">                                            <span class="comment">/* 累计的虚拟内存使用 */</span></span><br><span class="line"><span class="type">cputime_t</span> acct_timexpd;                 <span class="comment">/* Accumulated CPU time since last update */</span></span><br><span class="line">                                            <span class="comment">/* 自上次更新以来累计的CPU时间 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_CPUSETS</span></span><br><span class="line"><span class="type">nodemask_t</span> mems_allowed;               <span class="comment">/* Memory nodes allowed for this task */</span></span><br><span class="line">                                            <span class="comment">/* 此任务允许的内存节点 */</span></span><br><span class="line"><span class="type">int</span> mems_allowed_change_disable;       <span class="comment">/* Flag to disable changing mems_allowed */</span></span><br><span class="line">                                            <span class="comment">/* 禁止更改mems_allowed的标志 */</span></span><br><span class="line"><span class="type">int</span> cpuset_mem_spread_rotor;           <span class="comment">/* Current node index for memory spread */</span></span><br><span class="line">                                            <span class="comment">/* 内存扩展的当前节点索引 */</span></span><br><span class="line"><span class="type">int</span> cpuset_slab_spread_rotor;          <span class="comment">/* Current node index for slab spread */</span></span><br><span class="line">                                            <span class="comment">/* Slab扩展的当前节点索引 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_CGROUPS</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">css_set</span> __<span class="title">rcu</span> *<span class="title">cgroups</span>;</span>          <span class="comment">/* Control groups pointer */</span></span><br><span class="line">                                            <span class="comment">/* 控制组指针 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">cg_list</span>;</span>               <span class="comment">/* List head for control group list */</span></span><br><span class="line">                                            <span class="comment">/* 控制组列表的列表头 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_FUTEX</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">robust_list_head</span> __<span class="title">user</span> *<span class="title">robust_list</span>;</span> <span class="comment">/* List head for robust futexes */</span></span><br><span class="line">                                                <span class="comment">/* 健壮性futex的列表头 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_COMPAT</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">compat_robust_list_head</span> __<span class="title">user</span> *<span class="title">compat_robust_list</span>;</span> <span class="comment">/* Compatibility list head for robust futexes */</span></span><br><span class="line">                                                               <span class="comment">/* 兼容性健壮性futex的列表头 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">pi_state_list</span>;</span>         <span class="comment">/* List head for priority inheritance states */</span></span><br><span class="line">                                            <span class="comment">/* 优先级继承状态的列表头 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">futex_pi_state</span> *<span class="title">pi_state_cache</span>;</span>  <span class="comment">/* Cached futex priority inheritance state */</span></span><br><span class="line">                                            <span class="comment">/* 缓存的futex优先级继承状态 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_PERF_EVENTS</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">perf_event_context</span> *<span class="title">perf_event_ctxp</span>[<span class="title">perf_nr_task_contexts</span>];</span> <span class="comment">/* Per-task performance event contexts */</span></span><br><span class="line">                                                                       <span class="comment">/* 每个任务的性能事件上下文 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mutex</span> <span class="title">perf_event_mutex</span>;</span>           <span class="comment">/* Mutex to protect performance event context */</span></span><br><span class="line">                                            <span class="comment">/* 保护性能事件上下文的互斥锁 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">perf_event_list</span>;</span>        <span class="comment">/* List of performance events */</span></span><br><span class="line">                                            <span class="comment">/* 性能事件列表 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_NUMA</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mempolicy</span> *<span class="title">mempolicy</span>;</span>            <span class="comment">/* Memory policy for NUMA */</span></span><br><span class="line">                                            <span class="comment">/* NUMA的内存策略 */</span></span><br><span class="line"><span class="type">short</span> il_next;                          <span class="comment">/* Next node index for interleaving */</span></span><br><span class="line">                                            <span class="comment">/* 交错的下一个节点索引 */</span></span><br><span class="line"><span class="type">short</span> pref_node_fork;                   <span class="comment">/* Preferred node for task fork */</span></span><br><span class="line">                                            <span class="comment">/* 任务分支的首选节点 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">atomic_t</span> fs_excl;                       <span class="comment">/* Exclusive filesystem resources */</span></span><br><span class="line">                                            <span class="comment">/* 独占文件系统资源 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rcu_head</span> <span class="title">rcu</span>;</span>                    <span class="comment">/* RCU head used for call_rcu() */</span></span><br><span class="line">                                            <span class="comment">/* 用于call_rcu()的RCU头 */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pipe_inode_info</span> *<span class="title">splice_pipe</span>;</span>    <span class="comment">/* Cached pipe for splice operations */</span></span><br><span class="line">                                            <span class="comment">/* 用于splice操作的缓存管道 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_TASK_DELAY_ACCT</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_delay_info</span> *<span class="title">delays</span>;</span>         <span class="comment">/* Delays accounting */</span></span><br><span class="line">                                            <span class="comment">/* 延迟记账 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_FAULT_INJECTION</span></span><br><span class="line"><span class="type">int</span> make_it_fail;                       <span class="comment">/* Fault injection control */</span></span><br><span class="line">                                            <span class="comment">/* 故障注入控制 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">prop_local_single</span> <span class="title">dirties</span>;</span>       <span class="comment">/* Dirty limits and statistics */</span></span><br><span class="line">                                            <span class="comment">/* 脏页限制和统计 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_LATENCYTOP</span></span><br><span class="line"><span class="type">int</span> latency_record_count;               <span class="comment">/* Number of latency records */</span></span><br><span class="line">                                            <span class="comment">/* 延迟记录的数量 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">latency_record</span> <span class="title">latency_record</span>[<span class="title">LT_SAVECOUNT</span>];</span> <span class="comment">/* Array of latency records */</span></span><br><span class="line">                                                        <span class="comment">/* 延迟记录数组 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> timer_slack_ns;           <span class="comment">/* Timer slack in nanoseconds */</span></span><br><span class="line">                                            <span class="comment">/* 定时器的松弛时间（纳秒） */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> default_timer_slack_ns;   <span class="comment">/* Default timer slack in nanoseconds */</span></span><br><span class="line">                                            <span class="comment">/* 默认的定时器松弛时间（纳秒） */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">scm_work_list</span>;</span>        <span class="comment">/* List of SCM work items */</span></span><br><span class="line">                                            <span class="comment">/* SCM工作项列表 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_FUNCTION_GRAPH_TRACER</span></span><br><span class="line"><span class="type">int</span> curr_ret_stack;                     <span class="comment">/* Current index in the stack of function return addresses */</span></span><br><span class="line">                                            <span class="comment">/* 函数返回地址栈的当前索引 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ftrace_ret_stack</span> *<span class="title">ret_stack</span>;</span>     <span class="comment">/* Stack of return addresses for function tracing */</span></span><br><span class="line">                                            <span class="comment">/* 用于函数跟踪的返回地址栈 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> <span class="type">long</span> ftrace_timestamp;    <span class="comment">/* Timestamp for the last schedule in function graph tracing */</span></span><br><span class="line">                                            <span class="comment">/* 函数图跟踪中最后一次调度的时间戳 */</span></span><br><span class="line"><span class="type">atomic_t</span> trace_overrun;                 <span class="comment">/* Counter for trace buffer overruns, indicating the loss of trace data */</span></span><br><span class="line">                                            <span class="comment">/* 跟踪缓冲区溢出计数器，指示丢失的跟踪数据 */</span></span><br><span class="line"><span class="type">atomic_t</span> tracing_graph_pause;           <span class="comment">/* Atomic flag to pause/resume function graph tracing */</span></span><br><span class="line">                                            <span class="comment">/* 用于暂停/恢复函数图跟踪的原子标志 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_TRACING</span></span><br><span class="line"><span class="comment">/* state flags for use by tracers */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> trace;                    <span class="comment">/* Flags used by tracers to manage tracing states */</span></span><br><span class="line">                                            <span class="comment">/* 用于跟踪器管理跟踪状态的标志 */</span></span><br><span class="line"><span class="comment">/* bitmask of trace recursion */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> trace_recursion;          <span class="comment">/* Bitmask to handle recursion in tracing */</span></span><br><span class="line">                                            <span class="comment">/* 处理跟踪递归的位掩码 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> <span class="comment">/* CONFIG_TRACING */</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_CGROUP_MEM_RES_CTLR <span class="comment">/* memcg uses this to do batch job */</span></span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">memcg_batch_info</span> &#123;</span></span><br><span class="line"><span class="type">int</span> do_batch;                    <span class="comment">/* Incremented when batch uncharge is started */</span></span><br><span class="line">                                            <span class="comment">/* 批量未充电开始时递增 */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mem_cgroup</span> *<span class="title">memcg</span>;</span>           <span class="comment">/* Target memory cgroup for uncharge */</span></span><br><span class="line">                                            <span class="comment">/* 未充电的目标内存组 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> nr_pages;            <span class="comment">/* Number of pages uncharged */</span></span><br><span class="line">                                            <span class="comment">/* 未充电的页面数 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> memsw_nr_pages;       <span class="comment">/* Number of memory+swap pages uncharged */</span></span><br><span class="line">                                            <span class="comment">/* 未充电的内存+交换页面数 */</span></span><br><span class="line">&#125; memcg_batch;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_HAVE_HW_BREAKPOINT</span></span><br><span class="line"><span class="type">atomic_t</span> ptrace_bp_refcnt;              <span class="comment">/* Reference count for hardware breakpoints */</span></span><br><span class="line">                                            <span class="comment">/* 硬件断点的引用计数 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="进程状态"><a href="#进程状态" class="headerlink" title="进程状态"></a><strong>进程状态</strong></h2><p>进程状态由<code>volatile long state;</code>代码所定义，其中的state，我们在&#x2F;include&#x2F;linux&#x2F;sched.h中有着以下的宏定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_RUNNING0</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_INTERRUPTIBLE1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_UNINTERRUPTIBLE2</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __TASK_STOPPED4</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> __TASK_TRACED8</span></span><br><span class="line"><span class="comment">/* in tsk-&gt;exit_state */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> EXIT_ZOMBIE16</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> EXIT_DEAD32</span></span><br><span class="line"><span class="comment">/* in tsk-&gt;state again */</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_DEAD64</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_WAKEKILL128</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_WAKING256</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TASK_STATE_MAX512</span></span><br></pre></td></tr></table></figure><ul><li>可运行状态(TASK_RUNNING)<br>  进程要么在CPU上执行，要么准备执行。可中断的等待状态(TASK_INTERRUPTIBLE)进程被挂起(睡眠)，直到某个条件变为真。产生一个硬件中断，释放进程正等待的系统资源，或传递一个信号都是可以唤醒进程的条件(把进程的状态放回到TASK_RUNNING)。</li><li>不可中断的等待状态(TASK_UNINTERRUPTIBLE)<br>  与可中断的等待状态类似,但有一个例外,把信号传递到睡眠进程不能改变它的状态。这种状态很少用到，但在一些特定的情况下(进程必须等待，直到一个不能被中断的事件发生)，这种状态是很有用的。例如，当进程打开一个设备文件，其相应的设备驱动程序开始探测相应的硬件设备时会用到这种状态。探测完成以前,设备驱动程序不能被中断，否则，硬件设备会处于不可预知的状态。</li><li>暂停状态(TASK_STOPPED)<br>  进程的执行被暂停。当进程接收到SIGSTOP、SIGTSTP、SIGTTIN或SIGTTOU信号后，进入暂停状态。</li><li>跟踪状态(TASK_TRACED)<br>  进程的执行已由debugger程序暂停。当一个进程被另一个进程监控时(例如debugger执行ptrace()系统调用监控一个测试程序)，任何信号都可以把这个进程置于 TASK_TRACED状态。</li></ul><p>剩下两个进程状态是既可以存放在进程描述符的state字段中,也可以存放在exit_state字段中。从这两个字段的名称可以看出,只有当进程的执行被终止时,进程的状态才会变为这两种状态中的一种:</p><ul><li>僵死状态(EXIT_2OMBIE)<br>  进程的执行被终止，但是，父进程还没有发布wait4()或waitpid()系统调用来返回有关死亡进程的信息。发布wait()类系统调用前，内核不能丢弃包含在死进程描述符中的数据，因为父进程可能还需要它。</li><li>僵死撤消状态(EXIT_DEAD)<br>  最终状态:由于父进程刚发出wait4()或waitpid()系统调用，因而进程由系统删除。为了防止其他执行线程在同一个进程上也执行wait()类系统调用(这是一种竞争条件)，而把进程的状态由僵死(EXIT_ZOMBIE)状态改为死撤消状态(EXIT_DEAD)。</li></ul><img src="/2024/10/20/Linux/process/%E8%BF%9B%E7%A8%8B/image-20250616211945086.png" class title="image-20250616211945086"><h2 id="进程标识符-Process-ID"><a href="#进程标识符-Process-ID" class="headerlink" title="进程标识符-Process_ID"></a><strong>进程标识符-Process_ID</strong></h2><p>进程标识符（Process ID）是进程描述符中最重要的组成部分，是在当前 Linux 系统中唯一的一个非负整数，用于标识和对应唯一的进程。</p><p>在Linux中，有几个特殊的进程标识符所对应的进程。</p><ul><li>进程标识符0：对应的是交换进程（swapper），用于执行多进程的调用。</li><li>进程标识符1：对应的是初始化进程（init），在自举过程结束时由内核调用，对应的文件是&#x2F;sbin&#x2F;init，负责 Linux 的启动工作，这个进程在系统运行过程中是不会终止的，可以说当前操作系统中的所有进程都是这个进程衍生而来的。</li><li>进程标识符2：可能对应页守护进程（pagedaemon），用于虚拟存储系统的分页操作。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span> </span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span> </span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getpid</span><span class="params">(<span class="type">void</span>)</span>; </span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getppid</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><p>由于循环使用PID编号，内核必须通过管理一个pidmap-array位图来表示当前已分配的PID号和闲置的PID号。因为一个页框包含32768个位，所以在32位体系结构中pidmap-array位图存放在一个单独的页中。然而，在64位体系结构中，当内核分配了超过当前位图大小的PID号时，需要为PID位图增加更多的页。系统会一直保存这些页不被释放。</p><p>对于pid的分配，是在<a href="/computer_system/%E8%BF%9B%E7%A8%8B/%E5%88%9B%E5%BB%BA%E8%BF%9B%E7%A8%8B#%60copy_process%60"><code>copy_process()</code></a>过程中完成的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">struct</span> task_struct *<span class="title function_">copy_process</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span> clone_flags,</span></span><br><span class="line"><span class="params"><span class="type">unsigned</span> <span class="type">long</span> stack_start,</span></span><br><span class="line"><span class="params"><span class="keyword">struct</span> pt_regs *regs,</span></span><br><span class="line"><span class="params"><span class="type">unsigned</span> <span class="type">long</span> stack_size,</span></span><br><span class="line"><span class="params"><span class="type">int</span> __user *child_tidptr,</span></span><br><span class="line"><span class="params"><span class="keyword">struct</span> pid *pid,</span></span><br><span class="line"><span class="params"><span class="type">int</span> trace)</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">p</span>;</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (pid != &amp;init_struct_pid) &#123;</span><br><span class="line">retval = -ENOMEM;</span><br><span class="line">pid = alloc_pid(p-&gt;nsproxy-&gt;pid_ns); <span class="comment">// 分配pid</span></span><br><span class="line"><span class="keyword">if</span> (!pid)</span><br><span class="line"><span class="keyword">goto</span> bad_fork_cleanup_io;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">p-&gt;pid = pid_nr(pid); <span class="comment">//设置pid</span></span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="alloc-pid"><a href="#alloc-pid" class="headerlink" title="alloc_pid()"></a>alloc_pid()</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> pid *<span class="title function_">alloc_pid</span><span class="params">(<span class="keyword">struct</span> pid_namespace *ns)</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pid</span> *<span class="title">pid</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">pid_type</span> <span class="title">type</span>;</span></span><br><span class="line"><span class="type">int</span> i, nr;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pid_namespace</span> *<span class="title">tmp</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">upid</span> *<span class="title">upid</span>;</span></span><br><span class="line"></span><br><span class="line">pid = kmem_cache_alloc(ns-&gt;pid_cachep, GFP_KERNEL); <span class="comment">//分配pid结构体的内存</span></span><br><span class="line"><span class="keyword">if</span> (!pid)</span><br><span class="line"><span class="keyword">goto</span> out;</span><br><span class="line"></span><br><span class="line">tmp = ns;</span><br><span class="line"><span class="keyword">for</span> (i = ns-&gt;level; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">nr = alloc_pidmap(tmp); <span class="comment">//分配pid</span></span><br><span class="line"><span class="keyword">if</span> (nr &lt; <span class="number">0</span>)</span><br><span class="line"><span class="keyword">goto</span> out_free;</span><br><span class="line"></span><br><span class="line">pid-&gt;numbers[i].nr = nr; <span class="comment">//nr保存到pid结构体</span></span><br><span class="line">pid-&gt;numbers[i].ns = tmp;</span><br><span class="line">tmp = tmp-&gt;parent;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">get_pid_ns(ns);</span><br><span class="line">pid-&gt;level = ns-&gt;level;</span><br><span class="line"><span class="type">atomic_set</span>(&amp;pid-&gt;count, <span class="number">1</span>);</span><br><span class="line"><span class="keyword">for</span> (type = <span class="number">0</span>; type &lt; PIDTYPE_MAX; ++type)</span><br><span class="line">INIT_HLIST_HEAD(&amp;pid-&gt;tasks[type]); <span class="comment">//初始化pid的hlist结构体</span></span><br><span class="line"></span><br><span class="line">upid = pid-&gt;numbers + ns-&gt;level;</span><br><span class="line">spin_lock_irq(&amp;pidmap_lock);</span><br><span class="line"><span class="keyword">for</span> ( ; upid &gt;= pid-&gt;numbers; --upid)</span><br><span class="line">hlist_add_head_rcu(&amp;upid-&gt;pid_chain,</span><br><span class="line">&amp;pid_hash[pid_hashfn(upid-&gt;nr, upid-&gt;ns)]); <span class="comment">//建立pid_hash的关联关系</span></span><br><span class="line">spin_unlock_irq(&amp;pidmap_lock);</span><br><span class="line"></span><br><span class="line">out:</span><br><span class="line"><span class="keyword">return</span> pid;</span><br><span class="line"></span><br><span class="line">out_free:</span><br><span class="line"><span class="keyword">while</span> (++i &lt;= ns-&gt;level)</span><br><span class="line">free_pidmap(pid-&gt;numbers + i);</span><br><span class="line"></span><br><span class="line">kmem_cache_free(ns-&gt;pid_cachep, pid);</span><br><span class="line">pid = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">goto</span> out;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们来分析几个比较重要的地方：</p><h5 id="kmem-cache-alloc-ns-pid-cachep-GFP-KERNEL"><a href="#kmem-cache-alloc-ns-pid-cachep-GFP-KERNEL" class="headerlink" title="kmem_cache_alloc(ns-&gt;pid_cachep, GFP_KERNEL);"></a><code>kmem_cache_alloc(ns-&gt;pid_cachep, GFP_KERNEL);</code></h5><ol><li><strong><code>kmem_cache_alloc</code></strong>：<ul><li>这是内核中用于分配内存的函数，它从特定的内核缓存中分配对象。内核通过这种方法来提高分配效率，并减少内存碎片。</li><li>这里用 <code>kmem_cache_alloc</code> 从 PID 缓存中分配一个新的 <code>pid</code> 结构。</li><li>内核使用对象缓存（<a href>slab 缓存</a>）来存储固定大小的对象（如 <code>pid</code> 结构）。通过这种机制，可以避免频繁的通用内存分配带来的性能损耗，同时减少内存碎片化。</li></ul></li><li><strong><code>ns-&gt;pid_cachep</code></strong>：<ul><li><code>pid_cachep</code> 是当前命名空间 (<a href="#pid_namespace%E7%BB%93%E6%9E%84%E4%BD%93">namespace</a>) 中用于管理 PID 的缓存指针。每个命名空间都有自己独立的 PID 缓存，用于存储与 PID 相关的数据。</li></ul></li><li><strong><code>GFP_KERNEL</code></strong>：<ul><li>这是分配标志，表示分配是在内核上下文中执行的，允许内存分配时进行阻塞等待。这是内核中最常用的分配模式。</li></ul></li><li><strong><code>pid</code></strong>：<ul><li><code>pid</code> 是一个变量，用于保存分配到的 <code>pid</code> 结构（进程标识符结构）。</li></ul></li></ol><h5 id="alloc-pidmap-tmp"><a href="#alloc-pidmap-tmp" class="headerlink" title="alloc_pidmap(tmp)"></a><code>alloc_pidmap(tmp)</code></h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">alloc_pidmap</span><span class="params">(<span class="keyword">struct</span> pid_namespace *pid_ns)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="comment">//last_pid为上次分配出去的pid</span></span><br><span class="line">  <span class="type">int</span> i, offset, max_scan, pid, last = pid_ns-&gt;last_pid;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">pidmap</span> *<span class="title">map</span>;</span></span><br><span class="line"></span><br><span class="line">  pid = last + <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">if</span> (pid &gt;= pid_max)</span><br><span class="line">    pid = RESERVED_PIDS; <span class="comment">//默认为300</span></span><br><span class="line">    </span><br><span class="line">  offset = pid &amp; BITS_PER_PAGE_MASK; <span class="comment">//最高位值置0，其余位不变</span></span><br><span class="line">  <span class="built_in">map</span> = &amp;pid_ns-&gt;pidmap[pid/BITS_PER_PAGE]; <span class="comment">//找到目标pidmap</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">//当offset =0，则扫描一次;</span></span><br><span class="line">  <span class="comment">//当offset!=0，则扫描两次</span></span><br><span class="line">  max_scan = DIV_ROUND_UP(pid_max, BITS_PER_PAGE) - !offset;</span><br><span class="line">  <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt;= max_scan; ++i) &#123;</span><br><span class="line">    <span class="keyword">if</span> (unlikely(!<span class="built_in">map</span>-&gt;page)) &#123;</span><br><span class="line">      <span class="type">void</span> *page = kzalloc(PAGE_SIZE, GFP_KERNEL);</span><br><span class="line">      spin_lock_irq(&amp;pidmap_lock);</span><br><span class="line">      <span class="keyword">if</span> (!<span class="built_in">map</span>-&gt;page) &#123;</span><br><span class="line">        <span class="built_in">map</span>-&gt;page = page;</span><br><span class="line">        page = <span class="literal">NULL</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      spin_unlock_irq(&amp;pidmap_lock);</span><br><span class="line">      kfree(page);</span><br><span class="line">      <span class="keyword">if</span> (unlikely(!<span class="built_in">map</span>-&gt;page))</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//当pidmap还有可用pid时</span></span><br><span class="line">    <span class="keyword">if</span> (likely(<span class="type">atomic_read</span>(&amp;<span class="built_in">map</span>-&gt;nr_free))) &#123;</span><br><span class="line">      <span class="keyword">do</span> &#123;</span><br><span class="line">        <span class="comment">//当offset位空闲时返回该pid</span></span><br><span class="line">        <span class="keyword">if</span> (!test_and_set_bit(offset, <span class="built_in">map</span>-&gt;page)) &#123;</span><br><span class="line">          <span class="type">atomic_dec</span>(&amp;<span class="built_in">map</span>-&gt;nr_free); <span class="comment">//可用pid减一</span></span><br><span class="line">          set_last_pid(pid_ns, last, pid); <span class="comment">//设置last_pid</span></span><br><span class="line">          <span class="keyword">return</span> pid;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//否则，查询下一个非0的offset值</span></span><br><span class="line">        offset = find_next_offset(<span class="built_in">map</span>, offset);</span><br><span class="line">        根据offset转换成相应的pid</span><br><span class="line">        pid = mk_pid(pid_ns, <span class="built_in">map</span>, offset);</span><br><span class="line">      &#125; <span class="keyword">while</span> (offset &lt; BITS_PER_PAGE &amp;&amp; pid &lt; pid_max);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//当上述pid分配失败，则再次查找offset</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">map</span> &lt; &amp;pid_ns-&gt;pidmap[(pid_max<span class="number">-1</span>)/BITS_PER_PAGE]) &#123;</span><br><span class="line">      ++<span class="built_in">map</span>;</span><br><span class="line">      offset = <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="built_in">map</span> = &amp;pid_ns-&gt;pidmap[<span class="number">0</span>];</span><br><span class="line">      offset = RESERVED_PIDS;</span><br><span class="line">      <span class="keyword">if</span> (unlikely(last == offset))</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    pid = mk_pid(pid_ns, <span class="built_in">map</span>, offset);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h5 id="pid-namespace结构体"><a href="#pid-namespace结构体" class="headerlink" title="pid_namespace结构体"></a><code>pid_namespace结构体</code></h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pid_namespace</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">kref</span> <span class="title">kref</span>;</span>  </span><br><span class="line">  <span class="comment">/* 引用计数，用于管理 `pid_namespace` 的生命周期。通过 kref 机制，</span></span><br><span class="line"><span class="comment">     当引用计数减为 0 时，`pid_namespace` 会被释放。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">pidmap</span> <span class="title">pidmap</span>[<span class="title">PIDMAP_ENTRIES</span>];</span>  </span><br><span class="line">  <span class="comment">/* PID 位图数组，每个 pidmap 用于标记哪些 PID 是空闲的，</span></span><br><span class="line"><span class="comment">     哪些已被分配。位图是用来高效管理 PID 分配的核心数据结构。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">rcu_head</span> <span class="title">rcu</span>;</span>  </span><br><span class="line">  <span class="comment">/* 用于 RCU（Read-Copy Update）机制，当 `pid_namespace` 被释放时，</span></span><br><span class="line"><span class="comment">     确保在延迟删除期间不会破坏并发访问。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> last_pid;  </span><br><span class="line">  <span class="comment">/* 最近一次分配的 PID。这个字段用于生成下一个 PID（从上次分配的 PID 开始递增）。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="type">unsigned</span> <span class="type">int</span> nr_hashed;  </span><br><span class="line">  <span class="comment">/* 当前命名空间中被分配 PID 的数量。这个值反映了命名空间中活动进程的数量。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">child_reaper</span>;</span>  </span><br><span class="line">  <span class="comment">/* 指向该命名空间中充当 `init` 进程的任务结构指针。</span></span><br><span class="line"><span class="comment">     如果命名空间中没有任务存活，`child_reaper` 会负责回收孤儿进程。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">kmem_cache</span> *<span class="title">pid_cachep</span>;</span>  </span><br><span class="line">  <span class="comment">/* Slab 缓存，用于快速分配和回收 `pid` 结构体对象，</span></span><br><span class="line"><span class="comment">     减少内存分配的开销和碎片化。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="type">unsigned</span> <span class="type">int</span> level;  </span><br><span class="line">  <span class="comment">/* 命名空间的嵌套级别，`0` 表示初始命名空间，数字越大表示嵌套越深。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">pid_namespace</span> *<span class="title">parent</span>;</span>  </span><br><span class="line">  <span class="comment">/* 指向父命名空间。如果是初始命名空间（最顶层），则为 NULL。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 省略部分字段 ...</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">user_namespace</span> *<span class="title">user_ns</span>;</span>  </span><br><span class="line">  <span class="comment">/* 与当前 `pid_namespace` 相关联的用户命名空间。</span></span><br><span class="line"><span class="comment">     用户命名空间定义了与权限和 UID/GID 有关的隔离。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">work_struct</span> <span class="title">proc_work</span>;</span>  </span><br><span class="line">  <span class="comment">/* 用于延迟执行操作的 `work_struct` 结构体，</span></span><br><span class="line"><span class="comment">     主要和 `/proc` 文件系统的相关操作有关。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="type">kgid_t</span> pid_gid;  </span><br><span class="line">  <span class="comment">/* PID 的组 ID，决定了 `/proc` 文件系统中 `pid` 的所有者组权限。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> hide_pid;  </span><br><span class="line">  <span class="comment">/* 决定是否隐藏进程信息：</span></span><br><span class="line"><span class="comment">     - 0：所有用户均可查看其他进程的信息。</span></span><br><span class="line"><span class="comment">     - 1：仅允许用户查看与自己相关的进程。</span></span><br><span class="line"><span class="comment">     - 2：完全隐藏其他用户的进程信息。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="type">int</span> reboot;  </span><br><span class="line">  <span class="comment">/* 用于存储命名空间是否允许重启操作。 */</span></span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">ns_common</span> <span class="title">ns</span>;</span>  </span><br><span class="line">  <span class="comment">/* 通用命名空间结构，提供与内核命名空间接口相关的字段（如 `inum`）。 */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>PID命名空间，这是为系统提供虚拟化做支撑的功能。</p><h2 id="thread-info"><a href="#thread-info" class="headerlink" title="thread_info"></a><strong>thread_info</strong></h2><h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><p>Linux通过slab动态生成task_struct，那么在栈顶或栈底创建新的结构体thread_info即可，其中task指向其真正的task_struct结构体。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thread_info</span> &#123;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span>*<span class="title">task</span>;</span><span class="comment">/* main task structure */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">exec_domain</span>*<span class="title">exec_domain</span>;</span><span class="comment">/* execution domain */</span></span><br><span class="line">__u32flags;<span class="comment">/* low level flags */</span></span><br><span class="line">__u32status;<span class="comment">/* thread synchronous flags */</span></span><br><span class="line">__u32cpu;<span class="comment">/* current CPU */</span></span><br><span class="line"><span class="type">int</span>preempt_count;<span class="comment">/* 0 =&gt; preemptable,</span></span><br><span class="line"><span class="comment">   &lt;0 =&gt; BUG */</span></span><br><span class="line"><span class="type">mm_segment_t</span>addr_limit;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">restart_block</span>    <span class="title">restart_block</span>;</span></span><br><span class="line"><span class="type">void</span> __user*sysenter_return;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_32</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>           previous_esp;   <span class="comment">/* ESP of the previous stack in</span></span><br><span class="line"><span class="comment">   case of nested (IRQ) stacks</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">__u8supervisor_stack[<span class="number">0</span>];</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="type">int</span>uaccess_err;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="内核stack和thread-info结构的关系"><a href="#内核stack和thread-info结构的关系" class="headerlink" title="内核stack和thread_info结构的关系"></a>内核stack和thread_info结构的关系</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">union</span> <span class="title">thread_union</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">thread_info</span> <span class="title">thread_info</span>;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> <span class="built_in">stack</span>[THREAD_SIZE/<span class="keyword">sizeof</span>(<span class="type">long</span>)];</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"><span class="meta">#<span class="keyword">define</span> THREAD_SIZE        16384 \\8k</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> THREAD_START_SP        (THREAD_SIZE - 16)</span></span><br></pre></td></tr></table></figure><p>内核定义了一个thread_union的联合体，联合体的作用就是thread_info和stack共用一块内存区域。</p><img src="/2024/10/20/Linux/process/%E8%BF%9B%E7%A8%8B/image-20250616212105918.png" class title="image-20250616212105918"><p>我们获得当前内核栈的sp指针的地址，然后根据THREAD_SIZE对齐就可以获取thread_info结构的基地址，然后从thread_info.task就可以获取当前task_struct结构的地址了。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> get_current() (current_thread_info()-&gt;task)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> current get_current()</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * how to get the current stack pointer from C</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">register</span> <span class="type">unsigned</span> <span class="type">long</span> current_stack_pointer <span class="title function_">asm</span> <span class="params">(<span class="string">&quot;sp&quot;</span>)</span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * how to get the thread information struct from C</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="keyword">struct</span> thread_info *<span class="title function_">current_thread_info</span><span class="params">(<span class="type">void</span>)</span> __attribute_const__;</span><br><span class="line"> </span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="keyword">struct</span> thread_info *<span class="title function_">current_thread_info</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">struct</span> thread_info *)</span><br><span class="line">        (current_stack_pointer &amp; ~(THREAD_SIZE - <span class="number">1</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="进程切换"><a href="#进程切换" class="headerlink" title="进程切换"></a><strong>进程切换</strong></h2><h3 id="硬件上下文"><a href="#硬件上下文" class="headerlink" title="硬件上下文"></a>硬件上下文</h3><p>我们知道每个进程都有自己的地址空间，但是所有的进程却共享CPU寄存器。所以，在恢复进程执行之前，内核必须保证该进程在挂起时的寄存器值重新加载到CPU的寄存器中。</p><p>这些需要加载到CPU寄存器中的值就成为硬件上下文。硬件上下文是进程执行上下文的一个子集，进程执行上下文包含进程执行所需要的所有信息。在Linux中，进程的硬件上下文一部分存储在进程描述符中，而其它部分存储在内核态的栈中。</p><p>在下面的描述中，我们假设，prev指向旧进程，而next指向新进程。因此，我们就可以说，进程切换就是保存prev进程的硬件上下文，然后加载next进程的硬件上下文。因为进程的切换非常频繁，所以缩短保存和加载硬件上下文的时间就很重要了。</p><p>硬件上下文放在 task_struct 中的 thread（struct thread_struct）中的struct cpu_context中（arm64），但在x8664中又有所不同。我们来解析一下struct thread_struct：</p><h4 id="struct-thread-struct"><a href="#struct-thread-struct" class="headerlink" title="struct thread_struct"></a>struct thread_struct</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thread_struct</span> &#123;</span></span><br><span class="line"><span class="comment">/* TLS（线程本地存储）相关： */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">desc_struct</span><span class="title">tls_array</span>[<span class="title">GDT_ENTRY_TLS_ENTRIES</span>];</span></span><br><span class="line"><span class="comment">/* 每个线程可以有多个 TLS 段（最多3个，分别为 FS、GS 和 DS 段），</span></span><br><span class="line"><span class="comment">   这些段用于实现线程本地存储，在多线程环境下用于隔离数据访问。 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_32</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>sp0;</span><br><span class="line"><span class="comment">/* 用户态线程切换到内核态时的栈指针，专用于 x86_32 架构。 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>sp;</span><br><span class="line"><span class="comment">/* 当前栈指针，保存当前线程的栈位置（用户态或内核态）。 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_32</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>sysenter_cs;</span><br><span class="line"><span class="comment">/* 在 x86_32 架构中，保存 `SYSENTER` 指令的代码段选择子，用于快速系统调用。 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">short</span>es;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">short</span>ds;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">short</span>fsindex;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">short</span>gsindex;</span><br><span class="line"><span class="comment">/* 在 x86_64 架构中，保存段寄存器选择子。FS 和 GS 常用于线程本地存储。 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_X86_64</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>fsbase;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>gsbase;</span><br><span class="line"><span class="comment">/* 在 x86_64 架构中，FS 和 GS 寄存器的基址。</span></span><br><span class="line"><span class="comment">   这些值通常用于实现线程本地存储（TLS）。 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> fs;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> gs;</span><br><span class="line"><span class="comment">/* 在 x86_32 架构中，FS 和 GS 的段选择子或基址。 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Ptrace 调试相关状态： */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">perf_event</span>*<span class="title">ptrace_bps</span>[<span class="title">HBP_NUM</span>];</span></span><br><span class="line"><span class="comment">/* 硬件断点的状态，用于 ptrace 调试时保存断点的相关信息。 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>           debugreg6;</span><br><span class="line"><span class="comment">/* 调试寄存器 DR6 的值，用于捕获硬件断点或调试事件。 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>           ptrace_dr7;</span><br><span class="line"><span class="comment">/* 调试寄存器 DR7 的值，保存用户设置的调试状态。 */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 故障相关信息： */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>cr2;</span><br><span class="line"><span class="comment">/* 保存最近一次页错误的地址（由 CR2 寄存器提供）。 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>trap_nr;</span><br><span class="line"><span class="comment">/* 保存最近一次触发的异常号（Trap Number）。 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>error_code;</span><br><span class="line"><span class="comment">/* 保存最近一次异常的错误码（由硬件提供）。 */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_VM86</span></span><br><span class="line"><span class="comment">/* 虚拟 8086 模式相关信息（仅适用于 x86）： */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">vm86</span>*<span class="title">vm86</span>;</span></span><br><span class="line"><span class="comment">/* 当线程运行在虚拟 8086 模式下时，指向与虚拟 8086 模式相关的状态信息。 */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* IO 端口访问相关： */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>*io_bitmap_ptr;</span><br><span class="line"><span class="comment">/* 指向 IO 权限位图，用于限制线程对 IO 端口的访问权限。 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>iopl;</span><br><span class="line"><span class="comment">/* 当前线程的 IO 优先级级别（0-3，越低权限越高）。 */</span></span><br><span class="line"><span class="type">unsigned</span>io_bitmap_max;</span><br><span class="line"><span class="comment">/* IO 位图中最大的允许端口号（以字节为单位）。 */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 地址空间限制： */</span></span><br><span class="line"><span class="type">mm_segment_t</span>addr_limit;</span><br><span class="line"><span class="comment">/* 当前线程的地址空间限制（`USER_DS` 或 `KERNEL_DS`），</span></span><br><span class="line"><span class="comment">   决定线程是否运行在用户态还是内核态。 */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 用户空间访问错误状态： */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span>sig_on_uaccess_err:<span class="number">1</span>;</span><br><span class="line"><span class="comment">/* 如果用户态访问失败时，是否需要发送信号。 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span>uaccess_err:<span class="number">1</span>;</span><br><span class="line"><span class="comment">/* 线程最近一次用户空间访问是否失败的标志。 */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* 浮点和扩展处理器状态： */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">fpu</span><span class="title">fpu</span>;</span></span><br><span class="line"><span class="comment">/* 保存线程的浮点运算单元（FPU）和扩展处理器状态（如 AVX、SSE）。 */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * WARNING: &#x27;fpu&#x27; is dynamically-sized.  It *MUST* be at</span></span><br><span class="line"><span class="comment"> * the end.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">/* 注意：`fpu` 是动态大小的结构体，必须放在结构体末尾以便动态扩展。 */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h4 id="struct-pt-regs"><a href="#struct-pt-regs" class="headerlink" title="struct pt_regs"></a>struct pt_regs</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pt_regs</span> &#123;</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * C ABI says these regs are callee-preserved. They aren&#x27;t saved on kernel entry</span></span><br><span class="line"><span class="comment"> * unless syscall needs a complete, fully filled &quot;struct pt_regs&quot;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r15;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r14;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r13;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r12;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> bp;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> bx;</span><br><span class="line"><span class="comment">/* These regs are callee-clobbered. Always saved on kernel entry. */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r11;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r10;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r9;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> r8;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> ax;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> cx;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> dx;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> si;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> di;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * On syscall entry, this is syscall#. On CPU exception, this is error code.</span></span><br><span class="line"><span class="comment"> * On hw interrupt, it&#x27;s IRQ number:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> orig_ax;</span><br><span class="line"><span class="comment">/* Return frame for iretq */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> ip;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> cs;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> flags;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> sp;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> ss;</span><br><span class="line"><span class="comment">/* top of stack page */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h4 id="struct-thread-struct-1"><a href="#struct-thread-struct-1" class="headerlink" title="struct thread_struct"></a><strong><code>struct thread_struct</code></strong></h4><ul><li>保存线程特定的架构相关状态，如：<ul><li><code>sp</code>（栈指针）、<code>addr_limit</code>（地址空间限制）。</li><li>FPU 状态和调试寄存器。</li></ul></li></ul><h4 id="struct-pt-regs-1"><a href="#struct-pt-regs-1" class="headerlink" title="struct pt_regs"></a><strong><code>struct pt_regs</code></strong></h4><ul><li>保存中断或异常发生时的寄存器状态。</li></ul><h4 id="task-struct"><a href="#task-struct" class="headerlink" title="task_struct"></a><strong><code>task_struct</code></strong></h4><ul><li>包含线程的所有上下文信息，包括 <code>thread_struct</code> 和调度信息.</li></ul><h4 id="struct-fpu"><a href="#struct-fpu" class="headerlink" title="struct fpu"></a><strong><code>struct fpu</code></strong></h4><ul><li>保存线程的浮点处理单元（FPU）和扩展寄存器状态。</li></ul><h4 id="新旧方法："><a href="#新旧方法：" class="headerlink" title="新旧方法："></a>新旧方法：</h4><p>旧版本的linux利用x86架构提供的硬件支持，并通过远程调转指令（GNU-<code>ljump</code>；Intel-<code>jmp far</code>）进行进程切换，跳转到下一个进程的任务状态段（TSS）描述符。执行这条跳转指令的同时，CPU自动执行硬件上下文切换，保存旧的硬件上下文，加载新的硬件上下文。但是，linux2.6版本以后，通过软件进行进程切换，原因如下：</p><ul><li>通过一连串的mov指令，一步步执行切换，可以更好地控制加载数据的合法性。尤其是ds和es段寄存器中的值，有可能会被恶意用户篡改。如果使用远程跳转指令是无法进程数据检查的。</li><li>新旧方法所要求的时间是大致相同的。但是，优化硬件上下文的切换是不可能的，因为都是由CPU完成的，而Linux是使用软件代替硬件上下文切换的，所以有优化的空间，以便提高执行时间。</li></ul><h3 id="任务状态段-TSS"><a href="#任务状态段-TSS" class="headerlink" title="任务状态段-TSS"></a>任务状态段-TSS</h3><p>x86架构包含一个特殊的段寄存器，称为任务状态段（TSS），用来保存硬件上下文内容。尽管Linux不使用硬件上下文切换，但还是给每个不同CPU建立一个TSS。这么做，基于两个原因：</p><ul><li>当x86架构的CPU从用户态到内核态时，会从TSS中获取内核态的栈地址</li><li>用户态进程想要访问I&#x2F;O端口的时候，CPU需要访问存储在TSS中的I&#x2F;O权限位，判断进程是否被允许访问这个I&#x2F;O端口。那么，当用户态进程执行in或out指令时，I&#x2F;O控制单元到底做了什么呢？<ol><li>检查eflags寄存器中IOPL位（2位）。如果等于3，也就是超级用户权限，也就是进程对于这个I&#x2F;O端口来说就是一个超级用户，那么，直接执行I&#x2F;O指令。否则，继续执行检查。</li><li>访问tr寄存器，确定当前的TSS，以及正确的I&#x2F;O访问权限。</li><li>它检查I&#x2F;O端口对应的访问权限位。如果清零，指令被执行；否则，控制单元发出常规保护的异常。</li></ol></li></ul><p>内核中使用tss_struct结构体描述TSS。init_tss数组为系统中的每一个CPU包含一个tss_struct结构。每一次进程切换，内核更新TSS相关内容，使CPU控制单元能够安全地检索自己想要的信息。因而，TSS反映了当前运行在CPU上的进程的特权级别，但是当进程不运行的时候，无需维护这些信息。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">tss_struct</span> &#123;</span>  </span><br><span class="line"><span class="comment">/*  </span></span><br><span class="line"><span class="comment">* The fixed hardware portion. This must not cross a page boundary  </span></span><br><span class="line"><span class="comment">* at risk of violating the SDM&#x27;s advice and potentially triggering  </span></span><br><span class="line"><span class="comment">* errata.  </span></span><br><span class="line"><span class="comment">*/</span>  </span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">x86_hw_tss</span> <span class="title">x86_tss</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * The extra 1 is there because the CPU will access an</span></span><br><span class="line"><span class="comment"> * additional byte beyond the end of the IO permission</span></span><br><span class="line"><span class="comment"> * bitmap. The extra byte must be all 1 bits, and must</span></span><br><span class="line"><span class="comment"> * be within the limit.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span>io_bitmap[IO_BITMAP_LONGS + <span class="number">1</span>];</span><br><span class="line">&#125; __aligned(PAGE_SIZE);</span><br></pre></td></tr></table></figure><h4 id="结构分析"><a href="#结构分析" class="headerlink" title="结构分析"></a><strong>结构分析</strong></h4><h5 id="1-struct-x86-hw-tss"><a href="#1-struct-x86-hw-tss" class="headerlink" title="(1) struct x86_hw_tss"></a><strong>(1) <code>struct x86_hw_tss</code></strong></h5><p>这个字段是 TSS 的“硬件部分”，它是由 CPU 硬件定义并由操作系统使用的。主要内容如下：</p><ul><li><p><strong><code>struct x86_hw_tss</code> 的组成</strong>：</p><ul><li><strong>ESP0</strong>（堆栈指针 0）：指向内核态堆栈（在系统调用或中断时从用户态切换到内核态时使用）。</li><li><strong>SS0</strong>（堆栈段 0）：配合 ESP0 指向内核态堆栈。</li><li><strong>IO Map Base</strong>（I&#x2F;O 位图基地址）：用于控制对 I&#x2F;O 端口的访问。</li><li>其他与段寄存器、特权级、调试相关的字段。</li></ul><p>  在 x86_64 上，TSS 中的很多字段已经废弃，堆栈切换（ESP0）和 IO 位图是其最主要的用途。</p></li></ul><h5 id="2-io-bitmap"><a href="#2-io-bitmap" class="headerlink" title="(2) io_bitmap"></a><strong>(2) <code>io_bitmap</code></strong></h5><ul><li><strong>I&#x2F;O 权限位图（IO Permission Bitmap）</strong>：<ul><li>控制任务或线程对 I&#x2F;O 端口的访问。</li><li>如果某个线程试图访问未授权的 I&#x2F;O 端口，CPU 会触发一个异常。</li><li>在 Linux 中，<code>io_bitmap</code> 很少使用，大多数任务对所有 I&#x2F;O 端口都没有直接访问权限。</li></ul></li><li><strong>为何有额外的 1 字节</strong>：<ul><li>根据 x86 的规范，CPU 在访问 I&#x2F;O 位图时，会多读取一个额外的字节。</li><li>为了保证安全性，位图的额外字节必须设置为全 1（禁用访问），同时位图需要在限制范围内。</li></ul></li></ul><h3 id="进程切换的一般过程"><a href="#进程切换的一般过程" class="headerlink" title="进程切换的一般过程"></a>进程切换的一般过程</h3><p>（1）正在运行的用户态进程X<br>（2）发生中断——save cs:eip&#x2F;esp&#x2F;eflags(current) to kernel stack,then load cs:eip(entry of a specific ISR) and ss:esp(point to kernel stack).<br>（3）SAVE_ALL &#x2F;&#x2F;保存现场<br>（4）中断处理过程中或中断返回前调用了schedule()，其中的switch_to做了关键的进程上下文切换<br>（5） 标号1之后开始运行用户态进程Y(这里Y曾经通过以上步骤被切换出去过因此可以从标号1继续执行)<br>（6） restore_all &#x2F;&#x2F;恢复现场<br>（7）iret - pop cs:eip&#x2F;ss:esp&#x2F;eflags from kernel stack<br>（8）继续运行用户态进程Y</p><h3 id="进程切换源码结构"><a href="#进程切换源码结构" class="headerlink" title="进程切换源码结构"></a>进程切换源码结构</h3><img src="/2024/10/20/Linux/process/%E8%BF%9B%E7%A8%8B/image-20250616212152079.png" class title="image-20250616212152079"><p>我们只需要关注几个比较重要的地方：</p><ul><li>首先，schedule()函数会调用<code>next = pick_next_task(rq, prev);</code>，所做的工作就是根据调度算法策略，选取要执行的下一个进程。</li><li>其次，根据调度策略得到要执行的进程后，调用<code>context_switch(rq, prev, next);</code>，完成进程上下文切换。然后，调用<code>cppswitch_mm_irqs_off</code>  ,完成进程地址空间切换。其中，最关键的<code>switch_to(prev,next, prev);</code>切换堆栈和寄存器的状态。</li></ul><h3 id="switch-to"><a href="#switch-to" class="headerlink" title="switch_to"></a>switch_to</h3><p>进程硬件上下文的切换是由宏<code>switch_to</code>完成的。该宏的实现与硬件架构是息息相关的，要想理解它需要下一番功夫。switch_to是通过内联汇编操作的，在调试过程中无法进入，只能进入__switch_to函数。下面是基于X86架构下的该宏实现的汇编代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> switch_to(prev, next, last)\</span></span><br><span class="line"><span class="meta">do &#123;\</span></span><br><span class="line"><span class="meta"><span class="comment">/*\</span></span></span><br><span class="line"><span class="comment"><span class="meta"> * Context-switching clobbers all registers, so we clobber\</span></span></span><br><span class="line"><span class="comment"><span class="meta"> * them explicitly, via unused output variables.\</span></span></span><br><span class="line"><span class="comment"><span class="meta"> * (EAX and EBP is not listed because EBP is saved/restored\</span></span></span><br><span class="line"><span class="comment"><span class="meta"> * explicitly for wchan access and EAX is the return value of\</span></span></span><br><span class="line"><span class="comment"><span class="meta"> * __switch_to())\</span></span></span><br><span class="line"><span class="comment"><span class="meta"> */</span>\</span></span><br><span class="line"><span class="meta">unsigned long ebx, ecx, edx, esi, edi;\</span></span><br><span class="line"><span class="meta">\</span></span><br><span class="line"><span class="meta">asm volatile(<span class="string">&quot;pushfl\n\t&quot;</span><span class="comment">/* save    flags */</span>\</span></span><br><span class="line"><span class="meta">     <span class="string">&quot;pushl %%ebp\n\t&quot;</span><span class="comment">/* save    EBP   */</span>\<span class="comment">//保存当前进程的栈基址</span></span></span><br><span class="line">     <span class="string">&quot;movl %%esp,%[prev_sp]\n\t&quot;</span><span class="comment">/* save    ESP   */</span> \<span class="comment">//保存当前的栈顶</span></span><br><span class="line">     <span class="string">&quot;movl %[next_sp],%%esp\n\t&quot;</span><span class="comment">/* restore ESP   */</span> \<span class="comment">//这里实现内核堆栈的切换</span></span><br><span class="line">     <span class="string">&quot;movl $1f,%[prev_ip]\n\t&quot;</span><span class="comment">/* save    EIP   */</span>\   <span class="comment">//保存当前进程的EIP，next_ip一般是$1f，对于新创建的子进程是ret_from_fork</span></span><br><span class="line">     <span class="string">&quot;pushl %[next_ip]\n\t&quot;</span><span class="comment">/* restore EIP   */</span>\<span class="comment">//将下一个进程的起始位置压栈</span></span><br><span class="line">     __switch_canary\</span><br><span class="line">     <span class="string">&quot;jmp __switch_to\n&quot;</span><span class="comment">/* regparm call  */</span>\<span class="comment">//通过寄存器传参数，返回1f位置</span></span><br><span class="line">     <span class="string">&quot;1:\t&quot;</span>\</span><br><span class="line">     <span class="string">&quot;popl %%ebp\n\t&quot;</span><span class="comment">/* restore EBP   */</span>\<span class="comment">//弹出之前被调度时的ebp</span></span><br><span class="line">     <span class="string">&quot;popfl\n&quot;</span><span class="comment">/* restore flags */</span>\</span><br><span class="line">\</span><br><span class="line">     <span class="comment">/* output parameters */</span>\</span><br><span class="line">     : [prev_sp] <span class="string">&quot;=m&quot;</span> (prev-&gt;thread.sp),\<span class="comment">//保存当前进程的esp</span></span><br><span class="line">       [prev_ip] <span class="string">&quot;=m&quot;</span> (prev-&gt;thread.ip),\<span class="comment">//保存当前进程的eip</span></span><br><span class="line">       <span class="string">&quot;=a&quot;</span> (last),\</span><br><span class="line">\</span><br><span class="line">       <span class="comment">/* clobbered output registers: */</span>\</span><br><span class="line">       <span class="string">&quot;=b&quot;</span> (ebx), <span class="string">&quot;=c&quot;</span> (ecx), <span class="string">&quot;=d&quot;</span> (edx),\</span><br><span class="line">       <span class="string">&quot;=S&quot;</span> (esi), <span class="string">&quot;=D&quot;</span> (edi)\</span><br><span class="line">       \</span><br><span class="line">       __switch_canary_oparam\</span><br><span class="line">\</span><br><span class="line">       <span class="comment">/* input parameters: */</span>\</span><br><span class="line">     : [next_sp]  <span class="string">&quot;m&quot;</span> (next-&gt;thread.sp),\</span><br><span class="line">       [next_ip]  <span class="string">&quot;m&quot;</span> (next-&gt;thread.ip),\</span><br><span class="line">       \</span><br><span class="line">       <span class="comment">/* regparm parameters for __switch_to(): */</span>\</span><br><span class="line">       [prev]     <span class="string">&quot;a&quot;</span> (prev),\</span><br><span class="line">       [next]     <span class="string">&quot;d&quot;</span> (next)\</span><br><span class="line">\</span><br><span class="line">       __switch_canary_iparam\</span><br><span class="line">\</span><br><span class="line">     : <span class="comment">/* reloaded segment registers */</span>\</span><br><span class="line"><span class="string">&quot;memory&quot;</span>);\</span><br><span class="line">&#125; <span class="keyword">while</span> (<span class="number">0</span>)</span><br></pre></td></tr></table></figure><ul><li>首先，该宏具有3个参数，<code>prev</code>、<code>next</code>和<code>last</code><ul><li><code>prev</code>和<code>next</code>这2个参数很容易理解，分别指向新旧进程的描述符地址；</li><li><code>last</code>，是一个输出参数，用来记录是从哪个进程切换来的。</li></ul></li><li>为什么需要<code>last</code>参数呢？ 当进程切换涉及到3个进程的时候，3个进程分别假设为A、B、C。假设内核决定关掉A进程，激活B进程。在schedule函数中，prev指向A的描述符，而next指向B的描述符。只要switch_to宏使A失效，A的执行流就会冻结。后面，当内核想要重新激活A，必须关掉C进程，就要再执行一次switch_to宏，此时prev指向C，next指向A。当A进程想要继续执行之前的执行流时，会查找原先的内核态栈，发现prev等于A进程描述符，next等于B进程描述符。此时，调度器失去了对C进程的引用。保留这个引用非常有用，我们后面再讨论。</li></ul><p>![[switch_to().png]]</p><p><code>switch_to</code>宏的处理过程如下：</p><ul><li>将新旧进程描述符存放到CPU寄存器中：</li><li>保存旧进程的内核态栈，比如<code>eflags</code>和<code>ebp</code>寄存器的内容。</li><li>保存旧进程栈指针<code>esp</code>到<code>prev-&gt;thread.esp</code>中</li><li>操作数<code>484(%eax)</code>表明目的地址是寄存器<code>eax</code>中的地址加上<code>484</code>。将新进程的栈指针加载到<code>esp</code>寄存器中。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">movl <span class="number">484</span>(%edx), %esp    <span class="comment">/* restore ESP */</span></span><br></pre></td></tr></table></figure><ul><li>保存标签<code>1</code>的地址-&gt;<code>prev-&gt;thread.eip</code>。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">movl $<span class="number">1f</span>, <span class="number">480</span>(%eax)   <span class="comment">/* save EIP */</span></span><br></pre></td></tr></table></figure><ul><li>当被替换的进程重新恢复执行时，进程执行被标记为1的那条指令:</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&quot;1:\t&quot;</span> \ </span><br><span class="line"><span class="string">&quot;popl %%ebp\n\t&quot;</span> <span class="comment">/* restore EBP */</span> \ </span><br><span class="line"><span class="string">&quot;popfl\n&quot;</span> <span class="comment">/* restore flags */</span> \</span><br></pre></td></tr></table></figure><ul><li>跳转到<code>__switch_to()</code>函数执行，是一个C函数。</li></ul><p>至此，进程A被进程B取代：开始执行B进程的指令。第一步应该是先弹出<code>eflags</code>和ebp寄存器的值。拷贝eax寄存器的内容（第1步加载的）到last变量中。也就是说，last记录了被取代的进程。</p><h3 id="switch-to-1"><a href="#switch-to-1" class="headerlink" title="__switch_to()"></a>__switch_to()</h3><p>实际上大部分的进程切换工作是由__switch_to()函数完成的，它的参数是prev_p和next_p，分别指向旧进程和新进程。这个函数和普通的函数有些差别，因为__switch_to()函数从eax和edx寄存器中获取prev_p和next_p这两个参数（在分析switch_to宏的时候已经讲过），而不是像普通函数那样，从栈中获取参数。为了强制函数从寄存器中获取参数，内核使用<a href="/computer_system/other/gcc%E7%89%B9%E6%AE%8A%E7%94%A8%E6%B3%95#%60__attribute__%60"><code>__attribute__</code></a>和[<code>regparm</code>](&#x2F;computer_system&#x2F;other&#x2F;gcc特殊用法#<code>regparm</code>)进行声明。这是gcc编译器对C语言的一个非标准扩展。__switch_to()函数定义在<code>include/asm-i386/system.h</code>文件中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">__switch_to(<span class="keyword">struct</span> task_struct *prev_p, <span class="keyword">struct</span> task_struct *next_p) __attribute__(regparm(<span class="number">3</span>));</span><br></pre></td></tr></table></figure><p>这个函数执行的内容：</p><ul><li>执行__unlazy_fpu()宏，保存旧进程的FPU、MMX和XMM寄存器</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">__unlazy_fpu(prev_p);</span><br></pre></td></tr></table></figure><ul><li>执行smp_processor_id()宏，获取正在执行代码的CPU的ID。从thread_info结构的cpu成员中获取。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> cpu = smp_processor_id();</span><br></pre></td></tr></table></figure><ul><li>加载新进程的<code>next_p-&gt;thread.esp0</code>到当前CPU的TSS段中的esp0成员中。通过调用sysenter汇编指令从用户态切换到内核态引起的任何特权级别的改变都会导致将这个地址拷贝到esp寄存器中。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">load_sp0(tss, next);</span><br></pre></td></tr></table></figure><ul><li>将新进程的线程本地存储（TLS）段加载到当前CPU的GDT中。3个段选择器存储在进程描述符的tls_array数组中。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Load the per-thread Thread-Local Storage descriptor.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">load_TLS(next, cpu);</span><br></pre></td></tr></table></figure><ul><li>存储fs和gs段寄存器的内容到旧进程的prev_p-&gt;thread.fs和prev_p-&gt;thread.gs中。汇编指令如下：</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">movl %fs，<span class="number">40</span>(%esi)</span><br><span class="line">movl %gs，<span class="number">44</span>(%esi)</span><br></pre></td></tr></table></figure><ul><li>寄存器esi指向prev_p-&gt;thread结构。gs寄存器用来存放TLS段的地址。fs寄存器实际上windows使用。</li><li>加载新进程的fs或gs寄存器内容。数据来源是新进程的thread_struct描述符中对应的值。汇编语言如下：</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">movl <span class="number">40</span>(%ebx)，%fs</span><br><span class="line">movl <span class="number">44</span>(%ebx)，%gs</span><br></pre></td></tr></table></figure><ul><li>ebx寄存器指向next_p-&gt;thread结构。</li><li>载入新进程的调式寄存器中的信息。</li><li>更新TSS中的I&#x2F;O权限位（如果有必要的话）。也就是如果新旧进程对I&#x2F;O访问有自己特殊的要求的话就需要更改。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Now maybe handle debug registers and/or IO bitmaps</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (unlikely(task_thread_info(prev_p)-&gt;flags &amp; _TIF_WORK_CTXSW_PREV ||</span><br><span class="line">     task_thread_info(next_p)-&gt;flags &amp; _TIF_WORK_CTXSW_NEXT))</span><br><span class="line">__switch_to_xtra(prev_p, next_p, tss);</span><br></pre></td></tr></table></figure><h2 id="创建进程"><a href="#创建进程" class="headerlink" title="创建进程"></a><strong>创建进程</strong></h2><p>Linux创建进程采用fork()和exec()：</p><ul><li>fork: 采用复制当前进程的方式来创建子进程，此时子进程与父进程的区别仅在于pid, ppid以及资源统计量(比如挂起的信号)</li><li>exec：读取可执行文件并载入地址空间执行；一般称之为exec函数族，有一系列exec开头的函数，比如execl, execve等</li></ul><p>fork过程复制资源包括代码段，数据段，堆，栈。fork调用者所在进程便是父进程，新创建的进程便是子进程；在fork调用结束，从内核返回两次，一次继续执行父进程，一次进入执行子进程。</p><p>linux主要有三种复制手段：</p><ul><li>写时复制技术允许父子进程读相同的物理页。只要两者中有一个试图写一个物理页,内核就把这个页的内容拷贝到一个新的物理页,并把这个新的物理页分配给正在写的进程。</li><li>轻量级进程允许父子进程共享每进程在内核的很多数据结构,如页表(也就是整个用户态地址空间)、打开文件表及信号处理。</li><li>vfork()系统调用创建的进程能共享其父进程的内存地址空间。为了防止父进程重写子进程需要的数据,阻塞父进程的执行,一直到子进程退出或执行一个新的程序为止。</li></ul><h2 id="进程创建"><a href="#进程创建" class="headerlink" title="进程创建"></a>进程创建</h2><ul><li>Linux进程创建： 通过fork()系统调用创建进程</li><li>Linux用户级线程创建：通过pthread库中的pthread_create()创建线程</li><li>Linux内核线程创建： 通过kthread_create()</li></ul><p>Linux线程，也并非”轻量级进程”，在Linux看来线程是一种进程间共享资源的方式，线程可看做是跟其他进程共享资源的进程。</p><p>fork, vfork, clone根据不同参数调用do_fork()</p><ul><li>pthread_create: flags参数为 CLONE_VM, CLONE_FS, CLONE_FILES, CLONE_SIGHAND</li><li>fork: flags参数为 SIGCHLD</li><li>vfork: flags参数为 CLONE_VFORK, CLONE_VM, SIGCHLD</li></ul><h3 id="fork流程图"><a href="#fork流程图" class="headerlink" title="fork流程图"></a>fork流程图</h3><p>进程&#x2F;线程创建的方法fork(),pthread_create(), 万物归一，最终在linux都是调用do_fork方法。 当然还有vfork其实也是一样的， 通过系统调用到sys_vfork，然后再调用do_fork方法，该方法 现在很少使用，所以下图省略该方法。</p><img src="/2024/10/20/Linux/process/%E8%BF%9B%E7%A8%8B/image-20250616212246856.png" class title="image-20250616212246856"><p>程序执行流程：</p><ol><li>用户空间调用fork()方法;</li><li>经过syscall陷入内核空间, 内核根据系统调用号找到相应的sys_fork系统调用;</li><li>sys_fork()过程会在调用do_fork(), 该方法参数有一个flags很重要, 代表的是父子进程之间需要共享的资源; 对于进程创建flags&#x3D;SIGCHLD, 即当子进程退出时向父进程发送SIGCHLD信号;</li><li>do_fork(),会进行一些check过程,之后便是进入核心方法copy_process.</li></ol><p>参数定义：</p><table><thead><tr><th>宏定义</th><th>数值</th><th>注释</th></tr></thead><tbody><tr><td><code>CSIGNAL</code></td><td><code>0x000000ff</code></td><td>退出时发送的信号掩码</td></tr><tr><td><code>CLONE_VM</code></td><td><code>0x00000100</code></td><td>共享虚拟内存</td></tr><tr><td><code>CLONE_FS</code></td><td><code>0x00000200</code></td><td>共享文件系统信息</td></tr><tr><td><code>CLONE_FILES</code></td><td><code>0x00000400</code></td><td>共享已打开的文件</td></tr><tr><td><code>CLONE_SIGHAND</code></td><td><code>0x00000800</code></td><td>共享信号处理程序和被阻塞的信号</td></tr><tr><td><code>CLONE_PTRACE</code></td><td><code>0x00002000</code></td><td>跟踪子进程</td></tr><tr><td><code>CLONE_VFORK</code></td><td><code>0x00004000</code></td><td>父进程希望在子进程释放内存时被唤醒</td></tr><tr><td><code>CLONE_PARENT</code></td><td><code>0x00008000</code></td><td>拥有与克隆者相同的父进程</td></tr><tr><td><code>CLONE_THREAD</code></td><td><code>0x00010000</code></td><td>属于同一线程组</td></tr><tr><td><code>CLONE_NEWNS</code></td><td><code>0x00020000</code></td><td>创建新的命名空间组</td></tr><tr><td><code>CLONE_SYSVSEM</code></td><td><code>0x00040000</code></td><td>共享 System V 的 SEM_UNDO 语义</td></tr><tr><td><code>CLONE_SETTLS</code></td><td><code>0x00080000</code></td><td>为子进程创建新的 TLS</td></tr><tr><td><code>CLONE_PARENT_SETTID</code></td><td><code>0x00100000</code></td><td>在父进程中设置线程 ID</td></tr><tr><td><code>CLONE_CHILD_CLEARTID</code></td><td><code>0x00200000</code></td><td>在子进程中清除线程 ID</td></tr><tr><td><code>CLONE_DETACHED</code></td><td><code>0x00400000</code></td><td>未使用，已忽略</td></tr><tr><td><code>CLONE_UNTRACED</code></td><td><code>0x00800000</code></td><td>跟踪进程无法强制对子进程使用 CLONE_PTRACE</td></tr><tr><td><code>CLONE_CHILD_SETTID</code></td><td><code>0x01000000</code></td><td>在子进程中设置线程 ID</td></tr><tr><td><code>CLONE_NEWUTS</code></td><td><code>0x04000000</code></td><td>创建新的 UTS 名称空间组</td></tr><tr><td><code>CLONE_NEWIPC</code></td><td><code>0x08000000</code></td><td>创建新的 IPC 命名空间</td></tr><tr><td><code>CLONE_NEWUSER</code></td><td><code>0x10000000</code></td><td>创建新的用户命名空间</td></tr><tr><td><code>CLONE_NEWPID</code></td><td><code>0x20000000</code></td><td>创建新的 PID 命名空间</td></tr><tr><td><code>CLONE_NEWNET</code></td><td><code>0x40000000</code></td><td>创建新的网络命名空间</td></tr><tr><td><code>CLONE_IO</code></td><td><code>0x80000000</code></td><td>克隆 IO 上下文</td></tr></tbody></table><h2 id="fork源码"><a href="#fork源码" class="headerlink" title="fork源码"></a>fork源码</h2><h3 id="fork"><a href="#fork" class="headerlink" title="fork"></a><code>fork</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//fork系统调用 </span></span><br><span class="line">SYSCALL_DEFINE0(fork) &#123; </span><br><span class="line"><span class="keyword">return</span> do_fork(SIGCHLD, <span class="number">0</span>, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="vfork"><a href="#vfork" class="headerlink" title="vfork"></a><code>vfork</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SYSCALL_DEFINE0(vfork) &#123; </span><br><span class="line"><span class="keyword">return</span> _do_fork(CLONE_VFORK | CLONE_VM | SIGCHLD, <span class="number">0</span>, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="number">0</span>); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="clone"><a href="#clone" class="headerlink" title="clone"></a><code>clone</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//clone系统调用 </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_CLONE_BACKWARDS </span></span><br><span class="line">SYSCALL_DEFINE5(clone, <span class="type">unsigned</span> <span class="type">long</span>, clone_flags, <span class="type">unsigned</span> <span class="type">long</span>, newsp, <span class="type">int</span> __user *, parent_tidptr, <span class="type">unsigned</span> <span class="type">long</span>, tls, <span class="type">int</span> __user *, child_tidptr) <span class="meta">#<span class="keyword">elif</span> defined(CONFIG_CLONE_BACKWARDS2)</span></span><br><span class="line">SYSCALL_DEFINE5(clone, <span class="type">unsigned</span> <span class="type">long</span>, newsp, <span class="type">unsigned</span> <span class="type">long</span>, clone_flags, <span class="type">int</span> __user *, parent_tidptr, <span class="type">int</span> __user *, child_tidptr, <span class="type">unsigned</span> <span class="type">long</span>, tls) <span class="meta">#<span class="keyword">elif</span> defined(CONFIG_CLONE_BACKWARDS3) </span></span><br><span class="line">SYSCALL_DEFINE6(clone, <span class="type">unsigned</span> <span class="type">long</span>, clone_flags, <span class="type">unsigned</span> <span class="type">long</span>, newsp, <span class="type">int</span>, stack_size, <span class="type">int</span> __user *, parent_tidptr, <span class="type">int</span> __user *, child_tidptr, <span class="type">unsigned</span> <span class="type">long</span>, tls) </span><br><span class="line"><span class="meta">#<span class="keyword">else</span> </span></span><br><span class="line">SYSCALL_DEFINE5(clone, <span class="type">unsigned</span> <span class="type">long</span>, clone_flags, <span class="type">unsigned</span> <span class="type">long</span>, newsp, <span class="type">int</span> __user *, parent_tidptr, <span class="type">int</span> __user *, child_tidptr, <span class="type">unsigned</span> <span class="type">long</span>, tls) </span><br><span class="line"><span class="meta">#<span class="keyword">endif</span> </span></span><br><span class="line">&#123; </span><br><span class="line"><span class="keyword">return</span> _do_fork(clone_flags, newsp, <span class="number">0</span>, parent_tidptr, child_tidptr, tls); </span><br><span class="line">&#125; <span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><p>fork,vfork,clone最终都会调用do_fork方法，仅仅是flags不同。</p><h3 id="do-fork"><a href="#do-fork" class="headerlink" title="do_fork"></a><code>do_fork</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">long</span> <span class="title function_">do_fork</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span> clone_flags,</span></span><br><span class="line"><span class="params">        <span class="type">unsigned</span> <span class="type">long</span> stack_start,</span></span><br><span class="line"><span class="params">        <span class="type">unsigned</span> <span class="type">long</span> stack_size,</span></span><br><span class="line"><span class="params">        <span class="type">int</span> __user *parent_tidptr,</span></span><br><span class="line"><span class="params">        <span class="type">int</span> __user *child_tidptr)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">return</span> _do_fork(clone_flags, stack_start, stack_size,</span><br><span class="line">      parent_tidptr, child_tidptr, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>clone_flags：clone方法传递过程的flags，标记子进程从父进程中需要继承的资源清单</li><li>stack_start: 子进程用户态的堆栈地址,fork()过程该值为0, clone()过程赋予有效值</li><li>stack_size：不必要的参数，默认设置为0</li><li>parent_tidptr：用户态下父进程的tid地址</li><li>child_tidptr：用户态下子进程的tid地址</li></ul><h3 id="do-fork-1"><a href="#do-fork-1" class="headerlink" title="_do_fork"></a><code>_do_fork</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">long</span> _do_fork(<span class="type">unsigned</span> <span class="type">long</span> clone_flags,</span><br><span class="line">        <span class="type">unsigned</span> <span class="type">long</span> stack_start,</span><br><span class="line">        <span class="type">unsigned</span> <span class="type">long</span> stack_size,</span><br><span class="line">        <span class="type">int</span> __user *parent_tidptr,</span><br><span class="line">        <span class="type">int</span> __user *child_tidptr,</span><br><span class="line">        <span class="type">unsigned</span> <span class="type">long</span> tls)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">p</span>;</span></span><br><span class="line">  ...</span><br><span class="line">  </span><br><span class="line">  <span class="comment">//复制进程描述符</span></span><br><span class="line">  p = copy_process(clone_flags, stack_start, stack_size,</span><br><span class="line">       child_tidptr, <span class="literal">NULL</span>, trace, tls);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!IS_ERR(p)) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">completion</span> <span class="title">vfork</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">pid</span> *<span class="title">pid</span>;</span></span><br><span class="line"></span><br><span class="line">    trace_sched_process_fork(current, p);</span><br><span class="line">     <span class="comment">//获取新创建的子进程的pid</span></span><br><span class="line">    pid = get_task_pid(p, PIDTYPE_PID);</span><br><span class="line">    nr = pid_vnr(pid);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (clone_flags &amp; CLONE_PARENT_SETTID)</span><br><span class="line">      put_user(nr, parent_tidptr);</span><br><span class="line">      </span><br><span class="line">    <span class="keyword">if</span> (clone_flags &amp; CLONE_VFORK) &#123; <span class="comment">//仅用于vfork过程,初始化</span></span><br><span class="line">      p-&gt;vfork_done = &amp;vfork;</span><br><span class="line">      init_completion(&amp;vfork);</span><br><span class="line">      get_task_struct(p);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wake_up_new_task(p); <span class="comment">//唤醒子进程，分配CPU时间片</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (unlikely(trace)) <span class="comment">//告知ptracer，子进程已创建完成，并且已启动</span></span><br><span class="line">      ptrace_event_pid(trace, pid);</span><br><span class="line">      </span><br><span class="line">    <span class="keyword">if</span> (clone_flags &amp; CLONE_VFORK) &#123; <span class="comment">//仅用于vfork过程，等待子进程</span></span><br><span class="line">      <span class="keyword">if</span> (!wait_for_vfork_done(p, &amp;vfork))</span><br><span class="line">        ptrace_event_pid(PTRACE_EVENT_VFORK_DONE, pid);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    put_pid(pid); </span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    nr = PTR_ERR(p);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> nr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>过程：</p><ol><li>执行<code>copy_process</code>，复制进程描述符，pid分配也是这个过程完成</li><li>当为vfork，则执行相应的初始化过程</li><li>执行<code>wake_up_new_task</code>，唤醒子进程，分配CPU时间片</li><li>当为vfork，则父进程等待子进程执行exec函数来替换地址空间</li></ol><h3 id="copy-process"><a href="#copy-process" class="headerlink" title="copy_process"></a><code>copy_process</code></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">struct</span> task_struct *<span class="title function_">copy_process</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span> clone_flags,</span></span><br><span class="line"><span class="params">          <span class="type">unsigned</span> <span class="type">long</span> stack_start,</span></span><br><span class="line"><span class="params">          <span class="type">unsigned</span> <span class="type">long</span> stack_size,</span></span><br><span class="line"><span class="params">          <span class="type">int</span> __user *child_tidptr,</span></span><br><span class="line"><span class="params">          <span class="keyword">struct</span> pid *pid,</span></span><br><span class="line"><span class="params">          <span class="type">int</span> trace,</span></span><br><span class="line"><span class="params">          <span class="type">unsigned</span> <span class="type">long</span> tls)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">int</span> retval;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">p</span>;</span></span><br><span class="line">  <span class="type">void</span> *cgrp_ss_priv[CGROUP_CANFORK_COUNT] = &#123;&#125;;</span><br><span class="line">  ...</span><br><span class="line">  retval = security_task_create(clone_flags);</span><br><span class="line">  ...</span><br><span class="line">  </span><br><span class="line">  <span class="comment">//1. 执行dup_task_struct()，拷贝当前进程task_struct</span></span><br><span class="line">  p = dup_task_struct(current); </span><br><span class="line">  ...</span><br><span class="line">  rt_mutex_init_task(p); <span class="comment">//初始化mutex</span></span><br><span class="line">  </span><br><span class="line">  <span class="comment">//检查进程数是否超过上限</span></span><br><span class="line">  retval = -EAGAIN;</span><br><span class="line">  <span class="keyword">if</span> (<span class="type">atomic_read</span>(&amp;p-&gt;real_cred-&gt;user-&gt;processes) &gt;=</span><br><span class="line">      task_rlimit(p, RLIMIT_NPROC)) &#123;</span><br><span class="line">    <span class="keyword">if</span> (p-&gt;real_cred-&gt;user != INIT_USER &amp;&amp;</span><br><span class="line">        !capable(CAP_SYS_RESOURCE) &amp;&amp; !capable(CAP_SYS_ADMIN))</span><br><span class="line">      <span class="keyword">goto</span> bad_fork_free;</span><br><span class="line">  &#125;</span><br><span class="line">  current-&gt;flags &amp;= ~PF_NPROC_EXCEEDED;</span><br><span class="line"></span><br><span class="line">  retval = copy_creds(p, clone_flags);</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  <span class="comment">//检查nr_threads是否超过max_threads</span></span><br><span class="line">  retval = -EAGAIN;</span><br><span class="line">  <span class="keyword">if</span> (nr_threads &gt;= max_threads)</span><br><span class="line">    <span class="keyword">goto</span> bad_fork_cleanup_count;</span><br><span class="line"></span><br><span class="line">  delayacct_tsk_init(p);</span><br><span class="line">  p-&gt;flags &amp;= ~(PF_SUPERPRIV | PF_WQ_WORKER);</span><br><span class="line">  p-&gt;flags |= PF_FORKNOEXEC;</span><br><span class="line">  INIT_LIST_HEAD(&amp;p-&gt;children);</span><br><span class="line">  INIT_LIST_HEAD(&amp;p-&gt;sibling);</span><br><span class="line">  rcu_copy_process(p);</span><br><span class="line">  p-&gt;vfork_done = <span class="literal">NULL</span>;</span><br><span class="line">  spin_lock_init(&amp;p-&gt;alloc_lock); <span class="comment">//初始化自旋锁</span></span><br><span class="line">  init_sigpending(&amp;p-&gt;pending);   <span class="comment">//初始化挂起信号</span></span><br><span class="line">  </span><br><span class="line">  p-&gt;utime = p-&gt;stime = p-&gt;gtime = <span class="number">0</span>;</span><br><span class="line">  p-&gt;utimescaled = p-&gt;stimescaled = <span class="number">0</span>;</span><br><span class="line">  prev_cputime_init(&amp;p-&gt;prev_cputime);</span><br><span class="line">  p-&gt;default_timer_slack_ns = current-&gt;timer_slack_ns;</span><br><span class="line"></span><br><span class="line">  task_io_accounting_init(&amp;p-&gt;ioac);</span><br><span class="line">  acct_clear_integrals(p);</span><br><span class="line">  posix_cpu_timers_init(p);</span><br><span class="line"></span><br><span class="line">  p-&gt;start_time = ktime_get_ns(); <span class="comment">//初始化进程启动时间</span></span><br><span class="line">  p-&gt;real_start_time = ktime_get_boot_ns();</span><br><span class="line">  p-&gt;io_context = <span class="literal">NULL</span>;</span><br><span class="line">  p-&gt;audit_context = <span class="literal">NULL</span>;</span><br><span class="line">  cgroup_fork(p);</span><br><span class="line">  p-&gt;pagefault_disabled = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//执行调度器相关设置，将该task分配给一某个CPU </span></span><br><span class="line">  retval = sched_fork(clone_flags, p); </span><br><span class="line">  retval = perf_event_init_task(p);</span><br><span class="line">  retval = audit_alloc(p);</span><br><span class="line">  </span><br><span class="line">  <span class="comment">//拷贝进程的所有信息</span></span><br><span class="line">  shm_init_task(p); </span><br><span class="line">  retval = copy_semundo(clone_flags, p);</span><br><span class="line">  retval = copy_files(clone_flags, p);</span><br><span class="line">  retval = copy_fs(clone_flags, p);</span><br><span class="line">  retval = copy_sighand(clone_flags, p);</span><br><span class="line">  retval = copy_signal(clone_flags, p);</span><br><span class="line">  retval = copy_mm(clone_flags, p);</span><br><span class="line">  retval = copy_namespaces(clone_flags, p);</span><br><span class="line">  retval = copy_io(clone_flags, p);</span><br><span class="line">  retval = copy_thread_tls(clone_flags, stack_start, stack_size, p, tls);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (pid != &amp;init_struct_pid) &#123;</span><br><span class="line">    <span class="comment">//分配pid</span></span><br><span class="line">    pid = alloc_pid(p-&gt;nsproxy-&gt;pid_ns_for_children);</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">  p-&gt;pid = pid_nr(pid); <span class="comment">//设置pid</span></span><br><span class="line">  <span class="keyword">if</span> (clone_flags &amp; CLONE_THREAD) &#123;</span><br><span class="line">    p-&gt;exit_signal = <span class="number">-1</span>;</span><br><span class="line">    p-&gt;group_leader = current-&gt;group_leader;</span><br><span class="line">    p-&gt;tgid = current-&gt;tgid;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (clone_flags &amp; CLONE_PARENT)</span><br><span class="line">      p-&gt;exit_signal = current-&gt;group_leader-&gt;exit_signal;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      p-&gt;exit_signal = (clone_flags &amp; CSIGNAL);</span><br><span class="line">    p-&gt;group_leader = p;</span><br><span class="line">    p-&gt;tgid = p-&gt;pid;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  p-&gt;nr_dirtied = <span class="number">0</span>;</span><br><span class="line">  p-&gt;nr_dirtied_pause = <span class="number">128</span> &gt;&gt; (PAGE_SHIFT - <span class="number">10</span>);</span><br><span class="line">  p-&gt;dirty_paused_when = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  p-&gt;pdeath_signal = <span class="number">0</span>;</span><br><span class="line">  INIT_LIST_HEAD(&amp;p-&gt;thread_group);</span><br><span class="line">  p-&gt;task_works = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">  threadgroup_change_begin(current);</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  <span class="comment">//CLONE_PARENT再利用旧的父进程</span></span><br><span class="line">  <span class="keyword">if</span> (clone_flags &amp; (CLONE_PARENT|CLONE_THREAD)) &#123;</span><br><span class="line">    p-&gt;real_parent = current-&gt;real_parent;</span><br><span class="line">    p-&gt;parent_exec_id = current-&gt;parent_exec_id;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    p-&gt;real_parent = current;</span><br><span class="line">    p-&gt;parent_exec_id = current-&gt;self_exec_id;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  spin_lock(&amp;current-&gt;sighand-&gt;siglock);</span><br><span class="line">  copy_seccomp(p);</span><br><span class="line"></span><br><span class="line">  recalc_sigpending();</span><br><span class="line">  <span class="keyword">if</span> (signal_pending(current)) &#123;</span><br><span class="line">    spin_unlock(&amp;current-&gt;sighand-&gt;siglock);</span><br><span class="line">    write_unlock_irq(&amp;tasklist_lock);</span><br><span class="line">    retval = -ERESTARTNOINTR;</span><br><span class="line">    <span class="keyword">goto</span> bad_fork_cancel_cgroup;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (likely(p-&gt;pid)) &#123;</span><br><span class="line">    ptrace_init_task(p, (clone_flags &amp; CLONE_PTRACE) || trace);</span><br><span class="line"></span><br><span class="line">    init_task_pid(p, PIDTYPE_PID, pid);</span><br><span class="line">    <span class="keyword">if</span> (thread_group_leader(p)) &#123;</span><br><span class="line">      init_task_pid(p, PIDTYPE_PGID, task_pgrp(current));</span><br><span class="line">      init_task_pid(p, PIDTYPE_SID, task_session(current));</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (is_child_reaper(pid)) &#123;</span><br><span class="line">        ns_of_pid(pid)-&gt;child_reaper = p;</span><br><span class="line">        p-&gt;signal-&gt;flags |= SIGNAL_UNKILLABLE;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      p-&gt;signal-&gt;leader_pid = pid;</span><br><span class="line">      p-&gt;signal-&gt;tty = tty_kref_get(current-&gt;signal-&gt;tty);</span><br><span class="line">      list_add_tail(&amp;p-&gt;sibling, &amp;p-&gt;real_parent-&gt;children);</span><br><span class="line">      list_add_tail_rcu(&amp;p-&gt;tasks, &amp;init_task.tasks);</span><br><span class="line">      attach_pid(p, PIDTYPE_PGID);</span><br><span class="line">      attach_pid(p, PIDTYPE_SID);</span><br><span class="line">      __this_cpu_inc(process_counts);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      current-&gt;signal-&gt;nr_threads++;</span><br><span class="line">      <span class="type">atomic_inc</span>(&amp;current-&gt;signal-&gt;live);</span><br><span class="line">      <span class="type">atomic_inc</span>(&amp;current-&gt;signal-&gt;sigcnt);</span><br><span class="line">      list_add_tail_rcu(&amp;p-&gt;thread_group,</span><br><span class="line">            &amp;p-&gt;group_leader-&gt;thread_group);</span><br><span class="line">      list_add_tail_rcu(&amp;p-&gt;thread_node,</span><br><span class="line">            &amp;p-&gt;signal-&gt;thread_head);</span><br><span class="line">    &#125;</span><br><span class="line">    attach_pid(p, PIDTYPE_PID);</span><br><span class="line">    nr_threads++; <span class="comment">//线程个数+1</span></span><br><span class="line">  &#125;</span><br><span class="line">  total_forks++; <span class="comment">//进程forks次数加1</span></span><br><span class="line">  ...</span><br><span class="line">  <span class="keyword">return</span> p;</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line">fork_out:</span><br><span class="line">  <span class="keyword">return</span> ERR_PTR(retval);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol><li>执行dup_task_struct()，拷贝当前进程task_struct</li><li>检查进程数是否超过系统所允许的上限(默认32678)</li><li>执行sched_fork()，设置调度器相关信息，设置task进程状态为TASK_RUNNING，并分配CPU资源</li><li>执行copy_xxx()，拷贝进程的files, fs, mm, io, sighand, signal等信息</li><li>执行copy_thread_tls(), 拷贝子进程的内核栈信息</li><li>执行<a href="/computer_system/%E8%BF%9B%E7%A8%8B/%E8%BF%9B%E7%A8%8B%E6%A0%87%E8%AF%86%E7%AC%A6-Process_ID#alloc_pid()"><code>alloc_pid()</code></a>，为新进程分配新pid</li></ol><h4 id="dup-task-struct"><a href="#dup-task-struct" class="headerlink" title="dup_task_struct"></a><code>dup_task_struct</code></h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="keyword">struct</span> task_struct *<span class="title function_">dup_task_struct</span><span class="params">(<span class="keyword">struct</span> task_struct *orig)</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">tsk</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">thread_info</span> *<span class="title">ti</span>;</span></span><br><span class="line">  <span class="type">int</span> node = tsk_fork_get_node(orig);</span><br><span class="line">  <span class="type">int</span> err;</span><br><span class="line">  <span class="comment">//分配task_struct节点</span></span><br><span class="line">  tsk = alloc_task_struct_node(node);</span><br><span class="line">  <span class="comment">//分配thread_info节点</span></span><br><span class="line">  ti = alloc_thread_info_node(tsk, node);</span><br><span class="line">  err = arch_dup_task_struct(tsk, orig);</span><br><span class="line">  <span class="comment">//将thread_info赋值给当前新创建的task</span></span><br><span class="line">  tsk-&gt;<span class="built_in">stack</span> = ti;</span><br><span class="line"></span><br><span class="line">  setup_thread_stack(tsk, orig);</span><br><span class="line">  clear_user_return_notifier(tsk);</span><br><span class="line">  clear_tsk_need_resched(tsk);</span><br><span class="line">  set_task_stack_end_magic(tsk);</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  account_kernel_stack(ti, <span class="number">1</span>);</span><br><span class="line">  <span class="keyword">return</span> tsk;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该方法主要功能是创建task_struct和thread_info结构体。执行完该方法，子进程唯一不同是task-&gt;stack。</p><h4 id="sched-fork"><a href="#sched-fork" class="headerlink" title="sched_fork"></a><code>sched_fork</code></h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">sched_fork</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span> clone_flags, <span class="keyword">struct</span> task_struct *p)</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">unsigned</span> <span class="type">long</span> flags;</span><br><span class="line">  <span class="type">int</span> cpu = get_cpu();</span><br><span class="line"></span><br><span class="line">  __sched_fork(clone_flags, p);</span><br><span class="line"></span><br><span class="line">  <span class="comment">//标记进程为Running状态，用于保证实际上并没有运行，</span></span><br><span class="line">  <span class="comment">// 信号或许其他外部事件都无法唤醒该进程，同时把它插入运行队列</span></span><br><span class="line">  p-&gt;state = TASK_RUNNING;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//确保不会将提升的优先级传递到子进程</span></span><br><span class="line">  p-&gt;prio = current-&gt;normal_prio;</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  <span class="comment">//为进程p分配相应cpu</span></span><br><span class="line">  set_task_cpu(p, cpu);</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">  put_cpu();</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="alloc-pid-1"><a href="#alloc-pid-1" class="headerlink" title="alloc_pid()"></a><a href="/computer_system/%E8%BF%9B%E7%A8%8B/%E8%BF%9B%E7%A8%8B%E6%A0%87%E8%AF%86%E7%AC%A6-Process_ID#alloc_pid()"><code>alloc_pid()</code></a></h4><h2 id="拷贝过程："><a href="#拷贝过程：" class="headerlink" title="拷贝过程："></a>拷贝过程：</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//拷贝进程的所有信息</span></span><br><span class="line">  shm_init_task(p); </span><br><span class="line">  retval = copy_semundo(clone_flags, p);</span><br><span class="line">  retval = copy_files(clone_flags, p);</span><br><span class="line">  retval = copy_fs(clone_flags, p);</span><br><span class="line">  retval = copy_sighand(clone_flags, p);</span><br><span class="line">  retval = copy_signal(clone_flags, p);</span><br><span class="line">  retval = copy_mm(clone_flags, p);</span><br><span class="line">  retval = copy_namespaces(clone_flags, p);</span><br><span class="line">  retval = copy_io(clone_flags, p);</span><br><span class="line">  retval = copy_thread_tls(clone_flags, stack_start, stack_size, p, tls);</span><br></pre></td></tr></table></figure><h3 id="1-shm-init-task-p"><a href="#1-shm-init-task-p" class="headerlink" title="1. shm_init_task(p)"></a>1. <strong><code>shm_init_task(p)</code></strong></h3><ul><li><strong>作用</strong>: 初始化共享内存（Shared Memory）子系统的相关信息，为新创建的进程分配共享内存的相关结构。</li><li><strong>细节</strong>:<ul><li>共享内存是进程间通信（IPC）的重要机制之一。</li><li>这个函数会为新任务设置必要的共享内存结构，确保父子进程间可以共享或者分离它们的共享内存状态。</li></ul></li></ul><hr><h3 id="2-copy-semundo-clone-flags-p"><a href="#2-copy-semundo-clone-flags-p" class="headerlink" title="2. copy_semundo(clone_flags, p)"></a>2. <strong><code>copy_semundo(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝父进程的信号量撤销（Semaphore Undo）表到子进程。</li><li><strong>细节</strong>:<ul><li>信号量撤销是为了在进程退出时清理掉其在信号量上的未完成操作。</li><li>此函数确保子进程在继承信号量相关信息时，能够正确处理和清理信号量状态。</li></ul></li></ul><hr><h3 id="3-copy-files-clone-flags-p"><a href="#3-copy-files-clone-flags-p" class="headerlink" title="3. copy_files(clone_flags, p)"></a>3. <strong><code>copy_files(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝父进程的文件描述符表到子进程。</li><li><strong>细节</strong>:<ul><li>如果 <code>CLONE_FILES</code> 标志被设置，父子进程将共享文件描述符表。</li><li>如果标志未设置，子进程会得到父进程文件描述符表的副本。</li><li>这个函数处理的是文件描述符的引用计数和资源共享问题。</li></ul></li></ul><hr><h3 id="4-copy-fs-clone-flags-p"><a href="#4-copy-fs-clone-flags-p" class="headerlink" title="4. copy_fs(clone_flags, p)"></a>4. <strong><code>copy_fs(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝文件系统信息。</li><li><strong>细节</strong>:<ul><li>包括当前工作目录、根目录、挂载点等文件系统相关状态。</li><li>如果 <code>CLONE_FS</code> 标志被设置，父子进程会共享文件系统信息，否则将拷贝一份。</li></ul></li></ul><hr><h3 id="5-copy-sighand-clone-flags-p"><a href="#5-copy-sighand-clone-flags-p" class="headerlink" title="5. copy_sighand(clone_flags, p)"></a>5. <strong><code>copy_sighand(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝父进程的信号处理程序到子进程。</li><li><strong>细节</strong>:<ul><li>如果 <code>CLONE_SIGHAND</code> 标志被设置，父子进程共享信号处理程序。</li><li>负责拷贝或者共享信号处理程序的回调和状态。</li></ul></li></ul><hr><h3 id="6-copy-signal-clone-flags-p"><a href="#6-copy-signal-clone-flags-p" class="headerlink" title="6. copy_signal(clone_flags, p)"></a>6. <strong><code>copy_signal(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝父进程的信号相关状态到子进程。</li><li><strong>细节</strong>:<ul><li>包括未决信号（pending signals）、信号掩码（signal masks）等。</li><li>如果设置了线程组相关标志（如 <code>CLONE_THREAD</code>），信号状态可能会被共享。</li></ul></li></ul><hr><h3 id="7-copy-mm-clone-flags-p"><a href="#7-copy-mm-clone-flags-p" class="headerlink" title="7. copy_mm(clone_flags, p)"></a>7. <strong><code>copy_mm(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝父进程的内存描述符（Memory Descriptor，即 <code>mm_struct</code>）。</li><li><strong>细节</strong>:<ul><li>包括虚拟内存区域（VMA）、内存映射等。</li><li>如果设置了 <code>CLONE_VM</code> 标志，父子进程共享同一份 <code>mm_struct</code>，否则会为子进程创建一份新的内存描述符。</li></ul></li></ul><hr><h3 id="8-copy-namespaces-clone-flags-p"><a href="#8-copy-namespaces-clone-flags-p" class="headerlink" title="8. copy_namespaces(clone_flags, p)"></a>8. <strong><code>copy_namespaces(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝父进程的命名空间（Namespaces）到子进程。</li><li><strong>细节</strong>:<ul><li>包括进程命名空间、网络命名空间、挂载命名空间等。</li><li>如果设置了相应的命名空间克隆标志（如 <code>CLONE_NEWNS</code> 等），会创建新的命名空间；否则继承父进程的命名空间。</li></ul></li></ul><hr><h3 id="9-copy-io-clone-flags-p"><a href="#9-copy-io-clone-flags-p" class="headerlink" title="9. copy_io(clone_flags, p)"></a>9. <strong><code>copy_io(clone_flags, p)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝父进程的 IO 资源（如 IO 上下文）到子进程。</li><li><strong>细节</strong>:<ul><li>IO 上下文用于跟踪进程的 IO 操作和统计数据。</li><li>如果 <code>CLONE_IO</code> 被设置，父子进程共享 IO 资源，否则为子进程创建独立的 IO 上下文。</li></ul></li></ul><hr><h3 id="10-copy-thread-tls-clone-flags-stack-start-stack-size-p-tls"><a href="#10-copy-thread-tls-clone-flags-stack-start-stack-size-p-tls" class="headerlink" title="10. copy_thread_tls(clone_flags, stack_start, stack_size, p, tls)"></a>10. <strong><code>copy_thread_tls(clone_flags, stack_start, stack_size, p, tls)</code></strong></h3><ul><li><strong>作用</strong>: 拷贝线程上下文和 TLS（Thread Local Storage）。</li><li><strong>细节</strong>:<ul><li>拷贝 CPU 寄存器、堆栈指针等上下文信息，以便子进程能够正确运行。</li><li>如果需要创建线程，还会设置线程局部存储（TLS）的相关信息。</li><li>处理线程的启动入口和堆栈初始化。</li></ul></li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>流程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">do_fork </span><br><span class="line">_do_fork </span><br><span class="line">copy_process </span><br><span class="line">dup_task_struct </span><br><span class="line">sched_fork </span><br><span class="line">copy_xxx </span><br><span class="line">alloc_pid</span><br></pre></td></tr></table></figure><p>进程创建的核心实现在于copy_process()方法过程，而copy_process() 的主要实现在于copy_xxx()方法，根据不同的flags来决策采用何种拷贝方式。</p><ol><li>执行dup_task_struct()，拷贝当前进程task_struct</li><li>检查进程数是否超过系统所允许的上限(默认32678)</li><li>执行sched_fork()，设置调度器相关信息，设置task进程状态为TASK_RUNNING，并分配CPU资源</li><li>执行copy_xxx()，拷贝进程的相关资源信息<ul><li>copy_semundo: 当设置CLONE_SYSVSEM，则父子进程间共享SEM_UNDO状态</li><li>copy_files: 当设置CLONE_FILES，则只增加文件引用计数，不创建新的files_struct</li><li>copy_fs: 当设置CLONE_FS，且没有执行exec, 则设置用户数加1</li><li>copy_sighand: 当设置CLONE_SIGHAND, 则增加sighand-&gt;count计数</li><li>copy_signal: 拷贝进程信号</li><li>copy_mm：当设置CLONE_VM，则增加mm_users计数</li><li>copy_namespaces：一般情况，不需要创建新用户空间</li><li>copy_io： 当设置CLONE_IO，则父子进程间共享io context，增加nr_tasks计数</li><li>copy_thread_tls：设置子进程的寄存器等信息，从父进程拷贝thread_struct的sp0，sp,io_bitmap_ptr等成员变量值</li></ul></li><li>执行copy_thread_tls(), 拷贝子进程的内核栈信息</li><li>执行alloc_pid()，为新进程分配新pid</li></ol>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
          <category> process </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Linux内核设计与实现(1)</title>
      <link href="/2024/09/20/linux_kernel/Linux%E5%86%85%E6%A0%B8%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0-1/"/>
      <url>/2024/09/20/linux_kernel/Linux%E5%86%85%E6%A0%B8%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0-1/</url>
      
        <content type="html"><![CDATA[<p>我们主要从这五方面入手，了解 linux 内核是如何运行的：</p><ol><li><p>进程管理</p></li><li><p>内存管理</p></li><li><p>文件系统</p></li><li><p>设备驱动程序</p></li><li><p>网络</p></li></ol><h2 id="第-2-章-从内核出发"><a href="#第-2-章-从内核出发" class="headerlink" title="第 2 章 从内核出发"></a>第 2 章 从内核出发</h2><p> 可以从<a href="./Linux%E5%86%85%E6%A0%B8%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0(1)/https://www.kernel.org/">内核网站</a>中下载 Linux 的内核源码。可以使用<code>uname -r</code>查看内核 release 版本号。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git clone git://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux-2.6.git</span><br><span class="line">##更新到最新版本</span><br><span class="line">git pull</span><br></pre></td></tr></table></figure><p>也可以直接使用如下命令直接 clone 最新版本。然后使用<code>git checkout</code>来切换分支</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone git://git.kernel.org/pub/scm/linux/kernel/git/stable/linux-stable.git</span><br></pre></td></tr></table></figure><h4 id="2-1-使用补丁"><a href="#2-1-使用补丁" class="headerlink" title="2.1 使用补丁"></a>2.1 使用补丁</h4><p> Linux 中一般使用补丁的形式对发布的代码进行修改</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">patch -p1 &lt; .../patch-x.y.z</span><br></pre></td></tr></table></figure><h4 id="2-2-内核代码树"><a href="#2-2-内核代码树" class="headerlink" title="2.2 内核代码树"></a>2.2 内核代码树</h4><p> 内核源码树由很多目录组成，而大多数目录又包括更多的子目录，源码树的根目录以及子目录如下表所表示：</p><table><thead><tr><th align="left">目录</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left">arch</td><td align="left">特定体系结构的源码如：ARM，AMD64</td></tr><tr><td align="left">block</td><td align="left">块设备 I&#x2F;O 层</td></tr><tr><td align="left">crypto</td><td align="left">加密 API</td></tr><tr><td align="left">Documentation</td><td align="left">内核源码文档</td></tr><tr><td align="left">drivers</td><td align="left">设备驱动程序</td></tr><tr><td align="left">firmware</td><td align="left">使用某些驱动程序而需要的设备的固件</td></tr><tr><td align="left">fs</td><td align="left">VFS 和各种文件系统</td></tr><tr><td align="left">include</td><td align="left">内核头文件</td></tr><tr><td align="left">init</td><td align="left">内核引导和初始爱护</td></tr><tr><td align="left">ipc</td><td align="left">进程间通信代码</td></tr><tr><td align="left">kernel</td><td align="left">调度程序核心子系统</td></tr><tr><td align="left">lib</td><td align="left">通用内核函数</td></tr><tr><td align="left">mm</td><td align="left">内存管理子系统和 VM</td></tr><tr><td align="left">net</td><td align="left">网络子系统</td></tr><tr><td align="left">samples</td><td align="left">示例，示范代码</td></tr><tr><td align="left">scripts</td><td align="left">编译内核所用的脚本</td></tr><tr><td align="left">security</td><td align="left">Linux 安全模块</td></tr><tr><td align="left">sound</td><td align="left">语音子系统</td></tr><tr><td align="left">usr</td><td align="left">早期用户空间代码(所谓的 initramfs)</td></tr><tr><td align="left">tools</td><td align="left">在 Linux 开发中有用的工具</td></tr><tr><td align="left">virt</td><td align="left">虚拟化基础结构</td></tr></tbody></table><p>COPYING 是内个许可证，CREDITS 是内核开发者列表。MAINTAINERS 是维护者列表</p><h4 id="2-3-内核结构"><a href="#2-3-内核结构" class="headerlink" title="2.3 内核结构"></a>2.3 内核结构</h4><p>按功能，Linux 内核可以划分为 5 个不同的部分，每一部分处理一项明确的功能，同时又向其他内核组件提供这项功能。这种结构也反映到内核的源代码上——这 5 部分都形成了自己的子树。</p><ol><li><p>进程管理</p></li><li><p>内存管理</p></li><li><p>文件系统</p></li><li><p>设备驱动程序</p></li><li><p>网络</p></li></ol><h4 id="2-4-编译内核"><a href="#2-4-编译内核" class="headerlink" title="2.4 编译内核"></a>2.4 编译内核</h4><p>一般使用 make 命令来进行</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">##使用命令行选项</span><br><span class="line"></span><br><span class="line">make menuconfig</span><br><span class="line">##基于gtk+的图形工具</span><br><span class="line"></span><br><span class="line">make gconfig</span><br><span class="line">##使用默认体系结构配置</span><br><span class="line"></span><br><span class="line">make defconfig</span><br></pre></td></tr></table></figure><p>也可以使用如下的命令，将电脑中已有的配置文件作为编译配置选项</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">##将配置文件内容，添加到.config文件中</span><br><span class="line"></span><br><span class="line">zcat /proc/config.gz &gt; .config</span><br><span class="line">##使用配置文件编译</span><br><span class="line"></span><br><span class="line">make oldconfig</span><br><span class="line"></span><br><span class="line">make 进行编译</span><br></pre></td></tr></table></figure><h6 id="2-4-2-减少编译的垃圾信息"><a href="#2-4-2-减少编译的垃圾信息" class="headerlink" title="2.4.2 减少编译的垃圾信息"></a>2.4.2 减少编译的垃圾信息</h6><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">##使用如下命令，进行垃圾信息重定向到文件</span><br><span class="line"></span><br><span class="line">make &gt; .. /detritus</span><br><span class="line">##直接扔掉信息</span><br><span class="line"></span><br><span class="line">make &gt; /dev/null</span><br></pre></td></tr></table></figure><p>接下来运行如下命令，进行内核模块的安装</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">make modules_install</span><br></pre></td></tr></table></figure><h4 id="2-5-内核开发的特点"><a href="#2-5-内核开发的特点" class="headerlink" title="2.5 内核开发的特点"></a>2.5 内核开发的特点</h4><ul><li>内核编程时既不能访问 c 库也不能访问标准的 c 头文件:<ul><li>完整的 C 库太大太低效，因此不方便使用</li><li>基本头文件在顶级目录下的 include 目录中，体系结构相关的头文件在 arch&#x2F;&#x2F;include&#x2F;asm 目录下使用<code>##include &lt;asm/ioctl.h&gt;</code>进行头文件的包含。</li></ul></li><li>内核编程时必须使用 GNU C(GNU C 、ANSI C、标准 C、标准 c++区别和联系)<ul><li>GNU C 支持 inline 内联函数</li><li>使用<code>asm volatile(&quot;rdtsc&quot;:&quot;=a&quot; (low),&quot;=d&quot; (high))</code>嵌入汇编代码</li></ul></li><li>内核编程时缺乏像用户空间那样的内存保护机制<ul><li>用户进程中进行非法内存访问，内核会发现错误并发送 SIGSEGV 信号，结束整个进程。内核自己访问非法内存往往会造成内核死去。</li><li>内核中的内存都不分页，每用掉一个字节，物理内存就减少一个字节</li></ul></li><li>内核编程时难以执行浮点运算<ul><li>用户空间中的浮点操作，内核会完成浮点数到整数的转换。因此内核并不能完美的支持浮点数操作，因为它本身不能陷入。</li></ul></li><li>内核给每个进程只有一个很小的定长堆栈<ul><li>内核栈的准确大小随着体系结构而改变，内核栈的大小是两页，32 位是 8KB,64 位是 16KB。</li></ul></li><li>内核支持异步中断、抢占和 SMP(対称多处理器结构)，因此必须时刻注意同步和并发<ul><li>同时在大于两核处理器上执行内核代码，可能会同时访问共享的同一个资源</li><li>中断是异步到来的，完全不顾及当前正在执行的代码</li><li>内核可抢占，没有保护，可能导致几段代码同时访问相同的资源，通常使用自旋锁和信号量来解决</li></ul></li><li>要仔细考虑可移植的特性<ul><li>注意保持字节序、64 位对齐、不假定字长和页面长度等一系列准则等</li></ul></li></ul><h2 id="第-3-章-进程管理"><a href="#第-3-章-进程管理" class="headerlink" title="第 3 章 进程管理"></a>第 3 章 进程管理</h2><h6 id="3-1-进程"><a href="#3-1-进程" class="headerlink" title="3.1 进程"></a>3.1 进程</h6><p><strong>每一个线程都拥有一个独立的程序计数器、进程栈、一组进程寄存器</strong>。<em>内核调度的对象是线程，而不是进程</em> (<a href="./Linux%E5%86%85%E6%A0%B8%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0(1)/https://www.jianshu.com/p/8ff15d3a1dfd">从进程和线程了解浏览器的工作原理</a>)；在 Linux 中对线程和进程的区分度不是很大。线程之间共享虚拟内存，但是每个线程都拥有自己独立的虚拟处理器。每个进程都有自己的用户栈和内核栈，陷入内核时，将用户栈寄存器压入内核栈；出去时，重新返回用户栈寄存器，并清空内核栈–<strong>内核栈总是空的</strong></p>]]></content>
      
      
      <categories>
          
          <category> linux_kernel </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> Linux </tag>
            
            <tag> C/C++ </tag>
            
            <tag> 程序设计 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_12_并发编程</title>
      <link href="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/"/>
      <url>/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<p>​ 如果逻辑控制流在时间上重叠，那么它们就是<strong>并发的(concurent)<strong>这种常见的现象称为</strong>并发(concurrency)</strong>，出现在订算机系统的许多不同层面上。硬件异常处理程序、进程和 Unix 信号处理程序都是大家很熟悉的例子。</p><p>​ 到目前为止，我们主要将并发看做是一种操作系统内核用来运行多个应用程序的机制。但是，并发不仅仅局限于内核。它也可以在应用程序中扮演重要角色。例如，我们已经看到 Unix 信号处理程序如何允许应用响应异步事件，例如用户键人 ctrl-c，或者程序访问虚拟存储器的-个未定义的区域。应用级并发在其他情况下也是很有用的:</p><p><strong>并发和并行</strong></p><ul><li>并发：Concurrency，只要时间上重叠就算并发，可以是单处理器交替处理</li><li>并行：Parallel，属于并发的一种特殊情况（真子集），多核&#x2F;多 CPU 同时处理</li></ul><p>​ 使用应用级并发的应用程序称为<strong>并发程序(concurrent program)</strong>。现代操作系统提供了三种基本的构造并发程序的方法:</p><ul><li>进程。用这种方法，每个逻辑控制流都是一个进程，由<strong>内核来调度和维护</strong>。因为进程有独立的虚拟地址空间，想要和其他流通信，控制流必须使用某种显式的**进程间通信(interprocess communication，IPC)**机制。</li><li>IO 多路复用。在这种形式的并发编程中，应用程序在<strong>一个进程</strong>的上下文中显式地调度它们自己的逻辑流。逻辑流被模型化为<strong>状态机</strong>，数据到达文件描述符后，主程序显式地从一个状态转换到另一个状态。因为程序是一个单独的进程，所以所有的流都共享同一个地址空间。</li><li>线程。线程是运行在一个<strong>单一进程</strong>上下文中的逻辑流，由内核进行调度。你可以把线程看成是其他两种方式的混合体，像进程流一样由<strong>内核进行调度</strong>，而像 O 多路复用流一样共享同一个虚拟地址空间。</li></ul><h2 id="1-基于进程的并发编程"><a href="#1-基于进程的并发编程" class="headerlink" title="1.基于进程的并发编程"></a>1.基于进程的并发编程</h2><p>​ 构造并发程序最简单的方法就是用进程。个构造并发服务器的自然方法就是，在父进程中接受客户端连接请求，然后创建一个新的子进程来为每个新客户端提供服务。</p><p>​ 为了了解这是如何工作的，假设我们有两个客户端和一个服务器，服务器正在监听一个监听描述符(比如描述符 3)上的连接请求。现在假设服务器接受了客户端 1 的连接请求并返回一个已连接描述符(比如描述符 4)，如下图所示：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/30a6f3e35367485df9dd8980b2ecb0a0.png" class><p>​ 在接受连接请求之后，服务器派生一个子进程，这个子进程获得服务器描述符表的完整拷贝。子进程关闭它的拷贝中的监听描述符 3，而父进程关闭它的已连接描述符 4 的拷贝，因为不再需要这些描述符了。这就得到了下图中的状态：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/5b792b7814f72fc2aa3b43533509d4a4.png" class><p>​ 其中子进程正忙于为客户端提供服务。因为父、子进程中的已连接描述符都指向同一个文件表表项，**所以父进程关闭它的已连接描述符的拷贝是至关重要的。**否则，将永远不会释放已连接描述符 4 的文件表条目，<strong>而且由此引起的存储器泄漏将最终消耗尽可用的存储器</strong>，使系统崩溃。</p><p>​ 现在，假设在父进程为客户端 1 创建了子进程之后，它接受一个新的客户端 2 的连接请求,并返回一个新的已连接描述符(比如描述符 5)，如下图所示：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/a2bfed02b89e93218f0192c0ef46f32c.png" class><p>​ 然后，父进程又派生另一个子进程，这个子进程用已连接描述符 5 为它的客户端提供服务，如下图所示：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/27b515bd1bcb3880359c4813b31a2b96.png" class><p>​ 此时，父进程正在等待下一个连接请求，而两个子进程正在并发地为它们各自的客户端提供服务。</p><p>​</p><h4 id="1-1-基础进程的并发服务器"><a href="#1-1-基础进程的并发服务器" class="headerlink" title="1.1 基础进程的并发服务器"></a>1.1 基础进程的并发服务器</h4><p>​ 示例代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">sigchld_handler</span><span class="params">(<span class="type">int</span> sig)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span>(waitpid(<span class="number">-1</span>, <span class="number">0</span>, WNOHANG) &gt; <span class="number">0</span>)</span><br><span class="line">        ;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    signal(SIGCHLD, sigchld_handler); <span class="comment">// 回收僵死子进程资源</span></span><br><span class="line">    listenfd = open_listenfd();</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">      connfd = accept(...);</span><br><span class="line">      <span class="keyword">if</span> (fork() == <span class="number">0</span>) &#123; <span class="comment">// 子进程</span></span><br><span class="line">        close(listenfd); <span class="comment">// 关闭父进程 fd，不关闭问题不大，子进程结束时自动关闭</span></span><br><span class="line">        process(connfd);</span><br><span class="line">        close(connfd);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      close(connfd); <span class="comment">// 父进程关闭 fd。重要！否则永远不会释放 connfd 连接描述符，导致内存泄漏！</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>首先，通常服务器会运行很长的时间，所以我们必须要包括一个 SIGCHLD 处理程序，来回收僵死(zombie)子进程的资源(第 4~9 行)，因为当 SIGCHLD 处理程序执行时， SIGCHLD 信号是阻塞的。而 Unix 信号是不排队的，所以 SIGCHLD 处理程序必须准备好回收多个死子进程的资源。</li><li>其次，父子进程必须关闭它们各自的 connfd(分别为第 33 行和第 30 行)拷贝。就像我们已经提到过的，这对父进程而言尤为重要，它必须关闭它的已连接描述符，以避免存储器泄漏。</li><li>最后，因为套接字的文件表表项中的引用计数，直到父子进程的 connfd 都关闭了，到客户端的连接才会终止。</li></ul><h4 id="1-2-关于进程的优劣"><a href="#1-2-关于进程的优劣" class="headerlink" title="1.2 关于进程的优劣"></a>1.2 关于进程的优劣</h4><p>​ 对于在父、子进程间共享状态信息，进程有一个非常清晰的模型:共享文件表，但是不共享用户地址空间。进程有独立的地址空间既是优点也是缺点。这样一来，一个进程不可能不小心覆盖另一个进程的虚拟存储器，这就消除了许多令人迷惑的错误–这是一个明显的优点。</p><p>​ 另一方面，独立的地址空间使得进程共享状态信息变得更加困难。为了共享信息，它们必须使用显式的 IPC(进程间通信)机制。基于进程的设计的另一个缺点是，它们往往比较慢，因为进程控制和 IPC 的开销很高。</p><h2 id="2-基础-I-O-多路复用的并发编程"><a href="#2-基础-I-O-多路复用的并发编程" class="headerlink" title="2.基础 I&#x2F;O 多路复用的并发编程"></a>2.基础 I&#x2F;O 多路复用的并发编程</h2><p>​ 假设要求你编写一个 echo 服务器，它也能对用户从标准输入键人的交互命令做出响应。在这种情况下，服务器必须响应两个互相独立的 I&#x2F;0 事件:</p><ol><li>网络客户端发起连接请求，</li><li>用户在键盘上键人命令行。</li></ol><p>​ 我们先等待哪个事件呢?没有哪个选择是理想的。如果在 accept 中等待一个连接请求，我们就不能响应输人的命令。类似地，如果在 read 中等待一个输人命令，我们就不能响应任何连接请求。</p><p>​ 针对这种困境的一个解决办法就是<strong>I&#x2F;O 多路复用(Omultiplexing)技术</strong>。基本的思路就是使用 select 函数，要求内核挂起进程，只有在一个或多个 IO 事件发生后，才将控制返回给应用程序，就像在下面的示例中一样:</p><ul><li>当集合 {0，4}中任意描述符准备好读时返回。</li><li>当集合{1，2，7}中任意描述符准备好写时返回，</li><li>如果在等待一个 UO 事件发生时过了 152.13 秒，就超时</li></ul><p>​ 通过 <code>select</code> 函数，等待一组描述符 ready。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">select</span><span class="params">(<span class="type">int</span> n, fd_set *fdset, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>)</span>; <span class="comment">// 返回 ready fd 的个数，出错返回 -1</span></span><br><span class="line"></span><br><span class="line">FD_ZERO(fd_set *fdset);</span><br><span class="line">FD_CLR(<span class="type">int</span> fd, fd_set *fdset);</span><br><span class="line">FD_SET(<span class="type">int</span> fd, fd_set *fdset);</span><br><span class="line">FD_ISSET(<span class="type">int</span> fd, fd_set *fdset)</span><br></pre></td></tr></table></figure><p>​ select 函数处理类型为 fd_set 的集合，也叫做描述符集合。逻辑上，我们将描述符集合看成一个大小为 n 的位向量（在 2.1 节中介绍过）：</p><p>$$<br>b_{n−1},⋯,b_1,b_0<br>$$</p><p>每个位 b_k 对应于描述符 k。当且仅当 b_k&#x3D;1，描述符 k 才表明是描述符集合的一个元素。只允许你对描述符集合做三件事：</p><ol><li>分配它们.</li><li>将一个此种类型的变量赋值给另一个变量.</li><li>用 FD_ZERO、FD_SET、FD_CLR 和 FD_ISSET 宏来修改和检查它们.</li></ol><p>​ 针对我们的目的，select 函数有两个输入：一个称为<strong>读集合</strong>的描述符集合（fdset）和该读集合的基数（n）（实际上是任何描述符集合的最大基数）。select 函数会一直阻塞，直到读集合中至少有一个描述符准备好可以读。当且仅当一个从该描述符读取一个字节的请求不会阻塞时，描述符 k 就表示<strong>准备好可以读</strong>了。select 有一个副作用，它修改参数 fdset 指向的 fd_set，指明读集合的一个子集，称为<strong>准备好集合</strong>（ready set），这个集合是由读集合中准备好可以读了的描述符组成的。该函数返回的值指明了准备好集合的基数。注意，由于这个副作用，我们必须在每次调用 select 时都更新读集合。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">echo</span><span class="params">(<span class="type">int</span> connfd)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">command</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> listenfd, connfd;</span><br><span class="line">    <span class="type">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line">    fd_set read_set, ready_set;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    FD_ZERO(&amp;read_set);              <span class="comment">/* Clear read set */</span></span><br><span class="line">    FD_SET(STDIN_FILENO, &amp;read_set); <span class="comment">/* Add stdin to read set */</span></span><br><span class="line">    FD_SET(listenfd, &amp;read_set);     <span class="comment">/* Add listenfd to read set */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        ready_set = read_set;</span><br><span class="line">        Select(listenfd + <span class="number">1</span>, &amp;ready_set, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="keyword">if</span> (FD_ISSET(STDIN_FILENO, &amp;ready_set))</span><br><span class="line">            command(); <span class="comment">/* Read command line from stdin */</span></span><br><span class="line">        <span class="keyword">if</span> (FD_ISSET(listenfd, &amp;ready_set)) &#123;</span><br><span class="line">            clientlen = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> sockaddr_storage);</span><br><span class="line">            connfd = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line">            echo(connfd); <span class="comment">/* Echo client input until EOF */</span></span><br><span class="line">            Close(connfd);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">command</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="type">char</span> buf[MAXLINE];</span><br><span class="line">    <span class="keyword">if</span> (!Fgets(buf, MAXLINE, <span class="built_in">stdin</span>))</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>); <span class="comment">/* EOF */</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, buf); <span class="comment">/* Process the input command */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 一开始，我们用<code>open_listenfd </code>函数打开一个监听描述符（第 16 行），然后使用 FD_ZERO 创建一个空的读集合（第 18 行）：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/da48410221e5585ba37fe05d19849009.png" class><p>​ 接下来，在第 19 和 20 行中，我们定义由描述符 0（标准输入）和描述符 3（监听描述符）组成的读集合：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/4a1679f95f9ef2f765d34066c8efdd5c.png" class><p>​ 在这里，我们开始典型的服务器循环。但是我们不调用 accept 函数来等待一个连接请求，而是调用 select 函数，这个函数会一直阻塞，直到监听描述符或者标准输入准备好可以读（第 24 行）。例如，下面是当用户按回车键，因此使得标准输入描述符变为可读时，select 会返回的 ready_set 的值：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/069cc5056d8da4fa5e951c2d7b0cb7cd.png" class><p>​ 一旦 select 返回，我们就用 FD_ISSET 宏指令来确定哪个描述符准备好可以读了。如果是标准输入准备好了（第 25 行），我们就调用 command 函数，该函数在返回到主程序前，会读、解析和响应命令。如果是监听描述符准备好了（第 27 行），我们就调用 accept 来得到一个已连接描述符，然后调用 echo 函数，它会将来自客户端的每一行又回送回去，直到客户端关闭这个连接中它的那一端。</p><p>​ 虽然这个程序是使用 select 的一个很好示例，但是它仍然留下了一些问题待解决。问题是一旦它连接到某个客户端，就会连续回送输入行，直到客户端关闭这个连接中它的那一端。因此，如果键入一个命令到标准输入，你将不会得到响应，直到服务器和客户端之间结 束。一个更好的方法是更细粒度的多路复用，服务器每次循环（至多）回送一个文本行。</p><h4 id="2-1-基于-I-O-多路复用的并发事件驱动服务器"><a href="#2-1-基于-I-O-多路复用的并发事件驱动服务器" class="headerlink" title="2.1 基于 I&#x2F;O 多路复用的并发事件驱动服务器"></a>2.1 基于 I&#x2F;O 多路复用的并发事件驱动服务器</h4><p>​ I&#x2F;O 多路复用可以用做并发<strong>事件驱动</strong>（event-driven）程序的基础，在事件驱动程序中，某些事件会导致流向前推进。一般的思路是将逻辑流模型化为状态机。不严格地说，一个<strong>状态机</strong>（state machine）就是一组<strong>状态</strong>（state）、<strong>输入事件</strong>（input event）和<strong>转移</strong>（transition），其中转移是将状态和输入事件映射到状态。每个转移是将一个（输入状态，输入事件）对映射到一个输出状态。<strong>自循环</strong>（self-loop）是同一输入和输岀状态之间的转移。通常把状态机画成有向图，其中节点表示状态，有向弧表示转移，而弧上的标号表示输入事件。一个状态机从某种初始状态开始执行。每个输入事件都会引发一个从当前状态到下一状态的转移。</p><p>​ 对于每个新的客户端 k，基于 I&#x2F;O 多路复用的并发服务器会创建一个新的状态机 sk，并将它和已连接描述符联系起来。如下图示：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/78fcc7b5b63bb4e8a9260899093c6902.png" class><p>​ 每个状态机 sk 都有一个状态（“等待描述符 dk 准备好可读”）、一个输入事件（“描述符 dk 准备好可以读了”）和一个转移（“从描述符 dk 读一个文本行”）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span> <span class="comment">/* Represents a pool of connected descriptors */</span></span><br><span class="line">    <span class="type">int</span> maxfd;        <span class="comment">/* Largest descriptor in read_set */</span></span><br><span class="line">    fd_set read_set;  <span class="comment">/* Set of all active descriptors */</span></span><br><span class="line">    fd_set ready_set; <span class="comment">/* Subset of descriptors ready for reading */</span></span><br><span class="line">    <span class="type">int</span> nready;       <span class="comment">/* Number of ready descriptors from select */</span></span><br><span class="line">    <span class="type">int</span> maxi;         <span class="comment">/* High water index into client array */</span></span><br><span class="line">    <span class="type">int</span> clientfd[FD_SETSIZE];    <span class="comment">/* Set of active descriptors */</span></span><br><span class="line">    <span class="type">rio_t</span> clientrio[FD_SETSIZE]; <span class="comment">/* Set of active read buffers */</span></span><br><span class="line">&#125; pool;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> byte_cnt = <span class="number">0</span>; <span class="comment">/* Counts total bytes received by server */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> listenfd, connfd;</span><br><span class="line">    <span class="type">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line">    <span class="type">static</span> pool pool;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line">    init_pool(listenfd, &amp;pool);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">/* Wait for listening/connected descriptor(s) to become ready */</span></span><br><span class="line">        pool.ready_set = pool.read_set;</span><br><span class="line">        pool.nready = Select(pool.maxfd + <span class="number">1</span>, &amp;pool.ready_set, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* If listening descriptor ready, add new client to pool */</span></span><br><span class="line">        <span class="keyword">if</span> (FD_ISSET(listenfd, &amp;pool.ready_set)) &#123;</span><br><span class="line">            clientlen = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> sockaddr_storage);</span><br><span class="line">            connfd = Accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line">            add_client(connfd, &amp;pool);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Echo a text line from each ready connected descriptor */</span></span><br><span class="line">        check_clients(&amp;pool);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 上面展示了一个基于 I&#x2F;O 多路复用的并发事件驱动服务器的完整示例代码。一个 pool 结构里维护着活动客户端的集合（第 3 ~ 11 行）。在调用 init_pool 初始化池（第 27 行）之后，服务器进入一个无限循环。在循环的每次迭代中，服务器调用 select 函数来检测两种不同类型的输入事件：a）来自一个新客户端的连接请求到达，b）一个已存在的客户端的已连接描述符准备好可以读了。当一个连接请求到达时（第 35 行），服务器打开连接（第 37 行），并调用 add_client 函数，将该客户端添加到池里（第 38 行）。最后，服务器调用 check_clients 函数，把来自每个准备好的已连接描述符的一个文本行回送回去（第 42 行）。</p><p>​ init_pool 函数初始化客户端池。clientfd 数组表示已连接描述符的集合，其中整数 - 1 表示一个可用的槽位。初始时，已连接描述符集合是空的（第 5 ~ 7 行），而且监听描述符是 select 读集合中唯一的描述符（第 10 ~ 12 行）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">init_pool</span><span class="params">(<span class="type">int</span> listenfd, pool *p)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* Initially, there are no connected descriptors */</span></span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line">    p-&gt;maxi = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; FD_SETSIZE; i++)</span><br><span class="line">        p-&gt;clientfd[i] = <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Initially, listenfd is only member of select read set */</span></span><br><span class="line">    p-&gt;maxfd = listenfd;</span><br><span class="line">    FD_ZERO(&amp;p-&gt;read_set);</span><br><span class="line">    FD_SET(listenfd, &amp;p-&gt;read_set);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ add_client 函数添加一个新的客户端到活动客户端池中。在 clientfd 数组中找到一个空槽位后，服务器将这个已连接描述符添加到数组中，并初始化相应的 RIO 读缓冲区，这样一来我们就能够对这个描述符调用 rio_readlineb（第 8 ~ 9 行）。然后，我们将这个已连接描述符添加到 select 读集合（第 12 行），并更新该池的一些全局属性。maxfd 变量（第 15 ~ 16 行）记录了 select 的最大文件描述符。maxi 变量（第 17 ~ 18 行）记录的是到 clientfd 数组的最大索引，这样 check_clients 函数就无需搜索整个数组了。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">void add_client(int connfd, pool *p)</span><br><span class="line">&#123;</span><br><span class="line">    int i;</span><br><span class="line">    p-&gt;nready--;</span><br><span class="line">    for (i = 0; i &lt; FD_SETSIZE; i++) /* Find an available slot */</span><br><span class="line">        if (p-&gt;clientfd[i] &lt; 0) &#123;</span><br><span class="line">            /* Add connected descriptor to the pool */</span><br><span class="line">            p-&gt;clientfd[i] = connfd;</span><br><span class="line">            Rio_readinitb(&amp;p-&gt;clientrio[i], connfd);</span><br><span class="line"></span><br><span class="line">            /* Add the descriptor to descriptor set */</span><br><span class="line">            FD_SET(connfd, &amp;p-&gt;read_set);</span><br><span class="line"></span><br><span class="line">            /* Update max descriptor and pool high water mark */</span><br><span class="line">            if (connfd &gt; p-&gt;maxfd)</span><br><span class="line">                p-&gt;maxfd = connfd;</span><br><span class="line">            if (i &gt; p-&gt;maxi)</span><br><span class="line">                p-&gt;maxi = i;</span><br><span class="line">            break;</span><br><span class="line">        &#125;</span><br><span class="line">    if (i == FD_SETSIZE) /* Couldn’t find an empty slot */</span><br><span class="line">        app_error(&quot;add_client error: Too many clients&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 根据上图中的有限状态模型，select 函数检测到输入事件，而 add_client 函数创建一个新的逻辑流（状态机）。check_clients 函数回送输入行，从而执行状态转移，而且当客户端完成文本行发送时，它还要删除这个状态机。</p><h4 id="2-2-I-O-多路复用技术的优劣"><a href="#2-2-I-O-多路复用技术的优劣" class="headerlink" title="2.2 I&#x2F;O 多路复用技术的优劣"></a>2.2 I&#x2F;O 多路复用技术的优劣</h4><ul><li>单一进程上下文，共享数据容易。</li><li>事件驱动，不需要上下文切换，高效，有明显的性能优势。</li><li>编码复杂</li><li><strong>不能充分利用多核处理器</strong></li></ul><h2 id="3-基于线程的并发编程"><a href="#3-基于线程的并发编程" class="headerlink" title="3.基于线程的并发编程"></a>3.基于线程的并发编程</h2><p>​ <strong>线程</strong>（thread）就是运行在进程上下文中的逻辑流。在本书里迄今为止，程序都是由每个进程中一个线程组成的。但是现代系统也允许我们编写一个进程里同时运行多个线程的程序。线程由内核自动调度。每个线程都有它自己的<strong>线程上下文</strong>（thread context），包括一个唯一的整数<strong>线程 ID</strong>（Thread ID，TID）、栈、栈指针、程序计数器、通用目的寄存器和条件码。所有的运行在一个进程里的线程共享该进程的整个虚拟地址空间。</p><p>​ 基于线程的逻辑流结合了基于进程和基于 I&#x2F;O 多路复用的流的特性。同进程一样，线程由内核自动调度，并且内核通过一个整数 ID 来识别线程。同基于 I&#x2F;O 多路复用的流一样，多个线程运行在单一进程的上下文中，因此共享这个进程虚拟地址空间的所有内容，包括它的代码、数据、堆、共享库和打开的文件。</p><h4 id="3-1-线程执行模型"><a href="#3-1-线程执行模型" class="headerlink" title="3.1 线程执行模型"></a>3.1 线程执行模型</h4><p>​ 多线程的执行模型在某些方面和多进程的执行模型是相似的。思考下中的示例：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/0ff32d60536a44c18995b399239a3a05.png" class><p>​ 每个进程开始生命周期时都是单一线程，这个线程称为<strong>主线程</strong>（main thread）。在某一时刻，主线程创建一个<strong>对等线程</strong>（peer thread），从这个时间点开始，两个线程就并发地运行。最后，因为主线程执行一个慢速系统调用，例如 read 或者 sleep，或者因为被系统的间隔计时器中断，控制就会通过上下文切换传递到对等线程。对等线程会执行一段时间，然后控制传递回主线程，依次类推。</p><p>​ 在一些重要的方面，线程执行是不同于进程的。因为一个线程的上下文要比一个进程的上下文小得多，线程的上下文切换要比进程的上下文切换快得多。另一个不同就是线程不像进程那样，不是按照严格的父子层次来组织的。<strong>和一个进程相关的线程组成一个对等（线程）池</strong>，独立于其他线程创建的线程。主线程和其他线程的区别仅在于它总是进程中第一个运行的线程。<strong>对等（线程）池概念的主要影响是，一个线程可以杀死它的任何对等线程</strong>，或者等待它的任意对等线程终止。另外，每个对等线程都能读写相同的共享数据。</p><h4 id="3-2-Posix-线程"><a href="#3-2-Posix-线程" class="headerlink" title="3.2 Posix 线程"></a>3.2 Posix 线程</h4><p>​ Posix 线程（Pthreads）是在 C 程序中处理线程的一个标准接口。它最早出现在 1995 年，而且在所有的 Linux 系统上都可用。Pthreads 定义了大约 60 个函数，允许程序创建、杀死和回收线程，与对等线程安全地共享数据，还可以通知对等线程系统状态的变化。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, <span class="literal">NULL</span>);</span><br><span class="line">    Pthread_join(tid, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span> <span class="comment">/* Thread routine */</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello, world!\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 主线程创建一个对等线程，然后等待它的终止。对等线程输岀 “Hello, world!\n” 并且终止。当主线程检测到对等线程终止后，它就通过调用 exit 终止该进程。这是我们看到的第一个线程化的程序，所以让我们仔细地解析它。线程的代码和本地数据被封装在一个<strong>线程例程</strong>（thread routine）中。正如第二行里的原型所示，每个线程例程都以一个通用指针作为输入，并返回一个通用指针。如果想传递多个参数给线程例程，那么你应该将参数放到一个结构中，并传递一个指向该结构的指针。相似地，如果想要线程例程返回多个参数，你可以返回一个指向一个结构的指针。</p><p>​ 第 4 行标出了主线程代码的开始。主线程声明了一个本地变量 tid，可以用来存放对等线程的 ID（第 6 行）。主线程通过调用 pthread_create 函数创建一个新的对等线程（第 7 行）。当对 pthread_create 的调用返回时，主线程和新创建的对等线程同时运行，并且 tid 包含新线程的 ID。通过在第 8 行调用 pthread_join，主线程等待对等线程终止。最后，主线程调用 exit（第 9 行），终止当时运行在这个进程中的所有线程（在这个示例中就只有主线程）。</p><p>​ 第 12 ~ 16 行定义了对等线程的例程。它只打印一个字符串，然后就通过执行第 15 行中的 return 语句来终止对等线程</p><h4 id="3-3-创建线程"><a href="#3-3-创建线程" class="headerlink" title="3.3 创建线程"></a>3.3 创建线程</h4><p>​</p><p>​ 线程通过调用 pthread_create 函数来创建其他线程</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">void</span> *(func)(<span class="type">void</span> *);</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_create</span><span class="params">(<span class="type">pthread_t</span> *tid, <span class="type">pthread_attr_t</span> *attr,</span></span><br><span class="line"><span class="params">                   func *f, <span class="type">void</span> *arg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 若成功则返回 0，若出错则为非零。</span></span><br></pre></td></tr></table></figure><p>pthread_create 函数创建一个新的线程，并带着一个输入变量 arg，在新线程的上下文中运行线程例程 f。能用 attr 参数来改变新创建线程的默认属性。改变这些属性已超出我们学习的范围，在我们的示例中，总是用一个为 NULL 的参数来调用 pthread_create 函数。</p><p>当 pthread_create 返回时，参数 tid 包含新创建线程的 ID。新线程可以通过调用 pthread_self 函数来获得它自己的线程 ID。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">pthread_t</span> <span class="title function_">pthread_self</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回调用者的线程 ID。</span></span><br></pre></td></tr></table></figure><h4 id="3-4-终止线程"><a href="#3-4-终止线程" class="headerlink" title="3.4 终止线程"></a>3.4 终止线程</h4><p>​ 一个线程是以下列方式之一来终止的：</p><ul><li>当顶层的线程例程返回时，线程会<strong>隐式地</strong>终止。</li><li>通过调用 pthread_exit 函数，线程会<strong>显式地</strong>终止。如果主线程调用 pthread_exit，它会等待所有其他对等线程终止，然后再终止主线程和整个进程，返回值为 thread_return。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">pthread_exit</span><span class="params">(<span class="type">void</span> *thread_return)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 从不返回。</span></span><br></pre></td></tr></table></figure><ul><li>某个对等线程调用 Linux 的 exit 函数，该函数终止进程以及所有与该进程相关的线程。</li><li>另一个对等线程通过以当前线程 ID 作为参数调用 pthread_Cancel 函数来终止当前线程。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_cancel</span><span class="params">(<span class="type">pthread_t</span> tid)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 若成功则返回 0，若出错则为非零。</span></span><br></pre></td></tr></table></figure><h4 id="3-5-回收已终止线程的资源"><a href="#3-5-回收已终止线程的资源" class="headerlink" title="3.5 回收已终止线程的资源"></a>3.5 回收已终止线程的资源</h4><p>线程通过调用 pthread_join 函数等待其他线程终止:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_join</span><span class="params">(<span class="type">pthread_t</span> tid, <span class="type">void</span> **thread_return)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 若成功则返回 0，若出错则为非零。</span></span><br></pre></td></tr></table></figure><p>​ pthread_join 函数会阻塞，直到线程 tid 终止，将线程例程返回的通用 <strong>(void*)</strong> 指针赋值为 thread_return 指向的位置，然后回收已终止线程占用的所有内存资源。</p><p>注意，和 Linux 的 wait 函数不同，pthread_join 函数只能等待一个指定的线程终止。没有办法让 pthread_wait 等待任意一个线程终止。这使得代码更加复杂，因为它迫使我们去使用其他一些不那么直观的机制来检测进程的终止。实际上，Stevens 在【110】中就很有说服力地论证了这是规范中的一个错误</p><h4 id="3-6-分离线程"><a href="#3-6-分离线程" class="headerlink" title="3.6 分离线程"></a>3.6 分离线程</h4><p>​ 在任何一个时间点上，线程是<strong>可结合的</strong>（joinable）或者是<strong>分离的</strong>（detached）。一个可结合的线程能够被其他线程收回和杀死。在被其他线程回收之前，它的内存资源（例如栈）是不释放的。相反，一个分离的线程是不能被其他线程回收或杀死的。它的内存资源在它终止时由系统自动释放。</p><p>​ 默认情况下，线程被创建成可结合的。为了避免内存泄漏，每个可结合线程都应该要么被其他线程显式地收回，要么通过调用 pthread_detach 函数被分离。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_detach</span><span class="params">(<span class="type">pthread_t</span> tid)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 若成功则返回 0，若出错则为非零。</span></span><br></pre></td></tr></table></figure><p>​ pthread_detach 函数分离可结合线程 tid。线程能够通过以 pthread_self() 参数的 pthread_detach 调用来分离它们自己。</p><p>​ 尽管我们的一些例子会使用可结合线程，但是在现实程序中，有很好的理由要使用分离的线程。例如，一个高性能 Web 服务器可能在每次收到 Web 浏览器的连接请求时都创建一个新的对等线程。因为每个连接都是由一个单独的线程独立处理的，所以对于服务器而言，就很没有必要（实际上也不愿意）显式地等待每个对等线程终止。在这种情况下，每个对等线程都应该在它开始处理请求之前分离它自身，这样就能在它终止后回收它的内存资源了。</p><h4 id="3-7-初始化线程"><a href="#3-7-初始化线程" class="headerlink" title="3.7 初始化线程"></a>3.7 初始化线程</h4><p>​ pthread_once 函数允许你初始化与线程例程相关的状态。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">pthread_once_t</span> once_control = PTHREAD_ONCE_INIT;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">pthread_once</span><span class="params">(<span class="type">pthread_once_t</span> *once_control,</span></span><br><span class="line"><span class="params">                 <span class="type">void</span> (*init_routine)(<span class="type">void</span>))</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 总是返回 0。</span></span><br></pre></td></tr></table></figure><p>once_control 变量是一个全局或者静态变量，总是被初始化为 PTHREAD_ONCE_INIT。当你第一次用参数 once_control 调用 pthread_once 时，它调用 init_routine，这是一个没有输入参数、也不返回什么的函数。接下来的以 once_control 为参数的 pthread_once 调用不做任何事情。无论何时，当你需要动态初始化多个线程共享的全局变量时，pthread_once 函数是很有用的。</p><h4 id="3-8-基于线程的并发服务器"><a href="#3-8-基于线程的并发服务器" class="headerlink" title="3.8 基于线程的并发服务器"></a>3.8 基于线程的并发服务器</h4><p>​ 下图展示了基于线程的并发 echo 服务器的代码。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">echo</span><span class="params">(<span class="type">int</span> connfd)</span>;</span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> listenfd, *connfdp;</span><br><span class="line">    <span class="type">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> sockaddr_storage);</span><br><span class="line">        connfdp = Malloc(<span class="keyword">sizeof</span>(<span class="type">int</span>));</span><br><span class="line">        *connfdp = Accept(listenfd, (SA *) &amp;clientaddr, &amp;clientlen);</span><br><span class="line">        Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, connfdp);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Thread routine */</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> connfd = *((<span class="type">int</span> *)vargp);</span><br><span class="line">    Pthread_detach(pthread_self());</span><br><span class="line">    Free(vargp);</span><br><span class="line">    echo(connfd);</span><br><span class="line">    Close(connfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 整体结构类似于基于进程的设计。主线程不断地等待连接请求，然后创建一个对等线程处理该请求。虽然代码看似简单，但是有几个普遍而且有些微妙的问题需要我们更仔细地看一看。第一个问题是当我们调用 pthread_create 时，如何将已连接描述符传递给对等线程。最明显的方法就是传递一个指向这个描述符的指针，就像下面这样</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">connfd = Accept(listenfd, (SA *) &amp;clientaddr, &amp;clientlen);</span><br><span class="line">Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, &amp;connfd);</span><br></pre></td></tr></table></figure><p>然后，我们让对等线程间接引用这个指针，并将它赋值给一个局部变量，如下所示:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span> &#123;</span><br><span class="line">    <span class="type">int</span> connfd = *((<span class="type">int</span> *)vargp);</span><br><span class="line">    .</span><br><span class="line">    .</span><br><span class="line">    .</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 然而，这样可能会出错，因为它在对等线程的赋值语句和主线程的 accept 语句间引入了<strong>竞争</strong>（race）。如果赋值语句在下一个 accept 之前完成，那么对等线程中的局部变量 connfd 就得到正确的描述符值。然而，如果赋值语句是在 accept 之后才完成的，那么对等线程中的局部变量 connfd 就得到下一次连接的描述符值。那么不幸的结果就是，现在两个线程在同一个描述符上执行输入和输出。为了避免这种潜在的致命竞争，我们必须将 accept 返回的每个已连接描述符分配到它自己的动态分配的内存块，如第 20 ~ 21 行所示。我们会在 7.4 节中回过来讨论竞争的问题。</p><p>另一个问题是在线程例程中避免内存泄漏。既然不显式地收回线程，就必须分离每个线程，使得在它终止时它的内存资源能够被收回（第 31 行）。更进一步，我们必须小心释放主线程分配的内存块（第 32 行）</p><h2 id="4-多线程程序中的共享变量"><a href="#4-多线程程序中的共享变量" class="headerlink" title="4.多线程程序中的共享变量"></a>4.多线程程序中的共享变量</h2><p>​ 从程序员的角度来看，线程很有吸引力的一个方面是多个线程很容易共享相同的程序变量。然而，这种共享也是很棘手的。为了编写正确的多线程程序，我们必须对所谓的共享以及它是如何工作的有很清楚的了解。</p><p>为了理解 C 程序中的一个变量是否是共享的，有一些基本的问题要解答：</p><ol><li>线程的基础内存模型是什么？</li><li>根据这个模型，变量实例是如何映射到内存的？</li><li>最后，有多少线程引用这些实例？一个变量是<strong>共享的</strong>，当且仅当多个线程引用这个变量的某个实例。</li></ol><p>​ 为了让我们对共享的讨论具体化，我们将使用下图中的程序作为运行示例。尽管有些人为的痕迹，但是它仍然值得研究，因为它说明了关于共享的许多细微之处。示例程序由一个创建了两个对等线程的主线程组成。主线程传递一个唯一的 ID 给每个对等线程，每个对等线程利用这个 ID 输出一条个性化的信息，以及调用该线程例程的总次数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> N 2</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">char</span> **ptr; <span class="comment">/* Global variable */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="type">char</span> *msgs[N] = &#123;</span><br><span class="line">        <span class="string">&quot;Hello from foo&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Hello from bar&quot;</span></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    ptr = msgs;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">        Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, (<span class="type">void</span> *)i);</span><br><span class="line">    Pthread_exit(<span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> myid = (<span class="type">int</span>)vargp;</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> cnt = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;[%d]: %s (cnt=%d)\n&quot;</span>, myid, ptr[myid], ++cnt);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-1-线程内存模型"><a href="#4-1-线程内存模型" class="headerlink" title="4.1 线程内存模型"></a>4.1 线程内存模型</h4><p>​ 一组并发线程运行在一个进程的上下文中。每个线程都有它自己独立的线程上下文，包括线程 ID、栈、栈指针、程序计数器、条件码和通用目的寄存器值。每个线程和其他线程一起共享进程上下文的剩余部分。这包括整个用户虚拟地址空间，它是由只读文本（代码）、读&#x2F;写数据、堆以及所有的共享库代码和数据区域组成的。线程也共享相同的打开文件的集合。</p><p>​ 从实际操作的角度来说，让一个线程去读或写另一个线程的寄存器值是不可能的。另一方面，任何线程都可以访问共享虚拟内存的任意位置。如果某个线程修改了一个内存位置，那么其他每个线程最终都能在它读这个位置时发现这个变化。因此，寄存器是从不共享的，而虚拟内存总是共享的。</p><p>​ 各自独立的线程栈的内存模型不是那么整齐清楚的。这些栈被保存在虚拟地址空间的栈区域中，并且通常是被相应的线程独立地访问的。我们说通常而不是总是，是因为不同的线程栈是不对其他线程设防的。所以，如果一个线程以某种方式得到一个指向其他线程栈的指针，那么它就可以读写这个栈的任何部分。示例程序在第 26 行展示了这一点，其中对等线程直接通过全局变量 ptr 间接引用主线程的栈的内容。</p><h4 id="4-2-将变量映射到内存"><a href="#4-2-将变量映射到内存" class="headerlink" title="4.2 将变量映射到内存"></a>4.2 将变量映射到内存</h4><p>多线程的 C 程序中变量根据它们的存储类型被映射到虚拟内存：</p><ul><li>**全局变量。**全局变量是定义在函数之外的变量。在运行时，虚拟内存的读&#x2F;写区域只包含每个全局变量的一个实例，任何线程都可以引用。例如，第 5 行声明的全局变量 ptr 在虚拟内存的读&#x2F;写区域中有一个运行时实例。当一个变量只有一个实例时，我们只用变量名（在这里就是 ptr）来表示这个实例。</li><li>**本地自动变量。**本地自动变量就是定义在函数内部但是没有 static 属性的变量。在运行时，每个线程的栈都包含它自己的所有本地自动变量的实例。即使多个线程执行同一个线程例程时也是如此。例如，有一个本地变量 tid 的实例，它保存在主线程的栈中。我们用 <strong>tid.m</strong> 来表示这个实例。再来看一个例子，本地变量 myid 有两个实例，一个在对等线程。的栈内，另一个在对等线程 1 的栈内。我们将这两个实例分别表示为 <strong>myid.p0</strong> 和 <strong>myid.p1</strong>。</li><li>**本地静态变量。**本地静态变量是定义在函数内部并有 static 属性的变量。和全局变量一样，虚拟内存的读&#x2F;写区域只包含在程序中声明的每个本地静态变量的一个实例。例如，即使示例程序中的每个对等线程都在第 25 行声明了 cnt，在运行时，虚拟内存的读&#x2F;写区域中也只有一个 cnt 的实例。每个对等线程都读和写这个实例。</li></ul><h4 id="4-3-共享变量"><a href="#4-3-共享变量" class="headerlink" title="4.3 共享变量"></a>4.3 共享变量</h4><p>我们说一个变量 v 是共享的，当且仅当它的一个实例被一个以上的线程引用。例如，示例程序中的变量 cnt 就是共享的，因为它只有一个运行时实例，并且这个实例被两个对等线程引用。在另一方面，myid 不是共享的，因为它的两个实例中每一个都只被一个线程引用。然而，认识到像 msgs 这样的本地自动变量也能被共享是很重要的。</p><h2 id="5-用信号量同步线程"><a href="#5-用信号量同步线程" class="headerlink" title="5.用信号量同步线程"></a>5.用信号量同步线程</h2><p>​ 共享变量是十分方便，但是它们也引入了<strong>同步错误</strong>（synchronization error）的可能性。考虑下图中的程序 badcnt.c，它创建了两个线程，每个线程都对共享计数变量 cnt 加 1。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* WARNING: This code is buggy! */</span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span>; <span class="comment">/* Thread routine prototype */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Global shared variable */</span></span><br><span class="line"><span class="keyword">volatile</span> <span class="type">long</span> cnt = <span class="number">0</span>; <span class="comment">/* Counter */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">long</span> niters;</span><br><span class="line">    <span class="type">pthread_t</span> tid1, tid2;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Check input argument */</span></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;usage: %s &lt;niters&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    niters = atoi(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Create threads and wait for them to finish */</span></span><br><span class="line">    Pthread_create(&amp;tid1, <span class="literal">NULL</span>, thread, &amp;niters);</span><br><span class="line">    Pthread_create(&amp;tid2, <span class="literal">NULL</span>, thread, &amp;niters);</span><br><span class="line">    Pthread_join(tid1, <span class="literal">NULL</span>);</span><br><span class="line">    Pthread_join(tid2, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Check result */</span></span><br><span class="line">    <span class="keyword">if</span> (cnt != (<span class="number">2</span> * niters))</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;BOOM! cnt=%ld\n&quot;</span>, cnt);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;OK cnt=%ld\n&quot;</span>, cnt);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Thread routine */</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">long</span> i, niters = *((<span class="type">long</span> *)vargp);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; niters; i++)</span><br><span class="line">        cnt++;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>​ 因为每个线程都对计数器增加了 niters 次，我们预计它的最终值是 2 × niters。这看上去简单而直接。然而，当在 Linux 系统上运行 badcnt.c 时，我们不仅得到错误的答案，而且每次得到的答案都还不相同！</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">linux&gt; </span><span class="language-bash">./badcnt 1000000</span></span><br><span class="line">BOOM! cnt=1445085</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">linux&gt; </span><span class="language-bash">./badcnt 1000000</span></span><br><span class="line">BOOM! cnt=1915220</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">linux&gt; </span><span class="language-bash">./badcnt 1000000</span></span><br><span class="line">BOOM! cnt=1404746</span><br></pre></td></tr></table></figure><p>​ 那么哪里出错了呢？为了清晰地理解这个问题，我们需要研究计数器循环（第 40 ~ 41 行）的汇编代码，如图 所示:</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/774afd47f859629cc2ab9af4cc895f0f.png" class><p>我们发现，将线程 i 的循环代码分解成五个部分是很有帮助的：</p><ul><li><p><em>Hi</em>：在循环头部的指令块。</p></li><li><p><em>Li</em>：加载共享变量 cnt 到累加寄存器 %eaxi 的指令，这里 %eaxi 表示线程 i 中的寄存器 ％eax 的值。</p></li><li><p><em>Ui</em>：更新（增加）%eaxi 的指令。</p></li><li><p><em>Si</em>：将 ％eaxi 的更新值存回到共享变量 cnt 的指令。</p></li><li><p><em>Ti</em> ：循环尾部的指令块。</p></li></ul><p>注意头和尾只操作本地栈变量，而<em>Li</em>、 <em>Ui</em>和<em>Si</em>操作共享计数器变量的内容。</p><p>​ 当 badcnt.c 中的两个对等线程在一个单处理器上并发运行时，机器指令以某种顺序一个接一个地完成。因此，每个并发执行定义了两个线程中的指令的某种全序（或者交叉）。不幸的是，这些顺序中的一些将会产生正确结果，但是其他的则不会。</p><p>​ 这里有个关键点：**一般而言，你没有办法预测操作系统是否将为你的线程选择一个正确的顺序。**例如，图 12-18a 展示了一个正确的指令顺序的分步操作。在每个线程更新了共享变量 cnt 之后，它在内存中的值就是 2，这正是期望的值。</p><p>​ 另一方面，图 12-18b 的顺序产生一个不正确的 cnt 的值。会发生这样的问题是因为，线程 2 在第 5 步加载 cnt，是在第 2 步线程 1 加载 cnt 之后，而在第 6 步线程 1 存储它的更新值之前。因此，每个线程最终都会存储一个值为 1 的更新后的计数器值。我们能够借助于一种叫做<strong>进度图</strong>（progress graph）的方法来阐明这些正确的和不正确的指令顺序的概念，这个图我们将在下一节中介绍。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/b4694fdb9d23908ea2c43a4ed25f6721.png" class><h4 id="5-1-进度图"><a href="#5-1-进度图" class="headerlink" title="5.1 进度图"></a>5.1 进度图</h4><p><strong>进度图</strong>（progress graph）将 n 个并发线程的执行模型化为一条 n 维笛卡儿空间中的轨迹线。每条轴 k 对应于线程 k 的进度。每个点</p><p>$$<br>(I_1,I_2,⋯ ,I_n)<br>$$</p><p>​ 代表线程 k (k&#x3D;1,…,n)已经完成了指令<em>Ik</em>这一状态。图的原点对应于没有任何线程完成一条指令的初始状态。</p><p>​ 图 12-19 展示了 badcnt.c 程序第一次循环迭代的二维进度图。水平轴对应于线程 1，垂直轴对应于线程 2。点 (<em>L</em>1,<em>S</em>2) 对应于线程 1 完成了 <em>L</em>1 而线程 2 完成了 <em>S</em>2 的状态。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/b575ef2e636b438cf4011841791e16c6.png" class><p>​ 进度图将指令执行模型化为从一种状态到另一种状态的<strong>转换</strong>（transition）。转换被表示为一条从一点到相邻点的有向边。合法的转换是向右（线程 1 中的一条指令完成）或者向上（线程 2 中的一条指令完成）的。两条指令不能在同一时刻完成一对角线转换是不允许的。程序决不会反向运行，所以向下或者向左移动的转换也是不合法的。</p><p>​ 一个程序的执行历史被模型化为状态空间中的一条轨迹线。下图展示了下面指令顺序对应的轨迹线：</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/415a0e9afc53468afd783fdf49020d08.png" class><p>​ 对于线程 i，操作共享变量 cnt 内容的指令(<em>Li</em>,<em>Ui</em>,<em>Si</em>)构成了一个（关于共享变量 cnt 的）<strong>临界区</strong>（critical section），这个临界区不应该和其他进程的临界区交替执行。换句话说，我们想要确保每个线程在行它的临界区中的指令时，拥有对共享变量的<strong>互斥的访问</strong>（mutually exclusive access）。通常这种现象称为<strong>互斥</strong>（mutual exclusion）。</p><p>​ 在进度图中，两个临界区的交集形成的状态空间区域称为<strong>不安全区</strong>（unsafe region）。 下图展示了变量 cnt 的不安全区。注意，不安全区和与它交界的状态相毗邻，但并不包括这些状态。例如，状态(<em>H</em>1,<em>H</em>2) 和(<em>S</em>1,<em>S</em>2)毗邻不安全区，但是它们并不是不安全区的一部分。绕开不安全区的轨迹线叫做<strong>安全轨迹线</strong>（safe trajectory）。相反，接触到任何不安全区的轨迹线就叫做<strong>不安全轨迹线</strong>（unsafe trajectory）。下图给出了示例程序 badcnt.c 的状态空间中的安全和不安全轨迹线。上面的轨迹线绕开了不安全区域的左边和上边，所以是安全的。下面的轨迹线穿越不安全区，因此是不安全的。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/3f16efe1bb7bdac8734ef6a6a4eb6c59.png" class><p>​ 任何安全轨迹线都将正确地更新共享计数器。为了保证线程化程序示例的正确执行（实际上任何共享全局数据结构的并发程序的正确执行）我们必须以某种方式<strong>同步</strong>线程，使它们总是有一条安全轨迹线。一个经典的方法是基于信号量的思想，接下来我们就介绍它。</p><h4 id="5-2-信号量"><a href="#5-2-信号量" class="headerlink" title="5.2 信号量"></a>5.2 信号量</h4><p>​ Edsger Dijkstra，并发编程领域的先锋人物，提出了一种经典的解决同步不同执行线程问题的方法，这种方法是基于一种叫做<strong>信号量</strong>（semaphore）的特殊类型变量的。信号量 s 是具有非负整数值的全局变量，只能由两种特殊的操作来处理，这两种操作称为 P 和 V：</p><ul><li><em>P</em>(<em>s</em>)：如果 s 是非零的，那么 P 将 s 减 1，并且立即返回。如果 s 为零，那么就挂起这个线程，直到 s 变为非零，而一个 V 操作会重启这个线程。在重启之后，P 操作将 s 减 1，并将控制返回给调用者。</li><li><em>V(s)</em>：V 操作将 s 加 1。如果有任何线程阻塞在 P 操作等待 s 变成非零，那么 V 操作会重启这些线程中的一个，然后该线程将 s 减 1，完成它的 P 操作。</li></ul><p>​ P 中的测试和减 1 操作是不可分割的，<strong>也就是说，一旦预测信号量 s 变为非零，就会将 s 减 1，不能有中断</strong>。V 中的加 1 操作也是不可分割的，<strong>也就是加载、加 1 和存储信号量的过程中没有中断</strong>。注意，V 的定义中没有定义等待线程被重启动的顺序。唯一的要求是 V 必须只能重启一个正在等待的线程。因此，当有多个线程在等待同一个信号量时，你不能预测 V 操作要重启哪一个线程。</p><p>​ P 和 V 的定义确保了一个正在运行的程序绝不可能进入这样一种状态，也就是一个正确初始化了的信号量有一个负值。这个属性称为<strong>信号量不变性</strong>（semaphore invariant），为控制并发程序的轨迹线提供了强有力的工具，在下一节中我们将看到。</p><p>Posix 标准定义了许多操作信号量的函数:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_init</span><span class="params">(<span class="type">sem_t</span> *sem, <span class="number">0</span>, <span class="type">unsigned</span> <span class="type">int</span> value)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_wait</span><span class="params">(<span class="type">sem_t</span> *s)</span>; <span class="comment">/* P(s) */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sem_post</span><span class="params">(<span class="type">sem_t</span> *s)</span>; <span class="comment">/* V(s) */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回：若成功则为 0，若出错则为 -1。</span></span><br></pre></td></tr></table></figure><p>​ <code>sem_init </code>函数将信号量 sem 初始化为 value。每个信号量在使用前必须初始化。针对我们的目的，中间的参数总是零。程序分别通过调用 <code>sem_wait</code> 和 <code>sem_post </code>函数来执行 P 和 V 操作。为了简明，我们更喜欢使用下面这些等价的 P 和 V 的包装函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">P</span><span class="params">(<span class="type">sem_t</span> *s)</span>; <span class="comment">/* Wrapper function for sem_wait */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">V</span><span class="params">(<span class="type">sem_t</span> *s)</span>; <span class="comment">/* Wrapper function for sem_post */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回：无。</span></span><br></pre></td></tr></table></figure><h4 id="5-3-使用信号量来实现互斥"><a href="#5-3-使用信号量来实现互斥" class="headerlink" title="5.3 使用信号量来实现互斥"></a>5.3 使用信号量来实现互斥</h4><p>​ 信号量提供了一种很方便的方法来确保对共享变量的互斥访问。基本思想是将每个共享变量（或者一组相关的共享变量）与一个信号量 s（初始为 1）联系起来，然后用 P(s)<em>P</em>(<em>s</em>)和 V(s)<em>V</em>(<em>s</em>)操作将相应的临界区包围起来。</p><p>​ 以这种方式来保护共享变量的信号量叫做<strong>二元信号量</strong>（binary semaphore），因为它的值总是 0 或者 1。以提供互斥为目的的二元信号量常常也称为<strong>互斥锁</strong>（mutex）。在一个互斥锁上执行 P 操作称为对互斥锁加锁。类似地，执行 V 操作称为对互斥锁解锁。对一个互斥锁加了锁但是还没有解锁的线程称为占用这个互斥锁。一个被用作一组可用资源的计数器的信号量被称为<strong>计数信号量</strong>。</p><p>​ 下图中的进度图展示了我们如何利用二元信号量来正确地同步计数器程序示例。每个状态都标出了该状态中信号量 s 的值。关键思想是这种 P 和 V 操作的结合创建了一组状态，叫做<strong>禁止区</strong>（forbidden region），其中因为信号量的不变性，没有实际可行的轨迹线能够包含禁止区中的状态。而且，因为禁止区完全包括了不安全区，所以没有实际可行的轨迹线能够接触不安全区的任何部分。因此，每条实际可行的轨迹线都是安全的，而且不管运行时指令顺序是怎样的，程序都会正确地增加计数器值。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/6d970b7ef1cb368beb48839616304903.png" class><p>​ 从可操作的意义上来说，由 P 和 V 操作创建的禁止区使得在任何时间点上，在被包围的临界区中，不可能有多个线程在执行指令。换句话说，信号量操作确保了对临界区的互斥访问。</p><p>​ 总的来说，为了用信号量正确同步图 12-16 中的计数器程序示例，我们首先声明一个信号量 mutex，然后在主例程中将 mutex 初始化为 1，最后，我们通过把在线程例程中对共享变量 cnt 的更新包围 P 和 V 操作，从而保护它们：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="type">long</span> cnt = <span class="number">0</span>; <span class="comment">/* Counter */</span></span><br><span class="line"><span class="type">sem_t</span> mutex;           <span class="comment">/* Semaphore that protects counter */</span></span><br><span class="line"></span><br><span class="line">Sem_init(&amp;mutex, <span class="number">0</span>, <span class="number">1</span>); <span class="comment">/* mutex = 1 */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; niters; i++) &#123;</span><br><span class="line">    P(&amp;mutex);</span><br><span class="line">    cnt++;</span><br><span class="line">    V(&amp;mutex);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当我们运行这个正确同步的程序时，现在它每次都能产生正确的结果了。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">linux&gt; </span><span class="language-bash">./goodcnt 1000000</span></span><br><span class="line">OK cnt=2000000</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">linux&gt; </span><span class="language-bash">./goodcnt 1000000</span></span><br><span class="line">OK cnt=2000000</span><br></pre></td></tr></table></figure><h4 id="5-4-利用信号量来调度共享资源"><a href="#5-4-利用信号量来调度共享资源" class="headerlink" title="5.4 利用信号量来调度共享资源"></a>5.4 利用信号量来调度共享资源</h4><p>​ 除了提供互斥之外，信号量的另一个重要作用是调度对共享资源的访问。在这种场景中，一个线程用信号量操作来通知另一个线程，程序状态中的某个条件已经为真了。两个经典而有用的例子是<strong>生产者—消费者</strong>和<strong>读者—写者</strong>问题。</p><h6 id="1-生产者—消费者问题"><a href="#1-生产者—消费者问题" class="headerlink" title="1. 生产者—消费者问题"></a>1. 生产者—消费者问题</h6><p>​ 下图给出了生产者—消费者问题。生产者和消费者线程共享一个<strong>有 n 个槽的有限缓冲区</strong>。生产者线程反复地生成新的<strong>项目</strong>（item），并把它们插入到缓冲区中。消费者线程不断地从缓冲区中取出这些项目，然后消费（使用）它们。也可能有多个生产者和消费者的变种。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/1141fc563e561181abc16e721025af59.png" class><p>​ 因为插入和取出项目都涉及更新共享变量，所以我们必须保证对缓冲区的访问是互斥的。但是只保证互斥访问是不够的，我们还需要调度对缓冲区的访问。如果缓冲区是满的（没有空的槽位），那么生产者必须等待直到有一个槽位变为可用。与之相似，如果缓冲区是空的（没有可取用的项目），那么消费者必须等待直到有一个项目变为可用。</p><p>​ 生产者—消费者的相互作用在现实系统中是很普遍的。例如，在一个多媒体系统中，生产者编码视频帧，而消费者解码并在屏幕上呈现出来。缓冲区的目的是为了减少视频流的抖动，而这种抖动是由各个帧的编码和解码时与数据相关的差异引起的。缓冲区为生产者提供了一个槽位池，而为消费者提供一个已编码的帧池。另一个常见的示例是图形用户接口设计。生产者检测到鼠标和键盘事件，并将它们插入到缓冲区中。消费者以某种基于优先级的方式从缓冲区取出这些事件，并显示在屏幕上。</p><p>​ 在本节中，我们将开发一个简单的包，叫做 SBUF，用来构造生产者—消费者程序。在下一节里，我们会看到如何用它来构造一个基于预线程化（prethreading）的有趣的并发服务器。SBUF 操作类型为 sbuf_t 的有限缓冲区（如图所示）。项目存放在一个动态分配的 1 项整数数组（buf）中。front 和 rear 索引值记录该数组中的第一项和最后一项。三个信号量同步对缓冲区的访问。mutex 信号量提供互斥的缓冲区访问。slots 和 items 信号量分别记录空槽位和可用项目的数量。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> *buf;       <span class="comment">/* Buffer array */</span></span><br><span class="line">    <span class="type">int</span> n;          <span class="comment">/* Maximum number of slots */</span></span><br><span class="line">    <span class="type">int</span> front;      <span class="comment">/* buf[(front+1)%n] is first item */</span></span><br><span class="line">    <span class="type">int</span> rear;       <span class="comment">/* buf[rear%n] is last item */</span></span><br><span class="line">    <span class="type">sem_t</span> mutex;    <span class="comment">/* Protects accesses to buf */</span></span><br><span class="line">    <span class="type">sem_t</span> slots;    <span class="comment">/* Counts available slots */</span></span><br><span class="line">    <span class="type">sem_t</span> items;    <span class="comment">/* Counts available items */</span></span><br><span class="line">&#125; <span class="type">sbuf_t</span>;</span><br></pre></td></tr></table></figure><p>​ 下面给出了 SBUF 函数的实现。sbuf_init 函数为缓冲区分配堆内存，设置 front 和 rear 表示一个空的缓冲区，并为三个信号量赋初始值。这个函数在调用其他三个函数中的任何一个之前调用一次。sbuf_deinit 函数是当应用程序使用完缓冲区时，释放缓冲区存储的。sbuf_insert 函数等待一个可用的槽位，对互斥锁加锁，添加项目，对互斥锁解锁，然后宣布有一个新项目可用。sbuf_remove 函数是与 sbuf_insert 函数对称的。在等待一个可用的缓冲区项目之后，对互斥锁加锁，从缓冲区的前面取出该项目，对互斥锁解锁，然后发信号通知一个新的槽位可供使用。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;sbuf.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Create an empty, bounded, shared FIFO buffer with n slots */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">sbuf_init</span><span class="params">(<span class="type">sbuf_t</span> *sp, <span class="type">int</span> n)</span></span><br><span class="line">&#123;</span><br><span class="line">    sp-&gt;buf = Calloc(n, <span class="keyword">sizeof</span>(<span class="type">int</span>));</span><br><span class="line">    sp-&gt;n = n;                       <span class="comment">/* Buffer holds max of n items */</span></span><br><span class="line">    sp-&gt;front = sp-&gt;rear = <span class="number">0</span>;        <span class="comment">/* Empty buffer iff front == rear */</span></span><br><span class="line">    Sem_init(&amp;sp-&gt;mutex, <span class="number">0</span>, <span class="number">1</span>);      <span class="comment">/* Binary semaphore for locking */</span></span><br><span class="line">    Sem_init(&amp;sp-&gt;slots, <span class="number">0</span>, n);      <span class="comment">/* Initially, buf has n empty slots */</span></span><br><span class="line">    Sem_init(&amp;sp-&gt;items, <span class="number">0</span>, <span class="number">0</span>);      <span class="comment">/* Initially, buf has zero data items */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Clean up buffer sp */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">sbuf_deinit</span><span class="params">(<span class="type">sbuf_t</span> *sp)</span></span><br><span class="line">&#123;</span><br><span class="line">    Free(sp-&gt;buf);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Insert item onto the rear of shared buffer sp */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">sbuf_insert</span><span class="params">(<span class="type">sbuf_t</span> *sp, <span class="type">int</span> item)</span></span><br><span class="line">&#123;</span><br><span class="line">    P(&amp;sp-&gt;slots);                           <span class="comment">/* Wait for available slot */</span></span><br><span class="line">    P(&amp;sp-&gt;mutex);                           <span class="comment">/* Lock the buffer */</span></span><br><span class="line">    sp-&gt;buf[(++sp-&gt;rear) % (sp-&gt;n)] = item;  <span class="comment">/* Insert the item */</span></span><br><span class="line">    V(&amp;sp-&gt;mutex);                           <span class="comment">/* Unlock the buffer */</span></span><br><span class="line">    V(&amp;sp-&gt;items);                           <span class="comment">/* Announce available item */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Remove and return the first item from buffer sp */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sbuf_remove</span><span class="params">(<span class="type">sbuf_t</span> *sp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> item;</span><br><span class="line">    P(&amp;sp-&gt;items);                           <span class="comment">/* Wait for available item */</span></span><br><span class="line">    P(&amp;sp-&gt;mutex);                           <span class="comment">/* Lock the buffer */</span></span><br><span class="line">    item = sp-&gt;buf[(++sp-&gt;front) % (sp-&gt;n)]; <span class="comment">/* Remove the item */</span></span><br><span class="line">    V(&amp;sp-&gt;mutex);                           <span class="comment">/* Unlock the buffer */</span></span><br><span class="line">    V(&amp;sp-&gt;slots);                           <span class="comment">/* Announce available slot */</span></span><br><span class="line">    <span class="keyword">return</span> item;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h6 id="2-读者—写者问题"><a href="#2-读者—写者问题" class="headerlink" title="2. 读者—写者问题"></a>2. 读者—写者问题</h6><p>​ 读者—写者问题是互斥问题的一个概括。一组并发的线程要访问一个共享对象，例如一个主存中的数据结构，或者一个磁盘上的数据库。有些线程只读对象，而其他的线程只修改对象。修改对象的线程叫做<strong>写者</strong>。只读对象的线程叫做<strong>读者</strong>。写者必须拥有对对象的独占的访问，而读者可以和无限多个其他的读者共享对象。一般来说，有无限多个并发的读者和写者。</p><p>​ 读者—写者交互在现实系统中很常见。例如，一个在线航空预定系统中，允许有无限多个客户同时查看座位分配，但是正在预订座位的客户必须拥有对数据库的独占的访问。再来看另一个例子，在一个多线程缓存 Web 代理中，无限多个线程可以从共享页面缓存中取出已有的页面，但是任何向缓存中写入一个新页面的线程必须拥有独占的访问。</p><p>​ 读者—写者问题有几个变种，分别基于读者和写者的优先级。第一类读者—写者问题，读者优先，要求不要让读者等待，除非已经把使用对象的权限赋予了一个写者。换句话说，读者不会因为有一个写者在等待而等待。第二类读者—写者问题，写者优先，要求一旦一个写者准备好可以写，它就会尽可能快地完成它的写操作。同第一类问题不同，在一个写者后到达的读者必须等待，即使这个写者也是在等待。</p><p>​ 下面给出了一个对第一类读者—写者问题的解答。同许多同步问题的解答一样，这个解答很微妙，极具欺骗性地简单。信号量 w 控制对访问共享对象的临界区的访问。信号量 mutex 保护对共享变量 readcnt 的访问，readcnt 统计当前在临界区中的读者数量。每当一个写者进入临界区时，它对互斥锁 w 加锁，每当它离开临界区时，对 w 解锁。这就保证了任意时刻临界区中最多只有一个写者。另一方面，只有第一个进入临界区的读者对 w 加锁，而只有最后一个离开临界区的读者对 w 解锁。当一个读者进入和离开临界区时，如果还有其他读者在临界区中，那么这个读者会忽略互斥锁 w。这就意味着只要还有一个读者占用互斥锁 w，无限多数量的读者可以没有障碍地进入临界区。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Global variables */</span></span><br><span class="line"><span class="type">int</span> readcnt;    <span class="comment">/* Initially = 0 */</span></span><br><span class="line"><span class="type">sem_t</span> mutex, w; <span class="comment">/* Both initially = 1 */</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">reader</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        P(&amp;mutex);</span><br><span class="line">        readcnt++;</span><br><span class="line">        <span class="keyword">if</span> (readcnt == <span class="number">1</span>) <span class="comment">/* First in */</span></span><br><span class="line">            P(&amp;w);</span><br><span class="line">        V(&amp;mutex);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Critical section */</span></span><br><span class="line">        <span class="comment">/* Reading happens  */</span></span><br><span class="line"></span><br><span class="line">        P(&amp;mutex);</span><br><span class="line">        readcnt--;</span><br><span class="line">        <span class="keyword">if</span> (readcnt == <span class="number">0</span>) <span class="comment">/* Last out */</span></span><br><span class="line">            V(&amp;w);</span><br><span class="line">        V(&amp;mutex);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">writer</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        P(&amp;w);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Critical section */</span></span><br><span class="line">        <span class="comment">/* Writing happens  */</span></span><br><span class="line"></span><br><span class="line">        V(&amp;w);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 对这两种读者—写者问题的正确解答可能导致饥饿（starvation），饥饿就是一个线程无限期地阻塞，无法进展。例如，上图所示的解答中，如果有读者不断地到达，写者就可能无限期地等待。</p><h4 id="5-5-综合：基于预线程化的并发服务器"><a href="#5-5-综合：基于预线程化的并发服务器" class="headerlink" title="5.5 综合：基于预线程化的并发服务器"></a>5.5 综合：基于预线程化的并发服务器</h4><p>​ 我们已经知道了如何使用信号量来访问共享变量和调度对共享资源的访问。为了帮助你更清晰地理解这些思想，让我们把它们应用到一个基于称为<strong>预线程化</strong>（prethreading）技术的并发服务器上。</p><p>​ 在图 12-14 所示的并发服务器中，我们为每一个新客户端创建了一个新线程。这种方法的缺点是我们为每一个新客户端创建一个新线程，导致不小的代价。一个基于预线程化的服务器试图通过使用如图所示的生产者—消费者模型来降低这种开销。服务器是由一个主线程和一组工作者线程构成的。主线程不断地接受来自客户端的连接请求，并将得到的连接描述符放在一个有限缓冲区中。每一个工作者线程反复地从共享缓冲区中取出描述符，为客户端服务，然后等待下一个描述符。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/7a489ee00f2f31cb7105d9f665168967.png" class><p>​ 下面显示了我们怎样用 SBUF 包来实现一个预线程化的并发 echo 服务器。在初始化了缓冲区 sbuf（第 24 行）后，主线程创建了一组工作者线程（第 25 ~ 26 行）。然后它进入了无限的服务器循环，接受连接请求，并将得到的已连接描述符插入到缓冲区 sbuf 中。每个工作者线程的行为都非常简单。它等待直到它能从缓冲区中取出一个已连接描述符（第 39 行），然后调用 echo_cnt 函数回送客户端的输入。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;sbuf.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> NTHREADS 4</span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> SBUFSIZE 16</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">echo_cnt</span><span class="params">(<span class="type">int</span> connfd)</span>;</span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">sbuf_t</span> sbuf; <span class="comment">/* Shared buffer of connected descriptors */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i, listenfd, connfd;</span><br><span class="line">    <span class="type">socklen_t</span> clientlen;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    sbuf_init(&amp;sbuf, SBUFSIZE);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NTHREADS; i++)  <span class="comment">/* Create worker threads */</span></span><br><span class="line">        Pthread_create(&amp;tid, <span class="literal">NULL</span>, thread, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> sockaddr_storage);</span><br><span class="line">        connfd = Accept(listenfd, (SA *) &amp;clientaddr, &amp;clientlen);</span><br><span class="line">        sbuf_insert(&amp;sbuf, connfd); <span class="comment">/* Insert connfd in buffer */</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    Pthread_detach(pthread_self());</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">int</span> connfd = sbuf_remove(&amp;sbuf); <span class="comment">/* Remove connfd from buffer */</span></span><br><span class="line">        echo_cnt(connfd);                <span class="comment">/* Service client */</span></span><br><span class="line">        Close(connfd);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ <code>echo_cnt</code> 函数 是图 11-22 中的 echo 函数的一个版本，它在全局变量 byte_cnt 中记录了从所有客户端接收到的累计字节数。这是一段值得研究的有趣代码，因为它向你展示了一个从线程例程调用的初始化程序包的一般技术。在这种情况中，我们需要初始化 byte_cnt 计数器和 mutex 信号量。一个方法是我们为 SBUF 和 RIO 程序包使用过的，它要泰主线程显式地调用一个初始化函数。另外一个方法，在此显示的，是当第一次有某个线程调用 <code>echo_cnt</code> 函数时，使用<code> pthread_once</code> 函数（第 19 行）去调用初始化函数。这个方法的优点是它届程序包的使用更加容易。这种方法的缺点是每一次调用 <code>echo_ent</code> 都会导致调用 <code>pthread_once</code> 函数，而在大多数时候它没有做什么有用的事。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">int</span> byte_cnt; <span class="comment">/* Byte counter */</span></span><br><span class="line"><span class="type">static</span> <span class="type">sem_t</span> mutex;  <span class="comment">/* and the mutex that protects it */</span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">init_echo_cnt</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    Sem_init(&amp;mutex, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">    byte_cnt = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">echo_cnt</span><span class="params">(<span class="type">int</span> connfd)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    <span class="type">char</span> buf[MAXLINE];</span><br><span class="line">    <span class="type">rio_t</span> rio;</span><br><span class="line">    <span class="type">static</span> <span class="type">pthread_once_t</span> once = PTHREAD_ONCE_INIT;</span><br><span class="line"></span><br><span class="line">    Pthread_once(&amp;once, init_echo_cnt);</span><br><span class="line">    Rio_readinitb(&amp;rio, connfd);</span><br><span class="line">    <span class="keyword">while</span> ((n = Rio_readlineb(&amp;rio, buf, MAXLINE)) != <span class="number">0</span>) &#123;</span><br><span class="line">        P(&amp;mutex);</span><br><span class="line">        byte_cnt += n;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;server received %d (%d total) bytes on fd %d\n&quot;</span>,</span><br><span class="line">               n, byte_cnt, connfd);</span><br><span class="line">        V(&amp;mutex);</span><br><span class="line">        Rio_writen(connfd, buf, n);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 一旦程序包被初始化，echo_cnt 函数会初始化 RIO 带缓冲区的 I&#x2F;O 包（第 20 行），然后回送从客户端接收到的每一个文本行。注意，在第 23 ~ 25 行中对共享变量 byte_cnt 的访问是被 P 和 V 操作保护的。</p><h2 id="6-使用线程提高并行性"><a href="#6-使用线程提高并行性" class="headerlink" title="6.使用线程提高并行性"></a>6.使用线程提高并行性</h2><p>​ 到目前为止，在对并发的研究中，我们都假设并发线程是在单处理器系统上执行的。然而，大多数现代机器具有多核处理器。并发程序通常在这样的机器上运行得更快，因为操作系统内核在多个核上并行地调度这些并发线程，而不是在单个核上顺序地调度。在像繁忙的 Web 服务器、数据库服务器和大型科学计算代码这样的应用中利用这样的并行性是至关重要的，而且在像 Web 浏览器、电子表格处理程序和文档处理程序这样的主流应用中，并行性也变得越来越有用。</p><p>​ 上图给出了顺序、并发和并行程序之间的集合关系。所有程序的集合能够被划分成不相交的顺序程序集合和并发程序的集合。写顺序程序只有一条逻辑流。写并发程序有多条并发流。并行程序是一个运行在多个处理器上的并发程序。因此，并行程序的集合是并发程序集合的真子集。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/b0d8e7e8a7cac1ddc0a7fc0c4ff4fab0.png" class><p>​ 将任务分配到不同线程的最直接方法是将序列划分成 t 个不相交的区域，然后给，个不同的线程每个分配一个区域。为了简单，假设 n 是 t 的倍数，这样每个区域有以 n&#x2F;t 个元素。让我们来看看多个线程并行处理分配给它们的区域的不同方法。</p><p>​ 最简单也最直接的选择是将线程的和放入一个共享全局变量中，用互斥锁保护这个变量。下面给出了我们会如何实现这种方法。在第 28 ~ 33 行，主线程创建对等线程，然后等待它们结束。注意，主线程传递给每个对等线程一个小整数，作为唯一的线程 ID。每个对等线程会用它的线程 ID 来决定它应该计算序列的哪一部分。这个向对等线程传递一个小的唯一的线程 ID 的思想是一项通用技术，许多并行应用中都用到了它。在对等线程终止后，全局变量 gsum 包含着最终的和。然后主线程用闭合形式解答来验证结果（第 36 ~ 37 行）</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> MAXTHREADS 32</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">sum_mutex</span><span class="params">(<span class="type">void</span> *vargp)</span>; <span class="comment">/* Thread routine */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Global shared variables */</span></span><br><span class="line"><span class="type">long</span> gsum = <span class="number">0</span>;          <span class="comment">/* Global sum */</span></span><br><span class="line"><span class="type">long</span> nelems_per_thread; <span class="comment">/* Number of elements to sum */</span></span><br><span class="line"><span class="type">sem_t</span> mutex;            <span class="comment">/* Mutex to protect global sum */</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">long</span> i, nelems, log_nelems, nthreads, myid[MAXTHREADS];</span><br><span class="line">    <span class="type">pthread_t</span> tid[MAXTHREADS];</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Get input arguments */</span></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;nthreads&gt; &lt;log_nelems&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    nthreads = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">    log_nelems = atoi(argv[<span class="number">2</span>]);</span><br><span class="line">    nelems = (<span class="number">1L</span> &lt;&lt; log_nelems);</span><br><span class="line">    nelems_per_thread = nelems / nthreads;</span><br><span class="line">    sem_init(&amp;mutex, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Create peer threads and wait for them to finish */</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nthreads; i++) &#123;</span><br><span class="line">        myid[i] = i;</span><br><span class="line">        Pthread_create(&amp;tid[i], <span class="literal">NULL</span>, sum_mutex, &amp;myid[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nthreads; i++)</span><br><span class="line">        Pthread_join(tid[i], <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Check final answer */</span></span><br><span class="line">    <span class="keyword">if</span> (gsum != (nelems * (nelems - <span class="number">1</span>)) / <span class="number">2</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Error: result=%ld\n&quot;</span>, gsum);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 给出了每个对等线程执行的函数。在第 4 行中，线程从线程参数中提取出线程 ID，然后用这个 ID 来决定它要计算的序列区域（第 5 ~ 6 行）。在第 9 ~ 13 行中，线程在它的那部分序列上迭代操作，每次迭代都更新共享全局变量 gsum。注意，我们很小心地用 P 和 V 互斥操作来保护每次更新。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Thread routine for psum-mutex.c */</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">sum_mutex</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">long</span> myid = *((<span class="type">long</span> *)vargp);          <span class="comment">/* Extract the thread ID */</span></span><br><span class="line">    <span class="type">long</span> start = myid * nelems_per_thread; <span class="comment">/* Start element index */</span></span><br><span class="line">    <span class="type">long</span> end = start + nelems_per_thread;  <span class="comment">/* End element index */</span></span><br><span class="line">    <span class="type">long</span> i;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = start; i &lt; end; i++) &#123;</span><br><span class="line">        P(&amp;mutex);</span><br><span class="line">        gsum += i;</span><br><span class="line">        V(&amp;mutex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 我们在一个四核系统上，对一个大小为 n&#x3D;231<em>n</em>&#x3D;231 的序列运行 psum-mutex，测量它的运行时间（以秒为单位），作为线程数的函数，得到的结果难懂又令人奇怪：</p><table><thead><tr><th>线程数</th><th></th><th></th><th></th><th></th><th></th></tr></thead><tbody><tr><td>版本</td><td>1</td><td>2</td><td>4</td><td>8</td><td>16</td></tr><tr><td>psum-mutex</td><td>68</td><td>432</td><td>719</td><td>552</td><td>599</td></tr></tbody></table><p>​ 程序单线程顺序运行时非常慢，几乎比多线程并行运行时慢了一个数量级。不仅如此，使用的核数越多，性能越差。造成性能差的原因是相对于内存更新操作的开销，同步操作（P 和 V）代价太大。这突显了并行编程的一项重要教训：<strong>同步开销巨大，要尽可能避免。如果无可避免，必须要用尽可能多的有用计算弥补这个开销。</strong></p><p>​ 在我们的例子中，一种避免同步的方法是让每个对等线程在一个私有变量中计算它自己的部分和，这个私有变量不与其他任何线程共享，如图所示。主线程（图中未显示）定义一个全局数组 psum，每个对等线程 i 把它的部分和累积在 <strong>psum[i]</strong> 中。因为小心地给了每个对等线程一个不同的内存位置来更新，所以不需要用互斥锁来保护这些更新。唯一需要同步的地方是主线程必须等待所有的子线程完成。在对等线程结束后，主线程把 psum 向量的元素加起来，得到最终的结果。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Thread routine for psum-array.c */</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">sum_array</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">long</span> myid = *((<span class="type">long</span> *)vargp);          <span class="comment">/* Extract the thread ID */</span></span><br><span class="line">    <span class="type">long</span> start = myid * nelems_per_thread; <span class="comment">/* Start element index */</span></span><br><span class="line">    <span class="type">long</span> end = start + nelems_per_thread;  <span class="comment">/* End element index */</span></span><br><span class="line">    <span class="type">long</span> i;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = start; i &lt; end; i++) &#123;</span><br><span class="line">        psum[myid] += i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 在四核系统上运行 psum-array 时，我们看到它比 psum-mutex 运行得快好几个数量级：</p><table><thead><tr><th></th><th>线程数</th><th></th><th></th><th></th><th></th></tr></thead><tbody><tr><td>版本</td><td>1</td><td>2</td><td>4</td><td>8</td><td>16</td></tr><tr><td>psum-mutex</td><td>68.00</td><td>432.00</td><td>719.00</td><td>552.00</td><td>599.00</td></tr><tr><td>psum-array</td><td>7.26</td><td>3.64</td><td>1.91</td><td>1.85</td><td>1.84</td></tr></tbody></table><p>​ 在第 5 章中，我们学习到了如何使用局部变量来消除不必要的内存引用。下图展示了如何应用这项原则，让每个对等线程把它的部分和累积在一个局部变量而不是全局变量中。当在四核机器上运行 psum-local 时，得到一组新的递减的运行时间：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Thread routine for psum-local.c */</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">sum_local</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">long</span> myid = *((<span class="type">long</span> *)vargp);          <span class="comment">/* Extract the thread ID */</span></span><br><span class="line">    <span class="type">long</span> start = myid * nelems_per_thread; <span class="comment">/* Start element index */</span></span><br><span class="line">    <span class="type">long</span> end = start + nelems_per_thread;  <span class="comment">/* End element index */</span></span><br><span class="line">    <span class="type">long</span> i, sum = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = start; i &lt; end; i++) &#123;</span><br><span class="line">        sum += i;</span><br><span class="line">    &#125;</span><br><span class="line">    psum[myid] = sum;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><table><thead><tr><th>线程数</th><th></th><th></th><th></th><th></th><th></th></tr></thead><tbody><tr><td>版本</td><td></td><td>4</td><td>8</td><td>16</td><td></td></tr><tr><td>psum-mutex</td><td>68.00</td><td>432.00</td><td>719.00</td><td>552.00</td><td>599.00</td></tr><tr><td>psum-array</td><td>7.26</td><td>3.64</td><td>1.91</td><td>1.85</td><td>1.84</td></tr><tr><td>psum-local</td><td>1.06</td><td>0.54</td><td>0.28</td><td>0.29</td><td>0.30</td></tr></tbody></table><p>​ 我们可以学习到一个重要的经验，那就是写并行程序相当棘手。对代码看上去很小的改动可能会对性能有极大的影响。</p><h2 id="7-其他并发问题"><a href="#7-其他并发问题" class="headerlink" title="7.其他并发问题"></a>7.其他并发问题</h2><p>​ 你可能已经注意到了，一旦我们要求同步对共享数据的访问，那么事情就变得复杂得多了。迄今为止，我们已经看到了用于互斥和生产者—消费者同步的技术，但这仅仅是冰山一角。同步从根本上说是很难的问题，它引出了在普通的顺序程序中不会出现的问题。这一小节是关于你在写并发程序时需要注意的一些问题的（非常不完整的）综述。为了让事情具体化，我们将以线程为例描述讨论。不过要记住，这些典型问题是任何类型的并发流操作共享资源时都会出现的。</p><h4 id="7-1-线程安全"><a href="#7-1-线程安全" class="headerlink" title="7.1 线程安全"></a>7.1 线程安全</h4><p>​ 当用线程编写程序时，必须小心地编写那些具有称为<strong>线程安全性</strong>（thread safety）属性的函数。一个函数被称为<strong>线程安全的</strong>（thread-safe），当且仅当被多个并发线程反复地调用时，它会一直产生正确的结果。如果一个函数不是线程安全的，我们就说它是<strong>线程不安全的</strong>（thread-unsafe）。</p><p>我们能够定义出四个（不相交的）线程不安全函数类：</p><p>​ **第 1 类：不保护共享变量的函数。**我们在图 12-16 的 thread 函数中就已经遇到了这样的问题，该函数对一个未受保护的全局计数器变量加 1。将这类线程不安全函数变成线程安全的，相对而言比较容易：利用像 P 和 V 操作这样的同步操作来保护共享的变量。这个方法的优点是在调用程序中不需要做任何修改。缺点是同步操作将减慢程序的执行时间。</p><p>​ <strong>第 2 类：保持跨越多个调用的状态的函数。<strong>一个伪随机数生成器是这类线程不安全函数的简单例子。请参考下面的伪随机数生成器程序包。rand 函数是线程不安全的，因为</strong>当前调用的结果依赖于前次调用的中间结果</strong>。当调用 srand 为 rand 设置了一个种子后，我们从一个单线程中反复地调用 rand，能够预期得到一个可重复的随机数字序列。然而，如果多线程调用 rand 函数，这种假设就不再成立了。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">unsigned</span> next_seed = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* rand - return pseudorandom integer in the range 0..32767 */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="title function_">rand</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    next_seed = next_seed * <span class="number">1103515245</span> + <span class="number">12543</span>;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">unsigned</span>)(next_seed &gt;&gt; <span class="number">16</span>) % <span class="number">32768</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* srand - set the initial seed for rand() */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">srand</span><span class="params">(<span class="type">unsigned</span> new_seed)</span></span><br><span class="line">&#123;</span><br><span class="line">    next_seed = new_seed;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 使得像 rand 这样的函数线程安全的唯一方式是重写它，使得它不再使用任何 static 数据，而是依靠调用者在参数中传递状态信息。这样做的缺点是，程序员现在还要被迫修改调用程序中的代码。在一个大的程序中，可能有成百上千个不同的调用位置，做这样的修改将是非常麻烦的，而且容易出错。</p><p>​ **第 3 类：返回指向静态变量的指针的函数。**某些函数，例如 ctime 和 gethost-byname，将计算结果放在一个 static 变量中，然后返回一个指向这个变量的指针。如果我们从并发线程中调用这些函数，那么将可能发生灾难，因为正在被一个线程使用的结果会被另一个线程悄悄地覆盖了。</p><p>​ 有两种方法来处理这类线程不安全函数。一种选择是重写函数，使得调用者传递存放结果的变量的地址。这就消除了所有共享数据，但是它要求程序员能够修改函数的源代码。</p><p>​ 如果线程不安全函数是难以修改或不可能修改的（例如，代码非常复杂或是没有源代码可用），那么另外一种选择就是使用加锁—复制（lock-and-copy）技术。基本思想是将线程不安全函数与互斥锁联系起来。在每一个调用位置，对互斥锁加锁，调用线程不安全函数，将函数返回的结果复制到一个私有的内存位置，然后对互斥锁解锁。为了尽可能地减少对调用者的修改，你应该定义一个线程安全的包装函数，它执行加锁—复制，然后通过调用这个包装函数来取代所有对线程不安全函数的调用。例如，图 12-38 给出了 ctime 的一个线程安全的版本，利用的就是加锁—复制技术。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *<span class="title function_">ctime_ts</span><span class="params">(<span class="type">const</span> <span class="type">time_t</span> *timep, <span class="type">char</span> *privatep)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">char</span> *sharedp;</span><br><span class="line"></span><br><span class="line">    P(&amp;mutex);</span><br><span class="line">    sharedp = ctime(timep);</span><br><span class="line">    <span class="built_in">strcpy</span>(privatep, sharedp); <span class="comment">/* Copy string from shared to private */</span></span><br><span class="line">    V(&amp;mutex);</span><br><span class="line">    <span class="keyword">return</span> privatep;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ **第 4 类：调用线程不安全函数的函数。**如果函数 f 调用线程不安全函数 g，那么 f 就是线程不安全的吗？不一定。如果 g 是第 2 类函数，即依赖于跨越多次调用的状态，那么 f 也是线程不安全的，而且除了重写 g 以外，没有什么办法。然而，如果 g 是第 1 类或者第 3 类函数，那么只要你用一个互斥锁保护调用位置和任何得到的共享数据，f 仍然可能是线程安全的。在图 12-38 中我们看到了一个这种情况很好的示例，其中我们使用加锁—复制编写了一个线程安全函数，它调用了一个线程不安全的函数。</p><h4 id="7-2-可重入性"><a href="#7-2-可重入性" class="headerlink" title="7.2 可重入性"></a>7.2 可重入性</h4><p>​ 有一类重要的线程安全函数，叫做<strong>可重入函数</strong>（reentrant function），其特点在于它们具有这样一种属性：当它们被多个线程调用时，不会引用任何共享数据。尽管<strong>线程安全</strong>和<strong>可重入</strong>有时会（不正确地）被用做同义词，但是它们之间还是有清晰的技术差别，值得留意。下图展示了可重入函数、线程安全函数和线程不安全函数之间的集合关系。所有函数的集合被划分成不相交的线程安全和线程不安全函数集合。可重入函数集合是线程安全函数的一个真子集。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/875b0f4bbcc865987351ba3cb39916cb.png" class><p>​ 可重入函数通常要比不可重入的线程安全的函数高效一些，因为它们不需要同步操作。更进一步来说，将第 2 类线程不安全函数转化为线程安全函数的唯一方法就是重写它，使之变为可重入的。</p><p>​ 检査某个函数的代码并先验地断定它是可重入的，这可能吗？不幸的是，不一定能这样。如果所有的函数参数都是传值传递的（即没有指针），并且所有的数据引用都是本地的自动栈变量（即没有引用静态或全局变量），那么函数就是<strong>显式可重入的</strong>（explicitly reentrant），也就是说，无论它是被如何调用的，都可以断言它是可重入的。</p><p>​ 然而，如果把假设放宽松一点，允许显式可重入函数中一些参数是引用传递的（即允许它们传递指针），那么我们就得到了一个<strong>隐式可重入的</strong>（implicitly reentrant）函数，也就是说，如果调用线程小心地传递指向非共享数据的指针，那么它是可重入的。例如，图 12-40 中的 rand_r 函数就是隐式可重入的。</p><p>​ 我们总是使用术语<strong>可重入的</strong>（reentrant）既包括显式可重入函数也包括隐式可重入函数。然而，认识到可重入性有时既是调用者也是被调用者的属性，并不只是被调用者单独的属性是非常重要的。</p><h4 id="7-3-在线程化的程序中使用已存在的库函数"><a href="#7-3-在线程化的程序中使用已存在的库函数" class="headerlink" title="7.3 在线程化的程序中使用已存在的库函数"></a>7.3 在线程化的程序中使用已存在的库函数</h4><p>​ 大多数 Linux 函数，包括定义在标准 C 库中的函数（例如 malloc、free、realloc、printf 和 scanf）都是线程安全的，只有一小部分是例外。图 12-41 列出了常见的例外。（参考【110】可以得到一个完整的列表 Jstrtok 函数是一个已弃用的（不推荐使用）函数。asctime、ctime 和 localtime 函数是在不同时间和数据格式间相互来回转换时经常使用的函数。gethostbyname、gethostbyaddr 和 inet_ntoa 函数是已弃用的网络编程函数，已经分别被可重入的 getaddrinfo、getnameinfo 和 inet_ntop 函数取代（见第 11 章）。除了 rand 和 strtok 以外，所有这些线程不安全函数都是第 3 类的，它们返回一个指向静态变量的指针。如果我们需要在一个线程化的程序中调用这些函数中的某一个，对调用者来说最不惹麻烦的方法是加锁—复制。</p><table><thead><tr><th>线程不安全函数</th><th>线程不安全类</th><th>Linux 线程安全版本</th></tr></thead><tbody><tr><td>rand</td><td>2</td><td>rand_r</td></tr><tr><td>strtok</td><td>2</td><td>strtok_r</td></tr><tr><td>asctime</td><td>3</td><td>asctime_r</td></tr><tr><td>ctime</td><td>3</td><td>ctime_r</td></tr><tr><td>gethostbyaddr</td><td>3</td><td>gethostbyaddr_r</td></tr><tr><td>gethostbyname</td><td>3</td><td>gethostbyname_r</td></tr><tr><td>inet_ntoa</td><td>3</td><td>（无）</td></tr><tr><td>localtime</td><td>3</td><td>localtime_r</td></tr></tbody></table><p>然而，加锁—复制方法有许多缺点。</p><ul><li>首先，额外的同步降低了程序的速度。</li><li>第二，像 gethostbyname 这样的函数返回指向复杂结构的结构的指针，要复制整个结构层次，需要<strong>深层复制</strong>（deepcopy）结构。</li><li>第三，加锁—复制方法对像 rand 这样依赖跨越调用的静态状态的第 2 类函数并不有效。</li></ul><p>因此，Linux 系统提供大多数线程不安全函数的可重入版本。可重入版本的名字总是以 “_r” 后缀结尾。例如，asctime 的可重入版本就叫做 asctime_r。我们建议尽可能地使用这些函数。</p><h4 id="7-4-竞争"><a href="#7-4-竞争" class="headerlink" title="7.4 竞争"></a>7.4 竞争</h4><p>当一个程序的正确性依赖于一个线程要在另一个线程到达 y 点之前到达它的控制流中的 x 点时，就会发生<strong>竞争</strong>（race）。通常发生竞争是因为程序员假定线程将按照某种特殊的轨迹线穿过执行状态空间，而忘记了另一条准则规定：多线程的程序必须对任何可行的轨迹线都正确工作。</p><p>例子是理解竞争本质的最简单的方法。让我们来看看下面中的简单程序。主线程创建了四个对等线程，并传递一个指向一个唯一的整数 ID 的指针到每个线程。每个对等线程复制它的参数中传递的 ID 到一个局部变量中（第 22 行），然后输出一个包含这个 ID 的信息。它看上去足够简单，但是当我们在系统上运行这个程序时，我们得到以下不正确的结果：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* WARNING: This code is buggy! */</span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> N 4</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pthread_t</span> tid[N];</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">        Pthread_create(&amp;tid[i], <span class="literal">NULL</span>, thread, &amp;i);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">        Pthread_join(tid[i], <span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Thread routine */</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> myid = *((<span class="type">int</span> *)vargp);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello from thread %d\n&quot;</span>, myid);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 问题是由每个对等线程和主线程之间的竞争引起的。你能发现这个竞争吗？下面是发生的情况。当主线程在第 13 行创建了一个对等线程，它传递了一个指向本地栈变量 i 的指针。在此时，竞争出现在下一次在第 12 行对 i 加 1 和第 22 行参数的间接引用和赋值之间。如果对等线程在主线程执行第 12 行对 i 加 1 之前就执行了第 22 行，那么 myid 变量就得到正确的 ID。否则，它包含的就会是其他线程的 ID。令人惊慌的是，我们是否得到正确的答案依赖于内核是如何调度线程的执行的。在我们的系统中它失败了，但是在其他系统中，它可能就能正确工作，让程序员 “幸福地”察觉不到程序的严重错误.</p><p>​ 为了消除竞争，我们可以动态地为每个整数 ID 分配一个独立的块，并且传递给线程例程一个指向这个块的指针，如下面所示（第 12 ~ 14 行）。请注意线程例程必须释放这些块以避免内存泄漏。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> N 4</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pthread_t</span> tid[N];</span><br><span class="line">    <span class="type">int</span> i, *ptr;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">        ptr = Malloc(<span class="keyword">sizeof</span>(<span class="type">int</span>));</span><br><span class="line">        *ptr = i;</span><br><span class="line">        Pthread_create(&amp;tid[i], <span class="literal">NULL</span>, thread, ptr);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">        Pthread_join(tid[i], <span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Thread routine */</span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">thread</span><span class="params">(<span class="type">void</span> *vargp)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> myid = *((<span class="type">int</span> *)vargp);</span><br><span class="line">    Free(vargp);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello from thread %d\n&quot;</span>, myid);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="7-5-死锁"><a href="#7-5-死锁" class="headerlink" title="7.5 死锁"></a>7.5 死锁</h4><p>​ 信号量引入了一种潜在的令人厌恶的运行时错误，叫做<strong>死锁</strong>（deadlock），它指的是一组线程被阻塞了，等待一个永远也不会为真的条件。进度图对于理解死锁是一个无价的工具。例如，下图展示了一对用两个信号量来实现互斥的线程的进程图。</p><img src="/2024/09/05/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-12-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/7a71d94f5f90827cc50df7e9a608a5fc.png" class><p>从这幅图中，我们能够得到一些关于死锁的重要知识：</p><ul><li>程序员使用 P 和 V 操作顺序不当，以至于两个信号量的禁止区域重叠。如果某个执行轨迹线碰巧到达了<strong>死锁状态 d</strong>，那么就不可能有进一步的进展了，因为重叠的禁止区域阻塞了每个合法方向上的进展。换句话说，程序死锁是因为每个线程都在等待其他线程执行一个根不可能发生的 V 操作。</li><li>重叠的禁止区域引起了一组称为<strong>死锁区域</strong>（deadlock region）的状态。如果一个轨迹线碰巧到达了一个死锁区域中的状态，那么死锁就是不可避免的了。轨迹线可以进入死锁区域，但是它们不可能离开。</li><li>死锁是一个相当困难的问题，因为它不总是可预测的。一些幸运的执行轨迹线将绕开死锁区域，而其他的将会陷入这个区域。图 12-44 展示了每种情况的一个示例。对于程序员来说，这其中隐含的着实令人惊慌。你可以运行一个程序 1000 次不出任何问题，但是下一次它就死锁了。或者程序在一台机器上可能运行得很好，但是在另外的机器上就会死锁。最糟糕的是，错误常常是不可重复的，因为不同的执行有不同的轨迹线。</li></ul><p>​ 程序死锁有很多原因，要避免死锁一般而言是很困难的。然而，当使用二元信号量来实现互斥时，如图 12-44 所示，你可以应用下面的简单而有效的规则来避免死锁：</p><p>​ <strong>互斥锁加锁顺序规则：给定所有互斥操作的一个全序，如果每个线程都是以一种顺序获得互斥锁并以相反的顺序释放，那么这个程序就是无死锁的。</strong></p><p>​ 例如，我们可以通过这样的方法来解决图 12-44 中的死锁问题：在每个线程中先对 s 加锁，然后再对 t 加锁。图 12-45 展示了得到的进度图。</p><h2 id="8-小结"><a href="#8-小结" class="headerlink" title="8.小结"></a>8.小结</h2><p>​ 一个并发程序是由在时间上重叠的一组逻辑流组成的。在这一章中，我们学习了三种不同的构建并发程序的机制：进程、I&#x2F;O 多路复用和线程。我们以一个并发网络服务器作为贯穿全章的应用程序。</p><p>​ 进程是由内核自动调度的，而且因为它们有各自独立的虚拟地址空间，所以要实现共享数据，必须要有显式的 IPC 机制。事件驱动程序创建它们自己的并发逻辑流，这些逻辑流被模型化为状态机，用 I&#x2F;O 多路复用来显式地调度这些流。因为程序运行在一个单一进程中，所以在流之间共享数据速度很快而且很容易。线程是这些方法的混合。同基于进程的流一样，线程也是由内核自动调度的。同基于 I&#x2F;O 多路复用的流一样，线程是运行在一个单一进程的上下文中的，因此可以快速而方便地共享数据。</p><p>​ 无论哪种并发机制，同步对共享数据的并发访问都是一个困难的问题。提出对信号量的 P 和 V 操作就是为了帮助解决这个问题。信号量操作可以用来提供对共享数据的互斥访问，也对诸如生产者—消费者程序中有限缓冲区和读者—写者系统中的共享对象这样的资源访问进行调度。一个并发预线程化的 echo 服务器提供了信号量使用场景的很好的例子。</p><p>​ 并发也引入了其他一些困难的问题。被线程调用的函数必须具有一种称为线程安全的属性。我们定义了四类线程不安全的函数，以及一些将它们变为线程安全的建议。可重入函数是线程安全函数的一个真子集，它不访问任何共享数据。可重入函数通常比不可重入函数更为有效，因为它们不需要任何同步原语。竞争和死锁是并发程序中出现的另一些困难的问题。当程序员错误地假设逻辑流该如何调度时，就会发生竞争。当一个流等待一个永远不会发生的事件时，就会产生死锁。</p>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_11_网络编程</title>
      <link href="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/"/>
      <url>/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<p>​</p><p>​ 所有的网络应用程序都基于相同的基本编程模型，具有相似的整体逻辑结构，并且依赖于相同的编程接口.</p><p>​ 网络应用依赖于很多在系统研究中已经学习过的概念，例如，进程、信号、字节顺序、存储器映射以及动态存储分配，都扮演着重要的角色。还有一些新概念要掌握。我们需要理解基本的客户端-服务器编程模型，以及如何编写使用因特网提供的服务的客户端-服务器程序。最后，我们将把所有这些概念结合起来，开发一个小的但功能齐全的 Web 服务器，能够为真实的 Web 浏览器提供静态和动态的文本和图形内容。</p><h2 id="1-客户端-服务器编程模型"><a href="#1-客户端-服务器编程模型" class="headerlink" title="1.客户端-服务器编程模型"></a>1.客户端-服务器编程模型</h2><p>​ 每个网络应用都是基于<strong>客户端一服务器模型的（Client-Server Model）</strong>。采用这个模型，一个应用是由一个服务器进程和一个或者多个客户端进程组成。服务器管理某种资源，并且通过操作这种资源来为它的客户端提供某种服务。</p><p>客户端-服务器模型中的基本操作是 <strong>事务(transaction)</strong> 如下图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/1a22b50fb86ce85f64eb93ce88f65383.png" class><p>一个客户端一服务器事务由四步组成:</p><ol><li>当一个客户端需要服务时，它向服务器发送一个请求，发起一个事务。例如，当 Web 浏览器需要一个文件时，它就发送一个请求给 Web 服务器。</li><li>服务器收到请求后，解释它，并以适当的方式操作它的资源。例如，当 Web 服务器收到浏览器发出的请求后，它就读一个磁盘文件。</li><li>服务器给客户端发送一个响应，并等待下一个请求。例如,Web 服务器将文件发送回客户端。</li><li>客户端收到响应并处理它。例如，当 Web 浏览器收到来自服务器的一页后，它就在屏幕上显示此页。</li></ol><p>​ 请注意，这里的客户端和服务器指的是进程而非机器或主机（Host）。单台主机能够同时运行多个不同的客户端和服务器，客户端和服务器事务也可以在相同或不同的主机上执行。</p><h2 id="2-网络"><a href="#2-网络" class="headerlink" title="2.网络"></a>2.网络</h2><p>​ 对于一个主机而言，网络只是又一种 IO 设备，作为数据源和数据接收方，如下图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/f25c5e9557a4d4b7f57d3ca856d49215.png" class><p>一个插到 IO 总线扩展槽的适配器提供了到网络的物理接口。从网络上接收到的数据从适配器经过 IO 和存储器总线拷贝到存储器，典型地是通过 DMA 传送，相似地，数据也能从存储器拷贝到网络。</p><p>​ 网络是按地理邻近度组织的分层系统，其最低层是覆盖一个建筑物或校园的局域网（Local Area Network，LAN）。迄今为止最流行的局域网技术是以太网（Ethernet）：</p><p>​ 一个以太网段(Ethernet segment)包括一些电缆(通常是双绞线)和一个叫做集线器的小盒<br>子，如下图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/d9bced79ce60952052bca595f818854b.png" class><p>​ 以太网段的每根电缆一端连接到主机的适配器，而另一端则连接到集线器的一个端口上。集线器不加分辨地将从一个端口上收到的每个位复制到其他所有的端口上。因此，每台主机都能看到每个位。</p><p>​ 每个以太网适配器都有一个全球唯一的 48 位地址，它存储在这个适配器的非易失性存储器上。一台主机可以发送一段位，称为<strong>帧(frame)</strong>，到这个网段内其他任何主机。每个帧包括一些固定数量的**头部(header)**位，用来标识此帧的源和目的地址以及此帧的长度，此后紧随的就是数据位的有效载荷。每个主机适配器都能看到这个帧，但是只有目的主机实际读取它。</p><p>​ 使用一些电缆和叫做网桥(bridge)的小盒子，多个以太网段可以连接成较大的局域网，称为桥接以太网(bridged Bthermet)，如下图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/920265f6598423be6c019c3105f28a7a.png" class><p>​ 桥接以太网能够跨越整个建筑物或者校区。在一个桥接以太网里，一些电缆连接网桥与网桥，而另外一些连接网桥和集线器。这些电缆的带宽可以是不同的。</p><p>​ 为了简化局域网的表示，我们将把集线器和网桥以及连接它们的电缆画成一根水平线，如图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/9cec68584848c4e71e36ba4944e0d79a.png" class><p>​ 在层次的更高级别中，多个不兼容的局域网可以通过叫做**路由器(router)**的特殊计算机连接起来，组成一个<br>internet(互联网络)。每台路由器对于它所连接到的每个网络都有一个适配器(端口)。路由器也能连接高速点到点电话连接，这是称为 WAN(Wide-Area Network，广域网)：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/f29d99b731b3c30893613fbe4ec1fe3c.png" class><p>​ 互联网可以由使用完全不同且不兼容技术的局域网和广域网组成，这是它的一个<strong>关键特性</strong>。因此，我们必须在每台主机和路由器上运行协议软件（Protocol Software）来消除不同网络之间的差异。该软件实现的协议将管理主机和路由器如何协作以传输数据，它必须提供两个基本功能：</p><ul><li>命名方案（Naming Scheme）：为主机地址定义统一的格式，并为每台主机分配至少一个唯一标识它的互联网地址；</li><li>交付机制（Delivery Mechanism）：定义一种统一的方式将数据位封装为若干个块，即数据包（Packet）。其大小和源&#x2F;目的主机地址位于包的头部（Header），而源主机发送的数据位则在有效负载（Payload）之中。</li></ul><p>​ 下图展示了一个主机和路由器如何使用互联网络协议在不兼容的局域网间传送数据的示<br>例。这个互联网络示例由两个局域网通过一台路由器连接而成。</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/90d61c374dfe036a3ced9bbac66ee455.png" class><p>​ 一个客户端运行在主机 A 上主机 A 与 LAN1 相连，它发送了一串数据字节到运行在主机 B 上的服务器端，主机 B 则连接在 LAN2 上。这个过程包括 8 个基本步骤:</p><ol><li>运行在主机 A 上的客户端进行了一个系统调用，从客户端的虚拟地址空间拷贝数据到内核缓冲区中。</li><li>主机 A 上的协议软件通过在数据前附加互联网络包头和 LAN1 帧头，创建了一个 LAN1 的帧。互联网络包头寻址到互联网络主机 B。LAN1 帧头寻址到路由器。然后它传送此帧到适配器。注意，LAN1 帧的有效载荷是一个互联网络包，而互联网络包的有效载荷是实际的用户数据。这种封装是基本的网络互联方法之一。</li><li>LAN1 适配器拷贝该帧到网络上。</li><li>帧到达路由器时，路由器的 LAN1 适配器从电缆上读取它，并把它传送到协议软件。</li><li>路由器从互联网络包头中提取出目的互联网络地址，并用它作为路由表的索引，确定向哪里转发这个包，在本例中是 LAN2。路由器剥落旧的 LAN1 的帧头，加上寻址到主机 B 的新的 LAN2 帧头，并把得到的帧传送到适配器。</li><li>路由器的 LAN2 适配器拷贝该帧到网络上。</li><li>当此帧到达主机 B 时，它的适配器从电缆上读到此帧，并将它传送到协议软件。</li><li>最后，主机 B 上的协议软件剥落包头和帧头。当服务器进行一个读取这些数据的系统调用时，协议软件最终将得到的数据拷贝到服务器的虚拟地址空间。</li></ol><p>​ 我们的示例抓住了互联网络思想的精髓，<strong>封装是关键</strong>。</p><h2 id="3-全球-IP-互联网"><a href="#3-全球-IP-互联网" class="headerlink" title="3.全球 IP 互联网"></a>3.全球 IP 互联网</h2><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/39ac6a93417f352865bf6dbb3440d325.png" class><p>​ 几乎所有的现代计算机系统都支持 <strong>TCP&#x2F;IP 协议（Transmission Control Protocol&#x2F;Internet Protocol）</strong>，因此每台互联网主机上均运行着实现了该协议的软件。客户端和服务器使用 Socket 接口函数和 Unix I&#x2F;O 函数混合的方式进行通信。前者通常为系统调用，它们会请求内核**（Trap into Kernel）**调用 TCP&#x2F;IP 中的各种内核态函数。</p><p>在程序员看来，互联网是具有以下属性的主机集合：</p><ul><li>所有主机均映射到一组 32 位的 IP 地址；</li><li>所有 IP 地址均映射到一组标识符，即域名（Domain Name）；</li><li>一台主机上的进程可以通过连接（Connection）与其他任何主机上的进程通信。</li></ul><h4 id="3-1-IP-地址"><a href="#3-1-IP-地址" class="headerlink" title="3.1 IP 地址"></a>3.1 IP 地址</h4><p>IP 地址是一个无符号的 32 位整数。由于历史原因，网络程序将其存储在如下结构体中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* IP address structure */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span> &#123;</span></span><br><span class="line">    <span class="type">uint32_t</span>  s_addr; <span class="comment">/* Address in network byte order (big-endian) */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>​ 互联网中主机存储字节的顺序可能不同，因此 TCP&#x2F;IP 必须为整型数据项（如数据包头部的 IP 地址）定义一个统一的网络字节顺序（大端）。即使主机的字节顺序是小端，<code>in_addr</code>结构体中的 IP 地址也会以网络字节顺序存储。Unix 提供了用于转换字节顺序的函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="comment">// host to network</span></span><br><span class="line"><span class="type">uint32_t</span> <span class="title function_">htonl</span><span class="params">(<span class="type">uint32_t</span> hostlong)</span>;</span><br><span class="line"><span class="type">uint16_t</span> <span class="title function_">htons</span><span class="params">(<span class="type">uint16_t</span> hostshort)</span>;</span><br><span class="line"><span class="comment">// Returns: value in network byte order</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// network to host</span></span><br><span class="line"><span class="type">uint32_t</span> <span class="title function_">ntohl</span><span class="params">(<span class="type">uint32_t</span> netlong)</span>;</span><br><span class="line"><span class="type">uint16_t</span> <span class="title function_">ntohs</span><span class="params">(<span class="type">unit16_t</span> netshort)</span>;</span><br><span class="line"><span class="comment">// Returns: value in host byte order</span></span><br></pre></td></tr></table></figure><p>​ 为了便于人类阅读，IP 地址通常以点分十进制（Dotted-Decimal）的形式表示,每个字节由它的十进制值表示，并且用句点和其他字节间分开。例如，128.2.194.242 就是地址 0x8002c2f2 的点分十进制表示。应用程序可以使用函数<code>inet_pton</code>和<code>inet_ntop</code>对上述两种方式进行转换：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">inet_pton</span><span class="params">(AF_INET, <span class="type">const</span> <span class="type">char</span> *src, <span class="type">void</span> *dst)</span>;</span><br><span class="line"><span class="comment">// Returns: 1 if OK, 0 if src is invalid dotted decimal, −1 on error</span></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">char</span> *<span class="title function_">inet_ntop</span><span class="params">(AF_INET, <span class="type">const</span> <span class="type">void</span> *src, <span class="type">char</span> *dst,</span></span><br><span class="line"><span class="params">                      <span class="type">socklen_t</span> size)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to a dotted-decimal string if OK, NULL on error</span></span><br></pre></td></tr></table></figure><h4 id="3-2-域名"><a href="#3-2-域名" class="headerlink" title="3.2 域名"></a>3.2 域名</h4><p>​ 像 IP 地址这样较大的整数很难让人记住，因此互联网定义了一组更加人性化的域名集合并将其与 IP 地址映射。域名是由句点分隔的单词（字母、数字和破折号）序列，如<code>kittyhawk.cmcl.cs.cmu.edu</code>。域名的层级结构如下图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/65c0a8468bf4a649d5187899831dd9bb.png" class><p>​ 因特网定义了域名集合和 IP 地址集合之间的映射。直到 1988 年，这个映射都是通过一个叫做 HOSTS.TXT 的文本文件来手工维护的。从那以后，这个映射是通过分布世界范围内的数据库(称为 DNS(Domain Name System，域名系统))来维护的。从概念上而言，DNS 数据库由上百万的如图 11-11 所示的主机条目结构(host entry structure)组成的，其中每条定义了一组域名一个官方名字和一组别名)和一组 IP 地址之间的映射。从数学意义上讲，你可以认为每条主机条目就是一个域名和 IP 地址的等价类。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* DNS host entry structure */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">hostent</span> &#123;</span></span><br><span class="line"><span class="type">char</span>*h name;<span class="comment">/*Official domain name of host */</span></span><br><span class="line"><span class="type">char</span>**h aliases;<span class="comment">/*Null-terminated array ofdomain names */</span></span><br><span class="line"><span class="type">int</span>h_addrtype ;<span class="comment">/*Host address type(AF_INET)*/</span></span><br><span class="line"><span class="type">int</span>h_length;<span class="comment">/*Length of an address,in bytes */</span></span><br><span class="line"><span class="type">char</span>**h_addr <span class="built_in">list</span>;<span class="comment">/*Null-terminated array of in_addr structs */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>​ 因特网应用程序通过调用 <code>gethostbyname</code>和<code>gethostbyaddr</code> 函数，从 DNS 数据库中检索任意的主机条目。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> hostent *<span class="title function_">gethostbyname</span><span class="params">(constchar*ame)</span>;</span><br><span class="line">返回:若成功则为非<span class="literal">NULL</span>指针，若出错则为<span class="literal">NULL</span>指针，同时设置herrno。</span><br><span class="line"><span class="keyword">struct</span> hostent *<span class="title function_">gethostbyaddr</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *addr,<span class="type">int</span> len，<span class="number">0</span>)</span>;</span><br><span class="line">返回:若成功则为非NUIL，若出错则为NUIL指针，同时设置herrno。</span><br></pre></td></tr></table></figure><h4 id="3-3-连接"><a href="#3-3-连接" class="headerlink" title="3.3 连接"></a>3.3 连接</h4><p>​ 客户端和服务器通过连接来收发字节流并进行通信。连接是点对点，全双工（数据可以同时在两个方向上传输）且可靠的——除非发生一些灾难性的故障。</p><p>Socket 是连接的端点，每个 Socket 都对应了一个 Socket 地址。该地址由 IP 地址和 16 位整型的端口（Port）组成，表示为：<code>address:port</code>。</p><p>客户端 Socket 地址中的端口通常是其发起连接请求时由内核自动分配的，被称为临时端口（Ephemeral Port）；而服务器 Socket 地址中的端口则通常与服务永久关联，被称为知名端口（Well-known Port）。</p><p>连接由两个端点的 Socket 地址（即 Socket Pair）唯一标识，可以用元组表示为：<code>(cliaddr:cliport, servaddr:servport)</code></p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/4f42616f6ee040d112f81fdcdc4ec9d1.png" class><h2 id="4-套接字接口"><a href="#4-套接字接口" class="headerlink" title="4.套接字接口"></a>4.套接字接口</h2><p>​ **套接字接口(socket interface)**是一组函数，它们和 Unix I&#x2F;O 函数结合起来，用以创建网络应用。</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/4db5ab2dba12b1db1e5d9daa79361b91.png" class><h4 id="4-1-Socket-地址结构体"><a href="#4-1-Socket-地址结构体" class="headerlink" title="4.1 Socket 地址结构体"></a>4.1 Socket 地址结构体</h4><p>​ 从 Linux 内核的角度来看，Socket 是连接的一个端点；而从 Linux 程序的角度来看，Socket 则是一个与描述符对应的打开文件。IPv4 Socket 地址存储在 <code>sockaddr_in</code>类型的 16 字节结构体中</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* IP socket address structure */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span>  &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="type">uint16_t</span>        sin_family;  <span class="comment">/* Protocol family (always AF_INET) */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">uint16_t</span>        sin_port;    <span class="comment">/* Port number in network byte order */</span></span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span>  <span class="title">sin_addr</span>;</span>    <span class="comment">/* IP address in network byte order */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">char</span>   sin_zero[<span class="number">8</span>]; <span class="comment">/* Pad to sizeof(struct sockaddr) */</span></span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>​ <code>sin_family</code>字段为<code>AF_INET</code>，<code>sin_port</code>字段为 16 位端口号，<code>sin_addr</code>字段中包含 32 位 IP 地址。IP 地址和端口号始终以网络字节顺序（大端）存储。</p><p>​ 在调用函数<code>connect</code>、<code>bind</code>和<code>accept</code>时，我们需要传入一个指向 Socket 地址结构体的指针。由于 Socket 有多种类型，不同协议的 Socket 地址结构体类型也有所不同。如 IPv6 Socket 地址存储在<code>sockaddr_in6</code>类型的结构体中，<code>sin_family</code>字段为<code>AF_INET6</code>；Unix Domain Socket 地址存储在<code>sockaddr_un</code>类型的结构体中，<code>sin_family</code>字段为<code>AF_UNIX</code>。然而在 Socket 接口设计者所处的时代，C 还并不支持使用<code>void *</code>指针。于是他们只好重新定义一个适用于所有协议的<code>sockaddr</code>结构体，然后要求应用程序将任何与协议有关的结构体指针转换为这种通用的结构体指针：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Generic socket address structure (for connect, bind, and accept) */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="type">uint16_t</span>  sa_family;    <span class="comment">/* Protocol family */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">char</span>      sa_data[<span class="number">14</span>];  <span class="comment">/* Address data  */</span></span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>​ 使用时，要将<code>sockaddr_in</code>强制转换成<code>sockaddr</code>:</p><h4 id="4-2-socket函数"><a href="#4-2-socket函数" class="headerlink" title="4.2 socket函数"></a>4.2 <code>socket</code>函数</h4><p>​ 客户端和服务器使用<code>socket</code>函数创建一个 Socket 文件描述符：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">socket</span><span class="params">(<span class="type">int</span> domain, <span class="type">int</span> type, <span class="type">int</span> protocol)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: nonnegative descriptor if OK, −1 on erro</span></span><br></pre></td></tr></table></figure><p>我们通常使用以下参数调用该函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">clientfd = Socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>​ 其中，<code>AF_INET</code>代表使用 32 位 IP 地址，<code>SOCK_STREAM</code>表示 Socket 将成为连接的端点。该函数返回的描述符<code>clientfd</code>只是部分打开，还不能进行读写。</p><h4 id="4-3-connect函数"><a href="#4-3-connect函数" class="headerlink" title="4.3 connect函数"></a>4.3 <code>connect</code>函数</h4><p>​ 客户端调用<code>connect</code>函数与服务器建立连接：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">connect</span><span class="params">(<span class="type">int</span> clientfd, <span class="type">const</span> <span class="keyword">struct</span> sockaddr *addr,</span></span><br><span class="line"><span class="params"></span></span><br><span class="line"><span class="params">            <span class="type">socklen_t</span> addrlen)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ 该函数尝试连接 Socket 地址为<code>addr</code>的服务器，参数<code>addrlen</code>是结构体<code>sockaddr_in</code>的大小。<code>connect</code>函数在连接建立或发生错误前会一直阻塞，若建立成功则 Socket 描述符<code>clientfd</code>便可进行读写.并且得到的连接是由套接字对: (x:y,serv_addr.sin_addr:serv addr.sin port)</p><p>刻画的，其中 x 表示客户端的正地址，而 y 表示临时端口，它唯一地确定了客户端主机上的客户端进程。</p><h4 id="4-4-bind函数"><a href="#4-4-bind函数" class="headerlink" title="4.4 bind函数"></a>4.4 <code>bind</code>函数</h4><p>​ <code>bind</code>函数请求内核将参数<code>addr</code>中的服务器 Socket 地址与 Socket 描述符<code>sockfd</code>相关联，参数<code>addrlen</code>是结构体<code>sockaddr_in</code>的大小：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">bind</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">const</span> <span class="keyword">struct</span> sockaddr *addr,</span></span><br><span class="line"><span class="params"></span></span><br><span class="line"><span class="params">         <span class="type">socklen_t</span> addrlen)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><h4 id="4-5-listen函数"><a href="#4-5-listen函数" class="headerlink" title="4.5 listen函数"></a>4.5 <code>listen</code>函数</h4><p>​ <strong>默认情况下</strong>，内核假定<code>socket</code>函数创建的描述符是用于客户端连接的。因此服务器需要调用<code>listen</code>函数告诉内核参数<code>sockfd</code>用于服务器而非客户端：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">listen</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">int</span> backlog)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ 参数<code>backlog</code>是内核开始拒绝请求前应当排队的未完成连接请求数，通常设为 1024。</p><h4 id="4-6-accept函数"><a href="#4-6-accept函数" class="headerlink" title="4.6 accept函数"></a>4.6 <code>accept</code>函数</h4><p>​ 服务器调用<code>accept</code>函数等待客户端的连接请求到达监听描述符<code>listenfd</code>，然后将客户端 Socket 地址写入到<code>addr</code>中，最后返回一个可使用 Unix I&#x2F;O 函数与客户端通信的连接描述符：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">accept</span><span class="params">(<span class="type">int</span> listenfd, <span class="keyword">struct</span> sockaddr *addr, <span class="type">int</span> *addrlen)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: nonnegative connected descriptor if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ <code>accept</code> 函数等待来自客户端的连接请求到达侦听描述符 1istenfd，然后在 addr 中填写客户端的套接字地址，并返回一个<strong>已连接描述符(connected descriptor)</strong>，这个描述符可被用来利用 Unix I&#x2F;0 函数与客户端通信。</p><p>​ 监听描述符作为客户端发起连接请求的端点，通常只会创建一次，在服务器的生命周期内存在；连接描述符是客户端与服务器之间已建立的连接的端点，在每次服务器接受连接请求时创建，并且仅在服务器为客户端提供服务时存在：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/050955d1aa38c9528acd46a58c1ecd57.png" class><p>​ 在连接建立之后，客户端和服务器可以分别通过读写<code>clientfd</code>和<code>connfd</code>来传输数据。</p><h4 id="4-7-主机和服务转换"><a href="#4-7-主机和服务转换" class="headerlink" title="4.7 主机和服务转换"></a>4.7 主机和服务转换</h4><p>​ 我们可以将<code>getaddrinfo</code>和<code>getnameinfo</code>函数与 Socket 接口函数结合，编写适用于任何版本 IP 协议的网络程序。</p><h6 id="getaddrinfo函数"><a href="#getaddrinfo函数" class="headerlink" title="getaddrinfo函数"></a><code>getaddrinfo</code>函数</h6><p>​ <code>getaddrinfo</code>函数将主机名（或主机地址）和服务名（或端口号）转换为 Socket 地址结构体：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="type">int</span>     ai_flags;           <span class="comment">/* Hints argument flags */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">int</span>     ai_family;          <span class="comment">/* First arg to socket function */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">int</span>     ai_socktype;        <span class="comment">/* Second arg to socket function */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">char</span>    ai_protocol;        <span class="comment">/* Third arg to socket function  */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">char</span>    *ai_canonname;      <span class="comment">/* Canonical hostname */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">size_t</span>  ai_addrlen;         <span class="comment">/* Size of ai_addr struct */</span></span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span>  <span class="title">sockaddr</span> *<span class="title">ai_addr</span>;</span>  <span class="comment">/* Ptr to socket address structure */</span></span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span>  <span class="title">addrinfo</span> *<span class="title">ai_next</span>;</span>  <span class="comment">/* Ptr to next item in linked list */</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">getaddrinfo</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *host, <span class="type">const</span> <span class="type">char</span> *service,</span></span><br><span class="line"><span class="params"></span></span><br><span class="line"><span class="params">                <span class="type">const</span> <span class="keyword">struct</span> addrinfo *hints,</span></span><br><span class="line"><span class="params"></span></span><br><span class="line"><span class="params">                <span class="keyword">struct</span> addrinfo **result)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: 0 if OK, nonzero error code on error</span></span><br></pre></td></tr></table></figure><p>​ 该函数会根据 hints 指定的规范分配并初始化一个 addrinfo 结构体链表，其中每个结构体的 ai_addr 字段都指向一个与 host 和 service 对应的 Socket 地址，result 指向链表头部:</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/20220816134619.png" class><p>​ 参数<code>host</code>可以是域名，也可以是数字地址（如点分十进制 IP 地址）；参数<code>service</code>可以是服务名称（如 <code>http</code>），也可以是十进制端口号。如果我们不需要 Socket 地址中的主机名，就可以将<code>host</code>设为<code>NULL</code>。对于服务名来说也是如此，不过两者不能同时为<code>NULL</code>。</p><p>​ 客户端在调用该函数后会遍历上述链表，依次使用每个 Socket 地址作为参数调用<code>socket</code>和<code>connect</code>直至成功并建立连接；服务器在调用该函数后会遍历上述链表，依次使用每个 Socket 地址作为参数调用<code>socket</code>和<code>bind</code>直至成功且描述符被绑定到一个有效的 Socket 地址。</p><p>​ 细心的读者可能会疑惑为什么<code>getaddrinfo</code>会为同一个<code>host</code>和<code>service</code>初始化多个<code>addrinfo</code>结构体，这是因为：主机可能是多宿主的（Multihomed），可以通过多种协议（如 IPv4 和 IPv6）访问；客户端可以通过不同的 Socket 类型（如<code>SOCK_STREAM</code>和<code>SOCK_DGRAM</code>）访问相同的服务。因此通常我们会根据需求设置<code>hints</code>参数，以使函数生成我们期望的 Socket 地址。</p><p>​ 当<code>hints</code>作为参数传递时，只有<code>ai_family</code>、<code>ai_socktype</code>、<code>ai_protocol</code>和<code>ai_flags</code>字段可以被设置，其他字段必须为 0 或<code>NULL</code>。在实际使用中，我们调用 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/https://pubs.opengroup.org/onlinepubs/7908799/xsh/memset.html"><code>memset</code></a> 函数将<code>hints</code>归零，然后设置以下字段：</p><ul><li><p><code>ai_family</code>为<code>AF_INET</code>时，该函数将生成 IPv4 Socket 地址；<code>ai_family</code>为<code>AF_INET6</code>时，该函数将生成 IPv6 Socket 地址；</p></li><li><p>对于面向连接的网络应用程序，<code>ai_socktype</code>应当设为<code>SOCK_STREAM</code>；</p></li><li><pre><code>ai_flags<figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">  是能够修改函数默认行为的位掩码，主要包括：</span><br><span class="line"></span><br><span class="line">  - `AI_ADDRCONFIG`：仅当本地主机使用 IPv4 时生成 IPv4 Socket 地址；</span><br><span class="line">  - `AI_CANONNAME`：默认情况下，`addrinfo`结构体内的`ai_canonname`字段为`NULL`。若设置该掩码，函数会将链表中第一个`addrinfo`结构体内的`ai_canonname`字段指向主机的规范（官方）名称（如上图所示）；</span><br><span class="line">  - `AI_NUMERICSERV`：强制参数`service`使用端口号；</span><br><span class="line">  - `AI_PASSIVE`：服务器可以使用该函数生成的 Socket 地址创建监听描述符。在这种情况下，参数`host`应当设为`NULL`，表示服务器的所有 IP 地址均可用于连接（即`INADDR_ANY`或 0.0.0.0）；</span><br><span class="line"></span><br><span class="line">当`getaddrinfo`初始化`addrinfo`结构体链表时，它会填充除`ai_flags`之外的所有字段。`ai_family`、`ai_socktype`和`ai_protocol`可以直接传递给`socket`函数，`ai_addr`和`ai_addrlen`可以直接传递给`connect`和`bind`函数。因此我们能够使用它编写适用于任何版本 IP 协议的客户端和服务器。</span><br><span class="line"></span><br><span class="line">为了避免内存泄漏，应用程序最终必须调用`freeaddrinfo`函数释放链表：</span><br><span class="line"></span><br><span class="line">```c</span><br><span class="line">void freeaddrinfo(struct addrinfo *result);</span><br><span class="line"></span><br><span class="line">// Returns: nothing</span><br><span class="line"></span><br></pre></td></tr></table></figure></code></pre></li></ul><p><code>getaddrinfo</code>函数会返回非零错误码，应用程序可以调用<code>gai_strerror</code>函数将其转换为消息字符串：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">const char *gai_strerror(int errcode);</span><br><span class="line"></span><br><span class="line">// Returns: error message</span><br></pre></td></tr></table></figure><h6 id="getnameinfo函数"><a href="#getnameinfo函数" class="headerlink" title="getnameinfo函数"></a><code>getnameinfo</code>函数</h6><p>​ <code>getnameinfo</code>函数是<code>getaddrinfo</code>的逆函数，它将 Socket 地址结构体转换为对应的主机名和服务名：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">getnameinfo</span><span class="params">(<span class="type">const</span> <span class="keyword">struct</span> sockaddr *sa, <span class="type">socklen_t</span> salen,</span></span><br><span class="line"><span class="params"></span></span><br><span class="line"><span class="params">                <span class="type">char</span> *host, <span class="type">size_t</span> hostlen,</span></span><br><span class="line"><span class="params"></span></span><br><span class="line"><span class="params">                <span class="type">char</span> *service, <span class="type">size_t</span> servlen, <span class="type">int</span> flags)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: 0 if OK, nonzero error code on error</span></span><br></pre></td></tr></table></figure><p>​ 参数<code>sa</code>指向一个大小为<code>salen</code>字节的 Socket 地址结构体，<code>host</code>指向一个大小为<code>hostlen</code>字节的缓冲区，而<code>service</code>则指向一个大小为<code>servlen</code>字节的缓冲区。该函数将<code>sa</code>转换为主机名和服务名字符串，然后将它们复制到<code>host</code>和<code>service</code>指向的缓冲区。如果该函数返回非零错误代码，应用程序可以调用<code>gai_strerror</code>将其转换为消息字符串。</p><p>如果我们不需要主机名，就可以将<code>host</code>设为<code>NULL</code>。对于服务名来说也是如此，不过两者不能同时为<code>NULL</code>。</p><p>参数<code>flags</code>是修改函数默认行为的位掩码，包括：</p><ul><li><code>NI_NUMERICHOST</code>：默认情况下，函数将在<code>host</code>指向的缓冲区中生成一个域名。若设置该掩码，函数会生成一个数字地址字符串；</li><li><code>NI_NUMERICSERV</code>：默认情况下，函数将在<code>/etc/services</code>文件中查找并生成服务名。若设置该掩码，函数会跳过查找并生成端口号。</li></ul><p>如下示例程序使用<code>getaddrinfo</code>和<code>getnameinfo</code>函数实现域名解析：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> *<span class="title">p</span>, *<span class="title">listp</span>, <span class="title">hints</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> buf[MAXLINE];</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> rc, flags;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;usage: %s &lt;domain name&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Get a list of addrinfo records */</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="keyword">struct</span> addrinfo));</span><br><span class="line"></span><br><span class="line">    hints.ai_family = AF_INET;       <span class="comment">/* IPv4 only */</span></span><br><span class="line"></span><br><span class="line">    hints.ai_socktype = SOCK_STREAM; <span class="comment">/* Connections only */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((rc = getaddrinfo(argv[<span class="number">1</span>], <span class="literal">NULL</span>, &amp;hints, &amp;listp)) != <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;getaddrinfo error: %s\n&quot;</span>, gai_strerror(rc));</span><br><span class="line"></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Walk the list and display each IP address */</span></span><br><span class="line"></span><br><span class="line">    flags = NI_NUMERICHOST; <span class="comment">/* Display address string instead of domain name */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (p = listp; p; p = p-&gt;ai_next)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        getnameinfo(p-&gt;ai_addr, p-&gt;ai_addrlen, buf, MAXLINE, <span class="literal">NULL</span>, <span class="number">0</span>, flags);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, buf);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Clean up */</span></span><br><span class="line"></span><br><span class="line">    freeaddrinfo(listp);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="4-8-Socket-接口的辅助函数"><a href="#4-8-Socket-接口的辅助函数" class="headerlink" title="4.8 Socket 接口的辅助函数"></a>4.8 Socket 接口的辅助函数</h4><p><code>getaddrinfo</code>和 Socket 接口函数并不易于使用，我们可以使用更高级的辅助函数<code>open_clientfd</code>和<code>open_listenfd</code>包装它们。</p><h6 id="open-clientfd函数"><a href="#open-clientfd函数" class="headerlink" title="open_clientfd函数"></a><code>open_clientfd</code>函数</h6><p>​ 我们发现将 socket 和 connect 函数包装成一个叫做<code>open_clientfd </code>的辅助函数是很<br>方便的，客户端调用<code>open_clientfd</code>函数与服务器建立连接:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">open_clientfd</span><span class="params">(<span class="type">char</span> *hostname, <span class="type">char</span> *port)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: descriptor if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ 参数<code>hostname</code>是服务器所在的主机名，参数<code>port</code>是服务器监听的端口号。函数返回一个打开的 Socket 描述符，客户端可以使用 Unix I&#x2F;O 函数对其读写。该函数的代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">open_clientfd</span><span class="params">(<span class="type">char</span> *hostname, <span class="type">char</span> *port)</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> clientfd;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">listp</span>, *<span class="title">p</span>;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Get a list of potential server addresses */</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="keyword">struct</span> addrinfo));</span><br><span class="line"></span><br><span class="line">    hints.ai_socktype = SOCK_STREAM; <span class="comment">/* Open a connection */</span></span><br><span class="line"></span><br><span class="line">    hints.ai_flags = AI_NUMERICSERV; <span class="comment">/* ... using a numeric port arg. */</span></span><br><span class="line"></span><br><span class="line">    hints.ai_flags |= AI_ADDRCONFIG; <span class="comment">/* Recommended for connections */</span></span><br><span class="line"></span><br><span class="line">    getaddrinfo(hostname, port, &amp;hints, &amp;listp);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Walk the list for one that we can successfully connect to */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (p = listp; p; p = p-&gt;ai_next)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Create a socket descriptor */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ((clientfd = socket(p-&gt;ai_family, p-&gt;ai_socktype, p-&gt;ai_protocol)) &lt; <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">continue</span>; <span class="comment">/* Socket failed, try the next */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Connect to the server */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (connect(clientfd, p-&gt;ai_addr, p-&gt;ai_addrlen) != <span class="number">-1</span>)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">break</span>;       <span class="comment">/* Success */</span></span><br><span class="line"></span><br><span class="line">        Close(clientfd); <span class="comment">/* Connect failed, try another */</span></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Clean up */</span></span><br><span class="line"></span><br><span class="line">    freeaddrinfo(listp);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!p) <span class="comment">/* All connects failed */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">else</span>    <span class="comment">/* The last connect succeeded */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> clientfd;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h6 id="open-listenfd函数"><a href="#open-listenfd函数" class="headerlink" title="open_listenfd函数"></a><code>open_listenfd</code>函数</h6><p>​ 我们发现将 <code>socket</code>、<code>bind</code>和<code>listen</code>函数结合成一个叫做<code>open_listenfd</code> 的辅助函数是很有帮助的，服务器调用<code>open_listenfd</code>函数创建一个能够接受连接请求的监听描述符：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">open_listenfd</span><span class="params">(<span class="type">char</span> *port)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Returns: descriptor if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>参数<code>port</code>是服务器监听的端口号。该函数的代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">open_listenfd</span><span class="params">(<span class="type">char</span> *port)</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>, *<span class="title">listp</span>, *<span class="title">p</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> listenfd, optval = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Get a list of potential server addresses */</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="keyword">struct</span> addrinfo));</span><br><span class="line"></span><br><span class="line">    hints.ai_socktype = SOCK_STREAM;             <span class="comment">/* Accept connections */</span></span><br><span class="line"></span><br><span class="line">    hints.ai_flags = AI_PASSIVE | AI_ADDRCONFIG; <span class="comment">/* ... on any IP address */</span></span><br><span class="line"></span><br><span class="line">    hints.ai_flags |= AI_NUMERICSERV;            <span class="comment">/* ... using port number */</span></span><br><span class="line"></span><br><span class="line">    getaddrinfo(<span class="literal">NULL</span>, port, &amp;hints, &amp;listp);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Walk the list for one that we can bind to */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (p = listp; p; p = p-&gt;ai_next)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Create a socket descriptor */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ((listenfd = socket(p-&gt;ai_family, p-&gt;ai_socktype, p-&gt;ai_protocol)) &lt; <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">continue</span>; <span class="comment">/* Socket failed, try the next */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Eliminates &quot;Address already in use&quot; error from bind */</span></span><br><span class="line"></span><br><span class="line">        Setsockopt(listenfd, SOL_SOCKET, SO_REUSEADDR,</span><br><span class="line"></span><br><span class="line">                   (<span class="type">const</span> <span class="type">void</span> *)&amp;optval, <span class="keyword">sizeof</span>(<span class="type">int</span>));</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* Bind the descriptor to the address */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (bind(listenfd, p-&gt;ai_addr, p-&gt;ai_addrlen) == <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">break</span>;       <span class="comment">/* Success */</span></span><br><span class="line"></span><br><span class="line">        Close(listenfd); <span class="comment">/* Bind failed, try the next */</span></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Clean up */</span></span><br><span class="line"></span><br><span class="line">    freeaddrinfo(listp);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!p) <span class="comment">/* No address worked */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Make it a listening socket ready to accept connection requests */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (listen(listenfd, LISTENQ) &lt; <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        Close(listenfd);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> listenfd;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>在第 20 行中我们使用<code>Setsockopt</code>函数（见 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/http://csapp.cs.cmu.edu/2e/ics2/code/src/csapp.c">csapp.c</a>）配置服务器，使其能够在重新启动后立即开始接受连接请求。默认情况下，重新启动的服务器会在大约 30 秒内拒绝来自客户端的连接，这将严重影响调试。</p><h4 id="4-9-示例-Echo-客户端和服务器"><a href="#4-9-示例-Echo-客户端和服务器" class="headerlink" title="4.9 示例 Echo 客户端和服务器"></a>4.9 示例 Echo 客户端和服务器</h4><p>​ Client.c</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp/csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> clientfd;</span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> *host, *port, buf[MAXLINE];</span><br><span class="line"></span><br><span class="line">    <span class="type">rio_t</span> rio;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;usage: %s &lt;host&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    host = argv[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">    port = argv[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">    clientfd = Open_clientfd(host, port);</span><br><span class="line"></span><br><span class="line">    Rio_readinitb(&amp;rio, clientfd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (Fgets(buf, MAXLINE, <span class="built_in">stdin</span>) != <span class="literal">NULL</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        Rio_writen(clientfd, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line"></span><br><span class="line">        Rio_readlineb(&amp;rio, buf, MAXLINE);</span><br><span class="line"></span><br><span class="line">        Fputs(buf, <span class="built_in">stdout</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Close(clientfd);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>在与服务器建立连接之后，客户端进入 While 循环。它不断从标准输入中读取文本行（<code>Fgets</code>），然后将文本行发送到服务器（<code>Rio_writen</code>）。接下来再读取服务器的返回（<code>Rio_readlineb</code>），最终将结果打印到标准输出（<code>Fputs</code>）。当用户键入 Ctrl+D 时，循环中止，客户端随后关闭描述符<code>clientfd</code>。</p><p>该客户端连接的服务器代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp/csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> <span class="title">SA</span>;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">echo</span><span class="params">(<span class="type">int</span> connfd)</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">size_t</span> n;</span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> buf[MAXLINE];</span><br><span class="line"></span><br><span class="line">    <span class="type">rio_t</span> rio;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    Rio_readinitb(&amp;rio, connfd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> ((n = Rio_readlineb(&amp;rio, buf, MAXLINE)) != <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;server received %d bytes\n&quot;</span>, (<span class="type">int</span>)n);</span><br><span class="line"></span><br><span class="line">        Rio_writen(connfd, buf, n);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> listenfd, connfd;</span><br><span class="line"></span><br><span class="line">    <span class="type">socklen_t</span> clientlen;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_storage</span> <span class="title">clientaddr</span>;</span> <span class="comment">/* Enough space for any address */</span></span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> client_hostname[MAXLINE], client_port[MAXLINE];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    listenfd = Open_listenfd(argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        clientlen = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> sockaddr_storage);</span><br><span class="line"></span><br><span class="line">        connfd = accept(listenfd, (SA *)&amp;clientaddr, &amp;clientlen);</span><br><span class="line"></span><br><span class="line">        getnameinfo((SA *)&amp;clientaddr, clientlen, client_hostname, MAXLINE,</span><br><span class="line"></span><br><span class="line">                    client_port, MAXLINE, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Connected to (%s, %s)\n&quot;</span>, client_hostname, client_port);</span><br><span class="line"></span><br><span class="line">        echo(connfd);</span><br><span class="line"></span><br><span class="line">        Close(connfd);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>代码第 23 行声明的变量<code>clientaddr</code>是一个<code>sockaddr_storage</code>类型的 Socket 地址结构体，<code>accept</code>函数会在返回前将客户端的 Socket 地址填入其中。我们使用<code>sockaddr_storage</code>而非<code>sockaddr_in</code>的原因在于前者足够大，可以保存任何类型的 Socket 地址，从而使代码与协议独立（详见：<a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/https://stackoverflow.com/questions/16010622/reasoning-behind-c-sockets-sockaddr-and-sockaddr-storage">Reasoning behind C sockets sockaddr and sockaddr_storage</a>）。</p><p>服务器打开监听描述符后进入无限循环。它等待来自客户端的连接请求，打印客户端的主机名和端口，然后调用<code>echo</code>函数。该函数重复读取并写入文本行，直到<code>Rio_readlineb</code>遇到 EOF（对于网络连接，当一端的进程关闭连接，另一端的进程尝试读取流中的最后一个字节时将检测到 EOF）。一旦客户端和服务器均关闭了各自的描述符，连接便会终止。</p><p>请注意，示例的简单服务器一次只能处理一个客户端的连接请求，我们称这种类型的服务器为迭代服务器（Iterative Server）。更加复杂的并发服务器（Concurrent Server）则可以同时处理多个客户端的连接请求。</p><h2 id="5-Web-服务器"><a href="#5-Web-服务器" class="headerlink" title="5.Web 服务器"></a>5.Web 服务器</h2><h4 id="5-1-Web-内容"><a href="#5-1-Web-内容" class="headerlink" title="5.1 Web 内容"></a>5.1 Web 内容</h4><p>对于 Web 客户端和服务器，内容（Content）是与某种 MIME（Multipurpose Internet Mail Extensions）类型关联的字节序列：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/e302ee9a3cdf2f5f9833f9bf3ded37f7.png" class><p>Web 服务器通过两种不同方式向客户端提供内容：</p><ul><li>获取磁盘文件（静态内容）并将其返回给客户端；</li><li>运行一个可执行文件并将其输出结果（动态内容）返回给客户端。</li></ul><p>Web 服务器返回的每条内容都与其管理的某个文件相关联，每个文件都有一个唯一的名称，即 URL（Universal Resource Locator）。</p><h4 id="5-2-HTTP-事务"><a href="#5-2-HTTP-事务" class="headerlink" title="5.2 HTTP 事务"></a>5.2 HTTP 事务</h4><p>​ 我们可以使用<code>telnet</code>命令与互联网上任意 Web 服务器建立事务：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/7daca5f8fd81eb504a28b0820b0f42c8.png" class><h6 id="1-HTTP-请求"><a href="#1-HTTP-请求" class="headerlink" title="1. HTTP 请求"></a>1. HTTP 请求</h6><p>​ 一个 HTTP 请求的组成是这样的:一个请求行(request line)(第 5 行)，后面跟随零个或更多个请求报头(requestheader)(第 6 行)，再跟随一个空的文本行来终止报头列表(第 7 行)。一个请求行的形式是:<br>​ <code>&lt;method&gt;&lt;uri&gt;&lt;version</code><br>HTTP 支持许多不同的方法，包括 GET、POST、OPTIONS、HEAD、PUT、DELETE 和 TRACE。我们将只讨论广为应用的 GET 方法。GET 方法指导服务器生成和返回 URI(UniformResource Identifer，统一资源标识符)标识的内容。URI 是相应的 URL 的后缀，包括文件名和可选的参数。</p><h6 id="2-HTTP-响应"><a href="#2-HTTP-响应" class="headerlink" title="2. HTTP 响应"></a>2. HTTP 响应</h6><p>​ HTTP 响应和 HTTP 请求是相似的。一个 HTTP 响应的组成是这样的:一个响应行(response line)(第 8 行)后面跟随着零个或更多的响应报头(response header)(第 9~ 13 行)，再跟随一个终止报头的空行(第 14 行)，再跟随一个响应主体(response body)(第 15<del>17 行)。一个响应行的格式是<br><code>&lt;status code&gt;&lt;status message&gt;&lt;version&gt;</code><br>​ 版本字段描述的是响应所遵循的 HTTP 版本。状态码(status code)是一个三位的正整数指明对请求的处理。状态消息(status message)给出与错误代码等价的英文描述。下图列出了一些常见的状态码，以及它们相应的消息。第 9</del> 13 行的响应报头提供了关于响应的附加信息。针对我们的目的，两个最重要的报头是 Content-ype(第 12 行)，它告诉客户端响应主体中内容的 MIME 类型:以及 Content-Length(第 13 行)，用来指示响应主体的字节大小。<br>​ 第 14 行的终止响应报头的空文本行，其后跟随着响应主体，响应主体中包含着被请求的内容。</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-11-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/870bd781b9a3d3595e94f8ebea1d6609.png" class>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_10_系统级I/O</title>
      <link href="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/"/>
      <url>/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/</url>
      
        <content type="html"><![CDATA[<p>在 Unix 系统中，是通过使用由内核提供的系统级 Unix I&#x2F;O 函数来实现这些较高级别的 I&#x2F;0 函数的。大多数时候，高级别 IO 函数工作良好，没有必要直接使用 Unix I&#x2F;O。那么为什么还要麻烦地学习 Unix I&#x2F;O 呢?</p><ul><li>了解 UnixIO 将帮助你理解其他的系统概念。I&#x2F;O 是系统操作不可或缺的一部分，因此我们经常遇到 IO 和其他系统概念之间的循环依赖。例如，I&#x2F;O 在进程的创建和执行中扮演着关键的角色。反过来，进程创建又在不同进程间的文件共享中扮演着关键角色。因此，要真正理解 I&#x2F;O，你必须理解进程，反之亦然。在对存储器层次结构、链接和加载、进程以及虚拟存储器的讨论中，我们已经接触了 I0 的某些方面。既然你对这些概念有了比较好的理解，我们就能闭合这个循环，更加深入地研究 I&#x2F;O。</li><li>有时你除了使用 Unix I&#x2F;O 以外别无选择。在某些重要的情况下，使用高级 I&#x2F;O 函数不太可能，或者不太合适。例如，标准 I&#x2F;O 库没有提供读取文件元数据的方式，如文件大小或文件创建时间。另外，I&#x2F;O 库还存在一些问题，使得用它来进行网络编程非常冒险。</li></ul><h2 id="1-Unix-I-O"><a href="#1-Unix-I-O" class="headerlink" title="1.Unix I&#x2F;O"></a>1.Unix I&#x2F;O</h2><p>​ 一个 Unix 文件就是一个 m 个字节的序列:</p><p>$$<br>B_0,B_1,…,B_k,…,B_{m-1}<br>$$</p><p>​ 所有的 IO 设备，如网络、磁盘和终端，都被模型化为<strong>文件</strong>，而所有的输入和输出都被当做对相应文件的读和写来执行。这种将设备优雅地映射为文件的方式，允许 Unix 内核引出一个简单、低级的应用接口，称为 Unix I&#x2F;O，这使得所有的输入和输出都能以一种统一且一致的方式来执行:</p><ul><li><p>打开文件。一个应用程序通过要求内核打开相应的文件，来宣告它想要访问一个 IO 设备。内核返回一个小的非负整数，叫做描述符，它在后续对此文件的所有操作中标识这个文件。内核记录有关这个打开文件的所有信息。应用程序只需记住这个描述符。</p><p>​ Unix 外壳创建的每个进程开始时都有三个打开的文件:</p><ul><li>标准输入(描述符为 0)</li><li>标准输出(描述符为 1)</li><li>标准错误(描述符为 2)</li></ul><p>头文件&lt;unistd.h&gt;定义了常量</p><ul><li>STDIN_FILENO</li><li>STDOUT_FILENO</li><li>STDERR_FILENO</li></ul><p>它们可用来代替显式的描述符值。</p></li><li><p>改变当前的文件位置。对于每个打开的文件，内核保持着一个文件位置 k，初始为 0。这个<br>文件位置是从文件开头起始的字节偏移量。应用程序能够通过执行 seek 操作，显式地设置<br>文件的当前位置为 k。</p></li><li><p>读写文件。一个读操作就是从文件拷贝 n&gt;0 个字节到存储器，从当前文件位置 k 开始<br>然后将 k 增加到 k+n。给定一个大小为 m 字节的文件，当 k&gt;m 时执行读操作会触发一<br>个称为 end-of-file(EOF)的条件，应用程序能检测到这个条件。在文件结尾处并没有明确的“EOF 符号”</p><p>​ 类似地，写操作就是从存储器拷贝 n&gt;0 个字节到一个文件，从当前文件位置 k 开始然后更新 k。</p></li><li><p>关闭文件。当应用完成了对文件的访问之后，它就通知内核关闭这个文件。作为响应，内核释放文件打开时创建的数据结构，并将这个描述符恢复到可用的描述符池中。无论一个进程因为何种原因终止时，内核都会关闭所有打开的文件并释放它们的存储器资源。</p></li></ul><h4 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h4><p>每个 Linux 文件都有一个表征其在系统中角色的类型：</p><ul><li>常规文件（Regular File）：对于应用程序来说，常规文件分为仅包含 ASCII 或 Unicode 字符的文本文件（Text File）和二进制文件（Binary File）；但对于内核而言，两者没有区别。Linux 文本文件由一系列文本行（Text Line）组成，其中每一行都以换行符<code>\n</code>结尾；</li><li>目录（Directory）：目录是由链接（Link）数组构成的文件。链接将一个文件名映射到一个文件，该文件可能是另一个目录（如下图所示）。每个目录中至少包含两个链接：<code>.</code>指向目录本身，而<code>..</code>指向上级目录；</li><li>Socket：用于通过网络与另一个进程通信的文件。</li></ul><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/20220807220609.png" class><h2 id="2-打开和关闭文件"><a href="#2-打开和关闭文件" class="headerlink" title="2.打开和关闭文件"></a>2.打开和关闭文件</h2><p>​ 进程是通过调用 open 函数来打开一个已存在的文件或者创建一个新文件的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">open</span><span class="params">(<span class="type">char</span> *filename, <span class="type">int</span> flags, <span class="type">mode_t</span> mode)</span>;</span><br><span class="line"><span class="comment">// Returns: new file descriptor if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>返回的文件描述符(fd)是进程当前未打开的最小描述符。参数<code>flags</code>指示进程如何访问文件：</p><ul><li><code>O_RDONLY</code>：只读</li><li><code>O_WRONLY</code>：只写</li><li><code>O_RDWR</code>：读写</li></ul><p>该参数还可以与一个或多个位掩码进行或（<code>OR</code>）运算，这些位掩码提供写入的附加说明：</p><ul><li><code>O_CREAT</code>：如果文件不存在，则创建一个空文件；</li><li><code>O_TRUNC</code>：如果文件已经存在，则清空文件内容；</li><li><code>O_APPEND</code>：在每次写入操作之前，将文件位置设置为文件末尾。</li></ul><p>若文件已存在，参数<code>mode</code>应设为 0；反之，则设为新文件的访问权限位，可选项如下图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/6d1a7134c26b06592ac25b12c73d1a97.png" class><p>​ 作为上下文的一部分，每个进程都有一个 umask，它是通过调用 umask 函数来设置的。当进程通过带某个 mode 参数的 open 函数调用来创建一个新文件时，文件的访问权限位被设置为 mode&amp; ~umask。例如，假设我们给定下面的 mode 和 umask 默认值:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">define</span> DEF_MODE   S_IRUSR|S_IWUSR|S_IRGRP|S_IWGRP|S_IROTH|S_IWOTH</span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> DEF_UMASK  S_IWGRP|S_IWOTH</span></span><br><span class="line"></span><br><span class="line">umask(DEF_UMASK);</span><br><span class="line">fd = Open(<span class="string">&quot;foo.txt&quot;</span>, O_CREAT|O_TRUNC|O_WRONLY, DEF_MODE);</span><br></pre></td></tr></table></figure><p>​ 最后，进程通过调用 <code>close</code>函数关闭一个打开的文件,若文件描述符已关闭将引发错误:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">close</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><h2 id="3-读和写文件"><a href="#3-读和写文件" class="headerlink" title="3.读和写文件"></a>3.读和写文件</h2><p>​ 应用程序分别调用<code>read</code>和<code>write</code>函数来执行输入和输出：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">read</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *buf, <span class="type">size_t</span> n)</span>;</span><br><span class="line"><span class="comment">// Returns: number of bytes read if OK, 0 on EOF, −1 on error</span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">write</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">void</span> *buf, <span class="type">size_t</span> n)</span>;</span><br><span class="line"><span class="comment">// Returns: number of bytes written if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ <code>read</code>函数从描述符为<code>fd</code>的当前文件位置复制最多<code>n</code>个字节到内存中<code>buf</code>指向的位置，<code>write</code>函数则从内存中<code>buf</code>指向的位置复制最多<code>n</code>个字节到参数<code>fd</code>的当前位置。示例程序使用上述两种函数将标准输入以字节为单位复制到标准输出：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">char</span> c;</span><br><span class="line">    <span class="keyword">while</span>(Read(STDIN_FILENO, &amp;c, <span class="number">1</span>) != <span class="number">0</span>)</span><br><span class="line">        Write(STDOUT_FILENO, &amp;c, <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ <strong>ssizet 和 sizet 有些什么区别?</strong><br>​ 你可能已经注意到了，read 函数有一个 sizet 的输入参数和一个 ssizet 的返回值。那么这两种类型之间有什么区别呢?size t 被定义为 unsigned int，而 ssize t(有符号的大小)被定义为 int。read 函数返回一个有符号的大小，而不是一个无符号的大小，这是因为出错时它必须返回一 1。有趣的是，返回一个-1 的可能性使得 read 的最大值减小了一半,从 4GB 减小到了 2GB。</p><p>在某些情况下，读写操作传输的字节数小于应用程序请求的字节数。不足数（Short Count）的产生并不代表发生了错误，它可能由多种原因导致：</p><ul><li>读取时遇到 EOF：若我们对一个 20 字节的文件执行<code>read(fd, *buf, 50)</code>，那么第一次调用将返回一个 20 的不足数，第二次调用则返回 0（EOF）；</li><li>从终端读取文本行：若打开的文件是终端设备（即键盘和显示器），那么每次<code>read</code>调用都将传输一个文本行并返回一个与文本行大小相等的不足数；</li><li>读写 Socket：若打开的文件是 Socket，那么内部缓冲区限制和网络延迟将使读写操作返回不足数。</li></ul><p>因此除遇到 EOF 外，读写磁盘文件不会导致不足数的产生。但如果我们想要构建健壮而可靠的网络应用程序，就必须重复调用<code>read</code>和<code>write</code>以保证所有请求的字节均已被传输。</p><h2 id="4-用-RIO-包健壮地读写"><a href="#4-用-RIO-包健壮地读写" class="headerlink" title="4.用 RIO 包健壮地读写"></a>4.用 RIO 包健壮地读写</h2><p>​ **RIO(Robust I0，健壮的 I&#x2F;O)**会自动为你处理上文中所述的不足值。在像网络程序这样容易出现不足值的应用中，RIO 包提供了方便、健壮和高效的 IO。RIO 提供了两类不同的函数:</p><ul><li>无缓冲的输入输出函数。这些函数直接在存储器和文件之间传送数据，没有应用级缓冲<br>它们对将二进制数据读写到网络和从网络读写二进制数据尤其有用。</li><li>带缓冲的输入函数。从应用程序级别的缓冲区中读取文本行和二进制数据，与标准 I&#x2F;O（如<code>printf</code>）函数类似。该函数是**线程安全（Thread-safe）**的，并且可以对同一描述符任意交错（Interleave）。例如，我们可以从描述符中读取一些文本行，然后读取一些二进制数据，最后再读取一些文本行。</li></ul><h4 id="4-1-RIO-的无缓冲的输入输出函数"><a href="#4-1-RIO-的无缓冲的输入输出函数" class="headerlink" title="4.1 RIO 的无缓冲的输入输出函数"></a>4.1 RIO 的无缓冲的输入输出函数</h4><p>​ 通过调用<code>rio_readn</code>和 <code>rio_writen</code>函数，应用程序可以在存储器和文件之间直接传送数据。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_readn</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> n)</span>;</span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_writen</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> n)</span>;</span><br><span class="line"><span class="comment">// Returns: number of bytes transferred if OK, 0 on EOF (rio_readn only), −1 on error</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p><code>rio_readn</code>函数从参数<code>fd</code>的当前文件位置复制最多<code>n</code>个字节到内存中<code>usrbuf</code>指向的位置，<code>rio_writen</code>函数则从内存中<code>usrbuf</code>指向的位置复制最多<code>n</code>个字节到参数<code>fd</code>的当前位置。前者只有在遇到 EOF 时返回不足数，后者则从不返回不足数。</p><p>若上述函数被应用程序的信号处理程序的返回中断，它们会重新调用<code>read</code>和<code>write</code>函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_readn</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> n)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> nleft = n;</span><br><span class="line">    <span class="type">ssize_t</span> nread;</span><br><span class="line">    <span class="type">char</span> *bufp = usrbuf;</span><br><span class="line">    <span class="keyword">while</span> (nleft &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> ((nread = read(fd, bufp, nleft)) &lt; <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (errno == EINTR) <span class="comment">/* Interrupted by sig handler return */</span></span><br><span class="line">                nread = <span class="number">0</span>;      <span class="comment">/* and call read() again */</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">return</span> <span class="number">-1</span>;      <span class="comment">/* errno set by read() */</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nread == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">break</span>;              <span class="comment">/* EOF */</span></span><br><span class="line">        nleft -= nread;</span><br><span class="line">        bufp += nread;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (n - nleft);         <span class="comment">/* Return &gt;= 0 */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_writen</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> n)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> nleft = n;</span><br><span class="line">    <span class="type">ssize_t</span> nwritten;</span><br><span class="line">    <span class="type">char</span> *bufp = usrbuf;</span><br><span class="line">    <span class="keyword">while</span> (nleft &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> ((nwritten = read(fd, bufp, nleft)) &lt; <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (errno == EINTR) <span class="comment">/* Interrupted by sig handler return */</span></span><br><span class="line">                nwritten = <span class="number">0</span>;   <span class="comment">/* and call write() again */</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">return</span> <span class="number">-1</span>;      <span class="comment">/* errno set by write() */</span></span><br><span class="line">        &#125;</span><br><span class="line">        nleft -= nwritten;</span><br><span class="line">        bufp += nwritten;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> n;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-2-RIO-的带缓冲的输入函数"><a href="#4-2-RIO-的带缓冲的输入函数" class="headerlink" title="4.2 RIO 的带缓冲的输入函数"></a>4.2 RIO 的带缓冲的输入函数</h4><p>​ 假设我们需要编写一个计算文本文件行数的程序，最简单的方法便是调用<code>read</code>函数每次读取一个字节并检查是否有换行符。但由于<code>read</code>是系统调用，频繁的上下文切换将导致程序效率低下。</p><p>​ 更好的方法是调用包装函数<code>rio_readlineb</code>从内部读取缓冲区（Read Buffer）复制文本行，只有当缓冲区为空时才调用<code>read</code>以重新填充缓冲区。RIO<em>R<strong>I</strong>O</em> 包还为同时包含文本行和二进制数据的文件（如 HTTP 响应）提供了<code>rio_readn</code>函数的有缓冲版本，即<code>rio_readnb</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_readlineb</span><span class="params">(<span class="type">rio_t</span> *rp, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> maxlen)</span>;</span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_readnb</span><span class="params">(<span class="type">rio_t</span> *rp, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> n)</span>;</span><br><span class="line"><span class="comment">// Returns: number of bytes read if OK, 0 on EOF, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ 在调用上述两种有缓冲的输入函数前，我们需要为每个打开文件描述符调用一次<code>rio_readinitb</code>。该函数将描述符<code>fd</code>与地址<code>rp</code>处的读取缓冲区（类型为<code>rio_t</code>）相关联：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">define</span> RIO_BUFSIZE 8192</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> rio_fd;                <span class="comment">/* Descriptor for this internal buf */</span></span><br><span class="line">    <span class="type">int</span> rio_cnt;               <span class="comment">/* Unread bytes in internal buf */</span></span><br><span class="line">    <span class="type">char</span> *rio_bufptr;          <span class="comment">/* Next unread byte in internal buf */</span></span><br><span class="line">    <span class="type">char</span> rio_buf[RIO_BUFSIZE]; <span class="comment">/* Internal buffer */</span></span><br><span class="line">&#125; <span class="type">rio_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">rio_readinitb</span><span class="params">(<span class="type">rio_t</span> *rp, <span class="type">int</span> fd)</span></span><br><span class="line">&#123;</span><br><span class="line">    rp-&gt;rio_fd = fd;</span><br><span class="line">    rp-&gt;rio_cnt = <span class="number">0</span>;</span><br><span class="line">    rp-&gt;rio_bufptr = rp-&gt;rio_buf;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ RIO 包读取例程的核心是<code>rio_read</code>函数，它其实是<code>read</code>函数的有缓冲版本。若读取缓冲区中的未读字节数<code>rp-&gt;rio_cnt</code>为 0，则在循环内调用<code>read</code>函数对其填充；若读取缓冲区非空，则调用<code>memcpy</code>函数将<code>min(n, rp-&gt;rio_cnt)</code>字节从缓冲区复制到<code>usrbuf</code>指向的内存位置：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">ssize_t</span> <span class="title function_">rio_read</span><span class="params">(<span class="type">rio_t</span> *rp, <span class="type">char</span> *usrbuf, <span class="type">size_t</span> n)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> cnt;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (rp-&gt;rio_cnt &lt;= <span class="number">0</span>)</span><br><span class="line">    &#123; <span class="comment">/* Refill if buf is empty */</span></span><br><span class="line">        rp-&gt;rio_cnt = read(rp-&gt;rio_fd, rp-&gt;rio_buf,</span><br><span class="line">                           <span class="keyword">sizeof</span>(rp-&gt;rio_buf));</span><br><span class="line">        <span class="keyword">if</span> (rp-&gt;rio_cnt &lt; <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (errno != EINTR)           <span class="comment">/* Interrupted by sig handler return */</span></span><br><span class="line">                <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (rp-&gt;rio_cnt == <span class="number">0</span>)        <span class="comment">/* EOF */</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            rp-&gt;rio_bufptr = rp-&gt;rio_buf; <span class="comment">/* Reset buffer ptr */</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Copy min(n, rp-&gt;rio_cnt) bytes from internal buf to user buf */</span></span><br><span class="line">    cnt = n;</span><br><span class="line">    <span class="keyword">if</span> (rp-&gt;rio_cnt &lt; n)</span><br><span class="line">        cnt = rp-&gt;rio_cnt;</span><br><span class="line">    <span class="built_in">memcpy</span>(usrbuf, rp-&gt;rio_bufptr, cnt);</span><br><span class="line">    rp-&gt;rio_bufptr += cnt;</span><br><span class="line">    rp-&gt;rio_cnt -= cnt;</span><br><span class="line">    <span class="keyword">return</span> cnt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 对于一个应用程序，rio_read 函数和 Unix read 函数有同样的语义。在出错时，它返回值 -1，并且适当地设置 errno。在 EOF 时，它返回值 0。如果要求的字节数超过了读缓冲区内未读的字节的数量，它会返回一个不足值。两个函数的相似性使得很容易通过用 rio_read 代替 read 来创建不同类型的带缓冲的读函数。例如，用 rioread 代替 read。</p><p>​ 实际上，<code>rio_readnb</code>与<code>rio_readn</code>具有完全相同的结构，只不过我们用<code>rio_read</code>替换了<code>read</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_readnb</span><span class="params">(<span class="type">rio_t</span> *rp, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> n)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">size_t</span> nleft = n;</span><br><span class="line">    <span class="type">ssize_t</span> nread;</span><br><span class="line">    <span class="type">char</span> *bufp = usrbuf;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (nleft &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> ((nread = rio_read(rp, bufp, nleft)) &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="number">-1</span>;  <span class="comment">/* errno set by read() */</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nread == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">break</span>; <span class="comment">/* EOF */</span></span><br><span class="line">        nleft -= nread;</span><br><span class="line">        bufp += nread;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (n - nleft); <span class="comment">/* return &gt;= 0 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>类似地，<code>rio_readlineb</code>函数从文件<code>rp</code>中读取一个文本行并将其复制到内存中<code>usrbuf</code>指向的位置。循环内每次对<code>rio_read</code>的调用都会把读取缓冲区中的一个字节复制到<code>&amp;c</code>，然后检查它是否是换行符：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ssize_t</span> <span class="title function_">rio_readlineb</span><span class="params">(<span class="type">rio_t</span> *rp, <span class="type">void</span> *usrbuf, <span class="type">size_t</span> maxlen)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> n, rc;</span><br><span class="line">    <span class="type">char</span> c, *bufp = usrbuf;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (n = <span class="number">1</span>; n &lt; maxlen; n++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> ((rc = rio_read(rp, &amp;c, <span class="number">1</span>)) == <span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            *bufp++ = c;  <span class="comment">/* Copy rp to user buf */</span></span><br><span class="line">            <span class="keyword">if</span> (c == <span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                n++;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (rc == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (n == <span class="number">1</span>)</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">/* EOF, no data read */</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">break</span>;    <span class="comment">/* EOF, some data was read */</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">-1</span>;    <span class="comment">/* Error */</span></span><br><span class="line">    &#125;</span><br><span class="line">    *bufp = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> n - <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="5-读取文件元数据"><a href="#5-读取文件元数据" class="headerlink" title="5.读取文件元数据"></a>5.读取文件元数据</h2><p>​ 应用程序能够通过调用 <code>stat </code>和 <code>fstat</code> 函数，检索到关于文件的信息(有时也称为文件的元数据(metadata))。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">stat</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *filename, <span class="keyword">struct</span> stat *buf)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fstat</span><span class="params">(<span class="type">int</span> fd, <span class="keyword">struct</span> stat *buf)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ 函数<code>stat</code>使用文件名<code>*filename</code>作为输入，将信息填写到<code>stat</code>结构体中。<code>fstat</code>与之类似，但它的参数是文件描述符<code>fd</code>。结构体<code>stat</code>如下所示，我们只需关注字段<code>st_mode</code>和<code>st_size</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Metadata returned by the stat and fstat functions */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">stat</span>&#123;</span></span><br><span class="line"><span class="type">dev_t</span>st_dev;<span class="comment">/* Device */</span></span><br><span class="line"><span class="type">ino_t</span>  st_ino;<span class="comment">/* inode */</span></span><br><span class="line"><span class="type">mode_t</span> st_mode;<span class="comment">/* Protection and file type */</span></span><br><span class="line"><span class="type">nlink_t</span>st_nlink;<span class="comment">/* Number of hard links */</span></span><br><span class="line"><span class="type">uid_t</span>st_uid;<span class="comment">/*User ID of owner */</span></span><br><span class="line"><span class="type">gid_t</span>st_gid;<span class="comment">/*Group ID of owner */</span></span><br><span class="line"><span class="type">dev_t</span>st_rdev;<span class="comment">/*Device type(if inode device)*/</span></span><br><span class="line"><span class="type">off_t</span>st_size;<span class="comment">/* Total size,in bytes */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> st_blksize;<span class="comment">/*Blocksize for filesystem I/O */</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">long</span> st_blocks;<span class="comment">/*Number of blocks allocated */</span></span><br><span class="line"><span class="type">time_t</span>st_atime;<span class="comment">/* Time of last access */</span></span><br><span class="line"><span class="type">time_t</span>st_mtime;<span class="comment">/* Time of last modification */</span></span><br><span class="line"><span class="type">time_t</span>st_ctime;<span class="comment">/* Time of last change */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>​ <code>st_size</code>包含了文件的大小，而<code>st_mode</code>则包含了文件的访问权限和类型。。Unix 识别大量不同的文件类型。<strong>普通文件</strong>包括某种类型<strong>的二进制</strong>或<strong>文本数据</strong>。对于内核而言，文本文件和二进制文件毫无区别。目录文件包含关于其他文件的信息。套接字是一种用来通过网络与其他进程通信的文件。Unix 提供的宏指令根据 stmode 成员来确定文件的类型。下图列出了这些宏的一个子集。</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/477e0109e741db37bb3a9148ea7550a6.png" class><h2 id="6-读取目录内容"><a href="#6-读取目录内容" class="headerlink" title="6.读取目录内容"></a>6.读取目录内容</h2><p>​ 应用程序调用<code>opendir</code>和<code>readdir</code>函数读取目录中的内容：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dirent.h&gt;</span></span></span><br><span class="line">DIR *<span class="title function_">opendir</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to handle if OK, NULL on error</span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dirent.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> dirent *<span class="title function_">readdir</span><span class="params">(DIR *dirp)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to next directory entry if OK, NULL if no more entries or error</span></span><br></pre></td></tr></table></figure><p>​ 函数<code>opendir</code>以目录的路径名为参数，返回一个指向目录流（Directory Stream）的指针。流是对有序项目列表的抽象，此处指的是目录中条目的列表。函数<code>readdir</code>返回指向目录流中下一个条目的指针，每个条目都是一个<code>dirent</code>结构体：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">dirent</span> &#123;</span></span><br><span class="line">    <span class="type">ino_t</span> d_ino;       <span class="comment">/* inode number */</span></span><br><span class="line">    <span class="type">char</span>  d_name[<span class="number">256</span>]; <span class="comment">/* Filename */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>​ <code>d_name</code>是文件名，<code>d_ino</code>是文件的 inode 数。当发生错误时，<code>readdir</code>返回<code>NULL</code>并设置<code>errno</code>。</p><p>函数<code>closedir</code>关闭目录流并释放所有相关资源：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dirent.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">closedir</span><span class="params">(DIR *dirp)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 on success, −1 on error</span></span><br></pre></td></tr></table></figure><h2 id="7-共享文件"><a href="#7-共享文件" class="headerlink" title="7.共享文件"></a>7.共享文件</h2><p>​ 可以用许多不同的方式来共享 Unix 文件。除非你很清楚内核是如何表示打开的文件，否则文件共享的概念相当难懂。内核用三个相关的数据结构来表示打开的文件:</p><ul><li>描述符表(descriptor table)。每个进程都有它独立的描述符表，它的表项是由进程打开的文件描述符来索引的。每个打开的描述符表项指向文件表中的一个表项。</li><li>文件表(fle table)。打开文件的集合是由一张文件表来表示的，所有的进程共享这张表。每个文件表的表项组成(针对我们的目的)包括有当前的文件位置、引用计数(referencecount)(即当前指向该表项的描述符表项数)，以及一个指向 v-node 表中对应表项的指针关闭一个描述符会减少相应的文件表表项中的引用计数。内核不会删除这个文件表表项，直到它的引用计数为零。</li><li>v-node 表(v-node table)。同文件表一样，所有的进程共享这张 v-node 表。每个表项包含 stat 结构中的大多数信息，包括 stmode 和 stsize 成员。</li></ul><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/48a460a8f846d6a45edbb97323e43634.png" class><p>​ 上图展示了一个示例，其中描述符 1 和 4 通过不同的打开文件表表项来引用两个不同的文件。这是一种典型的情况，没有共享文件，并且每个描述符对应一个不同的文件。</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/76ae1bed0326e46ffde1e910d3c51f42.png" class><p>​ 上图所示，多个描述符也可以通过不同的文件表表项来引用同一个文件。例如，如果以同一个 filename 调用 open 函数两次，就会发生这种情况。关键思想是每个描述符都有它自己的文件位置，所以对不同描述符的读操作可以从文件的不同位置获取数据。</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/04613b08bc1a4dcf88a8b4538e1fb65b.png" class><p>​ 我们也能理解父子进程是如何共享文件的。假设在调用 fork 之前，父进程有如图 10-11 所示的打开文件。然后，上图展示了调用 fork 后的情况。子进程有一个父进程描述符表的副本。父子进程共享相同的打开文件表集合，因此共享相同的文件位置。一个很重要的结果就是在内核删除相应文件表表项之前，父子进程必须都关闭了它们的描述符。</p><h2 id="8-I-O-重定向"><a href="#8-I-O-重定向" class="headerlink" title="8.I&#x2F;O 重定向"></a>8.I&#x2F;O 重定向</h2><p>​ <code>dup2</code>函数将描述符表条目<code>oldfd</code>复制到<code>newfd</code>并覆盖其原始内容。如果<code>newfd</code>已经打开，则该函数在复制<code>oldfd</code>之前会先关闭<code>newfd</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">dup2</span><span class="params">(<span class="type">int</span> oldfd, <span class="type">int</span> newfd)</span>;</span><br><span class="line"><span class="comment">// Returns: nonnegative descriptor if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ 假设某进程的打开文件数据结构如上图 10.12 所示。描述符 1（标准输出）指向文件 A（如终端），描述符 4 指向文件 B（如磁盘上的文件），文件 A 和 B 的<code>refcnt</code>均为 1。那么该进程调用函数<code>dup2(4, 1)</code>后情况变为：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/b955d5545be0eb6663cfe6168677530b.png" class><p>​ 文件 A 被关闭，内核会删除其打开文件表和 v-node 表条目。两个描述符均指向文件 B，其<code>refcnt</code>已增加为 2。从现在开始，任何写入到标准输出的数据都会被重定向到文件 B。</p><h2 id="9-标准-I-O"><a href="#9-标准-I-O" class="headerlink" title="9.标准 I&#x2F;O"></a>9.标准 I&#x2F;O</h2><p>​ ANSI C 定义了一组高级输人输出函数，称为标准 I0 库，为程序员提供了 Unix I&#x2F;0 的较高级别的替代。这个库(libc)提供了打开和关闭文件的函数(fopen 和 fclose)、读和写字节的函数(fread 和 fwrite)、读和写字符串的函数(fgets 和 fputs)，以及复杂的格式化的 I&#x2F;O 所数(scanf 和 printf)。<br>​ 标准 IO 库将一个打开的文件模型化为一个流。对于程序员而言，一个流就是一个指向 FILE 类型的结构的指针。每个 ANSIC 程序开始时都有三个打开的流 stdin、stdout 和 stderr，分别对应于标准输人、标准输出和标准错误:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="keyword">extern</span> FILE *<span class="built_in">stdin</span>;  <span class="comment">/* Standard input (descriptor 0) */</span></span><br><span class="line"><span class="keyword">extern</span> FILE *<span class="built_in">stdout</span>; <span class="comment">/* Standard output (descriptor 1) */</span></span><br><span class="line"><span class="keyword">extern</span> FILE *<span class="built_in">stderr</span>; <span class="comment">/* Standard error (descriptor 2) */</span></span><br></pre></td></tr></table></figure><p>​ 类型为 FILE 的流是对文件描述符和流缓冲区的抽象。<strong>流缓冲区</strong>的目的和 RIO 读缓冲区的一样:就是使开销较高的 Unix I&#x2F;O 系统调用的数量尽可能的小。例如，假设我们有一个程序，它反复调用标准 IO 的 getc 函数，每次调用返回文件的下一个字符。当第一次调用 getc 时，库通过调用一次 read 函数来填充流缓冲区，然后将缓冲区中的第一个字节返回给应用程序。只要缓冲区中还有未读的字节，接下来对 getc 的调用就能直接从流缓冲区得到服务。</p><h2 id="9-综合-我该使用哪些-1-0-函数"><a href="#9-综合-我该使用哪些-1-0-函数" class="headerlink" title="9.综合:我该使用哪些 1&#x2F;0 函数"></a>9.综合:我该使用哪些 1&#x2F;0 函数</h2><p>​ Unix I&#x2F;O、标准 I&#x2F;O 和 <em>Rio</em> 包函数之间的关系如下图所示：</p><img src="/2024/09/02/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-10-%E7%B3%BB%E7%BB%9F%E7%BA%A7I-O/06e0473c31e4f344d476dc1facdfaa9d.png" class><p>​ 那么我们应当使用哪种 I&#x2F;O 函数呢？以下是一些建议：</p><ul><li>尽可能使用标准 I&#x2F;O 函数，它们是在磁盘和终端上执行 I&#x2F;O 操作的最佳选择；</li><li>不要使用<code>scanf</code>或<code>rio_readlineb</code>函数读取二进制文件，它们是专门为读取文本文件设计的；</li><li>将标准 I&#x2F;O 函数用于 <strong>Socket</strong> 时可能会出现一些令人讨厌的问题，因此我们应当在网络编程时使用 <strong><em>Rio</em> 包函数。</strong></li></ul><h2 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h2><p>一切都是文件，都可以用 “”打开 open –&gt; 读写 write&#x2F;read –&gt; 关闭 close” 模式来操作。输入&#x2F;输出(I&#x2F;O) 是指主存和外部设备(如磁盘，终端，网络)之间拷贝数据过程。</p>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_9_虚拟内存</title>
      <link href="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/"/>
      <url>/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/</url>
      
        <content type="html"><![CDATA[<p>​ 为了更加有效地管理内存并减少错误的发生，现代系统提供了一种对主存储器的抽象，即<strong>虚拟内存（Virtual Memory，VM）</strong>。虚拟内存是硬件异常、硬件地址转换、主存储器、磁盘文件和内核软件之间的优雅交互，它为每个进程提供了一个大的、统一的和私有的地址空间。</p><p>虚拟内存有以下三个重要功能：</p><ul><li>将主存储器作为磁盘的缓存，只保留主存中的活跃区域并根据需要不断地在两者之间传输数据；</li><li>为每个进程提供统一的地址空间，从而简化内存管理；</li><li>保护每个进程的地址空间不被其他进程所破坏。</li></ul><p>​ 虚拟存储器是计算机系统最重要的概念之一。它成功的一个主要原因就是因为它是沉默地自动地工作的，不需要应用程序员的任何干涉。既然虚拟存储器在幕后工作得如此之好，为什么程序员还需要理解它呢?有以下几个原因:</p><ul><li>虚拟存储器是中心的。虚拟存储器遍及计算机系统的所有层面，在硬件异常、汇编器、链接器、加载器、共享对象、文件和进程的设计中扮演着重要角色。理解虚拟存储器将帮助你更好地理解系统通常是如何工作的。</li><li>虚拟存储器是强大的。虚拟存储器给予应用程序强大的能力，可以创建和销毁存储器片(chunk)、将存储器片映射到磁盘文件的某个部分，以及与其他进程共享存储器。比如，你知道你可以通过读写存储器位置读或者修改一个磁盘文件的内容吗?或者是你可以加载一个文件的内容到存储器中，而不需要进行任何显式的拷贝吗?理解虚拟存储器将帮助你利用它的强大功能在你的应用程序中添加动力。</li><li>虚拟存储器是危险的。每次应用程序引用一个变量、间接引用一个指针，或者调用一个诸如 malloc 这样的动态分配程序时，它就会和虚拟存储器发生交互。如果虚拟存储器使用不当，应用将遇到复杂危险的与存储器有关的错误。例如，一个带有错误指针的程序可以立即崩溃于“段错误”或者“保护错误”，它可能在崩溃之前还默默地运行几个小时，或者是最令人惊慌地，运行完成却产生不正确的结果。理解虚拟存储器以及诸如 malloc 之<br>类的管理虚拟存储器的分配程序，可以帮助你避免这些错误。</li></ul><h2 id="1-物理和虚拟寻址"><a href="#1-物理和虚拟寻址" class="headerlink" title="1.物理和虚拟寻址"></a>1.物理和虚拟寻址</h2><p>​ 主存中的每个字节都有一个唯一的物理地址（Physical Address，PA），CPU 使用物理地址访问内存的方式被称为物理寻址（Physical Addressing）：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/26b4f68fc71ade4aba0f9fadb414b806.png" class><p>​ 如上图所示，当 CPU 执行这条加载指令时，它会生成一个有效物理地址，通过存储器总线，把它传递给主存。主存取出从物理地址 4 处开始的 4 字节的字，并将它返回给 CPU，CPU 会将它存放在一个寄存器里。</p><p>​ CPU 也可以通过<strong>虚拟地址（Virtual Address，VA）<strong>访问主存，只不过该地址在发送到主存之前需要被转换为适当的物理地址。这种寻址方式被称为</strong>虚拟寻址（Virtual Addressing）</strong>：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/a530c248a1e28be827d6154add76ea8b.png" class><p>​ 地址转换（Address Translation ）需要 CPU 硬件和操作系统之间密切合作，位于 CPU 芯片上的内存管理单元（Memory Management Unit，MMU）根据 <strong>页表（Page Table)</strong> 动态地将虚拟地址转换为物理地址。页表存储在主存中，其内容由操作系统维护。</p><h2 id="2-地址空间"><a href="#2-地址空间" class="headerlink" title="2.地址空间"></a>2.地址空间</h2><p>地址空间（Address Space）是一组有序的非负整数地址：</p><p>$$<br>{0,1,2,…}<br>$$</p><p>如果这些整数是连续的，我们就称其为线性地址空间（Linear Address Space）。在拥有虚拟内存的系统中，CPU 从 n 位线性地址空间中生成虚拟地址，该虚拟地址空间共有 N &#x3D; 2^n 个地址：</p><p>$$<br>{0,1,2,…,N−1}<br>$$</p><p>现代系统通常支持 32 位或 64 位虚拟地址空间。同样地，系统也有一个物理地址空间：</p><p>$$<br>{0,1,2,…,M−1}<br>$$</p><p>与虚拟地址空间不同，M 不一定为 2 的幂。但为了简化讨论，我们假设 M &#x3D; 2^m。</p><p>​ 地址空间明确地将数据对象（字节）和其属性（地址）区分开来，因此每个数据对象都可以有多个独立的地址，这便是虚拟内存的基本思想。主存中的每个字节都有一个从物理地址空间中选择的物理地址，以及一个从虚拟地址空间中选择的虚拟地址。</p><h2 id="3-虚拟内存作为缓存的工具"><a href="#3-虚拟内存作为缓存的工具" class="headerlink" title="3.虚拟内存作为缓存的工具"></a>3.虚拟内存作为缓存的工具</h2><p>​ 概念上而言，虚拟存储器(VM)被组织为<strong>一个由存放在磁盘上的 N 个连续的字节大小的单元组成的数组</strong>。每字节都有一个唯一的虚拟地址，这个唯一的虚拟地址是作为到数组的索引的。磁盘上数组的内容被缓存在主存中。和存储器层次结构中其他缓存一样，磁盘与主存之间以 Block 为单位传输数据。而在虚拟内存系统中，Block 被称为虚拟页面（Virtual Page，VP），其大小为 P &#x3D; 2^p。类似地，物理内存也可以被划分为多个大小为 P 的物理页面（Physical Page，PP)。</p><p>虚拟页面有以下三种状态：</p><ul><li>未分配（Unallocated）：没有被进程申请使用的页面，不占用任何磁盘空间；</li><li>未缓存（Uncached）：仅加载到磁盘而未缓存到主存中的页面；</li><li>已缓存（Cached）：已缓存在主存中的页面。</li></ul><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/78e3378142d7dc3ba0c0667c11db5534.png" class><p>​ 示例展示了一个有 8 个虚拟页的小虚拟存储器。虚拟页 0 和 3 还没有被分配，因此在磁盘上还不存在。虚拟页 1、4 和 6 被缓存在物理存储器中。页 2、5 和 7 已经被分配了，但是当前并未缓存在主存中。</p><h4 id="3-1-DRAM-缓存"><a href="#3-1-DRAM-缓存" class="headerlink" title="3.1 DRAM 缓存"></a>3.1 DRAM 缓存</h4><p>​ 我们将 CPU 和主存之间的 L1、L2 和 L3 级缓存称为 SRAM 缓存，而将主存中用来缓存虚拟页面的缓存称为 DRAM 缓存。</p><p>​ 与 SRAM 缓存相比，DRAM 缓存发生缓存缺失的成本很高（需要从磁盘中加载数据），因此虚拟页面往往比较大——通常为 4 KB 到 2 MB。DRAM 缓存是全关联型的，这样任何一个虚拟页面都可以放在任何一个物理页面中。</p><h4 id="3-2-页表"><a href="#3-2-页表" class="headerlink" title="3.2 页表"></a>3.2 页表</h4><p>​ 页表是由**页表条目（Page Table Entries，PTEs）**组成的数组。每个虚拟页面在页表都有一条 PTE，它在页表中的偏移量是固定的。每条 PTE 中都包含了一个表示该虚拟页面是否已缓存的有效位，以及一个 n 位的地址字段。若有效位为 1，则地址字段是缓存该页面的物理页面的起始地址；若有效位为 0 且地址字段为空，则代表该虚拟页面未分配；若有效位为 0 且地址字段非空，则地址字段是该页面在磁盘上的起始地址。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/08794d989540e2d86715fcb19c1fbbf3.png" class><p>如上图所示，系统中存在 8 个虚拟页面和 4 个物理页面：页面 1，2，4 和 7 缓存在 DRAM 中；页面 3 和 6 已分配但未缓存；页面 0 和 5 未分配。</p><h4 id="3-3-缺页故障"><a href="#3-3-缺页故障" class="headerlink" title="3.3 缺页故障"></a>3.3 缺页故障</h4><p>​ 我们将访问 DRAM 缓存时发生的缓存缺失称为缺页故障（Page Fault）。若 CPU 引用上图页面 3 中的某个字，地址转换硬件会从主存中读取 PTE 3 ，然后根据其有效位判断出该页面未缓存。缺页故障异常将调用内核中的异常处理程序，选择受害者页面（Victim Page）逐出主存。</p><p>在本例中，受害者页面为 VP 4，因此内核会先将 PTE 4 中的有效位重置为 0。如果该页面已发生更改，内核还需要将其复制回磁盘。接下来，内核把 VP 3 从磁盘复制到主存中的 PP 3，并更新 PTE 3 中的有效位。下图展示了异常处理程序返回后示例页表的状态：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/59a3848a159eff722efc06f6865881d8.png" class><h4 id="3-4-分配页面"><a href="#3-4-分配页面" class="headerlink" title="3.4 分配页面"></a>3.4 分配页面</h4><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/f661330612a1a14c0592c475fb443032.png" class><p>上图展示了分配一个新的虚拟页面（如进程调用<code>malloc</code>）后页表的变化。操作系统先在磁盘上开辟空间，然后更新 PTE 5 使 VP 5 指向磁盘上新创建的页面。</p><p>​ <strong>又是局部性救了我们</strong>：</p><p>​ 当我们中的许多人都了解了虚拟存储器的概念之后，我们的第一印象通常是它的效率应该是非常低的。因为不命中处罚很大，我们会担心页面调度会破坏程序性能。实际上，虚拟存储器工作得相当好，这主要归功于我们的老朋友局部性(locality)。尽管在整个运行过程中程序引用的不同页面的总数可能超出物理存储器总的大小，但是局部性原则保证了在任意时刻，程序将往往在一个较小的活动页面(active page)集合上工作，这个集合叫做工作集(working set)或者常驻集(residentset)。在初始开销，也就是将工作集页面调度到存储器中之后，接下来对这个工作集的引用将导致命中，而不会产生额外的磁盘流量。只要我们的程序有好的时间局部性，虚拟存储器系统就能工作得相当好。但是，当然不是所有的程序都能展现良好的时间局部性。如果工作集的大小超出了物理存储器的大小，那么程序将产生一种不幸的状态，叫做颠簸(thrashing)，这时页面将不断地换进换出。虽然虚拟存储器通常是有效的，但是如果一个程序性能慢得像爬一样，那么聪明的程序员会考虑是不是发生了颠簸。</p><h2 id="4-虚拟存储器作为存储器管理的工具"><a href="#4-虚拟存储器作为存储器管理的工具" class="headerlink" title="4.虚拟存储器作为存储器管理的工具"></a>4.虚拟存储器作为存储器管理的工具</h2><p>​ 操作系统为每个进程都维护了一个单独的页表，因此所有进程都拥有自己的虚拟地址空间：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/6d4bdd36d4a88baaf291aa121570fffb.png" class><p>​ 如上图所示，进程 i 的页表将 VP 1 映射到 PP 2，将 VP 2 映射到 PP 7；进程 j 的页表将 VP 1 映射到 PP 7，将 VP 2 映射到 PP 10。多个虚拟页面可以映射到同一个共享物理页面。VM 简化了链接、加载、代码和数据的共享以及应用程序的内存分配：</p><ul><li>简化链接：独立的虚拟地址空间允许每个进程使用相同的内存结构，因此链接器无需考虑可执行文件的代码和数据在物理内存中的实际位置。这种统一性极大地简化了链接器的设计和实现；</li><li>简化加载：若要将目标文件的 .text 和 .data 段加载到一个新进程的地址空间中，**加载器只需为它们分配虚拟页面，然后将其标记为未缓存，最后再将页表条目指向目标文件中对应的位置。**实际上加载器从未将任何数据从磁盘复制到主存中，代码和数据只有在被第一次引用时才会按需分页；</li></ul><p>将一组连续的虚拟页映射到任意一个文件中的任意位置的表示法称做<strong>存储器映射(memory mapping)</strong>。Unix 提供一个称为 mmap 的系统调用，允许应用程序自己做存储器映射。</p><ul><li>简化共享：操作系统可以将不同进程中的不同虚拟页面映射到相同的物理页面，从而实现进程之间代码和数据的共享；</li><li>简化内存分配：当应用程序申请额外的内存时，<strong>操作系统会为其分配一定数量的连续虚拟页面</strong>，然后将它们映射到任意位置的物理页面。这些物理页面无需连续，并且可以在物理内存中随机分布。</li></ul><h2 id="5-虚拟存储器作为存储器保护的工具"><a href="#5-虚拟存储器作为存储器保护的工具" class="headerlink" title="5.虚拟存储器作为存储器保护的工具"></a>5.虚拟存储器作为存储器保护的工具</h2><p>​</p><p>​ 任何现代计算机系统必须为操作系统提供手段来控制对存储器系统的访问。不应该允许一个用户进程修改它的只读文本段。而且也不应该允许它读或修改任何内核中的代码和数据结构。不应该允许它读或者写其他进程的私有存储器，并且不允许它修改任何与其他进程共享的虚拟页面，除非所有的共享者都显式地允许它这么做(通过调用明确的进程间通信系统调用)。</p><p>​ 我们可以在 PTE 中添加一些权限位来管理进程对页面的访问：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/2a27b556d0d4e6997e14bf78f398bf65.png" class><p>​ 如上图所示，SUP 表示是否只有在内核态运行的进程才能访问该页面，READ 和 WRITE 则分别表示页面是否可读写。例如，进程 i 在用户态中运行，那么它可以读取 VP 0，读取和写入 VP 1，但无法访问 VP 2。</p><p>​ 如果某条指令违反了上述权限，CPU 就会触发通用保护故障，并将控制权转移到内核中的异常处理程序。该处理程序会向问题进程发送一个 SIGSEGV 信号，Linux Shell 通常将此异常报告为<strong>分段故障（Segmentation Fault）</strong>。</p><h2 id="6-地址翻译"><a href="#6-地址翻译" class="headerlink" title="6.地址翻译"></a>6.地址翻译</h2><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/bedce0d3cdd7c195fbd7a4222e86012b.png" class><p>​ 形式上来说，地址翻译是一个 N 元素的虚拟地址空间(VAS)中的元素和一个 M 元素的物理地址空间(PAS)中元素之间的映射。</p><p><strong>MAP(A)</strong></p><ul><li><p>A’ 如果虚拟地址 A 处的数据在 PAS 的物理地址 A’处</p></li><li><p>NULL 如果虚拟地址 A 处的数据不在物理存储器中</p></li></ul><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/05efaef3d69509139b96bba236ad1e18.png" class><p>​ CPU 中的<strong>页表基址寄存器（Page Table Base Register ，PTBR</strong>）指向当前页表，n 位的虚拟地址由 p 位的虚拟页面**偏移量（Virtual Page Offset，VPO）<strong>和 n-p 位的</strong>虚拟页面编号（Virtual Page Number，VPN）<strong>组成。MMU 根据 VPN 的值选择对应的 PTE，如 VPN 0 选择 PTE 0，VPN 1 选择 PTE 1。由于物理页面和虚拟页面的大小相同，VPO 与物理页面偏移量（Physical Page Offset，PPO）也就相同，因此页表条目中的</strong>物理页面编号（Physical Page Number，PPN）**与 <strong>VPO</strong> 共同组成了转换后的物理地址。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/e8dfc363d9805b4ffed7f102c8132d3f.png" class><p>上图展示了页面命中时的 CPU 硬件操作步骤：</p><ol><li>处理器生成一个虚拟地址 VA 并发送到 MMU；</li><li>MMU 生成 PTE 地址 PTEA 并向高速缓存或主存发起请求；</li><li>高速缓存或主存将 PTE 返回给 MMU；</li><li>MMU 构造物理地址 PA 并将其发送到高速缓存或主存；</li><li>高速缓存或主存将请求的数据返回给处理器。</li></ol><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/278546386ea435a680d242580f4b3408.png" class><p>上图展示了缺页故障时的 CPU 硬件操作步骤：</p><ol><li>处理器生成一个虚拟地址 VA 并发送到 MMU；</li><li>MMU 生成 PTE 地址 PTEA 并向高速缓存或主存发起请求；</li><li>高速缓存或主存将 PTE 返回给 MMU；</li><li>PTE 中的有效位为 0，因此 MMU 触发异常并将控制权转移给内核中的异常处理程序；</li><li>处理程序从物理内存中选取受害者页面换出。若该页面已被修改，则还要将其复制到磁盘中；</li><li>处理程序将新页面换入并更新 PTE；</li><li>处理程序返回到原来的进程，之前引发缺页故障的指令重新执行。此时进程请求的页面已缓存，因此 CPU 随后的操作与页面命中时相同。</li></ol><h4 id="6-1-结合高速缓存和虚拟存储器"><a href="#6-1-结合高速缓存和虚拟存储器" class="headerlink" title="6.1 结合高速缓存和虚拟存储器"></a>6.1 结合高速缓存和虚拟存储器</h4><p>​ 大部分同时使用虚拟内存和高速缓存（SRAM 缓存）的系统均采用物理寻址的方式访问高速缓存。下图展示了两者的集成方式：</p><ul><li>注意： 页表条目可以缓存</li></ul><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/1dba5da0850b59d43fb8ff2b0cb67d2e.png" class><h4 id="6-2-利用-TLB-加速地址翻译"><a href="#6-2-利用-TLB-加速地址翻译" class="headerlink" title="6.2 利用 TLB 加速地址翻译"></a>6.2 利用 TLB 加速地址翻译</h4><p>​ CPU 每次生成虚拟地址时，MMU 都必须引用 PTE 才能完成地址转换。如果 PTE 位于主存而非高速缓存中，那么地址转换的速度将大大下降。大多数系统的 MMU 中包含了一个被称为**转换后备缓冲区（Translation Lookaside Buffer，TLB）**的小型 PTE 缓存，其每个缓存行中都有一个由单条 PTE 组成的 Block。用于集合选择和行匹配 的 Set Index 和 Tag 是从虚拟地址的 VPN 中提取的：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/6846577427e658025b185ad379fed5b7.png" class><p>​ 如果 TLB 有 T &#x3D; 2^<em>t</em> 个集合，则 Set Index（TLBI）由 VPN 中 t 个最低位组成，Tag（TLBT）由 VPN 中的剩余高位组成。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/7ef9aea621bed462fe8e94175e3fa767.png" class><p>​ 上图展示了 TLB 命中时的 CPU 硬件操作步骤。由于地址转换均在 CPU 芯片上的 MMU 中执行，因此速度很快：</p><ol><li>CPU 生成一个虚拟地址 VA；</li><li>MMU 向 TLB 发送 VPN 以请求 PTE；</li><li>TLB 将 PTE 返回给 MMU；</li><li>MMU 将虚拟地址转换为物理地址 PA 并发送到高速缓存或主存；</li><li>高速缓存或主存将请求的数据返回给处理器。</li></ol><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/55eeea9b46270d2a06c450bf6288ab50.png" class><p>上图展示了 TLB 未命中时的 CPU 硬件操作步骤。MMU 必须从高速缓存或主存中获取 PTE 并将其存储在 TLB 中，这可能会覆盖现有条目。</p><h4 id="6-3-多级页表"><a href="#6-3-多级页表" class="headerlink" title="6.3 多级页表"></a>6.3 多级页表</h4><p>在之前的讨论中，我们假设系统只使用单级页表进行地址转换。但如果地址空间有 32 位，一个页面 4 KB 并且一条 PTE 4 字节。那么即使应用程序只引用一小部分虚拟内存，我们也需要一个 4 MB 的页表常驻在内存中：</p><p>$$<br>n&#x3D;32,P&#x3D;4K&#x3D;2^{12},n(PTE)&#x3D;2^{np}&#x3D;220&#x3D;1M<br>$$</p><p>我们可以通过对页表分级来压缩页表的大小：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/0a2376af0cff698d6e0e70a617542cf0.png" class><p>​ 如上图所示，一级页表中有 1024 条 PTE，每条 PTE 都映射到一个包含 1024 个连续虚拟页面的地址空间块。每个地址空间块的大小为 1024 _ 4 KB &#x3D; 4 MB，因此 1024 条 PTE 就可以覆盖 32 位（4 MB _ 1024 &#x3D; 4 GB &#x3D; 2^{32} B）地址空间。</p><p>​ 如果地址空间块中的所有页面均未分配，则一级页表中对应的 PTE 为空（如上图中的 PTE 2 ～ 7）；如果地址空间块中至少有一个页面已分配，那么一级页表中对应的 PTE 就指向二级页表中该块的起始位置（如上图中的 PTE 0 ～ 1）。二级页表中的每条 PTE 都映射到一个 4 KB 的物理内存页，这与我们之前查看的单级页表相同。</p><p>​ 若一级页表中的 PTE 为空，那么二级页表内对应的条目就无需存在。多数应用程序的虚拟地址空间中大部分页面是未分配的，因此这将显著地降低页表的内存占用。另外，我们只需在主存中维护一级页表和被调用最为频繁的二级页表，其它的二级页表可以由操作系统按需创建和分页。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/7cd1ee52bc8ebf139d2503ba2b63c4d0.png" class><p>​ 在 k 级页表中，虚拟地址被划分为 k 个 VPN 和一个 VPO，VPN i（1 ≤ i ≤ k）是第 i 级页表的索引。除第 k 级页表外，每个页表中的 PTE 均指向下一级页表的起始位置，而第 k 级表内的每条 PTE 则保存了对应物理页面的 PPN。与单级页表一样，PPO 与 VPO 相同。MMU 必须先请求 k 个 PTE，然后才能确定 PPN 以生成完整的物理地址。TLB 可以缓存多级页表中的 PTE，这使得多级页表的地址转换速度并不比单级页表慢很多。</p><h2 id="7-Intel-Core-i7-Linux-内存系统"><a href="#7-Intel-Core-i7-Linux-内存系统" class="headerlink" title="7.Intel Core i7&#x2F;Linux 内存系统"></a>7.Intel Core i7&#x2F;Linux 内存系统</h2><p>​ 尽管底层的 Haswell 微架构能够支持完整的 64 位虚拟和物理地址空间，但目前 Core i7 仅提供了 48 位 (256 TB) 的虚拟地址空间和 52 位 (4 PB ) 的物理地址空间，以及一个支持 32 位 (4 GB) 虚拟和物理地址空间的兼容模式。</p><p>​ 下图给出了 Core i7 存储器系统的重要部分。**处理器包(processor package)**包括四个核，一个大的所有核共享的 L3 高速缓存，以及一个 DDR3 存储器控制器。每个核包含一个层次结构的 TLB、一个层次结构的数据和指令高速缓存，以及一组快速的点到点连接，这种连接是基于 Imntel QuickPath 技术的，是为了让一个核与其他核和外部 IO 桥直接通信。TLB 是虚拟寻址的,是四路组相连的。L1、L2 和 L3 高速缓存是物理寻址的，是八路组相连的，块大小为 64 字节页大小在启动时被配置为 4KB 或 4MB。Linux 使用的是 4KB 的页。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/16835a2054533f03604c76375313baca.png" class><h4 id="7-1-Core-i7-地址翻译"><a href="#7-1-Core-i7-地址翻译" class="headerlink" title="7.1 Core i7 地址翻译"></a>7.1 Core i7 地址翻译</h4><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/428c933bfea1e39ff826172866228239.png" class><p>​ 如上图所示，Core i7 使用四级页表结构。CR 3 控制寄存器中保存了一级 (L1) 页表的起始物理地址，其值是每个进程上下文的一部分并在上下文切换时恢复。48 位的虚拟地址包含了 36 位的 VPN 和 12 位（4 K &#x3D; 2^{12}）的 VPO，其中 VPN 又被划分为四个 9 位的地址空间块。</p><h4 id="7-2-Linux-虚拟内存系统"><a href="#7-2-Linux-虚拟内存系统" class="headerlink" title="7.2 Linux 虚拟内存系统"></a>7.2 Linux 虚拟内存系统</h4><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/c0247360aa69b9ba4972369cdb2c4a4b.png" class><p>​ 内核虚拟存储器包含内核中的代码和数据结构。内核虚拟存储器的某些区域被映射到所有进程共享的物理页面。例如，每个进程共享内核的代码和全局数据结构**。有趣的是，Linux 也将一组连续的虚拟页面(大小等于系统中 DRAM 的总量)映射到相应的一组连续的物理页面**。这就为内核提供了一种便利的方法来访问物理存储器中任何特定的位置，例如，当它需要访间页表，或在一些设备上执行存储器映射的 I&#x2F;O 操作，而这些设备被映射到特定的物理存储器位置时。</p><p>​ 内核虚拟存储器的其他区域包含每个进程都不相同的数据。例如，页表、内核在进程的上下文中执行代码时使用的栈，以及记录虚拟地址空间当前组织的各种数据结构。</p><h6 id="1-Linux-虚拟存储器区域"><a href="#1-Linux-虚拟存储器区域" class="headerlink" title="1. Linux 虚拟存储器区域"></a>1. Linux 虚拟存储器区域</h6><p>​ Linux 将虚拟内存划分为多个区域或段（Area 或 Segment），每个区域都是一些已分配且在某些方面相关的连续页面(chunk)。例如，代码段、数据段、堆、共享库段和用户栈分别是不同的区域。每个已分配的页面都属于某个区域，因此不属于任何区域的页面不存在也无法被进程引用。区域概念的引入使得 Linux 允许虚拟地址空间存在间隙.</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/d58c9760f89607eb642b9d8ab19b8ab5.png" class><p>​ 如上图所示，Linux 内核为每个进程维护了一个独特的数据结构<code>task_struct</code>，其字段包含或指向内核运行该进程所需的全部信息（如 PID、用户栈指针、可执行目标文件名称和程序寄存器等）。</p><p>​ 其中，字段<code>mm</code>指向<code>mm_struct</code>，该结构体描述了虚拟内存的当前状态。<code>mm_struct</code>中的<code>pgd</code>字段指向一级页表的起始位置，它在进程运行时被内核存储在 CR 3 控制寄存器中。而<code>mmap</code>字段则指向一个由<code>vm_area_structs</code>组成的链表。描述区域信息的结构体<code>vm_area_structs</code>包含以下字段：</p><ul><li><code>vm_start</code>：指向区域的起点；</li><li><code>vm_end</code>：指向区域的末端；</li><li><code>vm_prot</code>：描述该区域中所有页面的读&#x2F;写权限；</li><li><code>vm_flags</code>：描述该区域中的页面是否与其他进程共享；</li><li><code>vm_next</code>：指向链表中下一个<code>vm_area_structs</code>。</li></ul><h6 id="2-linux-缺页异常处理"><a href="#2-linux-缺页异常处理" class="headerlink" title="2. linux 缺页异常处理"></a>2. linux 缺页异常处理</h6><p>​ 假设 MMU 在试图翻译某个虚拟地址 A 时，触发了一个缺页。这个异常导致控制转移到内<br>核的缺页处理程序，处理程序随后就执行下面的步骤:</p><ol><li><p>虚拟地址 A 是合法的吗?换句话说，A 在某个区域结构定义的区域内吗?为了回答这个问题，的缺页处理程序搜索区域结构的链表，把 A 和每个区域结构中的 vm_start 和 vm_end 做比较。如果这个指令是不合法的，那么缺页处理程序就触发一个段错误，从而终止这个进程。这种情况在下图中标识为“1”。</p><p>因为一个进程可以创建任意数量的新虚拟存储器区域(使用在下一节中描述的 mmap 函数)，<br>所以顺序搜索区域结构的链表花销可能会很大。因此在实际中，Linux 使用某些我们没有显示出<br>来的字段，Linux 在链表中构建了一棵树，并在这棵树上进行查找。</p></li><li><p>试图进行的存储器访问是否合法?换句话说，进程是否有读、写或者执行这个区域内页<br>面的权限?例如，这个缺页是不是由一条试图对这个代码段里的只读页面进行写操作的存储指令<br>造成的?这个缺页是不是因为一个运行在用户模式中的进程试图从内核虚拟存储器中读取字造成<br>的?如果试图进行的访问是不合法的，那么缺页处理程序会触发一个保护异常，从而终止这个进<br>程。这种情况在图 9-28 中标识为“2”。</p></li><li><p>此刻，内核知道了这个缺页是由于对合法的虚拟地址进行合法的操作造成的。它是这样<br>来处理这个缺页的:选择一个牺牲页面，如果这个牺牲页面被修改过，那么就将它交换出去，换<br>人新的页面并更新页表。当缺页处理程序返回时，CPU 重新启动引起缺页的指令，这条指令将<br>再次发送 A 到 MMU。这次，MMU 就能正常地翻译 A，而不会再产生缺页中断了。</p></li></ol><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/78785d4bdb449b4051d3f00015fe1cc4.png" class><h2 id="8-内存映射"><a href="#8-内存映射" class="headerlink" title="8. 内存映射"></a>8. 内存映射</h2><p>​ Linux 使用内存映射（Memory Mapping）技术初始化虚拟内存区域并将其与磁盘上的“对象”相关联。该“对象”有两种类型：</p><ol><li><p>文件系统中的常规文件（Regular File）：文件被分成多个与页面大小相同的片段，而每个片段都包含了一个虚拟页面的初始内容。由于操作系统采用按需分页的策略，因此页面在第一次被 CPU 引用前不会被换入到物理内存中。如果虚拟内存区域比文件大，则多余部分用零填充；</p></li><li><p>匿名文件（Anonymous File）：由内核创建，其内容全部为二进制零。当 CPU 第一次引用该区域内的虚拟页面时，内核会先在物理内存中选择一个合适的受害者页面（若该页面已被修改则需要将其换出）并用二进制零将其覆盖，然后更新页表使虚拟页面指向被覆盖后的物理页面。整个过程中没有数据被换入到物理内存，因此该区域内的页面又被称为零需求页面（Demand-zero Page）。</p></li></ol><p>一旦我们使用内存映射初始化一个虚拟页面，它就会在由内核维护的交换文件（Swap File）与物理内存之间来回交换。交换文件又称交换区（Swap Area）或交换空间（Swap Space），<strong>它的大小限制了当前运行进程所能申请的虚拟页面总量</strong>。</p><h4 id="8-1-再看共享对象"><a href="#8-1-再看共享对象" class="headerlink" title="8.1 再看共享对象"></a>8.1 再看共享对象</h4><p>​ 存储器映射的概念来源于一个聪明的发现:如果虚拟存储器系统可以集成到传统的文件系统中，那么就能提供一种简单而高效的把程序和数据加载到存储器中的方法。<br>​ 正如我们已经看到的，进程这一抽象能够为每个进程提供自己私有的虚拟地址空间，可以免受其他进程的错误读写。不过，许多进程有同样的只读文本区域。例如，每个运行 Unix 外壳程序 tcsh 的进程都有相同的文本区域。而且，许多程序需要访问只读运行时库代码的相同拷贝。例如，每个 C 程序都需要来自标准 C 库的诸如 printf 这样的函数。那么，如果每个进程都在物理存储器中保持这些常用代码的复制拷贝，那就是极端的浪费了。幸运的是，存储器映射给我们提供了一种清晰的机制，用来控制多个进程如何共享对象。<br>​ 一个对象可以被映射到虚拟存储器的一个区域，要么作为共享对象，要么作为私有对象。如果一个进程将一个共享对象映射到它的虚拟地址空间的一个区域内，那么这个进程对这个区域的任何写操作，对于那些也把这个共享对象映射到它们虚拟存储器的其他进程而言也是可见的。而且，这些变化也会反映在磁盘上的原始对象中。<br>​ 另一方面，对一个映射到私有对象的区域做的改变，对于其他进程来说是不可见的，并且进程对这个区域所做的任何写操作都不会反映在磁盘上的对象中。一个映射到共享对象的虚拟存储器区域叫做共享区域。类似地，也有私有区域。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/0e5322bba76ef59fea5b50f2c15c93be.png" class><p>​ 如上图所示，两进程将相同的 共享目标文件映射到各自虚拟地址空间中的不同区域，而物理内存中只需存在单个文件副本。进程 1 对该共享区域的任何写入操作都对进程 2 可见，并且这些更改还会同步到磁盘上的原始文件。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/7c36b3fdf6d7f1ed5e62f62b4f25001e.png" class><p>​ 如上图 (a) 所示，两进程将私有目标文件映射到各自虚拟地址空间的不同区域。进程 1 对该私有区域的任何写入操作都对进程 2 不可见，并且这些更改也不会同步到磁盘上的原始文件。如上图 (b) 所示，当进程 2 试图修改该区域中的内容时，<strong>内核会在物理内存中为页面创建一个新副本并更新页表条目使其指向它</strong>。由于页面复制发生在写入操作前，这种技术被称为<strong>写时复制（Copy-on-Write）</strong>，这些区域则是<strong>私有写时复制的（Private Copy-on-Write）</strong>。</p><h4 id="8-2-再看fork函数"><a href="#8-2-再看fork函数" class="headerlink" title="8.2 再看fork函数"></a>8.2 再看<code>fork</code>函数</h4><p>​ 进程调用 <code>fork</code> 函数后，内核会为子进程分配一个唯一的 PID 并为其创建与父进程相同的<code>mm_struct</code>、<code>vm_area_structs</code>以及页表。当任意进程后续执行写入操作时，内核将使用写时复制技术创建新页面，这便保证了进程虚拟地址空间的私有性。</p><h4 id="8-3-再看execve函数"><a href="#8-3-再看execve函数" class="headerlink" title="8.3 再看execve函数"></a>8.3 再看<code>execve</code>函数</h4><p>​ 如果进程调用 <code>execve</code> 函数，如<code>execve(&quot;a.out&quot;, NULL, NULL)</code>，则加载并运行<code>a.out</code>的步骤如下：</p><ol><li><strong>删除已存在的用户区域</strong>。删除当前进程虚拟地址空间中用户区域的<code>vm_area_structs</code>；</li><li><strong>映射私有区域</strong>。为新程序的代码、数据、bss 和堆栈区域创建<code>vm_area_structs</code>。这些区域都是私有写时复制的，代码和数据区域被映射到<code>a.out</code>文件中的 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/https://koktlzz.github.io/posts/linking-note/##%E5%8F%AF%E9%87%8D%E5%AE%9A%E4%BD%8D%E7%9B%AE%E6%A0%87%E6%96%87%E4%BB%B6">.text 和 .data</a>，bss 区域则被映射到大小包含在<code>a.out</code>内的匿名文件。堆栈的初始长度均为 0，其页面是零需求的；</li><li><strong>映射共享区域</strong>。如果<code>a.out</code>文件链接了共享库，如 C 标准库<code>libc.so</code>，那么还需要把这些对象动态链接到程序中，并将其映射到虚拟地址空间中的共享区域内；</li><li><strong>设置程序计数器(PC)</strong>。使当前进程上下文中的程序计数器指向新程序代码区域的入口点。</li></ol><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/845975a26f00fa721b45a14beed48223.png" class><h4 id="8-4-使用-mmap-函数的用户级存储器映射"><a href="#8-4-使用-mmap-函数的用户级存储器映射" class="headerlink" title="8.4 使用 mmap 函数的用户级存储器映射"></a>8.4 使用 <code>mmap</code> 函数的用户级存储器映射</h4><p>Linux 进程可以使用 mmap 函数来创建新的虚拟存储器区域，并将对象映射到这些区域中。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="type">void</span>  *<span class="title function_">mmap</span><span class="params">(<span class="type">void</span> *start, <span class="type">size_t</span> length, <span class="type">int</span> prot, <span class="type">int</span> flags,</span></span><br><span class="line"><span class="params">            <span class="type">int</span> fd, <span class="type">off_t</span> offset)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to mapped area if OK, MAP_FAILED (−1) on error</span></span><br></pre></td></tr></table></figure><p>​ <code>mmap</code>函数请求内核创建一个起始地址为参数<code>start</code>的虚拟内存区域，该区域映射到文件描述符<code>fd</code>所指定的对象。连续对象的长度为参数<code>length</code>，其首部在文件中的偏移量为参数<code>offset</code>：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/6c7bf93e529ee550e548321ea3bb87a6.png" class><p>参数<code>prot</code>中包含了描述虚拟内存区域访问权限的位，即<code>vm_area_structs</code>中的<code>vm_prot</code>：</p><ul><li><code>PROT_EXEC</code>：该区域中的页面包含可执行指令；</li><li><code>PROT_READ</code>：可以阅读该区域中的页面；</li><li><code>PROT_WRITE</code>：可以写入该区域中的页面；</li><li><code>PROT_NONE</code>：无法访问该区域中的页面。</li></ul><p>参数<code>flag</code>中包含了描述了映射对象类型的位：</p><ul><li><code>MAP_SHARED</code>：共享对象；</li><li><code>MAP_PRIVATE</code>：私有写时复制对象；</li><li><code>MAP_ANON</code>：匿名对象，对应的虚拟页面是零需求页面。</li></ul><p>​ 让内核创建一个新的包含 size 字节的只读、私有、请求二进制零的虚拟存储器区域。如果调用成功，那么 bufp 包含新区域的地址。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bufp=Mmap(<span class="number">-1</span>,Size,PROT_READ，MAP_PRIVATEIMAP_ANON，O，<span class="number">0</span>);</span><br></pre></td></tr></table></figure><p><code>munmap</code>函数删除起始于虚拟地址<code>start</code>、长度为<code>length</code>的区域，后续对已删除区域的引用会引发分段故障。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">munmap</span><span class="params">(<span class="type">void</span> *start, <span class="type">size_t</span> length)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><h2 id="9-动态内存分配"><a href="#9-动态内存分配" class="headerlink" title="9.动态内存分配"></a>9.动态内存分配</h2><p>​ 虽然可以使用低级的 mmap 和 munmap 函数来创建和删除虚拟存储器的区域，但是 C 程序员还是会觉得当运行时需要额外虚拟存储器时，用**动态存储器分配器(dyamic memory allocator)**来创建虚拟内存区域。更方便，也有更好的可移植性。</p><p><strong>动态内存分配器为进程维护的虚拟内存区域被称为堆（Heap）</strong>，其一般结构为：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/88b3e2b633102a7fb450f185f304b3d4.png" class><p>​ <strong>堆向上增长</strong>，内核为每个进程都维护了一个指向堆顶的变量<code>brk</code>。分配器将堆看作一个包含<strong>不同尺寸 Block 的集合</strong>，<strong>每个 Block 都是一个连续的虚拟内存块</strong>。Block 有两种状态，已分配（Allocated）和空闲（Free）。所有分配器均显式地为应用程序分配 Block，但负责释放已分配 Block 的实体可能有所不同：</p><ul><li><strong>显式分配器</strong>：应用程序显式地释放已分配的 Block。C 和 C++ 程序分别调用<code>malloc</code>和<code>new</code>函数请求 Block，调用<code>free</code>和<code>delete</code>函数释放 Block；</li><li><strong>隐式分配器</strong>：分配器自行释放程序不再使用的已分配 Block，该过程被称为<strong>垃圾回收（Garbage Collection）</strong>。Lisp、ML 和 Java 等高级语言均采用这种方法。</li></ul><p>​ 本节剩下的部分讨论的是显式分配器的设计和实现。我们将在下一节中讨论隐式分配器。为了更具体，我们的讨论集中于管理堆存储器的分配器。然而，应该明白存储器分配是一个普遍的概念，可以出现在各种上下文中。例如，图形处理密集的应用程序就经常使用标准分配器来要求获得一大块虚拟存储器，然后使用与应用相关的分配器来管理块中的存储器，以支持图形节点的创建和销毁。</p><h4 id="9-1-malloc-和-free-函数"><a href="#9-1-malloc-和-free-函数" class="headerlink" title="9.1 malloc 和 free 函数"></a>9.1 malloc 和 free 函数</h4><p>C 标准库提供了一个称为 malloc 程序包的显式分配器。程序通过调用 malloc 函数来从堆中分配块。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">malloc</span><span class="params">(<span class="type">size_t</span> size)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to allocated block if OK, NULL on error</span></span><br></pre></td></tr></table></figure><p>​ <code>malloc</code>函数请求堆中的一块 Block 并返回指向该 Block 的指针。Block 的大小至少为参数<code>size</code>，并可能根据其保存的数据对象类型进行适当对齐。在 32 位编译模式下，Block 的地址始终为 8 的倍数，而在 64 位中则为 16 的倍数。如果执行<code>malloc</code>遇到问题，如程序请求的 Block 大小超过了可用的虚拟内存，则函数返回<code>NULL</code>并设置 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/https://man7.org/linux/man-pages/man3/errno.3.html"><code>errno</code></a>。我们还可以使用<code>malloc</code>的包装函数<code>calloc</code>，它会将分配的内存初始化为零。类似地，<code>realloc</code>函数可以更改已分配 Block 的大小</p><p>​</p><p>​ 还可以使用<code>sbrk</code>函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">sbrk</span><span class="params">(<span class="type">intptr_t</span> incr)</span>;</span><br><span class="line"><span class="comment">// Returns: old brk pointer on success, −1 on error</span></span><br></pre></td></tr></table></figure><p>​ <code>sbrk</code>函数将参数<code>incr</code>与内核中的<code>brk</code>指针相加以增大或缩小堆。若执行成功，则返回<code>brk</code>的旧值，否则将返回 -1 并将<code>errno</code>设置为<code>ENOMEM</code>。</p><p>​ 程序通过调用<code>free</code>函数来释放已分配的堆块：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">free</span><span class="params">(<span class="type">void</span> *ptr)</span>;</span><br><span class="line"><span class="comment">// Returns: nothing</span></span><br></pre></td></tr></table></figure><p>​ <code>free</code>函数将参数<code>ptr</code>指向的 Block 释放，而这些 Block 必须是由<code>malloc</code>、<code>calloc</code>或<code>realloc</code>分配的。该函数没有返回值，因此很容易产生一些令人费解的运行时错误。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/ed12d6c6d8d77d14f4befcb68d01c124.png" class><p>上图展示了 C 程序如何使用<code>malloc</code>和<code>free</code>管理一个大小为 16 字（字长为 4 字节）的堆。图中的每个方框代表一个字，每个被粗线分隔的矩形代表一个 Block。有阴影的 Block 代表已分配，无阴影的 Block 则代表空闲。</p><ul><li>如上图 (a) 所示，程序请求一个 4 字的 Block，<code>malloc</code>从空闲块中切出一个 4 字的 Block 并返回指向该 Block 中第一个字的指针<code>p1</code>；</li><li>如上图 (b) 所示，程序请求一个 5 字的 Block，<code>malloc</code>从空闲块中切出一个 6 字的 Block 以实现双字对齐；</li><li>如上图 (c) 所示，程序请求一个 6 字的 Block，<code>malloc</code>从空闲块中切出一个 6 字的 Block；</li><li>如上图 (d) 所示，程序释放图 (b) 中分配的 Block。<code>free</code>返回后，指针<code>p2</code>依然指向已释放的 Block，因此程序不应在重新初始化<code>p2</code>前继续使用它；</li><li>如上图 (e) 所示，程序请求一个 2 字的 Block。<code>malloc</code>从上一步释放的 Block 中切出一部分并返回指向新 Block 的指针<code>p4</code>。</li></ul><h4 id="9-2-动态内存分配的原因"><a href="#9-2-动态内存分配的原因" class="headerlink" title="9.2 动态内存分配的原因"></a>9.2 动态内存分配的原因</h4><p>​ 在程序实际运行之前，我们可能并不知道某些数据结构的大小。程序使用动态存储器分配的最重要的原因是<strong>经常直到程序实际运行时，它们才知道某些数据结构的大小</strong>。示例 C 程序将<code>n</code>个 ASCII 整型从标准输入读取到数组<code>array[MAXN]</code>中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> MAXN 15213</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="built_in">array</span>[MAXN];</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i, n;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;n);</span><br><span class="line">    <span class="keyword">if</span> (n &gt; MAXN)</span><br><span class="line">        app_error(<span class="string">&quot;Input file too big&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;<span class="built_in">array</span>[i]);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 由于我们无法预测<code>n</code>的值，因此只能将数组大小写死为<code>MAXN</code>。<code>MAXN</code>的值是任意的，可能超出系统可用的虚拟内存量。另外，一旦程序想要读取一个比<code>MAXN</code>还大的文件，唯一的办法就是增大<code>MAXN</code>的值并重新编译程序。如果我们在运行时根据<code>n</code>的大小动态分配内存，以上问题便迎刃而解：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> *<span class="built_in">array</span>, i, n;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;n);</span><br><span class="line">    <span class="built_in">array</span> = (<span class="type">int</span> *)Malloc(n * <span class="keyword">sizeof</span>(<span class="type">int</span>));</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;<span class="built_in">array</span>[i]);</span><br><span class="line">    <span class="built_in">free</span>(<span class="built_in">array</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="9-3-对分配器的要求和目标"><a href="#9-3-对分配器的要求和目标" class="headerlink" title="9.3 对分配器的要求和目标"></a>9.3 对分配器的要求和目标</h4><p>显式分配器必须在若干限制条件下运行：</p><ul><li>处理任意顺序的请求：分配器不能对<code>malloc</code>和<code>free</code>的请求顺序作出假设。例如，分配器不能假设所有的<code>malloc</code>都紧跟一个与之匹配的<code>free</code>；</li><li>立即响应请求：分配器不可以对请求重新排序或缓冲（Buffer）以提高性能；</li><li>仅使用堆：分配器使用的数据结构必须存储在堆中；</li><li>对齐 Block：分配器必须对齐 Block 以使其能够容纳任何类型的数据对象；</li><li>不修改已分配的 Block：分配器无法修改、移动或压缩已分配的 Block。</li></ul><p>衡量分配器性能的指标有：</p><ul><li>吞吐量（Throughput）：单位时间内完成的请求数；</li><li>内存利用率（Memory Utilization）：即堆内存的使用率。</li></ul><p>最大化吞吐量和最大化内存利用率之间存在矛盾，因此我们在设计分配器时需要找到二者的平衡。</p><h4 id="9-4-碎片"><a href="#9-4-碎片" class="headerlink" title="9.4 碎片"></a>9.4 碎片</h4><p>​</p><p>​ 造成堆利用率很低的主要原因是一种称为**碎片(fagmentation)**的现象，当虽然有未使用的存储器但不能用来满足分配请求时，就会发生这种现象。有两种形式的碎片:<strong>内部碎片(intermalfragmentation)<strong>和</strong>外部碎片(external fragmentation)</strong>。</p><ul><li>内部碎片（Internal Fragmentation）：已分配的 Block 比进程请求的 Block（即 Payload）大，通常因分配器为满足对齐要求而产生；</li><li>外部碎片（External Fragmentation）：空闲内存充足但却没有空闲的 Block 能够满足分配请求。例如堆中有 4 个空闲的字且分布在两个不相邻的 Block 上，此时若进程申请一个 4 字的 Block 就会出现外部碎片。</li></ul><p>​ 内部碎片很容易量化，因为它只是已分配 Block 与 Payload 之间大小差异的总和，其数量仅取决于先前的请求模式和分配器的实现方式；外部碎片则难以量化，因为它还要受到未来请求模式的影响。为了减少外部碎片的产生，分配器力求维护少量较大的空闲 Block 而非大量较小的空闲 Block。</p><h4 id="9-5-实现难点"><a href="#9-5-实现难点" class="headerlink" title="9.5 实现难点"></a>9.5 实现难点</h4><p>​</p><p>​ 我们可以想象出的最简单的分配器会把堆组织成一个大的字节数组，还有一个指针 p，初始指向这个数组的第一个字节。为了分配 size 个字节，malloc 将 p 的当前值保存在栈里，将 p 增加 size，并将 p 的旧值返回到调用函数。free 只是简单地返回到调用函数，而不做其他任何事情。<br>​ 这个简单的分配器是设计中的一种极端情况。因为每个 ma1loc 和 free 只执行很少量的指吞吐率会极好。然而，因为分配器从不重复使用任何块，存储器利用率将极差。一个实际的分配器要在吞吐率和利用率之间把握好平衡，就必须考虑以下几个问题:</p><ul><li>空闲块组织:我们如何记录空闲块?</li><li>放置:我们如何选择一个合适的空闲块来放置一个新分配的块?</li><li>分割:在我们将一个新分配的块放置到某个空闲块之后，我们如何处理这个空闲块中的剩余部分?</li><li>合并:我们如何处理一个刚刚被释放的块?本节剩下的部分将更详细地讨论这些问题。因为像放置、分割以及合并这样的基本技术贯穿在许多不同的空闲块组织中，所以我们将在一种叫做隐式空闲链表的简单空闲块组织结构中来介绍它们。</li></ul><h4 id="9-6-隐式空闲链表"><a href="#9-6-隐式空闲链表" class="headerlink" title="9.6 隐式空闲链表"></a>9.6 隐式空闲链表</h4><p>​ 大多数分配器通过将一些数据结构嵌入到 Block 中以分辨其<strong>边界和状态</strong>，例如：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/f4db00fff25920c010449ef777dcc779.png" class><p>​ 如上图所示，Block 由一个单字（四字节）的<strong>头部（Header）</strong>、有效负载（Payload）和一些额外**填充（Padding）**组成，头部中包含了 Block 的大小（Block Size）和状态信息（Allocated or Free）。如果系统采用双字对齐策略，那么每个 Block 的大小始终为 8 的倍数，其二进制表达的后 3 位始终为 0。因此我们可以仅在头部中存储该字段的前 29 位，剩余 3 位用来存储其他信息。上图中的位“a”便指示了此 Block 是已分配的还是空闲的。填充的大小是任意的，它可能是分配器为了避免外部碎片产生而设置的，也可能是为了满足对齐要求而存在的。</p><p>例如，假设我们有已分配的块，大小为 24(0x18)字节。那么它的头部将是:</p><p><code>0x00000018|0x1=0x00000019</code></p><p>类似地，一个块大小为 40(0x28)字节的空闲块有如下的头部:</p><p><code>0x00000028|0x0=0x00000028</code></p><p>​ 头部后面就是应用调用 malloc 时请求的有效载荷。有效载荷后面是一片不使用的填充块，其大小可以是任意的。需要填充有很多原因，比如，填充可能是分配器策略的一部分，用来对付外部碎片，或者也需要用它来满足对齐要求。</p><p>基于这种 Block 格式，我们可以将堆组织成一系列连续的已分配 Block 和空闲 Block：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/758e1ded8db1b51f85a0c00b1f2ac2fc.png" class><p>​ Block 通过其头部中的<strong>大小字段隐式地链接起来</strong>（addr(next_block) &#x3D; addr(current_block) + block_size），因此我们将这种堆组织方式称为隐式空闲链表（Implicit Free List），分配器必须遍历堆中所有的 Block 才能得到全部空闲的 Block。我们还需要一个特殊的 Block 以标记堆的结尾，如上图中的 “0&#x2F;1”。隐式空闲链表的优点是简单，但任何搜索空闲 Block 的操作（如放置新分配的 Block）的成本都与堆中 Block 的总数成正比。</p><h4 id="9-7-放置新分配的-Block"><a href="#9-7-放置新分配的-Block" class="headerlink" title="9.7 放置新分配的 Block"></a>9.7 放置新分配的 Block</h4><p>​ 分配器搜索空闲链表，查找一个足够大可以放置所请求当一个应用请求一个 k 字节的块时，块的空闲块。分配器执行这种搜索的方式是由<strong>放置策略(placement policy)<strong>确定的。一些常见的策略是</strong>首次造配(frst fit)</strong>、<strong>下一次适配(next fit)<strong>和</strong>最佳适配(best fit)</strong>:</p><ul><li>第一次拟合（First Fit）：从头开始遍历空闲链表并选择第一个满足条件的 Block；</li><li>下一次拟合（Next Fit）：从上一次搜索停止的地方开始遍历空闲链表并选择第一个满足条件的 Block；</li><li>最佳拟合（Best Fit）：遍历所有 Block 并选择满足条件且最小的 Block</li></ul><p>第一次拟合的优点是较大的 Block 通常存留在链表末尾，但一些较小的 Block 也会散落在链表开头，这将增加搜索较大 Block 的时间。如果链表开头存在大量较小的 Block，下一次拟合就比第一次拟合快很多。然而研究表明，下一次拟合的内存利用率比第一次拟合低。最佳拟合的内存利用率通常比其他两种策略高，但对隐式空闲链表来说，其搜索时间显然要比它们慢很多。</p><h4 id="9-8-分割空闲的-Block"><a href="#9-8-分割空闲的-Block" class="headerlink" title="9.8 分割空闲的 Block"></a>9.8 分割空闲的 Block</h4><p>​ 如果分配器找到了合适的 Block 并将整个 Block 分配给程序，就有可能产生内部碎片。为了避免这一问题，分配器可以将选取的 Block 分成一个已分配的 Block 和一个新的空闲 Block：</p><p>​ 然而，如果匹配不太好，那么分配器通常会选择将这个空闲块分割为两部分。第一部分变成分配块，而剩下的变成一个新的空闲块。下图展示了分配器如何分割 8 个字的空闲块，来满足一个应用的对堆存储器 3 个字的请求：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/a76b3b1566d8c2fde6add934ebbfc7b4.png" class><h4 id="9-9-获取额外的堆内存"><a href="#9-9-获取额外的堆内存" class="headerlink" title="9.9 获取额外的堆内存"></a>9.9 获取额外的堆内存</h4><p>​ 如果分配器无法找到合适的 Block，它可以尝试将<strong>物理相邻的空闲 Block 合并以获取更大的 Block</strong>。但如果仍然无法满足请求，分配器便会调用<code>sbrk</code>函数向内核请求额外的堆内存并将其转换为一个新的空闲 Block。</p><h4 id="9-10-合并空闲的-Block"><a href="#9-10-合并空闲的-Block" class="headerlink" title="9.10 合并空闲的 Block"></a>9.10 合并空闲的 Block</h4><p>​ 当分配器释放一个已分配块时，可能有其他空闲块与这个新释放的空闲块相邻。这些邻接的空闲块可能引起一种现象，叫做<strong>假碎片(fault fragmentation)</strong>，就是有许多可用的空闲块被切割成小的、无法使用的空闲块。比如，下图展示了释放图 9-37 中分配的块后得到的结果：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/97dc0e2fe0e88d58d80de31739a9dd1e.png" class><p>​ 结果是两个相邻的空闲块，每一个的有效载荷都为 3 个字。因此，接下来一个对 4 个字有效载荷的请求就会失败，即使两个空闲块的合计大小足够大，可以满足这个请求。</p><p>​ 为了解决假碎片问题，任何实际的分配器都必须合并相邻的空闲块，这个过程称为合并(coalescing)。这就出现了一个重要的策略决定，那就是何时执行合并。分配器可以选择<strong>立即合并(immediate coalescing)</strong>，也就是在每次一个块被释放时，就合并所有的相邻块。或者它也可以选择<strong>推迟合并(deferred coalescing)</strong>，也就是等到某个稍晚的时候再合并空闲块。例如，分配器可以推迟合并，直到某个分配请求失败，然后扫描整个堆，合并所有的空闲块。</p><p>​ 立即合并很简单明了，可以在常数时间内执行完成，但是对于某些请求模式，<strong>这种方式会产生一种形式的抖动，块会反复地合并</strong>，然后马上分割。例如，在图 9-38 中，反复地分配和释放一个 3 个字的块将产生大量不必要的分割和合并。在对分配器的讨论中，我们会假设使用立即合并，但是你应该了解，快速的分配器通常会选择某<strong>种形式的推迟合并</strong>。</p><h4 id="9-11-带边界标记的合并"><a href="#9-11-带边界标记的合并" class="headerlink" title="9.11 带边界标记的合并"></a>9.11 带边界标记的合并</h4><p>​ 我们把即将释放的 Block 称为<strong>当前块（Current Block）</strong>，其头部指向下一个 Block 的头部（addr(next_block) &#x3D; addr(current_block) + block_size）。因此我们很容易判断下一个 Block 是否空闲，并且只需将当前 Block 头部中的大小字段与之相加即可完成合并。</p><p>​ 但若要合并上一个 Block，我们只能遍历整个链表，并在到达当前 Block 前不断记下上一个 Block 的位置。因此对于隐式空闲链表，每次调用<code>free</code>函数，合并上一个 Block 的时间与堆内存的大小成正比。</p><p>​ 我们可以在每个 Block 末尾都添加一个头部的副本以使合并 Block 的时间变为常数，这种技术被称为边界标记（Boundary Tags）：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/e1c09f1f69199ea680604743b875c0e4.png" class><p>​ 上一个 Block 的尾部始终与当前 Block 的头部相距一个字长，因此分配器可以通过检查上一个 Block 的尾部来确定其位置和状态。</p><p>考虑当分配器释放当前块时所有可能存在的情况:</p><ol><li>前面的块和后面的块都是已分配的。</li><li>前面的块是已分配的，后面的块是空闲的。</li><li>前面的块是空闲的，而后面的块是已分配的，</li><li>前面的和后面的块都是空闲的。</li></ol><p>​ 下图展示了分配器是如何使用边界标记合并 Block 的：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/8a5a3119d483b10be0f532b074385573.png" class><ol><li>在情况 1 中，两个邻接的块都是已分配的，因此不可能进行合并。所以当前块的状态只是简单地从已分配变成空闲。</li><li>在情况 2 中，当前块与后面的块合并。用当前块和后面块的大小的和来更新当前块的头部和后面块的脚部。</li><li>在情况 3 中，前面的块和当前块合并。用两个块大小的和来更新前面块的头部和当前块的脚部。</li><li>在情况 4 中，要合并所有的三个块形成一个单独的空闲块，用三个块大小的和来更新前面块的头部和后面块的脚部。在每种情况中，合并都是在常数时间内完成的。</li></ol><p>​ 由于每个 Block 都包含头部和尾部，当 Block 数量较多时，边界标记显著地增加了内存的开销。<strong>考虑到分配器只有在上一个 Block 空闲时才需要获取其尾部内的 Block 大小</strong>，因此我们可以将上一个 Block 的状态存储在当前 Block 头部的<strong>多余低位中</strong>，这样已分配的 Block 便不需要尾部了。</p><h4 id="9-12-显式空闲链表"><a href="#9-12-显式空闲链表" class="headerlink" title="9.12 显式空闲链表"></a>9.12 显式空闲链表</h4><p>​ 由于分配 Block 的时间与 Block 的总数成正比，隐式空闲链表不适用于通用分配器。我们可以在每个空闲 Block 中加入一个指向上一个空闲 Block 的前驱（Predecessor）指针和一个指向下一个空闲 Block 的后继（Successor）指针，这样堆的组织结构就变成了一个双向链表，我们称其为显式空闲链表（Explicit Free List）。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/805594378ff24f425c41a550224902d6.png" class><p>​ 如果采用第一次拟合策略，显式空闲链表分配 Block 的时间与空闲 Block 的数量成正比，而释放 Block 的时间则取决于空闲 Block 的排序方式：</p><ul><li>后进先出（Last-in First-out，LIFO）：将刚被释放的 Block 插入到链表开头，因此释放 Block 的时间为常数，并且可以通过边界标记使合并 Block 的时间也为常数；</li><li>按地址顺序（Address Order）：使链表中每个 Block 的地址均小于其后继 Block 的地址。在这种情况下，释放 Block 需要一定的时间来寻找合适的位置，但堆内存的利用率比后进先出高。</li></ul><p>显式空闲链表的缺点在于指针的引入增加了空闲 Block 的大小，这将增大<strong>内部碎片</strong>发生的可能性。</p><h4 id="9-13-分离式空闲链表"><a href="#9-13-分离式空闲链表" class="headerlink" title="9.13 分离式空闲链表"></a>9.13 分离式空闲链表</h4><p>​ 就像我们已经看到的，一个使用单向空闲块链表的分配器需要与空闲块数量呈线性关系的时间来分配块。一种流行的减少分配时间的方法，通常称为<strong>分离存储(segregatedstorage)</strong>，就是维护<strong>多个空闲链表</strong>，其中每个链表中的块有大致相等的大小。一般的思路是将所有可能的块大小分成一些等价类，也叫做<strong>大小类(size class)</strong>。有很多种方式来定义大小类。例如，我们可以根据 2 的幂来划分块大小:</p><p>​ <strong>{1},{2},{3,4},{5–8},…,{1025–2048},{2049–4096},{4097–∞}</strong></p><p>也可以让每个较小的 Block 独自成为一个大小类，较大的 Block 依然按 2 的幂划分：</p><p>​ <strong>{1},{2},{3},…,{1024},{1025–2048},{2049–4096},{4097–∞}</strong></p><p>​ 每个空闲链表都属于某个大小类，因此我们可以将堆看成一个按大小类递增的空闲链表数组。当进程请求一个 Block 时，分配器会根据其大小在适当的空闲链表中搜索。如果找不到满足要求的 Block，它便会继续搜索下一个链表。</p><p>​ 不同的分离式空闲链表在定义大小类的方式、合并 Block 的时机以及是否允许分割 Block 等方面有所不同，其中最基本的两种类型为<strong>简单分离存储（Simple Segregated Storage）<strong>和</strong>分离适配（Segregated Fits）</strong>。</p><ol><li>简单分离存储</li></ol><p>​ 在简单分离存储中，空闲链表内每个 Block 的大小均等于其所属大小类中最大的元素。如某个大小类为 {17-32}，则其对应的空闲链表中 Block 的大小都是 32。当进程请求一个 Block 时，分配器选取满足请求的空闲链表并分配其中第一个 Block；当某个 Block 被释放后，分配器将其插入到合适的空闲链表前面。因此，简单分离存储分配和释放 Block 的时间均为常量。</p><p>​ 一个显著的缺点是，简单分离存储很容易造成内部和外部碎片。因为空闲块是不会被分割的，所以可能会造成内部碎片。更糟的是，因为不会合并空闲块，所以某些引用模式会引起极多的外部碎片。</p><ol start="2"><li>分离适配</li></ol><p>​ 使用分离适配方法，分配器维护着一个空闲链表的数组。每个空闲链表是和一个大小类相关联的，并且被组织成某种类型的显式或隐式链表。每个链表包含潜在的大小不同的块，这些块的大小是大小类的成员。有许多种不同的分离适配分配器。这里，我们描述了一种简单的版本。</p><p>​ 为了分配一个块，我们必须确定请求的大小类，并且对适当的空闲链表做首次适配，查找一个合适的块。如果我们找到了一个，那么我们(可选地)分割它，并将剩余的部分插入到适当的空闲链表中。如果我们找不到合适的块，那么就搜索下一个更大的大小类的空闲链表。如此重复，直到找到一个合适的块。如果空闲链表中没有合适的块，那么我们就向操作系统请求额外的堆存储器，从这个新的堆存储器中分配出一个块，将剩余部分放置在适当的大小类中。要释放一个块，我们执行合并，并将结果放置到相应的空闲链表中。</p><p>​ 分离适配方法是一种常见的选择，C 标准库中提供的 GNU <code>malloc</code>包就是采用的这种方法因为这种方法既快速，对存储器的使用也很有效率。搜索时间减少了，因为搜索被限制在堆的某个部分，而不是整个堆。存储器利用率得到了改善，因为有一个有趣的事实:对分离空闲链表的简单的首次适配搜索，其存储器利用率近似于对整个堆的最佳适配搜索的存储器利用率。</p><p>​ 3. 伙伴系统</p><p>​ 伙伴系统(buddy system)是分离适配的一种特例，其中每个大小类都是 2 的幂。基本的思路是假设一个堆的大小为 2”个字，我们为每个块大小 2“维护一个分离空闲链表，其中 0&lt;k≤m。请求块大小向上舍人到最接近的 2 的幂。最开始时，只有一个大小为 2”个字的空闲块。<br>​ 为了分配一个大小为 2“的块，我们找到第一个可用的、大小为 2 的块，其中 k≤j≤ m。如果 j&#x3D;k，那么我们就完成了。否则，我们递归地二分割这个块，直到 j&#x3D;k。当我们进行这样的分割时，每个剩下的半块(也叫做伙伴)被放置在相应的空闲链表中。要释放一个大小为 2 的块，我们继续合并空闲的伙伴。当我们遇到一个已分配的伙伴时，我们就停止合并。关于伙伴系统的一个关键事实是，给定地址和块的大小，很容易计算出它的伙伴的地址。例如，一个块大小为 32 字节，地址为：</p><p>​ xxx…x00000<br>它的伙伴的地址为：<br>​ xxx…x10000<br>换句话说，一个块的地址和它的伙伴的地址只有一位不相同，伙伴系统分配器的主要优点是它的快速搜索和快速合并。主要缺点是要求块大小为 2 的幂可能导致显著的内部碎片。因此，伙伴系统分配器不适合通用目的的工作负载。然而，对于某些特定应用的工作负载，其中块大小预先知道是 2 的幂，伙伴系统分配器就很有吸引力了。</p><h2 id="10-垃圾收集"><a href="#10-垃圾收集" class="headerlink" title="10. 垃圾收集"></a>10. 垃圾收集</h2><p>​ 垃圾回收器（Garbage Collector）是一种动态存储分配器，它会自动释放程序不再需要的 Block（垃圾）。</p><h4 id="10-1-垃圾收集器的基本知识"><a href="#10-1-垃圾收集器的基本知识" class="headerlink" title="10.1 垃圾收集器的基本知识"></a>10.1 垃圾收集器的基本知识</h4><p>​</p><p>​ 垃圾回收器将内存看作一个有向可达性图：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/ad931fca0fb534e66d367e94bd7d525d.png" class><p>​ 图中的节点被分为一组根节点（Root Nodes）和一组堆节点（Heap Nodes），每个堆节点都对应于一个堆中已分配的 Block。有向边 <em>p</em>→<em>q</em>表示 Block <em>p</em> 中的某个位置指向 Block <em>q</em> 中的某个位置。根节点对应于不在堆中却包含了指向堆的指针的位置，这些位置可以是寄存器、栈中的变量或可读写数据区域中的全局变量。</p><p>​ 如果根节点与堆节点之间存在一条有向路径，我们就称该堆节点是<strong>可达的（Reachable）</strong>。在任何时刻，不可达的节点都与程序不再使用的 Block 对应。垃圾回收器<strong>定期释放</strong>不可达节点并将其返回到空闲链表。</p><p>​ ML 和 Java 等语言的垃圾回收器对应用程序使用指针的方式进行了严格的限制，因此它可以维护一个精确的可达性图，从而回收所有的垃圾。而 C 和 C++ 等语言的垃圾回收器则无法保证可达性图的精确性，一些不可达的节点可能被错误地识别为可达的，我们称其为<strong>保守垃圾回收器（Conservative Garbage Collector）</strong>。</p><p>​ 收集器可以按需提供它们的服务，或者它们可以作为一个和应用并行的独立线程，不断地更新可达图和回收垃圾。例如，考虑如何将一个 C 程序的保守的收集器加入到已存在的 malloc 包中，如图所示：</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/180d20d57ac10a4c1e76571cec65ab1e.png" class><p>​ 无论何时需要堆空间，应用都会用通常的方式调用 malloc。如果 malloc 找不到一个合适的空闲块，那么它就调用垃圾收集器，希望能够回收一些垃圾到空闲链表。收集器识别出垃圾块，并通过调用 free 函数将它们返回给堆。关键的思想是收集器代替应用去调用 free。当对收集器的调用返回时，malloc 重试，试图发现一个合适的空闲块。如果还是失败了，那么它就会向操作系统要求额外的存储器。最后，malloc 返回一个指向请求块的指针(如果成功)或者返回一个空指针(如果不成功)。</p><h4 id="10-2-Mark-Sweep-垃圾收集器"><a href="#10-2-Mark-Sweep-垃圾收集器" class="headerlink" title="10.2 Mark&amp;Sweep 垃圾收集器"></a>10.2 Mark&amp;Sweep 垃圾收集器</h4><p>Mark&amp;Sweep 是常用的垃圾回收算法之一，它分为两个阶段：</p><ul><li>标记（Mark）阶段：标记所有可达的根节点后代。通常我们将 Block 头部的多余低位之一用于指示该 Block 是否被标记；</li><li>清除（Sweep）阶段：释放所有未标记且已分配的 Block</li></ul><p>为了更好地理解 Mark&amp;Sweep 算法，我们作出以下假设：</p><ul><li><code>ptr</code>：由<code>typedef void *ptr</code>定义的类型；</li><li><code>ptr isPtr(ptr p)</code>：若<code>p</code>指向已分配 Block 中的某个字，则返回指向该 Block 起始位置的指针<code>b</code>，否则返回<code>NULL</code>；</li><li><code>int blockMarked(ptr b)</code>：如果该 Block 已被标记则返回<code>true</code>；</li><li><code>int blockAllocated(ptr b)</code>：如果该 Block 已分配则返回<code>true</code>；</li><li><code>void markBlock(ptr b)</code>：标记 Block；</li><li><code>int length(ptr b)</code>：返回 Block 除头部外的字长；</li><li><code>void unmarkBlock(ptr b)</code>：将 Block 的状态从已标记转换为未标记；</li><li><code>ptr nextBlock(ptr b)</code>：返回指向下一个 Block 的指针。</li></ul><p>那么此算法就可以用下图中的伪码表示:</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/bb2f274c0af56a0404d20277955af535.png" class><p>​ 在标记阶段，垃圾回收器为每个根节点调用一次<code>mark</code>函数。若<code>p</code>未指向已分配且未标记的 Block，则该函数直接返回。否则，它标记该 Block 并将其中的每个字作为参数递归地调用自身（<code>mark(b[i])</code>）。此阶段结束时，任何未标记且已分配的 Block 都是不可达的；在扫描阶段，垃圾回收器只调用一次<code>sweep</code>函数。该函数遍历堆中的每一个 Block，释放所有已分配且未标记的 Block。</p><img src="/2024/08/31/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-9-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98/498b2bceec9f573c81c483527665374a.png" class><p>​ 上图中的每个方框代表一个字，每个被粗线分隔的矩形代表一个 Block，而每个 Block 都有一个单字的头部。最初，堆中有 6 个已分配且未标记的 Block。Block 3 中包含指向 Block 1 的指针，Block 4 中包含指向 Block 3 和 6 的指针。根节点指向 Block 4，因此 Block 1、3、4 和 6 从根节点可达，它们会被垃圾回收器标记。在扫描阶段完成后，剩余不可达的 Block 2 和 5 将被释放。</p><h2 id="小结："><a href="#小结：" class="headerlink" title="小结："></a>小结：</h2><p>设计一个 malloc 和 gc（在后面的博客中我会贴出）</p>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_8_异常控制流</title>
      <link href="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/"/>
      <url>/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/</url>
      
        <content type="html"><![CDATA[<p>​ 在计算机的运行过程中，程序计数器依次指向一系列的值：<em>a0,a1,…,an</em>。其中，<em>ak</em> 是其对应指令 <em>Ik</em> 的地址。从<em>ak</em> 到 <em>ak</em>+1 的转换被称为<strong>控制转移（Control Transfer）</strong>，一系列的控制转移则被称为<strong>处理器的控制流（Control Flow）</strong>。</p><p>​ 最简单的控制流便是程序中的指令按顺序执行，即 <em>Ik</em> 与 <em>Ik+1</em> 在内存中相邻。不过，这种“平滑”的控制流通常因指令的跳转、调用和返回而突然改变，此时 <em>Ik</em> 便不再与 <em>Ik+1</em> 相邻。</p><p>​ <strong>程序内部的状态是由程序变量表示的</strong>，控制流使得程序可以对其更新做出反应。但系统还必须能够应对自身状态的变化，它们无法被内部程序变量捕获，甚至不一定与程序的执行有关。比如，数据包在到达网络适配器后需要被存储到内存中；程序请求磁盘中的数据时需要得知其何时可用；父进程必须在其子进程终止时收到通知等。</p><p>现代系统通过**异常控制流（Exceptional Control Flow，ECF）**来处理上述情况，它应用于计算机系统的所有级别中。</p><ul><li>比如在硬件层，硬件检测到的事件会触发控制突然转移到异常处理程序。</li><li>在操作系统层，内核通过上下文转换将控制从一个用户进程转移到另一个用户进程。</li><li>在应用层，一个进程可以发送信号到另一个进程，而接收者会将控制突然转移到它的一个信号处理程序。一个程序可以通过回避通常的栈规则，并执行到其他函数中任意位置的非本地跳转来对错误做出反应。</li></ul><p>作为程序员，理解 ECF 很重要，这有很多原因:</p><ul><li>理解 ECF 将帮助你理解重要的系统概念。ECF 是操作系统用来实现 IO、进程和虚拟存储器的基本机制。在能够真正理解这些重要概念之前，你必须理解 ECF。</li><li>理解 ECF 将帮助你理解应用程序是如何与操作系统交互的。应用程序通过使用一个叫做陷阱(trap)或者系统调用(systemcall)的 ECF 形式，向操作系统请求服务。比如，向磁盘写数据、从网络读取数据、创建一个新进程，以及终止当前进程，都是通过应用程序调用系统调用来实现的。理解基本的系统调用机制将帮助你理解是如何向应用提供这些服务的理解 ECF 将帮助你编写有趣的新应用程序。操作系统为应用程序提供了强大的 ECF 机制用来创建新进程、等待进程终止、通知其他进程系统中的异常事件，以及检测和响应这些事件。如果你理解这些 ECF 机制，那么你就能用它们来编写诸如 Unix 外壳和 Web 服务器之类的有趣程序了。</li><li>理解 ECF 将帮助你理解并发。ECF 是计算机系统中实现并发的基本机制。中断应用程序、进程和线程(它们的执行在时间上是重叠的)执行的异常处理程序和中断应用程序执行的信号处理程序都是在运行中的并发的例子。理解 ECF 是理解并发的第一步。我们会在第 12 章中更详细地研究并发。</li><li>理解 ECF 将帮助你理解软件异常如何工作。像 C+ 和 Java 这样的语言通过 try、catch 以及 throw 语句来提供软件异常机制。软件异常允许程序进行非本地跳转(违反通常的调用&#x2F;返回栈规则的跳转)来响应错误情况。非本地跳转是一种应用层 ECF，在 C 中是通过 setimp 和 longjmp 函数提供的。理解这些低级函数将帮助你理解高级软件异常如何得以实现。</li></ul><h2 id="1-异常"><a href="#1-异常" class="headerlink" title="1.异常"></a>1.异常</h2><p>​ **异常（Exception）**是为了响应处理器状态改变而在控制流中突然发生的变化，其基本思想如下图所示：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/4b1aeafc00f074c455db1328b70d86cf.png" class><p>​ 处理器状态的变化被称为<strong>事件（Event）</strong>，它可能与当前指令（<em>Icurr</em>）的执行直接相关，比如算术溢出或除数为零；也可能与当前指令的执行无关，比如系统计时器关闭或 I&#x2F;O 请求完成。</p><h4 id="1-1-异常处理"><a href="#1-1-异常处理" class="headerlink" title="1.1 异常处理"></a>1.1 异常处理</h4><p>​ 异常处理涉及到软件和硬件的密切合作，因此很容易将不同组件执行的工作相混淆。系统中每种可能的异常都对应了一个唯一的非负整数，即<strong>异常数字（Exception Number）</strong>。当计算机系统启动时，操作系统会初始化一个跳转表，也称<strong>异常表（Exception Table）</strong>。异常数字是异常表的索引，其中的每个条目 k 均包含了异常 k 的处理程序地址,异常表的格式格式如图所示：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/f54bd5d2165b4e0c7d4c4f155306f4a7.png" class><p>​ 异常表的起始地址放在一个叫做<strong>异常表基址寄存器(exception table base register)<strong>的特殊 CPU 寄存器里，当处理器检测到事件的发生时，首先将确定异常数字，然后根据异常表进行一个间接过程调用(异常)，到一个专门设计用来处理这类事件的</strong>操作系统子程序(异常处理程序(exception handler))</strong> 。调用过程如图所示：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/bf678f8a3d619f1ce6e8fb3edac5e7ae.png" class><p>异常与过程调用类似，但也有一些重要区别：</p><ul><li>异常的返回地址要么是当前指令（<em>Icurr</em>），要么是下一条指令（<em>Inext</em>）；</li><li>处理器还会将一些额外的处理器状态信息压入栈中。当处理程序返回后，这些信息是重启被中断程序所必需的；</li><li>异常处理程序在内核态运行，因此可以访问所有系统资源</li></ul><h4 id="1-2-异常的分类"><a href="#1-2-异常的分类" class="headerlink" title="1.2 异常的分类"></a>1.2 异常的分类</h4><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/8b5f71e1c11db0b5a93f3e534bca3dd8.png" class><h6 id="1-中断"><a href="#1-中断" class="headerlink" title="1.中断"></a>1.中断</h6><p>​ **中断（Interrupt）**异步发生，因为它是由处理器外部的 I&#x2F;O 设备发出的信号产生的。</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/2c8d40e058dfb01af8705778057184a3.png" class><p>​ 当前指令执行完毕后，处理器注意到中断引脚变高，于是从系统总线读取异常数字，然后调用对应的中断处理程序。当处理程序返回时，它将控制权返回给下一条指令。随后程序继续执行，就好像中断从未发生过一样。</p><p>其余几种异常作为当前指令的执行结果<strong>同步发生</strong>，我们称之为<strong>故障指令（Faulting Instruction）</strong>。</p><h6 id="2-陷阱和系统调用"><a href="#2-陷阱和系统调用" class="headerlink" title="2.陷阱和系统调用"></a>2.陷阱和系统调用</h6><p>​ <strong>陷阱（Trap）<strong>是</strong>有意</strong>的异常，是执行一条指令的结果。与中断处理程序一样，陷阱处理程序也将控制返回给下一条指令。其最重要的用途是在用户程序和内核之间提供接口，即<strong>系统调用（System Call）</strong>。</p><p>​ 用户程序通过系统调用向内核请求服务，如读取文件（<code>read</code>）、创建新进程（<code>fork</code>）、加载新程序（<code>execve</code>）和终止当前进程（<code>exit</code>）等。</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/04a892ee33872e2cb91694b94f67b74b.png" class><p>​ 在程序员看来，系统调用和常规函数没有什么区别。但常规函数运行在<strong>用户态(user mode)</strong>，因此其可执行的指令类型受限，也只能访问用户栈。而系统调用运行在<strong>内核态(kernel mode)</strong>，能够执行特权指令并访问内核栈。</p><h6 id="3-故障"><a href="#3-故障" class="headerlink" title="3.故障"></a>3.故障</h6><p>**故障（Faulting）**是由一些错误状况引起的异常，而这些错误情况有可能被处理程序修正，否则将返回到内核中的中止例程（图中的“abort”）：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/ea2f4d53d9c773d236c43ab3228cf316.png" class><h6 id="4-中止"><a href="#4-中止" class="headerlink" title="4.中止"></a>4.中止</h6><p>与故障相比，引发中止（Abort）的错误状况无法挽救。通常是硬件出现问题，如 RAM 位损坏引起的奇偶校验错误。中止处理程序永远不会将控制权返回给应用程序：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/d74c0fa7b9e720f42ef509078eb3c973.png" class><h4 id="1-3-Linux-x86-64-系统中的异常"><a href="#1-3-Linux-x86-64-系统中的异常" class="headerlink" title="1.3 Linux&#x2F;x86-64 系统中的异常"></a>1.3 Linux&#x2F;x86-64 系统中的异常</h4><table><thead><tr><th align="center">Exception Number</th><th align="center">Description</th><th align="center">Exception Class</th></tr></thead><tbody><tr><td align="center">0</td><td align="center">Divide Error</td><td align="center">Fault</td></tr><tr><td align="center">13</td><td align="center">General Protection Fault</td><td align="center">Fault</td></tr><tr><td align="center">14</td><td align="center">Page Fault</td><td align="center">Fault</td></tr><tr><td align="center">18</td><td align="center">Machine Check</td><td align="center">Abort</td></tr><tr><td align="center">32-255</td><td align="center">OS-defined Exception</td><td align="center">Interrupt or Trap</td></tr></tbody></table><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/ddc0bc7038e66dca8a8b96a41a8f0dab.png" class title="中文版"><ul><li>除法故障（Divide Error）：当应用程序尝试除以 0 或除法指令的结果对目标操作数来说太大时，就会发生除法故障。Unix 不会试图纠正除法故障，而是直接中止程序；</li><li>一般保护故障（General Protection Fault）：一般保护故障出现的原因有很多，通常是程序引用了未定义的虚拟内存区域，或试图向只读文本段写入。Linux 不会试图纠正该故障，而 Shell 一般将其报告为分段故障（Segmentation Faults）；</li><li>缺页故障（Page Fault）：程序引用不在内存而在磁盘上的虚拟页面会导致缺页故障。处理程序将磁盘上合适的虚拟内存页面映射到物理内存页面，然后重新执行故障指令；</li><li>机器检查（Machine Check）：一旦系统在执行指令期间检测到致命的硬件错误，便会发生机器检查。处理程序永远不会将控制权返回给应用程序。</li></ul><h4 id="1-4-系统调用"><a href="#1-4-系统调用" class="headerlink" title="1.4 系统调用"></a>1.4 系统调用</h4><p>x86-64</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/20220213214429.png" class title="Linux&#x2F;x86-64 的系统调用"><p>IA32</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/99ac5a2a8e16aad2548d4232b21078c6.png" class title="Linux&#x2F;IA32 的系统调用"><p>上图中的每个系统调用都有一个唯一的数字，对应了内核中跳转表的偏移量。</p><ul><li>注意，该跳转表与上文提到的异常表不同。</li></ul><p>​ C 标准库为大多数系统调用提供了一组包装函数（Wrapper Function），它们比直接使用系统调用更加方便。系统调用及其相关的包装函数被统称为系统级函数。举例来说，我们可以使用系统级函数<code>write</code>代替<code>printf</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    write(<span class="number">1</span>, <span class="string">&quot;hello, world\n&quot;</span>, <span class="number">13</span>);</span><br><span class="line">    _exit(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ X86-64 系统通过<code>syscall</code>指令使用系统调用，其所有参数均通过寄存器传递。按照惯例，寄存器 %rax 保存系统调用编号，寄存器 %rdi、%rsi、%rdx、%r10、%r8 和 %r9 依次保存各参数的值。系统调用的返回值将写入到寄存器 %rax 中，若为负则表示发生了与负<code>errno</code>相关的错误。因此，上面的程序可以直接用汇编语言表示为：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/20220213221637.png" class><h2 id="2-进程"><a href="#2-进程" class="headerlink" title="2.进程"></a>2.进程</h2><p>​ 异常是允许操作系统提供**进程(process)**的概念所需要的基本构造块，进程是计算机科学中最深刻最成功的概念之一。<br>​ 当我们在一个现代系统上运行一个程序时，会得到一个假象，就好像我们的程序是系统中当前运行着的唯一的程序。我们的程序好像是独占地使用处理器和存储器。处理器就好像是无间断地一条接一条地执行程序中的指令。最后，我们程序中的代码和数据好像是系统存储器中唯一的对象。这些假象都是通过进程的概念提供给我们的。</p><p>​ 进程是正在执行的程序的实例，系统中的每个程序都在进程的**上下文(context)**中运行。上下文由程序正确运行所需的状态组成，包括存储在内存中的程序代码和数据、栈、通用寄存器中的内容、程序计数器、环境变量以及打开 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/https://en.wikipedia.org/wiki/File_descriptor">文件描述符</a> 。</p><p>进程为应用提供了两个关键抽象：</p><ul><li>一个独立的逻辑控制流，让我们产生程序独占处理器的错觉；</li><li>一个私有的地址空间，让我们产生程序独占内存的错觉。</li></ul><h4 id="2-1-逻辑控制流"><a href="#2-1-逻辑控制流" class="headerlink" title="2.1 逻辑控制流"></a>2.1 逻辑控制流</h4><p>​ 即使在系统中通常有许多其他程序在运行，进程也可以向每个程序提供一种假象，好像它在独占地使用处理器。如果想用调试器单步执行程序，我们会看到一系列的程序计数器(PC)的值，这些值唯一地对应于包含在程序的可执行目标文件中的指令，或者是包含在运行时动态链接到程序的共享对象中的指令。这个 PC 值的序列叫做逻辑控制流，或者简称<strong>逻辑流</strong>。</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/bbbb9e617557b2f9d8020ed515f93d3d.png" class><p>进程轮流使用处理器。每个进程执行其流程的一部分，然后在其他进程执行时被<strong>抢占(preempted)（即暂时挂起）</strong>。</p><h4 id="2-2-并发流"><a href="#2-2-并发流" class="headerlink" title="2.2 并发流"></a>2.2 并发流</h4><p>​ 一个逻辑流的执行在时间上与另一个流重叠，称为<strong>并发流(Concurrent Flow)</strong>，这两个流被称为并发地运行。更准确地说，流 X 和 Y 互相并发，当且仅当 X 在 Y 开始之后和 Y 结束之前开始,或者 Y 在 X 开始之后和 X 结束之前开始。例如，在图 8-12 中，进程 A 和 B 并发地运行，A 和 C 也一样。另一方面，B 和 C 没有并发地运行，<strong>因为 B 的最后一条指令在 C 的第一条指令之前执行。</strong><br>​ 多个流并发地执行的一般现象称为<strong>并发(concurrency)</strong>。一个进程和其他进程轮流运行的概念称为<strong>多任务(multitasking)</strong>。一个进程执行它的控制流的一部分的每一时间段叫做<strong>时间片(time slice)</strong>。因此，多任务也叫做<strong>时间分片(time slicing)</strong>。<br>​ 注意，并发的思想与流运行的处理器核数或者计算机数无关。如果两个流在时间上重叠，那么它们就是并发的，即使它们是运行在同一个处理器上的。然而，有时我们会发现确认并行流是很有帮助的，它是<strong>并发流的一个真子集</strong>。如果两个流并发地运行在不同的处理器核或者计算机上，那么我们称它们为<strong>并行流(parallel fow)</strong>，它们并行地运行(runninginparallel)，且并行地执行(parallel execution)。</p><h4 id="2-3-私有地址空间"><a href="#2-3-私有地址空间" class="headerlink" title="2.3 私有地址空间"></a>2.3 私有地址空间</h4><p>​ 进程为程序提供了独享的私有地址空间，与空间内特定地址相关的内存字节通常不能被其他任何进程读取或写入。尽管私有地址空间的内容不同，但其具有相同的组织结构（图中的“%esp”应为“%rsp”）：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/359ff97e4360fe3385dbe5369f5fadd8.png" class><p>​ 地址空间底部是为用户程序保留的，代码段总是从地址 0x400000 开始。地址空间顶部是为内核保留的，包含了内核为进程执行指令（如系统调用）时使用的代码、数据和栈。</p><h4 id="2-4-用户模式和内核模式"><a href="#2-4-用户模式和内核模式" class="headerlink" title="2.4 用户模式和内核模式"></a>2.4 用户模式和内核模式</h4><p>​ 处理器通过保存在控制寄存器中的模式位（Mode Bit）来识别进程当前享有的特权。当模式位被设置时，进程运行在内核态（Kernel Mode），反之则运行在用户态（User Mode）。在内核态中运行的程序可以执行指令集中的任意指令，并且能够访问系统中的任意位置。而在用户态中运行的程序则受到限制，只能使用系统调用间接地访问内核代码和数据。</p><p>​ 应用程序的进程只能通过异常来从用户态切换到内核态。当异常发生且控制转移到异常处理程序时，处理器切换到内核态。随后异常处理程序在内核态中运行，处理器将在它返回时切换回用户态。</p><p>​ Linux 提供了一种聪明的机制，叫做&#x2F;proc 文件系统，它允许用户模式进程访问内核数据结构的内容。&#x2F;proc 文件系统将许多内核数据结构的内容输出为一个用户程序可以读的文本文件的层次结构。比如，你可以使用&#x2F;proc 文件系统找出一般的系统属性，如 CPU 类型(&#x2F;proc&#x2F;cpuinfo)，或者某个特殊的进程使用的存储器段(&#x2F;proc&#x2F;<process id>&#x2F;maps)。2.6 版本的 Limux 内核引人 &#x2F;sys 文件系统，它输出关于系统总线和设备的额外的低层信息。</process></p><h4 id="2-5-上下文切换"><a href="#2-5-上下文切换" class="headerlink" title="2.5 上下文切换"></a>2.5 上下文切换</h4><p>​ 内核为每个进程维持一个<strong>上下文(context)</strong>。上下文就是内核重新启动一个被抢占的进程所需的状态。它由一些对象的值组成，这些对象包括通用目的寄存器、浮点寄存器、程序计数器、用户栈、状态寄存器、内核栈和各种内核数据结构，比如描绘地址空间的页表、包含有关当前进程信息的进程表，以及包含进程已打开文件的信息的文件表。</p><p>​ 在进程执行期间，内核可以暂时挂起当前进程并重启先前被抢占的进程，这一行为被称为<strong>调度（Scheduling）</strong>，是由内核中称为**调度器(scheduler)<strong>的代码处理的。当内核选择一个新的进程运行时，我们就说内核调度了这个进程。在内核调度了一个新的进程运行后，它就抢占当前进程，并使用一种称为上下文切换的机制来将控制转移到新的进程，内核调度新进程是通过</strong>上下文切换（Context Switch）**机制来实现的，该机制：</p><ul><li>保存当前进程的上下文；</li><li>恢复之前被抢占进程的上下文；</li><li>将控制权转移给新进程。</li></ul><p>​ 程序执行系统调用时可能会发生上下文切换。比如系统调用<code>read</code>需要访问磁盘中的数据，内核可以通过上下文切换来调度另一个进程，这样就无需等待数据从磁盘加载到内存中。</p><p>​ 中断也可能引发上下文切换。比如，所有的系统都有某种产生周期性定时器中断的机制，典型的为每 1 毫秒或每 10 毫秒。每次发生定时器中断时，内核就能判定当前进程已经运行了足够长的时间了，并切换到一个新的进程。</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/36335ce328791991ef540186329e22cb.png" class><p>​ 该图展示了一对进程 A 和 B 之间上下文切换的示例。在这个例子中，初始地，进程 A 运行在用户模式中，直到它通过执行系统调用 read 陷入到内核。内核中的陷阱处理程序请求来自磁盘控制器的 DMA 传输，并且安排在磁盘控制器完成从磁盘到存储器的数据传输后，磁盘中断处理器。</p><p>​ 磁盘取数据要用一段相对较长的时间(数量级为几十毫秒)，所以内核执行从进程 A 到进程 B 的上下文切换，而不是在这个间歇时间内等待，什么都不做。注意在切换之前，内核正代表进程 A 在用户模式下执行指令。在切换的第一部分中，内核代表进程 A 在内核模式下执行指令。然后在某一时刻，它开始代表进程 B(仍然是内核模式下)执行指令。在切换之后，内核代表进程 B 在用户模式下执行指令。</p><p>​ 随后，进程 B 在用户模式下运行一会儿，直到磁盘发出一个中断信号，表示数据已经从磁盘传送到了存储器。内核判定进程 B 已经运行了足够长的时间了，就执行一个从进程 B 到进程 A 的上下文切换，将控制返回给进程 A 中紧随在系统调用 read 之后的那条指令。进程 A 继续运行，直到下一次异常发生，依此类推。</p><p><strong>高速缓存污染(pollution)和异常控制流</strong></p><p>​ 一般而言，硬件高速缓存存储器不能和诸如中断和上下文切换这样的异常控制流很好地交互。如果当前进程被一个中断暂时中断，那么对于中断处理程序来说高速缓存是冷的(cold)译者注:“高速缓存是冷的”意思是程序所需要的数据都不在高速缓存中)。如果处理程序从主存中访问了足够多的表项，那么当被中断的进程继续时，高速缓存对它来说也是冷的了。在这种情况下，我们就说中断处理程序污染(pollute)了高速缓存。使用上下文切换也会发生类似的现象。当一个进程在上下文切换后继续执行时，高速缓存对于应用程序而言也是冷的，必须再次热身。</p><h2 id="3-系统调用错误处理"><a href="#3-系统调用错误处理" class="headerlink" title="3.系统调用错误处理"></a>3.系统调用错误处理</h2><p>​ 当执行 Unix 系统级函数遇到错误时，它们会返回 -1 并设置全局整型变量<code>errno</code>的值。因此我们可以在程序中检查调用是否发生错误，如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> ((pid = fork()) &lt; <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;fork error: %s\n&quot;</span>, strerror(errno));</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>strerror</code>函数会根据<code>errno</code>的值返回相关的文本字符串。我们定义一个<strong>错误报告（Error-reporting）函数</strong>以简化上述代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">unix_error</span><span class="params">(<span class="type">char</span> *msg)</span> <span class="comment">/* Unix-style error */</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;%s: %s\n&quot;</span>, msg, strerror(errno));</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ((pid = fork()) &lt; <span class="number">0</span>)</span><br><span class="line">    unix_error(<span class="string">&quot;fork error&quot;</span>);</span><br></pre></td></tr></table></figure><p>我们还可以进一步地将代码简化为一个**错误处理（Error-handling）**函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">pid_t</span> <span class="title function_">Fork</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="keyword">if</span> ((pid = fork()) &lt; <span class="number">0</span>)</span><br><span class="line">        unix_error(<span class="string">&quot;Fork error&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> pid;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">pid = Fork();</span><br></pre></td></tr></table></figure><p>​ 这样我们便能够使用包装函数<code>Fork</code>代替<code>fork</code>及其错误检查代码。本书使用的包装函数均定义在 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/http://csapp.cs.cmu.edu/2e/ics2/code/include/csapp.h"><code>csapp.h</code></a> 和 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/http://csapp.cs.cmu.edu/2e/ics2/code/src/csapp.c"><code>csapp.c</code></a> 中。</p><h2 id="4-进程控制"><a href="#4-进程控制" class="headerlink" title="4.进程控制"></a>4.进程控制</h2><h4 id="4-1-获取进程-ID"><a href="#4-1-获取进程-ID" class="headerlink" title="4.1 获取进程 ID"></a>4.1 获取进程 ID</h4><p>​ 每个进程都有一个唯一且大于 0 的进程 ID（PID）。函数<code>getpid</code>返回调用进程的 PID，而函数<code>getppid</code>则返回创建调用进程的进程（即父进程） 的 PID。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getpid</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getppid</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><p>二者返回值的类型均为<code>pid_t</code>，它在 Linux 系统的<code>sys/types.h</code>文件中被定义为<code>int</code>。</p><h4 id="4-2-创建和终止进程"><a href="#4-2-创建和终止进程" class="headerlink" title="4.2 创建和终止进程"></a>4.2 创建和终止进程</h4><p>在程序员看来，进程有三种状态：</p><ul><li>运行（Running）：该进程要么在 CPU 中执行，要么在等待内核调度；</li><li>停止（Stopped）：进程的执行被挂起(suspend)，且不会被调度。当收到 SIGSTOP、SIGTSTPSIDTTIN 或者 SIGTTOU 信号时，进程就停止，并且保持停止直到它收到一个 SIGCONT 信号，在这个时刻，进程再次开始运行。(信号是一种软件中断的形式，将在 5 节中详细描述。)</li><li>终止（Terminated）：进程永远地停止了。进程会因为三种原因终止:1)收到一个信号，该信号的默认行为是终止进程，2)从主程序返回，3)调用 exit 函数。</li></ul><p>函数<code>exit</code>会以参数<code>status</code>作为退出状态终止进程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">exit</span><span class="params">(<span class="type">int</span> status)</span>;</span><br></pre></td></tr></table></figure><p>父进程可以调用<code>fork</code>函数来创建一个新的子进程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">fork</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><p>​ 子进程将获得一个与父进程相同但独立的用户级虚拟内存空间副本，包括代码、数据、堆、共享库和用户栈等。它还会得到与父进程相同的打开文件描述符副本，因此能够读写任意父进程打开的文件。父进程和子进程之间最显著的区别便是 <strong>PID</strong> 不同。</p><p>​ 函数<code>fork</code><strong>执行一次却返回两次</strong>：在父进程中返回子进程的 PID，在子进程中返回 0。由于子进程的 PID 始终大于 0 ，我们可以通过返回值判断程序在哪个进程中执行。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">include <span class="string">&quot;csapp.h&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    pid = Fork();</span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>)</span><br><span class="line">    &#123; <span class="comment">/* Child */</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;child : x=%d\n&quot;</span>, ++x);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Parent */</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;parent: x=%d\n&quot;</span>, --x);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该程序编译后运行的可能结果为：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">linux&gt; </span><span class="language-bash">./fork</span></span><br><span class="line">parent: x=0</span><br><span class="line">child : x=2</span><br></pre></td></tr></table></figure><p>从结果我们可以看出：</p><ul><li>调用一次，返回两次。fork 函数被父进程调用一次，但是却返回两次。一次是返回到父进程，一次是返回到新创建的子进程。对于只创建一个子进程的程序来说，这还是相当简单直接的。但是具有多个 fork 实例的程序可能就会令人迷惑，需要仔细地推敲了。</li><li>并发执行。父进程和子进程是并发运行的独立进程。内核能够以任意方式交替执行它们的逻辑控制流中的指令。当我们在系统上运行这个程序时，父进程先完成它的 printf 语句，然后是子进程。然而，在另一个系统上可能正好相反。一般而言，作为程序员，我们决不能对不同进程中指令的交替执行做任何假设。</li><li>相同的但是独立的地址空间。如果能够在 fork 函数在父进程和子进程中返回后立即暂停这两个进程，我们会看到每个进程的地址空间都是相同的。每个进程有相同的用户栈、相同的本地变量值、相同的堆、相同的全局变量值，以及相同的代码。因此，在我们的示例程序中，当 fork 函数在第 8 行返回时，本地变量 x 在父进程和子进程中都为 1。然而，因为父进程和子进程是独立的进程，它们都有自己的私有地址空间。父进程和子进程对 x<br>所做的任何改变都是独立的，不会反映在另一个进程的存储器中。这就是为什么当父进程和子进程调用它们各自的 printf 语句时，它们中的变量 x 会有不同的值的原因。</li><li>共享文件当运行这个示例程序时，我们注意到父进程和子进程都把它们的输出显示在屏<br>幕上。原因是子进程继承了父进程所有的打开文件。当父进程调用 fork 时，stdout 文<br>件是被打开的，并指向屏幕。子进程继承了这个文件，因此它的输出也是指向屏幕的。</li></ul><p>绘制进程图（Process Graph）对理解<code>fork</code>函数很有帮助，如：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/cbc3c80a45df3ecad9fdece41e741bdf.png" class><h4 id="4-3-回收子进程"><a href="#4-3-回收子进程" class="headerlink" title="4.3 回收子进程"></a>4.3 回收子进程</h4><p>进程终止后，内核不会立即将其移除。它需要被其父进程回收（Reap），否则将成为僵尸（Zombie）进程。当父进程回收其终止的子进程时，内核将子进程的退出状态传递给父进程，然后再丢弃它。</p><p>如果父进程终止，内核会安排<code>init</code>进程（PID 为 1）“收养”孤儿进程；如果父进程在终止前没有回收僵尸子进程，那么则由<code>init</code>进程回收它们。</p><p>进程通过调用函数<code>waitpid</code>等待其子进程终止或停止：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">waitpid</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">int</span> *statusp, <span class="type">int</span> options)</span>;</span><br><span class="line"><span class="comment">// Returns: PID of child if OK, 0 (if WNOHANG), or −1 on error</span></span><br></pre></td></tr></table></figure><p>默认情况下（参数<code>options</code>为 0 时），函数<code>waitpid</code>会暂停调用进程，直至其等待集（Wait Set）中的某个子进程终止。该函数始终返回第一个终止的子进程 PID。此时，终止的子进程已被回收，内核从系统中删除了它的所有痕迹。</p><ol><li><p><strong>判定等待集合的成员</strong></p><p>等待集合的成员是由参数 pid 来确定的:</p><ul><li>如果 pid&gt;0，那么等待集合就是一个单独的子进程，它的进程 等于 pid。</li><li>如果 pid&#x3D;-1，那么等待集合就是由父进程所有的子进程组成的。</li></ul><p>waitpid 函数还支持其他类型的等待集合，包括 Unix 进程组，对此我们将不做讨论。</p></li><li><p><strong>修改默认行为</strong></p><p>我们可以通过修改参数<code>options</code>的值来改变函数<code>waitpid</code>的行为：</p><ul><li><code>WNOHANG</code>：如果等待集中的子进程还未终止，则立即返回 0；</li><li><code>WUNTRACED</code>：暂停调用进程执行，直到等待集中的进程终止或停止（默认情况下仅返回终止的子进程 PID）；</li><li><code>WCONTINUED</code>：暂停调用进程执行，直到等待集中的进程终止或等待集中停止的进程收到 SIGCONT 信号恢复。</li></ul></li><li><p><strong>检查已回收子进程退出状态</strong></p><p>若参数<code>statusp</code>不为<code>NULL</code>，那么<code>waitpid</code>还会将返回的子进程状态信息编码到<code>status</code>中（<code>*statusp = status</code>）。<code>wait.h</code>文件定义了几个用于解释参数<code>status</code>的宏：</p><ul><li><code>WIFEXITED(status)</code>：如果子进程正常终止（比如调用<code>exit</code>或返回），则返回<code>True</code>；</li><li><code>WEXITSTATUS(status)</code>：如果<code>WIFEXITED()</code>返回<code>True</code>，则返回终止子进程的退出状态；</li><li><code>WIFSIGNALED(status)</code>：如果子进程由于未捕获的信号而终止，则返回<code>True</code>；</li><li><code>WTERMSIG(status)</code>：如果<code>WIFSIGNALED()</code>返回<code>True</code>，则返回导致子进程终止的信号编号；</li><li><code>WIFSTOPPED(status)</code>：如果返回的子进程当前已停止，则返回<code>True</code>；</li><li><code>WSTOPSIG(status)</code>：如果<code>WIFSTOPPED()</code>返回<code>True</code>，则返回导致子进程停止的信号编号；</li><li><code>WIFCONTINUED(status)</code>：如果子进程收到 SIGCONT 信号后恢复，则返回<code>True</code>。</li></ul></li><li><p><strong>错误条件</strong></p><p>如果调用进程没有子进程，那么 waitpid 返回-1，并且设置 errno 为 ECHILD。如果 waitpid 函数被一个信号中断，那么它返回-1，并设置 errno 为 EINTR。</p></li></ol><p>函数<code>wait</code>是<code>waitpid</code>的简化版本，<code>wait(&amp;status)</code>等效于<code>waitpid(-1, &amp;status, 0)</code>。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">define</span> N 2</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> status, i;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Parent creates N children */</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">        <span class="keyword">if</span> ((pid = Fork()) == <span class="number">0</span>) <span class="comment">/* child */</span></span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">100</span> + i);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Parent reaps N children in no particular order */</span></span><br><span class="line">    <span class="keyword">while</span> ((pid = waitpid(<span class="number">-1</span>, &amp;status, <span class="number">0</span>)) &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (WIFEXITED(status))</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;child %d terminated normally with exit status=%d\n&quot;</span>, pid, WEXITSTATUS(status));</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;child %d terminated abnormally\n&quot;</span>, pid);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* The only normal termination is if there are no more children */</span></span><br><span class="line">    <span class="keyword">if</span> (errno != ECHILD)</span><br><span class="line">        unix_error(<span class="string">&quot;waitpid error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如示例程序所示，父进程首先调用<code>Fork</code>创建了两个退出状态唯一的子进程（<code>exit(100+i)</code>）。 随后在 While 循环的测试条件中通过<code>waitpid</code>等待其所有的子进程终止，并打印子进程的退出状态。最终所有的子进程均被回收，<code>waitpid</code>返回 -1 且将全局变量<code>errno</code>设为<code>ECHILD</code>，函数执行完毕。</p><p>在 Linux 系统上运行该程序时，它会产生以下输出：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">linux&gt; </span><span class="language-bash">./waitpid1</span></span><br><span class="line">child 22966 terminated normally with exit status=100</span><br><span class="line">child 22967 terminated normally with exit status=101</span><br></pre></td></tr></table></figure><p>值得注意的是，父进程回收子进程的顺序是随机的。我们可以对上述程序进行一定 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/http://csapp.cs.cmu.edu/2e/ics2/code/ecf/waitpid2.c">修改</a>，从而使其按子进程的 PID 顺序输出。</p><h4 id="4-4-休眠"><a href="#4-4-休眠" class="headerlink" title="4.4 休眠"></a>4.4 休眠</h4><p>​</p><p>函数<code>sleep</code>可以让进程暂停执行一段时间：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> <span class="title function_">sleep</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> secs)</span>;</span><br><span class="line"><span class="comment">// Returns: seconds left to sleep</span></span><br></pre></td></tr></table></figure><p>如果请求的暂停时间已经过去，则函数返回 0；如果该进程被信号中断，则返回剩余的暂停时间。</p><p>函数<code>pause</code>会使调用进程进入休眠状态，直至收到信号。该函数始终返回 -1:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pause</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><h4 id="4-5-加载并运行程序"><a href="#4-5-加载并运行程序" class="headerlink" title="4.5 加载并运行程序"></a>4.5 加载并运行程序</h4><p>函数<code>execve</code>在当前进程的上下文中加载并运行一个新程序：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">execve</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *filename, <span class="type">const</span> <span class="type">char</span> *argv[], <span class="type">const</span> <span class="type">char</span> *envp[])</span>;</span><br></pre></td></tr></table></figure><p>参数<code>filename</code>是加载并运行的可执行文件名称，<code>argv</code>和<code>envp</code>则分别是参数和环境变量列表。函数<code>execve</code>通常没有返回值，仅在出现错误时返回 -1。</p><p>变量<code>argv</code>指向一个以<code>NULL</code>为结尾的指针数组，其中的每个元素都指向一个参数字符串。一般来说，<code>argv[0]</code>是可执行目标文件名称；变量<code>envp</code>也指向一个以<code>NULL</code>结尾的指针数组，其中的每个元素均指向一个环境变量字符串，每个字符串都是一个<code>name=value</code>形式的键值对。两者的数据结构如下：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/ae914cfeb058062b268d69da354c80f3.png" class><p><code>execve</code>加载文件名后，会调用启动代码。 启动代码设置栈并将控制权传递给新程序的<code>main</code>函数，其原型为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[], <span class="type">char</span> *envp[])</span>;</span><br></pre></td></tr></table></figure><p><code>main</code>函数执行时的用户栈结构如下图所示，从栈底到栈顶分别是：环境变量字符串、参数字符串、指向环境变量字符串的指针数组和指向参数字符串的指针数组。该函数的三个参数分别保存在不同的寄存器中：参数<code>argc</code>给出数组<code>argv[]</code>中的非空指针数量；参数<code>argv</code>指向数组<code>argv[]</code>中第一个元素；参数<code>envp</code>则指向数组<code>envp[]</code>中的第一个元素：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/97c29ce1fd15a29770b5c8bef051ec30.png" class><p>Linux 提供了几个用于操作环境变量数组的函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="type">char</span> *<span class="title function_">getenv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">setenv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name, <span class="type">const</span> <span class="type">char</span> *newvalue, <span class="type">int</span> overwrite)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">unsetenv</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *name)</span>;</span><br></pre></td></tr></table></figure><p>如果数组中包含以参数<code>name</code>为键的字符串，则函数<code>getenv</code>返回其对应的值，函数<code>unsetenv</code>删除该字符串，函数<code>setenv</code>将值替换为参数<code>newvalue</code>（<code>overwrite</code>非零时）；如果不存在以<code>name</code>为键的字符串，则函数<code>setenv</code>会将<code>name=newvalue</code>添加到数组中。</p><p>​ <strong>程序与进程</strong></p><p>​ 这是一个适当的地方，停下来，确认一下你理解了程序和进程之间的区别。程序是一堆代码和数据;程序可以作为目标模块存在于磁盘上，或者作为段存在于地址空间中。进程是执行中程序的一个具体的实例;程序总是运行在某个进程的上下文中。如果你想要理解 fork 和 execve 函数，理解这个差异是很重要的。fork 函数在新的子进程中运行相同的程序，新的子进程是父进程的一个复制品。execve 函数在当前进程的上下文中加载并运行一个新的程序。它会覆盖当前进程的地址空间，但并没有创建一个新进程。新的程序仍然有相同的 PID，并且继承了调用<br>execve 函数时已打开的所有文件描述符。</p><h4 id="4-6-使用-fork-和-execve-运行程序"><a href="#4-6-使用-fork-和-execve-运行程序" class="headerlink" title="4.6 使用 fork 和 execve 运行程序"></a>4.6 使用 fork 和 execve 运行程序</h4><p>​ Unix shell 和 Web 服务器等程序大量使用了<code>fork</code>和<code>execve</code>函数。外壳是一个交互型的应用级程序，它代表用户运行其他程序。最早的外壳是 sh 程序，后面出现了些变种，比如 csh、tcsh、ksh 和 bash。外壳执行一系列的读&#x2F;求值(read&#x2F;evaluate)步骤然后终止。读步骤读取来自用户的一个命令行。求值步骤解析命令行，并代表用户运行程序。本书提供了一个简单的 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/http://csapp.cs.cmu.edu/2e/ics2/code/ecf/shellex.c">shell 程序</a>，其缺陷在于没有回收任何后台运行的子进程。我们需要使用下一节介绍的信号来解决这一问题。</p><h2 id="5-信号"><a href="#5-信号" class="headerlink" title="5.信号"></a>5.信号</h2><p>​ 信号（Signal）是一种高级的异常控制流，它允许进程和内核将某些类型的系统事件通知给其他进程。</p><p>​ 一个信号就是一条小消息，它通知进程系统中发生了一个某种类型的事件。Linux 系统上支持的 30 种不同类型的信号。在外壳命令行上输入“man 7 signal”就能得到这个列表。</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/7a833933ce9bfcbdef02aa0e1776a7b5.png" class><p>​ 低级别的硬件异常由内核中的异常处理程序处理，通常不会对用户进程可见，而信号则可以将此类异常暴露给用户进程。如果一个进程试图除以 0，内核就会向它发送一个 SIGFPE（编号 8）信号。</p><h4 id="5-1-信号术语"><a href="#5-1-信号术语" class="headerlink" title="5.1 信号术语"></a>5.1 信号术语</h4><p>发送信号到目标进程需要完成两个步骤：</p><ul><li>发送（传递）信号：内核通过更新目标进程上下文中的某些状态来向目标进程发送信号。发送信号的原因有两种：① 内核检测到系统事件的发生，如被 0 除错误或子进程终止等；② 进程调用了<code>kill</code>函数（将在下一节介绍）。进程可以向自己发送信号；</li><li>接收信号：当内核强制目标进程以某种方式对信号做出响应时，它便接收到了信号。该进程可以通过执行用户级别的信号处理程序（Signal Handler）来忽略、终止或捕获信号。</li></ul><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/df7c97d463c49fa506c323610ab7b4c2.png" class><p>​ 已发送但还未接收的信号被称为<strong>待处理信号（Pending Signal）</strong>。在任意时间点，相同类型的待处理信号最多只能有一个。这意味着如果一个进程已经有一个类型为 k 的待处理信号，那么后续所有发送给该进程的 k 类型信号都将被丢弃。进程还可以选择性地**阻塞（Block）**某些信号的接收。</p><h4 id="5-2-发送信号"><a href="#5-2-发送信号" class="headerlink" title="5.2 发送信号"></a>5.2 发送信号</h4><p>​ Linux 系统提供了大量向进程发送信号的机制。所有这些机制都是基于**进程组(process group)**这个概念的。</p><h6 id="1-进程组"><a href="#1-进程组" class="headerlink" title="1. 进程组"></a>1. <strong>进程组</strong></h6><p>​ 每个进程都属于一个进程组（Process Group），它由一个正整数的进程组 ID 所标识。<code>getpgrp</code>函数返回当前进程的进程组 ID：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">getpgrp</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><p>默认情况下，子进程与其父进程属于同一个进程组。进程可以通过<code>setpgid</code>函数改变自己或另一个进程的进程组：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setpgid</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">pid_t</span> pgid)</span>;</span><br></pre></td></tr></table></figure><p>该函数会把进程<code>pid</code>的进程组更改为<code>pgid</code>。若将参数<code>pid</code>或<code>pgid</code>设为 0，则相当于使用调用进程的 PID 作为参数。举例来说，如果进程 15213 调用函数<code>setpgid(0, 0)</code>，那么将会创建一个进程组 ID 为 15213 的新进程组，并使该进程加入此组。</p><h6 id="2-用-bin-kill-程序发送信号"><a href="#2-用-bin-kill-程序发送信号" class="headerlink" title="2. 用 &#x2F;bin&#x2F;kill 程序发送信号"></a>2. <strong>用 &#x2F;bin&#x2F;kill 程序发送信号</strong></h6><p>​ &#x2F;bin&#x2F;kill 程序可以可以向另外的进程发送任意的信号。比如，命令:</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">linux&gt;</span><span class="language-bash">/bin/kill -9 15213</span></span><br></pre></td></tr></table></figure><p>发送信号 9(SIGKILL)给进程 15213。一个为负的 PID 会导致信号被发送到进程组 PID 中的每个进程。比如，命令:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">unix&gt;/bin/kill -9 -15213</span><br></pre></td></tr></table></figure><p>发送一个 SIGKIL 信号给进程组 15213 中的每个进程。注意，在此我们使用完整路径&#x2F;bin&#x2F;kill，因为有些 Linux 外壳有自己内置的 kill 命令。</p><h6 id="3-用键盘发送信号"><a href="#3-用键盘发送信号" class="headerlink" title="3. 用键盘发送信号"></a>3. <strong>用键盘发送信号</strong></h6><p>​ Unix Shell 使用**任务（Job）**表示单个命令行（如<code>ls | sort</code>）创建的进程，同一时间内只能有一个前台任务和多个后台任务。</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/30afcac9e0147e4dcf6227e8fa9f4c73.png" class><p>在键盘上输入 Ctrl+C 会使内核向前台进程组中的所有进程发送 SIGINT 信号，这将终止前台任务。同样，输入 Ctrl+Z 会使内核向前台进程组中的所有进程发送 SIGTSTP 信号，这将停止（挂起）前台任务。</p><h6 id="4-用-kill-函数发送信号"><a href="#4-用-kill-函数发送信号" class="headerlink" title="4. 用 kill 函数发送信号"></a>4. 用 kill 函数发送信号</h6><p>​ 进程可以调用<code>kill</code>函数向其他进程（包括其自身）发送信号：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">kill</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">int</span> sig)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>若参数<code>pid</code>大于 0，则该函数将编号为<code>sig</code>的信号发送给进程<code>pid</code>；若参数<code>pid</code>等于 0，则该函数将信号发送给调用进程所在进程组中的所有进程；如果参数<code>pid</code>小于 0，则该函数将信号发送给进程组 ID 为<code>|pid|</code>的进程组中的所有进程。</p><h6 id="5-使用-alarm-函数发送信号"><a href="#5-使用-alarm-函数发送信号" class="headerlink" title="5. 使用 alarm 函数发送信号"></a>5. 使用 alarm 函数发送信号</h6><p>​ 进程可以调用<code>alarm</code>函数向自己发送 SIGALRM 信号：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> <span class="title function_">alarm</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> secs)</span>;</span><br><span class="line"><span class="comment">// Returns: remaining seconds of previous alarm, or 0 if no previous alarm</span></span><br></pre></td></tr></table></figure><p>内核将在<code>secs</code>秒后向调用进程发送 SIGALRM 信号，取消所有之前设置的<code>alarm</code>，并返回其剩余的秒数。</p><h4 id="5-3-接收信号"><a href="#5-3-接收信号" class="headerlink" title="5.3 接收信号"></a>5.3 接收信号</h4><p>当内核将进程 p 从内核态切换到用户态时，它会检查 p <strong>未阻塞且未处理（Pending &amp; ~Blocked）<strong>的信号集。通常该集合为空，内核将控制权转移给 p 的逻辑控制流中的下一条指令。但如果该集合非空，内核就会选择信号集中的某个信号 k 并强制 p 接收它。信号将触发进程完成一些</strong>动作（Action）</strong>，预定义的默认动作有：</p><ul><li>进程终止；</li><li>进程终止并转储核心（Dump Core，即将代码和数据内存段的镜像写入磁盘）；</li><li>进程停止（暂停），直到接收 SIGCONT 信号重新启动；</li><li>进程忽略该信号。</li></ul><p>每种信号的默认动作见 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/##5.%E4%BF%A1%E5%8F%B7">图 8.25</a>。除 SIGSTOP 和 SIGKILL 信号外，进程还可以通过函数<code>signal</code>修改信号的默认动作：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="title function_">void</span> <span class="params">(*<span class="type">sighandler_t</span>)</span><span class="params">(<span class="type">int</span>)</span>;</span><br><span class="line"><span class="type">sighandler_t</span> <span class="title function_">signal</span><span class="params">(<span class="type">int</span> signum, <span class="type">sighandler_t</span> handler)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to previous handler if OK, SIG_ERR on error (does not set errno)</span></span><br></pre></td></tr></table></figure><ul><li>若参数<code>handler</code>为<code>SIG_IGN</code>，则<code>signum</code>类型的信号将会被忽略；</li><li>若参数<code>handler</code>为<code>SIG_DFL</code>，则<code>signum</code>类型的信号的动作将恢复为默认；</li><li>若参数<code>handler</code>为<strong>用户定义的信号处理程序地址</strong>，则进程接收到<code>signum</code>类型的信号后会调用该程序，这种方法被称为安装处理程序（Installing Handler）。在这种情况下，调用处理程序被称为捕获信号（Catching Signal），执行处理程序被称为<strong>处理信号（Handling Signal）</strong>。</li></ul><p>如果我们在示例程序运行时按下 Ctrl+C，该进程就不会直接终止而是输出一段信息后才终止：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">handler</span><span class="params">(<span class="type">int</span> sig)</span> <span class="comment">/* SIGINT handler */</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Caught SIGINT\n&quot;</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* Install the SIGINT handler */</span></span><br><span class="line">    <span class="keyword">if</span> (signal(SIGINT, handler) == SIG_ERR)</span><br><span class="line">        unix_error(<span class="string">&quot;signal error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    Pause(); <span class="comment">/* Wait for the receipt of a signal */</span></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>信号处理程序还可以被其他处理程序中断（信号 <em>s≠t</em>）：</p><img src="/2024/08/30/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-8-%E5%BC%82%E5%B8%B8%E6%8E%A7%E5%88%B6%E6%B5%81/20220223154049.png" class><h4 id="5-4-阻塞信号"><a href="#5-4-阻塞信号" class="headerlink" title="5.4 阻塞信号"></a>5.4 阻塞信号</h4><p>Linux 为阻塞信号提供了显式和隐式的实现机制：</p><ul><li>隐式：默认情况下，内核会阻塞任何与处理程序当前正在处理的信号类型相同的未处理信号。比如上图 8.31 中，若信号 <em>t</em> 的类型与 <em>s</em> 相同，则 <em>t</em> 将在处理程序 <em>S</em> 返回前持续挂起；</li><li>显式：应用程序可以调用<code>sigprocmask</code>等函数阻塞信号或解除信号的阻塞。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigprocmask</span><span class="params">(<span class="type">int</span> how, <span class="type">const</span> <span class="type">sigset_t</span> *<span class="built_in">set</span>, <span class="type">sigset_t</span> *oldset)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigemptyset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigfillset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigaddset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>, <span class="type">int</span> signum)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigdelset</span><span class="params">(<span class="type">sigset_t</span> *<span class="built_in">set</span>, <span class="type">int</span> signum)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigismember</span><span class="params">(<span class="type">const</span> <span class="type">sigset_t</span> *<span class="built_in">set</span>, <span class="type">int</span> signum)</span>;</span><br><span class="line"><span class="comment">// Returns: 1 if member, 0 if not, −1 on error</span></span><br></pre></td></tr></table></figure><p><code>sigprocmask</code>函数可以改变当前阻塞信号的集合（设为<code>blocked</code>），具体行为取决于参数<code>how</code>的值：</p><ul><li><code>SIG_BLOCK</code>：将参数<code>set</code>中的信号阻塞（<code>blocked = blocked | set</code>）；</li><li><code>SIG_UNBLOCK</code>：为<code>set</code>中的信号解除阻塞（<code>blocked = blocked &amp; ~set</code>）；</li><li><code>SIG_SETMASK</code>：将阻塞信号集合设为<code>set</code>（<code>blocked = set</code>）。</li></ul><p>如果参数<code>oldset</code>非空，则先前<code>blocked</code>的值会存储在<code>oldset</code>中。</p><p>除此之外，函数<code>sigemptyset</code>将<code>set</code>初始化为空集；<code>sigfillset</code>将所有信号加入到<code>set</code>中；<code>sigaddset</code>将编号为<code>signum</code>的信号加入到<code>set</code>中；<code>sigdelset</code>将编号为<code>signum</code>的信号从<code>set</code>中删除；如果<code>signum</code>信号在<code>set</code>中，则函数<code>sigismember</code>返回 1，否则返回 0。</p><p>示例程序暂时阻塞了 SIGINT 信号的接收：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">sigset_t</span> mask, prev_mask;</span><br><span class="line">sigemptyset(&amp;mask);</span><br><span class="line">sigaddset(&amp;mask, SIGINT);</span><br><span class="line"><span class="comment">/* Block SIGINT and save previous blocked set */</span></span><br><span class="line">sigprocmask(SIG_BLOCK, &amp;mask, &amp;prev_mask);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Code region that will not be interrupted by SIGINT</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Restore previous blocked set, unblocking SIGINT */</span></span><br><span class="line">sigprocmask(SIG_SETMASK, &amp;prev_mask, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure><h4 id="5-5-编写信号处理程序"><a href="#5-5-编写信号处理程序" class="headerlink" title="5.5 编写信号处理程序"></a>5.5 编写信号处理程序</h4><p>​</p><h6 id="1-安全的信号处理"><a href="#1-安全的信号处理" class="headerlink" title="1. 安全的信号处理"></a>1. 安全的信号处理</h6><p>​ 如果处理程序和主程序并发地访问同一个全局数据结构，就会发生不可预知的严重问题。因此我们在编写信号处理程序时应当遵循以下守则：</p><ul><li>使信号处理程序尽可能的简单；</li><li>仅调用异步信号安全（Async-Signal-Safe）的函数。这种函数一般只访问局部变量，或者不会被其他信号处理程序中断。值得注意的是，许多常用的函数，如<code>printf</code>、<code>sprintf</code>、<code>malloc</code>和<code>exit</code>等并非异步信号安全。调用<code>write</code>函数是信号处理程序生成输出的唯一安全方法；</li><li>保存并恢复变量<code>errno</code>：许多 Linux 异步信号安全函数返回错误时会设置变量<code>errno</code>的值，因此可能会干扰程序中其他依赖<code>errno</code>的部分。当处理程序有返回时，我们应当在调用前将<code>errno</code>保存到局部变量中，并在返回前恢复其值；</li><li>访问全局数据结构时阻塞所有信号；</li><li>假设主程序和信号处理程序共享全局变量<code>g</code>，处理程序更新<code>g</code>的值，主程序定期读取<code>g</code>的值。优化的编译器会从寄存器中读取已缓存的<code>g</code>，因此主函数中的<code>g</code>可能永远不变，并且每次对<code>g</code>的引用也都是安全的。若使用<code>volatile</code>声明全局变量，如<code>volatile int g;</code>，那么当代码引用<code>g</code>时，编译器就会从内存中读取其值。在这种情况下，我们应当临时阻塞信号以保护对<code>g</code>的访问；</li><li>在常见的设计中，处理程序通过写入全局标识（Flag）来记录信号的接收。若使用<code>sig_atomic_t</code>类型声明标识，如<code>volatile sig_atomic_t flag;</code>，那么便可以保证<code>flag</code>写入的原子性（Atomic&#x2F;Uninterruptible）。</li></ul><h6 id="2-正确的信号处理"><a href="#2-正确的信号处理" class="headerlink" title="2. 正确的信号处理"></a>2. 正确的信号处理</h6><p>​ 上文提到，父进程必须回收子进程以避免在系统中留下僵尸进程，但我们也希望父进程可以在子进程运行时自由地执行其他任务。因此我们使用 SIGCHILD 处理程序来回收子进程，而不是显式地调用<code>waitpid</code>等待子进程终止：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">handler1</span><span class="params">(<span class="type">int</span> sig)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((pid = waitpid(<span class="number">-1</span>, <span class="literal">NULL</span>, <span class="number">0</span>)) &lt; <span class="number">0</span>)</span><br><span class="line">        unix_error(<span class="string">&quot;waitpid error&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Handler reaped child %d\n&quot;</span>, (<span class="type">int</span>)pid);</span><br><span class="line">    Sleep(<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i, n;</span><br><span class="line">    <span class="type">char</span> buf[MAXBUF];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (signal(SIGCHLD, handler1) == SIG_ERR)</span><br><span class="line">        unix_error(<span class="string">&quot;signal error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Parent creates children */</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (Fork() == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">fprintf</span>(<span class="string">&quot;Hello from child %d\n&quot;</span>, (<span class="type">int</span>)getpid());</span><br><span class="line">            Sleep(<span class="number">1</span>);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Parent waits for terminal input and then processes it */</span></span><br><span class="line">    <span class="keyword">if</span> ((n = read(STDIN_FILENO, buf, <span class="keyword">sizeof</span>(buf))) &lt; <span class="number">0</span>)</span><br><span class="line">        unix_error(<span class="string">&quot;read&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Parent processing input\n&quot;</span>);</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">        ;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>示例程序中，父进程安装了处理程序<code>handler1</code>并创建三个子进程。它等待来自终端的输入，然后进入 While 循环。每当一个子进程终止时，内核将发送一个 SIGCHLD 信号通知父进程。父进程捕获信号后回收子进程，输出一段信息然后返回。</p><p>​ 在 Linux 上运行该程序得到的输出结果为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">linux&gt; ./signal1</span><br><span class="line">Hello from child <span class="number">14073</span></span><br><span class="line">Hello from child <span class="number">14074</span></span><br><span class="line">Hello from child <span class="number">14075</span></span><br><span class="line">Handler reaped child</span><br><span class="line">Handler reaped child</span><br><span class="line">CR</span><br><span class="line">Parent processing input</span><br></pre></td></tr></table></figure><p>​ 父进程创建了三个子进程，然而却只回收了两个，这是因为同一时间内相同类型的未处理信号最多只能有一个。信号处理程序在处理第一个信号时，第二个信号到达并被添加到未处理信号集中。由于已有一个未处理的 SIGCHLD 信号，此时若第三个信号到达便会被直接丢弃。当处理程序返回后，内核发现第二个信号还未处理，于是强制父进程接收该信号并重新执行处理程序。等到处理程序再次返回，父进程就不再有任何未处理的 SIGCHLD 信号了。</p><p>我们可以让处理程序在被调用时尽可能多地回收子进程以解决这一问题：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">handler2</span><span class="params">(<span class="type">int</span> sig)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> ((pid = waitpid(<span class="number">-1</span>, <span class="literal">NULL</span>, WNOHANG)) &gt; <span class="number">0</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Handler reaped child %d\n&quot;</span>, (<span class="type">int</span>)pid);</span><br><span class="line">    <span class="keyword">if</span> (errno != ECHILD)</span><br><span class="line">        unix_error(<span class="string">&quot;waitpid error&quot;</span>);</span><br><span class="line">    Sleep(<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h6 id="3-可移植的信号处理"><a href="#3-可移植的信号处理" class="headerlink" title="3. 可移植的信号处理"></a>3. 可移植的信号处理</h6><p>​ 不同的系统有着不同的信号处理语义，因此 Posix 标准定义了<code>sigaction</code>函数，它允许用户在安装信号处理程序时明确地指定他们想要的语义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigaction</span><span class="params">(<span class="type">int</span> signum,  <span class="keyword">struct</span> sigaction *act,</span></span><br><span class="line"><span class="params">              <span class="keyword">struct</span> sigaction *oldact)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 if OK, −1 on error</span></span><br></pre></td></tr></table></figure><p>然而<code>sigaction</code>函数十分笨重，因此我们常使用它的包装函数<code>Signal</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">handler_t</span> *<span class="title function_">Signal</span><span class="params">(<span class="type">int</span> signum, <span class="type">handler_t</span> *handler)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">action</span>, <span class="title">old_action</span>;</span></span><br><span class="line">    action.sa_handler = handler;</span><br><span class="line">    sigemptyset(&amp;action.sa_mask); <span class="comment">/* Block sigs of type being handled */</span></span><br><span class="line">    action.sa_flags = SA_RESTART; <span class="comment">/* Restart syscalls if possible */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sigaction(signum, &amp;action, &amp;old_action) &lt; <span class="number">0</span>)</span><br><span class="line">        unix_error(<span class="string">&quot;Signal error&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> (old_action.sa_handler);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="5-6-避免并发错误"><a href="#5-6-避免并发错误" class="headerlink" title="5.6 避免并发错误"></a>5.6 避免并发错误</h4><p>​ 上文提到，我们永远无法预测两个同步（并发）运行的函数的调用顺序。如果调用顺序会影响结果的正确性，那么这种错误就被称为竞争（Race）。我们可以通过阻塞相关信号来避免这一问题。</p><h4 id="5-7-显式等待信号"><a href="#5-7-显式等待信号" class="headerlink" title="5.7 显式等待信号"></a>5.7 显式等待信号</h4><p>​ 有时候主程序需要显式等待某个信号处理程序运行完毕。例如 Linux Shell 创建前台任务后，必须等待任务终止并被 SIGCHLD 处理程序回收，然后才能接收下一条用户命令。示例程序展示了其基本思想：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">volatile</span> <span class="type">sig_atomic_t</span> pid;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">sigchld_handler</span><span class="params">(<span class="type">int</span> s)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> olderrno = errno;</span><br><span class="line">    pid = waitpid(<span class="number">-1</span>, <span class="literal">NULL</span>, <span class="number">0</span>);</span><br><span class="line">    errno = olderrno;</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">void</span> <span class="title function_">sigint_handler</span><span class="params">(<span class="type">int</span> s)</span></span><br><span class="line">&#123;</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">sigset_t</span> mask, prev;</span><br><span class="line">    Signal(SIGCHLD, sigchld_handler);</span><br><span class="line">    Signal(SIGINT, sigint_handler);</span><br><span class="line">    Sigemptyset(&amp;mask);</span><br><span class="line">    Sigaddset(&amp;mask, SIGCHLD);</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        Sigprocmask(SIG_BLOCK, &amp;mask, &amp;prev); <span class="comment">/* Block SIGCHLD */</span></span><br><span class="line">        <span class="keyword">if</span> (Fork() == <span class="number">0</span>)                      <span class="comment">/* Child */</span></span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">        <span class="comment">/* Parent */</span></span><br><span class="line">        pid = <span class="number">0</span>;</span><br><span class="line">        Sigprocmask(SIG_SETMASK, &amp;prev, <span class="literal">NULL</span>); <span class="comment">/* Unblock SIGCHLD */</span></span><br><span class="line">        <span class="comment">/* Wait for SIGCHLD to be received (wasteful) */</span></span><br><span class="line">        <span class="keyword">while</span> (!pid)</span><br><span class="line">            ;</span><br><span class="line">        <span class="comment">/* Do some work after receiving SIGCHLD */</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 父进程先为信号 SIGCHLD 和 SIGINT 安装处理程序，然后创建子进程并将全局变量<code>pid</code>设为 0，最后进入自旋循环（<code>while (!pid)</code>）。子进程终止后，<code>pid</code>变为非 0，于是父进程退出自旋循环。为了防止父进程进入自旋循环前接收到 SIGCHLD ，我们需要在创建子进程之前阻塞该信号。</p><p>这段代码是正确的，但自旋循环会浪费处理器资源。我们可以将其改为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (!pid) <span class="comment">/* Race! */</span></span><br><span class="line">    pause();</span><br></pre></td></tr></table></figure><p>问题在于：如果父进程在 While 的条件测试之后而<code>pause</code>的执行之前接收到 SIGCHLD，那么程序就会永远休眠。我们还可以将<code>pause</code>改为<code>sleep</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (!pid) <span class="comment">/* Too slow! */</span></span><br><span class="line">    sleep(<span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>这样虽然避免了竞争问题，但会增加程序的运行时间。正确的解决方案是调用函数<code>sigsuspend</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigsuspend</span><span class="params">(<span class="type">const</span> <span class="type">sigset_t</span> *mask)</span>;</span><br><span class="line"><span class="comment">// Returns: -1</span></span><br></pre></td></tr></table></figure><p>该函数使用参数<code>mask</code>替换当前的阻塞信号集合，然后暂停进程直至其接收信号。如果该信号的动作是终止进程，则进程终止且不从<code>sigsuspend</code>返回；如果该信号的动作是运行一个处理程序，则<code>sigsuspend</code>在处理程序返回后返回，并将阻塞信号集合的状态恢复。</p><p>实际上它等效于下列函数组合的原子性（Atomic，即不可中断）版本：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sigprocmask(SIG_SETMASK, &amp;mask, &amp;prev);</span><br><span class="line">pause();</span><br><span class="line">sigprocmask(SIG_SETMASK, &amp;prev, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure><p>因此我们可以将示例函数修改为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    Sigprocmask(SIG_BLOCK, &amp;mask, &amp;prev); <span class="comment">/* Block SIGCHLD */</span></span><br><span class="line">    <span class="keyword">if</span> (Fork() == <span class="number">0</span>)                      <span class="comment">/* Child */</span></span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    <span class="comment">/* Wait for SIGCHLD to be received */</span></span><br><span class="line">    pid = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (!pid)</span><br><span class="line">        sigsuspend(&amp;prev);</span><br><span class="line">    <span class="comment">/* Optionally unblock SIGCHLD */</span></span><br><span class="line">    Sigprocmask(SIG_SETMASK, &amp;prev, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="comment">/* Do some work after receiving SIGCHLD */</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;.&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="6-非本地跳转"><a href="#6-非本地跳转" class="headerlink" title="6. 非本地跳转"></a>6. 非本地跳转</h2><p>​ C 提供了一种用户级别的异常控制流，即<strong>非本地跳转（Nonlocal Jump）</strong>。它无需完成正常的调用&#x2F;返回序列，就可以将控制权从一个函数直接转移到另一个当前正在执行的函数。非本地跳转是通过<code>setjmp</code>和<code>longjmp</code>函数实现的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;setjmp.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setjmp</span><span class="params">(jmp_buf env)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">sigsetjmp</span><span class="params">(sigjmp_buf env, <span class="type">int</span> savesigs)</span>;</span><br><span class="line"><span class="comment">// Returns: 0 from setjmp, nonzero from longjmps</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">longjmp</span><span class="params">(jmp_buf env, <span class="type">int</span> retval)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">siglongjmp</span><span class="params">(sigjmp_buf env, <span class="type">int</span> retval)</span>;</span><br><span class="line"><span class="comment">// Never returns</span></span><br></pre></td></tr></table></figure><ul><li><code>setjmp</code>函数将当前调用环境（Calling Environment，包括程序计数器、栈指针和通用寄存器等），保存在参数<code>env</code>指定的缓冲区中并返回 0。</li><li><code>longjmp</code>函数会从<code>env</code>缓冲区恢复调用环境，然后触发最近调用的<code>setjmp</code>函数的返回。这种情况下，<code>setjmp</code>会返回一个非零值<code>retval</code>。</li><li>在信号处理程序中，我们使用<code>sigsetjmp</code>和<code>siglongjmp</code>代替它们。</li></ul><p>非局部跳转的一个重要应用是可以在检测到某些错误条件时，从深度嵌套的函数调用中立即返回。我们使用非本地跳转直接返回到常见的错误处理程序，无需费力地<strong>展开栈（Unwind Stack）</strong>。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&quot;csapp.h&quot;</span></span></span><br><span class="line"></span><br><span class="line">jmp_buf buf;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> error1 = <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> error2 = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">foo</span><span class="params">(<span class="type">void</span>)</span>, <span class="title function_">bar</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">switch</span> (setjmp(buf))</span><br><span class="line">    &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">        foo();</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Detected an error1 condition in foo\n&quot;</span>);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Detected an error2 condition in foo\n&quot;</span>);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Unknown error condition in foo\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Deeply nested function foo */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">foo</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (error1)</span><br><span class="line">        longjmp(buf, <span class="number">1</span>);</span><br><span class="line">    bar();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">bar</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (error2)</span><br><span class="line">        longjmp(buf, <span class="number">2</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>示例程序中主函数首先调用<code>setjmp</code>保存当前调用环境，然后依次调用函数<code>foo</code>和<code>bar</code>。 一旦函数执行发生错误，它们会立即通过<code>longjmp</code>从<code>setjmp</code>返回。<code>setjmp</code>的非零返回值表示错误的类型，因此我们可以在代码中的某处对其进行处理。</p><p>非局部跳转的另一个重要应用是从信号处理程序跳转到特定代码位置，而不是像往常那样返回到因信号中断的指令。</p>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_7_链接</title>
      <link href="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/"/>
      <url>/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/</url>
      
        <content type="html"><![CDATA[<p>​ <strong>链接(linking)</strong> 是将各种代码和数据部分收集起来并组合成为一个单一文件的过程，这个文件可<strong>被加载</strong>到存储器并执行。链接可以执行于 <strong>编译时(compile time)</strong> ，也就是在源代码被翻译成机器代码时;也可以执行于 <strong>加载时(loadtime)</strong> ，也就是在程序被 <strong>加载器(loader)</strong> 加载到存储器并执行时;甚至执行于 <strong>运行时(run time)</strong> ，由应用程序来执行。在早期的计算机系统中，链接是手动执行的。在现代系统中，链接是由叫做 <strong>链接器(linker)</strong> 的程序自动执行的。<br>​ 链接器在软件开发中扮演着一个关键的角色，因为它们使得 <strong>分离编译(separate compilation)</strong> 成为可能。我们不用将一个大型的应用程序组织为一个巨大的源文件，而是可以把它分解为更小、更好管理的模块，可以独立地修改和编译这些模块。当我们改变这些模块中的一个时，只需简单地重新编译它，并重新链接应用，而不必重新编译其他文件。</p><h2 id="1-编译器驱动程序"><a href="#1-编译器驱动程序" class="headerlink" title="1.编译器驱动程序"></a>1.编译器驱动程序</h2><p>​ 大多数编译系统都提供了一个<strong>编译器驱动（Compiler Driver）</strong>，它可以根据用户需求调用<strong>语言预处理器（Language Preprocessor）</strong>、编译器、汇编器和链接器等。例如要在 GNU 编译系统中构建下列程序，我们可以 使用命令：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcc -02 -g -o p main.c swap.c</span><br></pre></td></tr></table></figure><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/c5dbec50679efcb44fdf4986480d2b7d.png" class><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/8686ddb2b4177e202d5493a7cdd4a93c.png" class><p>​ 上图概括了驱动程序在将示例程序从 ASCI 码源文件翻译成可执行目标文件时的行为。(如果你想看看这些步骤，用-v 选项来运行 GCC)</p><p>驱动程序首先运行 C 预处理器(cpp)，它将 C 源程序 main.c 翻译成一个 <strong>ASCII 码的中间文件</strong> main.i.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cpp [other arguments] main.c /tmp/main.i</span><br></pre></td></tr></table></figure><p>接下来，驱动程序运行 C 编译器(cc1)，它将 main.i 翻译成一个<strong>ASCI 汇编语言文件</strong>main.s.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cc1 /tmp/main.i main.c -02 [other arguments] -o /tmp/main.s</span><br></pre></td></tr></table></figure><p>然后，驱动程序运行汇编器(as)，它将 main.s 翻译成一个**可重定位目标文件(relocatable object file)**main.o .</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">as [other arguments] -o /tmp/main.o /tmp/main.s</span><br></pre></td></tr></table></figure><p>驱动程序经过相同的过程生成 swap.0。最后，它运行链接器程序 1d，将 main.o 和 swap.o 以及一些必要的系统目标文件组合起来，创建一个**可执行目标文件(executable object file)**p .</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ld -o p[system object files and args]/tmp/main.o /tmp/swap.o</span><br></pre></td></tr></table></figure><p>要运行可执行文件户，我们在命令行上输人它的名字:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./p</span><br></pre></td></tr></table></figure><p>外壳调用操作系统中一个叫做<strong>加载器的函数，它拷贝可执行文件 p 中的代码和数据到存储器</strong>，然后将控制转移到这个程序的开头，</p><h2 id="2-静态链接"><a href="#2-静态链接" class="headerlink" title="2.静态链接"></a>2.静态链接</h2><p>​ <strong>静态链接器(Static Linker)<strong>将</strong>可重定位目标文件</strong>和<strong>命令行参数</strong>作为输入，生成完全链接的可执行目标文件。<strong>可重定位目标文件</strong>由各种代码和数据组成，指令、初始化的全局变量和未初始化的变量分别处于不同部分。</p><p>链接器需要完成两个主要任务：</p><ul><li><strong>符号解析（Symbol Resolution）</strong>：目标文件定义并引用符号，每个符号对应一个函数、全局变量或静态变量（即使用<code>static</code>声明的任何变量）。符号解析的目的是将每个符号引用与一个符号定义相关联；</li><li><strong>重定位（Relocation）</strong>：编译器和汇编器生成的代码和数据段是从地址 0 开始的，链接器会重定位所有的符号定义并修改其对应的符号引用。</li></ul><p>注意：<strong>目标文件只是字节块的集合</strong>，其中可能包含代码、数据或指导链接器和加载器的数据结构。链接器将各个块连接在一起，确定整个块的运行时位置，并修改代码和数据块中的不同位置。编译器和汇编器在生成目标文件时已经完成了大部分工作，因而链接器对目标机器的了解甚少。</p><h2 id="3-目标文件"><a href="#3-目标文件" class="headerlink" title="3.目标文件"></a>3.目标文件</h2><p>目标文件（Object File）有三种形式：</p><ul><li><strong>可重定位目标文件</strong>：包含二进制代码和数据，可以在编译时与其他可重定位目标文件组合以创建可执行目标文件；</li><li><strong>可执行目标文件</strong>：包含二进制代码和数据形式，可直接被复制到内存中执行；</li><li><strong>共享目标文件</strong>：一种<strong>特殊类型的可重定位目标文件</strong>，可以在加载时或运行时被加载到内存中并动态链接。</li></ul><h2 id="4-可重定位目标文件"><a href="#4-可重定位目标文件" class="headerlink" title="4.可重定位目标文件"></a>4.可重定位目标文件</h2><p>​ 典型的 ELF（Executable and Linkable Format）可重定位目标文件格式如下图所示：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/494e335da73a3b07ea5e1507763be10b.png" class><ul><li><p><strong>ELF 头（ELF Header）</strong>：开头是一个**表征系统字长（Word Size）<strong>和</strong>字节顺序（Byte Ordering）**的 16 字节序列。其余部分包括 ELF 头的大小、目标文件的类型（如可重定位、可执行或共享）、机器类型（如 x86-64）、**节头表（Section Header Table）<strong>的文件偏移量以及其中</strong>条目(entry)**的大小和数量；</p></li><li><p>节头表：描述了目标文件中每个 Section 的位置和大小；</p></li><li><p>Section：位于 ELF 头和节头表之间，包括：</p><ul><li>.text：编译后程序的机器码；</li><li>.rodata：只读数据，例如<code>printf</code>中的格式字符串，Switch 语句的跳转表等；</li><li>.data：<strong>已初始化的全局变量和静态变量</strong>。非静态局部变量在运行时位于栈中，不会出现在 .data 或 .bss 中；</li><li>.bss：<strong>未初始化的静态变量，以及初始化为 0 的全局变量和静态变量</strong>。<strong>此 Section 只是一个占位符，在目标文件中不占用实际空间</strong>，因此可以提升空间效率。这些变量在运行时被分配到内存中，初始值为零；</li><li>.symtab ：一个保存了在<strong>程序中被定义和引用的函数和全局变量信息</strong>的符号表（Symbol Table）。与编译器中的符号表不同，.symtab 中的符号表不包含任何局部变量；</li><li>.rel.text：当链接器将目标文件与其他文件组合时，.text 中的许多位置都需要被修改，而 .rel.text 中则保存了与之相关的重定位信息。通常，任何调用外部函数或引用全局变量的指令都需要被修改，而调用局部函数的指令则不变。可执行目标文件一般不需要重定位信息，因此这部分可以省略；</li><li>.rel.data：被引用或定义的任何全局变量的重定位信息。通常，所有初始值为全局变量地址或外部定义函数地址的已初始化全局变量都需要被修改；</li><li>.debug：调试符号表，仅在使用<code>-g</code>选项调用编译器驱动时出现；</li><li>.line：原始程序中行号与 .text 中机器代码指令之间的映射关系，仅在使用<code>-g</code>选项调用编译器驱动时出现；</li><li>.strtab：一个以<code>NULL</code>结尾，包含 .symtab 和 .debug 中的符号表以及 Section 名称的字符串序列。</li></ul></li></ul><h2 id="5-符号和符号表"><a href="#5-符号和符号表" class="headerlink" title="5.符号和符号表"></a>5.符号和符号表</h2><p>​ 每个目标文件都有一个符号表，其中包含了该文件所定义和引用的符号信息。符号有以下三种：</p><ul><li><strong>全局符号（Global Symbols）</strong>：由该文件定义并且可以被其他文件引用的符号；</li><li><strong>外部符号（Externals）</strong>：被该文件引用但由其他文件定义的符号；</li><li><strong>局部符号（Local Symbols）</strong>：由该文件定义且无法被其他文件引用的符号，即使用<code>static</code>声明的函数和变量。</li></ul><p>​ 注意：非静态局部变量在运行时位于栈中，与链接器无关。而静态局部变量则保存在 .data 或 .bss 中，编译器会在符号表中为其创建名称唯一的局部符号。例如同一文件中的两个函数都定义了静态局部变量<code>x</code>：</p><p><strong>给 C 语言初学者建议:利用 static 属性隐藏变量和函数名字</strong></p><p>C 程序员使用 static 属性在模块内部隐藏变量和函数声明，就像你在 Java 和 C++中使用 public 和 private 声明一样。C 源代码文件扮演模块的角色。任何声明带有 static 属性的全局变量或者函数都是模块私有的。类似地，任何声明为不带 static 属性的全局变量和函数都是公共的，可以被其他模块访问。尽可能用 static 属性来保护你的变量和函数是很好的编程习惯。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">int f()</span><br><span class="line">&#123;</span><br><span class="line">    static int x = 0;</span><br><span class="line">    return x;</span><br><span class="line">&#125;</span><br><span class="line">int g()</span><br><span class="line">&#123;</span><br><span class="line">    static int x = 1;</span><br><span class="line">    return x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 那么编译器可能将<code>x.1</code>作为函数<code>f()</code>中的变量符号，将<code>x.2</code>作为函数<code>g()</code>中的变量符号发送给汇编器。<strong>汇编器使用接收到的<code>.s</code>文件中的符号构建符号表</strong>，其中每个条目的数据结构为：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/cd1122519cf50a75b23b03a187d9abf8.png" class><ul><li><code>name</code>：符号名在字符串表 .strtab 中的偏移量；</li><li><code>value</code>：对于可重定位目标文件是符号在其 Section 中的偏移量，对于可执行目标文件是符号的运行时地址；</li><li><code>size</code>：符号的大小；</li><li><code>type</code>：符号的类型；</li><li><code>binding</code>：符号是局部的还是全局的；</li><li><code>section</code>：符号所在的 Section 在节头表中的索引。</li></ul><p>值得一提的是，有三个伪 Section 在节头表中没有条目：</p><ul><li>ABS：不应重定位的符号；</li><li>UNDEF：在此文件中引用但在其他文件中定义的符号；</li><li>COMMON：未初始化的全局符号</li></ul><p>上述三个 Section 仅存在于可重定位目标文件，在可执行目标文件中并不存在。我们可以使用 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/https://man7.org/linux/man-pages/man1/readelf.1.html">READELF</a> 工具阅读目标文件中的内容，<a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/##1.%E7%BC%96%E8%AF%91%E5%99%A8%E9%A9%B1%E5%8A%A8%E7%A8%8B%E5%BA%8F">main.c</a> 生成的目标文件符号表条目如下：</p><p>​ READELF 通过整数索引 Ndx 标识每个 Section，1 表示 .text，3 表示 .data。全局符号<code>main</code>和<code>array</code>分别位于上述两个 Section 首部，因此其偏移量<code>value</code>均为 0。外部符号<code>sum</code>未在本文件中定义，位于 UNDEF。</p><p>​ 在这个例子中，我们看到一个关于全局符号 buf 定义的条目，它是一个位于.data 节中偏移为零(即 value)处的 8 字节目标。其后跟随着的是全局符号 main 的定义，它是个位于.text 节中偏移为零处的 17 字节函数。最后一个条目来自对外部符号 swap 的引用 READELF 用一个整数索引来标识每个节。Ndx&#x3D;1 表示.text 节，而 Ndx&#x3D;3 表示.data 节。</p><h2 id="6-符号解析"><a href="#6-符号解析" class="headerlink" title="6.符号解析"></a>6.符号解析</h2><p>​ 链接器将每个符号引用与符号表中的符号定义相关联以完成<strong>符号解析（Symbol Resolution）</strong>。当编译器遇到未在当前文件中定义的符号时，它会假设该符号已在其他文件中定义，然后生成对应的符号表条目。如果链接器无法在任何输入文件中找到该符号的定义，那么它就会终止链接。</p><p>不同文件可能定义了相同名称的全局符号。对于这种情况，链接器要么直接报错，要么选取其中之一。</p><p><strong>对 C++和 Java 中链接器符号的毁坏(mangling)</strong><br>C++和 Java 都允许重载方法，这些方法在源代码中有相同的名字，却有不同的参数列表。那么链接器是如何区别这些不同的重载函数之间的差异呢?C++和 Java 中能使用重载函数，是因为编译器将每个唯一的方法和参数列表组合编码成一个对链接器来说唯一的名字。这种编码过程叫做<strong>毁坏(mangling)</strong>，而相反的过程叫做<strong>恢复(demangling)</strong>。</p><p>​ C++ 和 Java ，使用<strong>兼容</strong>的毁坏策略。允许重载名称相同但参数列表不同的方法。编译器会将每个方法和参数列表组合为一个唯一的名称，这样链接器就可以区分它们。例如，<code>Foo::bar(int, long)</code>会被编码为<code>bar__3Fooil</code>。其中，3 代表类名 Foo 的字符数，i 和 l 则分别代表参数列表中的<code>int</code>和<code>long</code>。</p><h4 id="6-1-解析名称重复的符号"><a href="#6-1-解析名称重复的符号" class="headerlink" title="6.1 解析名称重复的符号"></a>6.1 解析名称重复的符号</h4><p>​ Linux 编译系统会在编译时将全局符号分为两种类型：函数和已初始化的全局变量是<strong>强符号（strong）</strong>，未初始化的全局变量是<strong>弱符号（weak）</strong>。汇编器将符号的强弱信息隐式地编码到目标文件的符号表中。</p><p>链接器解析名称重复的符号的规则为：</p><ul><li>不允许多个强符号名称重复；</li><li>若一个强符号和多个弱符号名称重复，选择强符号；</li><li>若多个弱符号名称重复，从中任选其一。</li></ul><p>注意：最新版本的 GCC（如 GCC 10）默认使用标识位<code>-fno-common</code>，因此若程序包含多个名称重复的弱符号将引发链接错误</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* foo3.c */</span></span><br><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">f</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> x = <span class="number">15213</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    f();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;x = %d\n&quot;</span>, x);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">/* bar3.c */</span><br><span class="line">int x;</span><br><span class="line"></span><br><span class="line">void f()</span><br><span class="line">&#123;</span><br><span class="line">    x = 15212;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>​ 示例程序中，文件<code>bar3.c</code>中的函数<code>f</code>是强符号，文件<code>foo3.c</code>中的函数<code>f</code>是弱符号，因此主函数的输出<code>x</code>的值为 15212。</p><p>​ 上文提到，未初始化的全局变量保存在 COMMON 中，而未初始化的静态变量，以及初始化为 0 的全局变量和静态变量保存在 .bss 中。这是因为前者是弱符号，编译器无法知晓其他文件中是否定义了相同名称的变量，必须将其分配到 COMMON 中并随后由链接器处理。已初始化为 0 的全局变量是强符号，根据第二条解析规则，该变量一定是唯一的，因此编译器可以安心地把它放到 .bss 中。静态变量无法被其他文件引用，自然也无需担心名称重复的问题。</p><h4 id="6-2-与静态库相链接"><a href="#6-2-与静态库相链接" class="headerlink" title="6.2 与静态库相链接"></a>6.2 与静态库相链接</h4><p>​ <strong>编译系统将一些相关的目标模块打包到一个文件中，该文件被称为<em>静态库（Static Library</em>）</strong>。在构建可执行目标文件时，链接器<strong>仅复制</strong>静态库中被应用程序引用的<strong>目标模块</strong>，从而减小了磁盘和内存中可执行文件的大小。静态库为我们提供了 I&#x2F;O、字符串操作和数学运算等标准函数。</p><p>​ 在 Linux 系统中，静态库以特定的文件格式（后缀为<code>.a</code>）存储在磁盘上。应用程序可以在命令行中指定文件名来使用静态库中定义的任何函数（实际上，C 编译器驱动总是将<code>libc.a</code>传递给链接器），如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">linux&gt; gcc main.c /usr/lib/libm.a /usr/lib/libc.a</span><br></pre></td></tr></table></figure><p>​ 在 Unix 系统中，静态库以一种称为存档(archive)的特殊文件格式存放在磁盘中。存档文件是一组连接起来的可重定位目标文件的集合，有一个头部用来描述每个成员目标文件的大小和位置。存档文件名由后缀.a 标识。为了使我们对库的讨论更加形象具体，假设我们想在一个叫做 1ibvector.a 的静态库中提供下图中的向量例程。</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/7fc9b0388a3eb5f45da37c592fd4f439.png" class><p>我们使用 AR 命令将下列程序打包为静态库文件<code>libvector.a</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">linux&gt; gcc -c addvec.c multvec.c</span><br><span class="line">linux&gt; ar rcs libvector.a addvec.o multvec.o</span><br></pre></td></tr></table></figure><p>接下来再编写一个程序<code>main2.c</code>调用该静态库，头文件<code>vector.h</code>定义了库文件中的函数原型：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/5fa82c697df7838df12800b6d2b7ba3e.png" class><p>最后使用如下命令编译并链接 main2.o 和 libvector.a：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">linux&gt; gcc -c main2.c</span><br><span class="line">linux&gt; gcc -<span class="type">static</span> -o p2 main2.o ./libvector.a</span><br></pre></td></tr></table></figure><p><strong><code>-static</code>参数表示链接器应当构建一个完全链接的可执行目标文件</strong>，该文件可以被加载到内存中运行而无需进一步地链接。完整的链接流程如下图所示：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/baa40568b0803a179e778c83047e5c7d.png" class><h4 id="6-3-静态库的符号解析"><a href="#6-3-静态库的符号解析" class="headerlink" title="6.3 静态库的符号解析"></a>6.3 静态库的符号解析</h4><p>​ 符号解析时，链接器会按照从左到右的顺序依次扫描命令行中的目标文件和静态库。在这个过程中，E 为可重定位目标文件的集合，U 为被引用但还未找到定义的符号，D 为已扫描过的文件定义的符号，开始时三者均为空。</p><ul><li>若命令行中的输入文件为可重定位目标文件，则链接器将其添加到 E 中并更新 U 和 D 中的符号；</li><li>若命令行中的输入文件为静态库，则链接器会将 U 中的符号与该静态库中定义的符号相匹配。匹配成功的模块会被添加到 E 中，随后链接器更新 U 和 D 中的符号。当 U 和 D 中的符号不再改变时，匹配结束，任何不在 E 中的静态库模块都将被直接丢弃；</li><li>若扫描全部完成时 U 为空，则链接器合并并重定位 E 中所有的目标文件以构建可执行文件。否则，链接器将报错并终止。</li></ul><p>链接器的这种行为限制了命令行中的文件顺序。如果定义符号的静态库出现在引用该符号的目标文件之前，链接就会失败。</p><h2 id="7-重定位"><a href="#7-重定位" class="headerlink" title="7.重定位"></a>7.重定位</h2><p>符号解析完成后，链接器会将代码中的每个符号引用与一个符号定义相关联。接下来，链接器开始对目标文件重定位：</p><ul><li>重定位 Section 和符号定义：链接器将所有输入模块中相同类型的 Section 合并为一个新的聚合 Section，然后将运行时地址分配给每个 Section 和符号；</li><li>在 Section 内重定位符号引用：链接器修改代码和数据段中的每个符号引用，使其指向正确的运行时地址。为了执行这一步，它依赖于**重定位条目（Relocation Entry）**这种数据结构。</li></ul><h4 id="7-1-重定位条目"><a href="#7-1-重定位条目" class="headerlink" title="7.1 重定位条目"></a>7.1 重定位条目</h4><p>​ 汇编器在生成目标文件时，并不知晓代码、数据和引用的外部符号在内存中的最终位置。它只会为每个引用生成一个重定位条目（Relocation Entry），指导链接器如何修改它们。上文提到，代码的重定位条目放在 .rel.text 中，数据的重定位条目则放在 .rel.data 中。</p><p>ELF 重定位条目的数据结构为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">long</span> offset;    <span class="comment">/* Offset of the reference to relocate */</span></span><br><span class="line">    <span class="type">long</span> type:<span class="number">32</span>,   <span class="comment">/* Relocation type */</span></span><br><span class="line">         symbol:<span class="number">32</span>; <span class="comment">/* Symbol table index */</span></span><br><span class="line">    <span class="type">long</span> addend;    <span class="comment">/* Constant part of relocation expression */</span></span><br><span class="line">&#125; Elf64_Rela;</span><br></pre></td></tr></table></figure><p><code>offset</code>是被修改的引用在其 Section 中的偏移量；<code>symbol</code>是引用指向的符号在符号表中的索引；<code>type</code>告知链接器如何修改引用；<code>addend</code>是一个有符号常量，某些类型的重定位使用它来偏置被修改的引用值。</p><p>最基本的两种重定位类型为：</p><ul><li>R_X86_64_PC32：使用 32 位 PC 相对地址重定位引用。当 CPU 执行一条使用 PC 相对地址的指令时，它会将指令中的目标地址与 PC 当前值（即下一条指令在内存中的地址）相加得到有效地址（在 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/https://koktlzz.github.io/posts/machine-level-representation-of-programs-note/##%E8%B7%B3%E8%BD%AC%E6%8C%87%E4%BB%A4">跳转指令</a> 一节中我们讨论过这一问题）；</li><li>R_X86_64_32：使用 32 位绝对地址重定位引用。CPU 直接使用指令中的目标地址作为有效地址，无需进一步地修改。</li></ul><h4 id="7-2-重定位符号引用"><a href="#7-2-重定位符号引用" class="headerlink" title="7.2 重定位符号引用"></a>7.2 重定位符号引用</h4><p>链接器的重定位算法的伪代码如图所示：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/d447b5578bb67aa0b5fe84c54ea52858.png" class><p>​</p><p>​ 链接器遍历每个 Section（<code>s</code>）中的每个重定位条目（<code>r</code>）。为了使描述具体化，假设每个节(Section)s 是一个字节数组，每个重定位条目 r 是一个类型为 Elf32_Rela 的结构体。另外，还设当算法运行时，链接器已经为每个节(用 ADDR(S)表示)和每个符号都选择了运行时地址(用 ADDR(r.symbol)表示)。第 3 行计算的是需要被重定位的 4 字节引用的数组 s 中的地址（<code>*refptr</code>是指令中的目标地址）。如果这个引用使用的是 PC 相对寻址，那么它就用第 5<del>9 行来重定位。如果该引用使用的是绝对寻址，它就通过第 11</del> 13 行来重定位。</p><p>我们使用命令<code>objdump -dx main.o</code>得到汇编器为 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/##1.%E7%BC%96%E8%AF%91%E5%99%A8%E9%A9%B1%E5%8A%A8%E7%A8%8B%E5%BA%8F">main.c</a> 生成的机器码和重定位条目：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/64d46db6560dbda755c64d50acaea0d1.png" class><h6 id="7-2-1-PC-相对地址重定位"><a href="#7-2-1-PC-相对地址重定位" class="headerlink" title="7.2.1 PC 相对地址重定位"></a>7.2.1 PC 相对地址重定位</h6><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/004701c6a73489a46cb86d6e2e9b5982.png" class><p>​ 从这个列表中，我们看到 ca11 指令开始于节偏移 0x6 处，由 1 个字节的操作码 0xe8 和随后的 32 位引用 0xfffffffc(十进制-4)组成，它是以小端法字节顺序存储的。我们还看到下一行显示的是这个引用的重定位条目。(回想一下，重定位条目和指令实际上是存放在目标文件的不同节中的。OBJDUMP 工具为了方便将它们显示在一起。)重定位条目 r 由 3 个字段组成:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">r.offset = 0x7</span><br><span class="line">r.symbol = swap</span><br><span class="line">r.type   = R_X86_64_PC32</span><br><span class="line">r.addend = -4</span><br></pre></td></tr></table></figure><p>​</p><p>​ 这些字段告诉链接器修改开始于偏移量 0x7.处的 32 位 PC 相对引用，使得在运行时它指向 swap()。现在，假设链接器已经确定: ADDR(s)&#x3D;ADDR(.text)&#x3D;0x80483b4 和 ADDR(r.symbol)&#x3D;ADDR(swap)&#x3D;0x80483c8。那么首先我们可以计算得到该引用的运行时地址为：（第七行）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">refaddr = ADDR(s)  + r.offset</span><br><span class="line">= 0x80483b4 + 0x7</span><br><span class="line">        = 0x80483bb</span><br></pre></td></tr></table></figure><p>然后，它将引用从当前值(-4)修改为 0x9，使得它在运行时指向 swap()(第 8 行):</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">*refptr = (unsigned) (ADDR(r.symbol) + r.addend - refaddr)</span><br><span class="line">        = (unsigned) (0x80483c8       + (-4)     - 0x80483bb)</span><br><span class="line">        = (unsigned) (0x9)</span><br></pre></td></tr></table></figure><p>在得到的可执行目标文件中，ca11 指令有如下的重定位的形式:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">80483ba:  e8 09 00 00 00         ca11  80483c8 &lt;swap&gt;swap();</span><br></pre></td></tr></table></figure><p>在运行时，ca11 指令将存放在地址 0x80483ba 处。当 CPU 执行 ca11 指令时，PC 的值为 0x80483bf，即紧随在 ca11 指令之后的指令的地址。为了执行这条指令，CPU 执行以下的步骤:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">1.push PC onto stack</span><br><span class="line">2.PC&lt;-PC+0x9=0x80483bf+0x9=0x80483c8</span><br></pre></td></tr></table></figure><p>因此，要执行的下一条指令就是 swap 程序的第一条指令.</p><p>你可能想知道为什么汇编器会将 ca11 指令中的引用的初始值设置为-4。汇编器用这个值作为偏移量，是因为 PC 总是指向当前指令的<strong>下一条指令</strong>。在有不同指令大小和编码方式的不同的机器上，该机器的汇编器会使用不同的偏移量。这是一个很有用的技巧，它允许链接器透明地重定位引用，很幸运地不用知道某一台机器的指令编码。</p><h6 id="7-2-2-重定位绝对引用"><a href="#7-2-2-重定位绝对引用" class="headerlink" title="7.2.2 重定位绝对引用"></a>7.2.2 重定位绝对引用</h6><p>回想图 7-1 中我们的示例程序，swap.o 模块将全局指针 bufp0 初始化为指向全局数组 buf 的第一个元素的地址:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">int *bufpO =&amp;buf[0];</span><br></pre></td></tr></table></figure><p>该引用的重定位条目是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">r.offset = <span class="number">0x0</span></span><br><span class="line">r.symbol = buf</span><br><span class="line">r.type   = R_X86_64_32</span><br><span class="line">r.addend = <span class="number">0</span></span><br></pre></td></tr></table></figure><p>因为 bufp0 是一个已初始化的数据目标，那么它将被存放在可重定位目标模块 swap.o 的.data 节中。因为它被初始化为一个全局数组的地址，所以它需要被重定位。下面是 swap.o 中。data 节的反汇编列表:</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/fe0d062a26dbdf47994c1694f99f3d34.png" class><p>我们看到.data 节包含一个 32 位引用，bufp0 指针的值为 0x0。重定位条目告诉链接器这是一个 32 位绝对引用，开始于偏移 0 处，必须重定位使得它指向符号 buf。现在，假设链接器已经确定:</p><p>ADDR(r.symbol)&#x3D;ADDR(buf)&#x3D;0x8049454，那么首先我们可以计算得到该引用的运行时地址为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">*refptr = (<span class="type">unsigned</span>) (ADDR(r.symbol) + *refptr)</span><br><span class="line">        = (<span class="type">unsigned</span>) (<span class="number">0x8049454</span>      + <span class="number">0</span>)</span><br><span class="line">        = (<span class="type">unsigned</span>) (<span class="number">0x8049454</span>)</span><br></pre></td></tr></table></figure><p>在得到的可执行目标文件中，引用有下面的重定位形式:</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/5f970249487f23287b62ef56bdb8ad2e.png" class><p>总而言之，链接器决定在运行时变量 bufp0 将放置在存储器地址 0x804945c 处，并且被初始化为 0x8049454，这个值就是 buf 数组的运行时地址。</p><h2 id="8-可执行目标文件"><a href="#8-可执行目标文件" class="headerlink" title="8.可执行目标文件"></a>8.可执行目标文件</h2><p>ELF 可执行目标文件的结构如下：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/839cb440a8e32a0044485b652368eff6.png" class><p>​ ELF 头描述了文件的整体格式，并包含了程序在运行时执行的第一条指令的地址。.init 定义了一个名为<code>_init</code>的函数，它将被程序的初始化代码所调用。其余 Section 与可重定位目标文件类似，只不过它们已被重定位到运行时的内存地址。正因如此，该文件中没有 .rel.text 和 .rel.data。</p><p>​ ELF 可执行文件被设计得很容易加载到存储器，可执行文件的**连续的片(chunk)**被映射到连续的存储器段。**段头部表(segment header table)**描述了这种映射关系。下图 展示了可执行文件 p 的段头部表，是由 OBJDUMP 显示的。</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/bc91362eb5f5fde40acfa2e586f8f7c6.png" class><p>​ 从段头部表中，我们会看到根据可执行目标文件的内容初始化两个存储器段。</p><p>​ 第一个段(代码段)对齐到一个 4KB(2”)的边界，有读&#x2F;执行许可，开始于存储器地址 0x08048000 处，总共的存储器大小是 0x448 字节，并且被初始化为可执行目标文件的头 0x448 个字节，其中包括 ELF 头部、段头部表以及.init、.text 和.rodata 节。<br>​ 第二个段(数据段)被对齐到一个 4KB 的边界，有读&#x2F;写许可开始于存储器地址 0x08049448 处，总的存储器大小为 0x104 字节，并用从文件偏移 0x448 处开始的 0xe8 个字节初始化，在这种情况下，偏移 0x448 处正是.data 节的开始。该段中剩下的字节对应于运行时将被初始化为零的.bss 数据。</p><p>对于每个内存段，链接器必须选择一个起始地址 vaddr，使得：</p><p>​ <em>vaddr</em> <em>mod</em> <em>align</em>&#x3D; <em>off</em> <em>mod</em> <em>align</em></p><p>其中，off 是该内存段中第一个 Section 在目标文件中的偏移量，align 是程序头表中指定的对齐方式。这种对齐要求是一种优化，它可以使目标文件被更加有效地加载到内存中。</p><h2 id="9-加载可执行目标"><a href="#9-加载可执行目标" class="headerlink" title="9.加载可执行目标"></a>9.加载可执行目标</h2><p>下图展示了 Linux 程序的运行时内存结构：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/f5b0f8944ad577db9c0399d5a96b7a27.png" class><p>​ 加载器首先根据程序头表将可执行目标文件中的块复制到内存中的代码和数据段，然后跳转到<strong>程序入口</strong>，即<code>_start_</code>函数（在系统目标文件<code>crt1.o</code>中定义）的地址。该函数再调用<code>libc.so</code>中定义的系统启动函数<code>__libc_start_main</code>，由它初始化执行环境，之后在从.text 和.init 节中调用了初始化例程后，启动代码调用 atexit 例程，这个程序附加了一系列在应用程序正常中止时应该调用的程序。exit 函数运行 atexit 注册的函数，然后通过调用_exit 将控制返回给操作系统。接着，启动代码调用应用程序的 main 程序，它会开始执行我们的 C 代码。在应用程序返回之后，启动代码调用 exit 程序，它将控制返回给操作系统调用用户级的主函数并处理其返回。</p><p>​ 加载器实际上是如何工作的?<br>​ 我们对于加载的描述从概念上来说是准确的，但也不完全准确。为了理解加载实际上是如何工作的，你必须理解进程、虚拟存储器和存储器映射的概念，这些我们还没有加以讨论。在后面的第 8 章和第 9 章中遇到这些概念时，我们将重新回到加载的问题上，并逐渐向你揭开它的神秘面纱。<br>​ 对于不够有耐心的读者，下面是关于加载实际上是如何工作的一个概述:Unix 系统中的每个程序都运行在一个进程上下文中，有自己的虚拟地址空间。当外壳运行一个程序时，父外壳进程生成一个子进程，它是父进程的一个复制品。子进程通过 execve 系统调用启动加载器。加载器删除子进程现有的虚拟存储器段，并创建一组新的代码、数据、堆和栈段。新的栈和堆段被初始化为零。通过将虚拟地址空间中的页映射到可执行文件的页大小的片(chunk)，新的代码和数据段被初始化为可执行文件的内容。最后，加载器跳转到 start 地址，它最终会调用应用程序的 main 函数。除了一些头部信息，在加载过程中没有任何从磁盘到存储器的数据拷贝。直到 CPU 引用一个被映射的虚拟页才会进行拷贝，此时，操作系统利用它的页面调度机制自动将页面从磁盘传送到存储器。</p><h2 id="10-动态链接共享库"><a href="#10-动态链接共享库" class="headerlink" title="10.动态链接共享库"></a>10.动态链接共享库</h2><p>​ 静态库会被定期维护和更新，因此程序员需要知晓其变动并将重新链接程序。此外，几乎所有 C 程序都会使用一些标准 I&#x2F;O 函数，例如<code>printf</code>。这些函数的代码将在运行时被复制到每个进程的代码段中，从而导致严重的内存浪费。</p><p>​ <strong>共享库（Shared Libraries）<strong>可以解决上述静态库的缺点。它是一种可以在</strong>加载时或运行时于任意内存地址加载并与程序链接的目标模块</strong>，该过程被称为<strong>动态链接（Dynamic Linking）</strong>。共享库在 Linux 系统中以<code>.so</code>为后缀，而在 Windows 系统中则被称为 DLL（Dynamic Linking Libraries）。</p><p>​ 在任意文件系统中，每个共享库都只有一个<code>.so</code>文件。与静态库不同的是，该文件中的代码和数据可以被引用该库的所有可执行文件共享，而不需要复制到可执行文件中。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">/* $begin main2 */</span><br><span class="line">/* main2.c */</span><br><span class="line">##include &lt;stdio.h&gt;</span><br><span class="line">##include &quot;vector.h&quot;</span><br><span class="line"></span><br><span class="line">int x[2] = &#123;1, 2&#125;;</span><br><span class="line">int y[2] = &#123;3, 4&#125;;</span><br><span class="line">int z[2];</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    addvec(x, y, z, 2);</span><br><span class="line">    printf(&quot;z = [%d %d]\n&quot;, z[0], z[1]);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">/* $end main2 */</span><br></pre></td></tr></table></figure><p>动态链接过程如下图所示：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/5b972378b0fdee43bd8cda4aa7b126b2.png" class><p>我们使用如下指令将 addvec.c 和 multvec.c 构建为共享库文件<code>libvector.so</code>:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">linux&gt; gcc -shared -fpic -o libvector.so addvec.c multvec.c</span><br></pre></td></tr></table></figure><p>其中，<code>-fpic</code>指示编译器生成 <strong>与位置无关代码（Position-Independent Code</strong>），而<code>-shared</code>则指示链接器创建共享目标文件。一旦共享库文件创建成功，就可以将其链接到示例程序中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">inux&gt; gcc -o prog2l main2.c ./libvector.so</span><br></pre></td></tr></table></figure><p>我们需要明确的是，<code>libvector.so</code>的任意代码和数据都没有被复制到可执行文件<code>prog2l</code>中。链接器只会复制一些重定位和符号表信息，它们将在加载时用于解析引用了共享库的符号。</p><p>加载器随后读取可执行文件中包含的动态链接器路径，加载并运行它。动态链接器也是一个共享库文件，如 Linux 系统的<code>ld-linux.so</code>。它通过执行以下重定位操作来完成链接：</p><ul><li>将<code>libc.so</code>的代码和数据重定位到某个内存段；</li><li>将<code>libvector.so</code>中的代码和数据重定位到另一个内存段；</li><li>将<code>prog2l</code>中所有引用了共享库的符号重定位。</li></ul><p>最终，动态链接器将控制权转移给应用程序，共享库的位置不会在程序执行期间改变。</p><h2 id="11-从应用程序中加载和链接共享库"><a href="#11-从应用程序中加载和链接共享库" class="headerlink" title="11.从应用程序中加载和链接共享库"></a>11.从应用程序中加载和链接共享库</h2><p>​ 应用程序还可以在运行时请求动态链接器加载和链接共享库，其应用场景包括：Windows 应用程序的开发人员使用共享库来分发软件更新；现代 Web 服务器使用<strong>动态链接有效地更新或添加功能</strong>。</p><p>Linux 系统为应用程序提供了一些简单接口以实现上述功能：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dlfcn.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">dlopen</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *filename, <span class="type">int</span> flag)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to handle if OK, NULL on error</span></span><br></pre></td></tr></table></figure><p>函数<code>dlopen</code>加载并链接共享库文件<code>filename</code>，参数<code>flag</code>可以是<code>RTLD_GLOBAL</code>、<code>RTLD_NOW</code>和<code>RTLD_LAZY</code>中的一个或多个（详见 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/https://man7.org/linux/man-pages/man3/dlopen.3.html">dlopen</a>）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">#<span class="meta">#<span class="keyword">include</span> <span class="string">&lt;dlfcn.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> *<span class="title function_">dlsym</span><span class="params">(<span class="type">void</span> *handle, <span class="type">char</span> *symbol)</span>;</span><br><span class="line"><span class="comment">// Returns: pointer to symbol if OK, NULL on error</span></span><br></pre></td></tr></table></figure><p>类似的接口函数还有 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/https://man7.org/linux/man-pages/man3/dlsym.3.html">dlsym</a>、<a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/https://man7.org/linux/man-pages/man3/dlclose.3.html">dlclose</a> 和 <a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/https://man7.org/linux/man-pages/man3/dlerror.3.html">dlerror</a>。<a href="./CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/http://csapp.cs.cmu.edu/2e/ics2/code/link/dll.c">示例程序</a> 展示了应用程序是如何调用它们来动态链接共享库的。</p><p>​ 共享库和 Java 本地接口</p><p>​ Java 定义了一个标准调用规则，叫做 Java 本地接口(Java Native Interface，JNI)，它允许 Java 程序调用“本地的”C 和 C++函数。JNI 的基本思想是将本地 C 函数，如 foo，编译到共享库中，如 foo.so。当一个正在运行的 Java 程序试图调用画数 fo0 时，Java 解释程序利用 dlopen 接口(或者与其类似的接口)动态链接和加载 foo.so，然后再调用 foo。</p><h2 id="12-与位置无关代码"><a href="#12-与位置无关代码" class="headerlink" title="12.与位置无关代码"></a>12.与位置无关代码</h2><p>​ 现代系统在编译共享库时会生成一种无需重定位即可被加载到内存中任意位置的代码，即与位置无关代码（Position-Independent Code，PIC），这样共享库就能被多个正在运行的进程同时引用。</p><h4 id="7-12-1-PIC-数据引用"><a href="#7-12-1-PIC-数据引用" class="headerlink" title="7.12.1 PIC 数据引用"></a>7.12.1 PIC 数据引用</h4><p>​ 编译器在 PIC 数据段的开头创建了一个全局偏移量表（Global Offset Table，GOT），其中的每个条目都对应一个被目标模块引用的全局符号。编译器还会为这些条目生成重定位记录。加载时，动态链接器重定位每个 GOT 条目，使其包含被引用符号的绝对地址。每个引用了全局符号的目标模块都有自己的 GOT。</p><p>下图展示了示例共享库<code>libvector.so</code>中的 GOT：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/20220331211838.png" class><p>​ 无论我们在何处加载共享模块，其数据段与代码段之间的距离始终相同。因此，代码段中的<code>addl</code>与数据段中的 GOT[3] 之间的偏移量是一个运行时常量。当函数<code>addvec</code>引用全局变量<code>addcnt</code>时，先通过<code>0x2008b9(%rip)</code>计算得到 GOT[3] 的地址，然后从中读取加载时被动态链接器赋予的<code>addcnt</code>的绝对地址。</p><h4 id="7-12-2-PIC-函数调用"><a href="#7-12-2-PIC-函数调用" class="headerlink" title="7.12.2 PIC 函数调用"></a>7.12.2 PIC 函数调用</h4><p>​ PIC 函数调用的运行时地址是在该函数第一次被调用时确定的，这种技术被称为延迟绑定（Lazy Binding）。当应用程序导入了一个包含成百上千个函数的共享库（如<code>libc.so</code>），却只调用其中一小部分的函数时，这种技术可以大大减少加载时不必要的重定位操作。</p><p>​ 延迟绑定是通过 GOT 和过程链接表（Procedure Linkage Table，PLT）共同实现的。只要目标模块调用了共享库中定义的函数，那么它就有自己的 GOT 和 PLT。上文提到，GOT 是数据段的一部分，而 PLT 则是代码段的一部分。</p><p>GOT 和 PLT 在运行时协同工作解析函数地址的过程如下图所示：</p><img src="/2024/08/28/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-7-%E9%93%BE%E6%8E%A5/20220401173947.png" class><p>​ 可执行文件中每个对共享库函数的调用都与 PLT 数组中的条目对应。其中，PLT[0] 是跳转到动态链接器的特殊条目，PLT[1] 对应系统启动函数<code>__libc_start_main</code>。从 PLT[2] 开始的条目对应用户代码调用的函数，如图中的<code>addvec</code>。</p><p>​ 当与 PLT 一起使用时，GOT [0] 和 GOT[1] 包含了动态连接器在解析函数地址时所需的信息，GOT[2] 是动态链接器的入口点。其余的每个条目均对应于一个在运行时需要被解析地址的调用函数，以及一个 PLT 条目。例如，GOT[4] 和 PLT[2] 与<code>addvec</code>对应。</p><p>程序第一次调用<code>addvec</code>并解析其地址的过程如上图（a）所示：</p><ol><li>PLT[2] 是该函数的入口，程序首先调用它；</li><li>PLT[2] 中的第一条指令间接跳转到 GOT[4]。由于最初每个 GOT 条目都指向对应 PLT 条目中的第二条指令，因此控制权将转移到 PLT[2] 中的第二条指令；</li><li>PLT[2] 中的第二条指令将<code>addvec</code>的 ID 0x1 压入栈中，第三条指令跳转到 PLT[0]；</li><li>PLT[0] 中的第一条指令将 *GOT[1] 压入栈中，第二条指令通过 GOT[2] 间接跳转到动态链接器。动态链接器根据被压入栈中的两个条目确定<code>addvec</code>的运行时地址并用它覆盖 GOT[4]，最终将控制权转移给<code>addvec</code>。</li></ol><p>程序再次调用<code>addvec</code>的过程如上图（b）所示：</p><ol><li>程序依然首先调用 PLT[2]；</li><li>此时 GOT[4] 指向了<code>addvec</code>，因此控制权将被直接转移到该函数。</li></ol><h2 id="13-库插入"><a href="#13-库插入" class="headerlink" title="13.库插入"></a>13.库插入</h2><p>​ <strong>库插入（Library Interpositioning</strong>）能够拦截程序对共享库函数的调用，并执行用户自定义的代码。基于这项技术，我们可以计算库函数的调用次数，验证并跟踪其输入和输出的值，甚至将其替换为完全不同的函数。</p><p>库插入的基本思想是创建一个与库函数原型相同的包装函数，然后“欺骗”系统调用包装函数而非库函数。通常，包装函数会执行自己的逻辑，调用库函数并将其返回值传递给调用者。</p><p>库插入可以在编译时、链接时以及运行时使用。</p><h2 id="14-处理目标文件的工具"><a href="#14-处理目标文件的工具" class="headerlink" title="14.处理目标文件的工具"></a>14.处理目标文件的工具</h2><p>​ 在 Linux 系统中有大量可用的工具可以帮助你理解和处理目标文件。特别地，GNU bimutils<br>包尤其有帮助，而且可以运行在每个 Linux 平台上。</p><ul><li>AR:创建静态库，插入、删除、列出和提取成员。</li><li>STRINGS:列出一个目标文件中所有可打印的字符串。</li><li>STRIP:从目标文件中删除符号表信息。</li><li>NM:列出一个目标文件的符号表中定义的符号，</li><li>SIZE:列出目标文件中节的名字和大小。</li><li>READELF:显示一个目标文件的完整结构，包括 ELF 头中编码的所有信息。包含 SIZE 和</li><li>NM 的功能。</li><li>OBJDUMP:所有二进制工具之母。能够显示一个目标文件中所有的信息。它最大的作用</li><li>是反汇编.text 节中的二进制指令。</li></ul><p>Unix 系统为操作共享库还提供了 LDD 程序:</p><ul><li>LDD:列出一个可执行文件在运行时所需要的共享库</li></ul>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_6_存储器层次结构</title>
      <link href="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/"/>
      <url>/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/</url>
      
        <content type="html"><![CDATA[<p>​ <strong>存储器系统(memory system)</strong> 是一个具有不同容量、成本和访问时间的存储设备的层次结构。CPU 寄存器保存着最常用的数据。靠近 CPU 的小的、快速的 <strong>高速缓存存储器(cache memory)</strong> 作为一部分存储在相对慢速的 <strong>主存储器(main memory，简称主存)</strong> 中的数据和指令的缓冲区域。主存暂时存放存储在容量较大的、慢速磁盘上的数据，而这些磁盘常常又作为存储在通过网络连接的其他机器的磁盘或磁带上的数据的缓冲区域。</p><p>​ 这里就是计算机系统中一个基本而持久的思想:如果你理解了系统是如何将数据在存储器层次结构中上上下下移动的，那么你就可以编写你的应用程序，使得它们的数据项存储在层次结构中较高的地方，在那里 CPU 能更快地访问到它们。</p><p>​ 这个思想围绕着计算机程序的一个称为 <strong>局部性(locality)</strong> 的基本属性。具有良好局部性的程序倾向于一次又一次地访问相同的数据项集合，或是倾向于访问邻近的数据项集合。具有良好局部性的程序比局部性差的程序更多地倾向于从存储器层次结构中较高层次处访问数据项，因此运行得更快。例如，不同的矩阵乘法核心程序执行相同数量的算术操作，但是有不同程度的局部性，它们的运行时间可以相差 20 倍!。我们向你展示如何分析 C 程序的局部性，而且我们还介绍改进你的程序中局部性的技术。你还会学到一种描绘某台机器上存储器层次结构的性能的有趣方法，称为 <strong>“存储器山”(memory mountain)</strong> ，它给出的读访问时间是局部性的一个数。</p><p>执行指令时访问数据所需的周期数：</p><ol><li>CPU 寄存器：0 个周期</li><li>L1L3 高速缓存：475 个周期</li><li>主存：上百个周期</li><li>磁盘：几千万个周期</li></ol><h2 id="1-存储技术"><a href="#1-存储技术" class="headerlink" title="1.存储技术"></a>1.存储技术</h2><p><strong>几种基本的存储技术</strong></p><ol><li><strong>随机访问存储器</strong>，分为两类：</li><li><ol><li><strong>RAM</strong>，同时也是<strong>易失性存储器</strong>，也分为两类：</li><li><ol><li><strong>SRAM</strong>：静态随机访问存储器，速度快，价格高。多用来作为高速缓存存储器。</li><li><strong>DRAM</strong>：动态随机访问存储器，速度慢，价格低。多用来作为主存和图形系统的帧缓冲器</li></ol></li><li><strong>ROM</strong>，同时也是<strong>非易失性存储器</strong>。<strong>闪存</strong>属于 ROM，<strong>固态硬盘</strong>就是基于闪存开发而来。</li></ol></li><li>机械硬盘</li><li>固态硬盘（SSD）</li></ol><h4 id="1-1-随机访问存储器"><a href="#1-1-随机访问存储器" class="headerlink" title="1.1 随机访问存储器"></a>1.1 随机访问存储器</h4><p>​ 随机访问存储器(Random-Access Memory，RAM)分为两类:静态的和动态的。静态 RAM(SRAM)比动态 RAM(DRAM)更快，但也贵得多。SRAM 用来作为高速缓存存储器，既可以在 CPU 芯片上，也可以在片下。DRAM 用来作为主存以及图形系统的帧缓冲区。典型地，一个桌面系统的 SRAM 不会超过几兆字节，但是 DRAM 却有几百或几千兆字节。</p><h6 id="1-静态-RAM（SRAM）"><a href="#1-静态-RAM（SRAM）" class="headerlink" title="1.静态 RAM（SRAM）"></a>1.静态 RAM（SRAM）</h6><p>SRAM 将每个位存储在一个双稳态的存储器单元内。每个单元由六个晶体管组成。</p><p>双稳态即该电路无限期地稳定保持在两个不同的电压状态。</p><p>对于 SRAM，只要有电，就永远地保持它的值。即使有干扰，当干扰消除，电路也会恢复到稳定</p><h6 id="2-动态-RAM（DRAM）"><a href="#2-动态-RAM（DRAM）" class="headerlink" title="2.动态 RAM（DRAM）"></a>2.动态 RAM（<strong>DRAM</strong>）</h6><p>DRAM 将每个位存储为对一个电容的充电。每个 DRAM 单元由一个电容和一个访问晶体管组成。</p><p>DRAM 对干扰非常敏感。当电容的电压被扰乱后，就永远不会恢复了</p><h6 id="3-SRAM-和-DRAM-的区别"><a href="#3-SRAM-和-DRAM-的区别" class="headerlink" title="3.SRAM 和 DRAM 的区别"></a><strong>3.SRAM 和 DRAM 的区别</strong></h6><p>只要有电源，SRAM 是持续的。与 DRAM 不同，不需要刷新。SRAM 的存取比 DRAM 快。SRAM 对诸如光和电噪声之类的干扰不敏感。其代价是 SRAM 电池比 DRAM 电池使用更多的晶体管，因此密度更低，价格更贵，消耗更多电力。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/f0bdbfd58ae7b34da811ae7aac5560ef.png" class><h6 id="4-传统的-DRAM"><a href="#4-传统的-DRAM" class="headerlink" title="4.传统的 DRAM"></a>4.传统的 DRAM</h6><p>​ DRAM 芯片中的单元(位)被分成 d 个<strong>超单元(supercell)</strong>，每个超单元都由 w 个 DRAM 单元组成，w 一般为 8。一个<em>dw</em>的 DRAM 总共存储了<em>dw</em>位信息。当从 DRAM 中读取数据时，一次可以读取一个超单元的数据（可以近似的将超单元理解为一个字节）。信息通过称为**引脚(pin)**的外部连接器流入和流出芯片。每个引脚携带一个 1 位的信号。</p><p>DRAM 中的超单元按行列组织，DRAM 中还包含一个行缓冲区。</p><p><strong>内存控制器</strong> <strong>依次</strong> 将行地址和列地址发送给 DRAM，DRAM 将对应的超单元的内容发回给内存控制器以实现读取数据。行地址和列地址共享相同的 DRAM 芯片地址引脚</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/q4HjUwednvJBWAK.png" class><p><strong>从 DRAM 中读取超单元的步骤：</strong></p><ol><li>内存控制器发来行地址 i，DRAM 将整个第 i 行复制到内部的行缓冲区。称为<strong>RAS(Row Access Strobe，行访问选通脉冲)请求</strong>。</li><li>内存控制器发来列地址 j，DRAM 从行缓冲区中复制出超单元 (i,j) 并发送给内存控制器。<strong>称为 CAS(Column<br>Access Strobe，列访问选通脉冲)请求</strong>。</li></ol><ul><li>注意：RAS 和 CAS 请求共享相同的 DRAM 地址引脚。</li></ul><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/byWis1uH8FIBPkR.png" class><h6 id="5-内存模块"><a href="#5-内存模块" class="headerlink" title="5.内存模块"></a>5.内存模块</h6><p>​ 许多 DRAM 芯片封装在<strong>内存模块</strong>中，插到主板的扩展槽上。常用的是<strong>双列直插内存模块 (DIMM)，以 64 位为块与内存控制器交换数据</strong>。</p><p>​ 比如一个内存模块包含 8 个 DRAM 芯片，每个 DRAM 包含 8M 个超单元，每个超单元存储一个字节（8bit）。<strong>使用 8 个 DRAM 芯片上相同地址处的超单元来表示一个 64 位字</strong>，DRAM 0 存储第一个字节，DRAM 1 存储第 2 个字节，依此类推。</p><p>​ 要取出内存地址 A 处的一个字，内存控制器先将 A 转换为一个超单元地址 (i,j)，然后内存模块将 i,j 广播到每个 DRAM。作为响应，每个 DRAM 输出它的 (i,j) 超单元的 8 位内容，合并成一个 64 位字，再返回给内存控制器。</p><p><strong>主存由多个内存模块连接到内存控制器聚合成。</strong></p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/iGR7WzclrCwB1Dv.png" class><p>接下来，设 r 表示一个 DRAM 阵列中的行数，c 表示列数，b.r 表示行寻址所需的位数,b.c 表示列寻址所需的位数。对于下面每个 DRAM，确定 2 的幂数的阵列维数，使得 max(b.r，b.c)最小，max(b.r，b.c)是对阵列的行或列寻址所需的位数中较大的值。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/4AKBRfjX6mgVU2H.png" class><h6 id="6-增强的-DRAM"><a href="#6-增强的-DRAM" class="headerlink" title="6.增强的 DRAM"></a>6.增强的 DRAM</h6><p>有一些经过优化的 DRAM：</p><ol><li><strong>快页模式 DRAM (FPM DRAM)</strong>：当连续访问位于同一行的超单元时，第二次以后，FPM DRAM 可以直接从行缓冲区获取数据。</li><li><strong>扩展数据输出 DRAM (EDO DRAM)</strong>：FPM DRAM 的一个增强的形式，更快一些。</li><li><strong>同步 DRAM (****SDRAM****)</strong>：常规的、FPM 和 EDO 都是异步的。从效果而言，SDRAM 可以比异步存储器更快地输出它的超单元的内容。</li><li><strong>双倍数据速率同步 DRAM(DDR SDRAM)</strong>：对 SDRAM 的一种增强，使速度翻倍。不同的 DDR SDRAM 以提高有效带宽的很小的预留缓冲区的大小来划分：DDR(2 位)、DDR2(4 位)、DDR3(8 位)。位越多速度越快，近乎翻倍。</li><li><strong>视频 RAM (VRAM)</strong>：用在图形系统的帧缓冲区中，其思想与 FPM DRAM 类似。VRAM 允许对内存进行并行地读和写。因此系统可以在写下一次更新的新值时（写），用帧缓冲区的像素刷屏幕（读）。</li></ol><h6 id="7-非易失性存储器"><a href="#7-非易失性存储器" class="headerlink" title="7.非易失性存储器"></a><strong>7.非易失性存储器</strong></h6><p>​ DRAM 和 SRAM 会在断电后丢失信息，因此是易失性存储器。<strong>ROM</strong> 是非易失性存储器，在断电后仍保存着信息。</p><p>ROM 是只读存储器，但是实际上有些 ROM 既可以读也可以写。</p><p><strong>几种常见的非易失性存储器：</strong></p><ol><li><strong>可编程 ROM (PROM)</strong>：只能被编程一次。</li><li><strong>可擦写可编程 ROM (EPROM)</strong>：可以被擦除和重编程上千次。</li><li><strong>电子可擦除 PROM (EEPROM)</strong>：类似于 EPROM，但是可以被重编程十万次。</li><li><strong>闪存</strong>：基于 EEPROM 的一种存储技术。闪存无处不在，<strong>固态硬盘就是一种基于闪存的磁盘驱动器</strong>。</li></ol><p>存储在 ROM 设备中的程序通常称为<strong>固件(frmware)</strong>。当一个计算机系统通电以后，它会运行存储在 ROM 中的固件。一些系统在固件中提供了少量基本的输入和输出函数–例如，PC 的 **BIOS(基本输人&#x2F;输出系统)**例程。复杂的设备，像图形卡和磁盘驱动控制器，也依赖固件翻译来自 CPU 的 IO(输入&#x2F;输出)请求。</p><h6 id="8-访问主存"><a href="#8-访问主存" class="headerlink" title="8.访问主存"></a>8.访问主存</h6><p>​ 数据流通过称为<strong>总线(bus)<strong>的共享电子电路在处理器和 DRAM 主存之间来来回回。每次 CPU 和主存之间的数据传送都是通过一系列步骤来完成的，这些步骤称为</strong>总线事务(bustransaction)</strong>。</p><ul><li><p>**读事务(read transaction)**从主存传送数据到 CPU。</p></li><li><p>**写事务(write transaction)**从 CPU 传送数据到主存。</p></li></ul><p>总线是一组并行的导线，能携带地址、数据和控制信号。</p><p><strong>系统总线</strong>连接 CPU 和 IO 桥接器，<strong>内存总线</strong>连接 IO 桥接器和主存。IO 桥同时也连接着 <strong>I&#x2F;O 总线</strong>。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/a3dcf3bc826d53044f98037023c53acb.png" class><p><strong>读事务</strong>的三个步骤：</p><ol><li>CPU 将地址 A 放到内存总线上。</li><li>主存从总线读出 A，取出字 x，然后将 x 放到总线上。</li><li>CPU 从总线读出字 x，并将它复制到相应寄存器中</li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/339f2d400708f6d104959422ba43df8b.png" class><p><strong>写事务</strong>的三个步骤：</p><ol><li>CPU 将地址 A 放到内存总线。主存读出这个地址，并等待数据字。</li><li>CPU 将数据字 y 放到总线上。</li><li>主存从总线读数据字 y，并将它存储在地址 A。</li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/3ac92a496f20898fcec5902f146c054c.png" class><h4 id="1-2-磁盘存储"><a href="#1-2-磁盘存储" class="headerlink" title="1.2 磁盘存储"></a>1.2 磁盘存储</h4><h6 id="1-磁盘构造"><a href="#1-磁盘构造" class="headerlink" title="1. 磁盘构造"></a><strong>1. 磁盘构造</strong></h6><p>略</p><h6 id="2-磁盘容量"><a href="#2-磁盘容量" class="headerlink" title="2. 磁盘容量"></a><strong>2. 磁盘容量</strong></h6><p>略</p><h6 id="3-磁盘操作"><a href="#3-磁盘操作" class="headerlink" title="3. 磁盘操作"></a><strong>3. 磁盘操作</strong></h6><p>略</p><h6 id="4-逻辑磁盘块"><a href="#4-逻辑磁盘块" class="headerlink" title="4.逻辑磁盘块"></a>4.<strong>逻辑磁盘块</strong></h6><p>略</p><h6 id="5-连接-I-O-设备"><a href="#5-连接-I-O-设备" class="headerlink" title="5. 连接 I&#x2F;O 设备"></a>5. 连接 I&#x2F;O 设备</h6><p>系统总线与内存总线都是与 CPU 相关的，而 IO 总线与 CPU 无关。</p><p>Intel 的**外部设备互连总线（PCI）**就是一种 IO 总线（广播总线）。</p><p>IO 总线速度相比于系统总线和内存总线慢，但是可以容纳种类繁多的第三方 IO 设备。</p><p>连接到 IO 总线的<strong>三种设备</strong>：</p><ol><li><strong>通用串行总线（USB）</strong>：USB 总线是一个广泛使用的<strong>标准</strong>，连接各种 IO 设备，包括键盘、鼠标等。</li><li>**显卡&#x2F;显示适配器：**负责代表 CPU 在显示器上画像素。</li><li>**主机总线适配器：**连接磁盘。常总的磁盘接口是 <strong>SCSI 和 SATA</strong>。其中 SCSI 比 SATA 更快也更贵。</li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/200afce0c40dc9a454550bfbddf9a422.png" class><h6 id="6-访问磁盘"><a href="#6-访问磁盘" class="headerlink" title="6.访问磁盘"></a><strong>6.访问磁盘</strong></h6><p>​ CPU 使用**内存映射 I&#x2F;O (memory-mapped I&#x2F;O)**来向 I&#x2F;O 设备发射命令。在使用内存映射 IO 的系统中，地址空间中有一块地址是专为与 IO 设备通信保留的，每个这样的地址称为一个 IO 端口。当一个设备连接到总线时，它与一个或多个端口相关联。</p><p>假设磁盘控制器映射到端口 0xa0，<strong>读一个磁盘扇区的步骤如下：</strong></p><ol><li>CPU 依次发送命令字、逻辑块号、目的内存地址三条指令到 地址 0xa0，发起一个磁盘读。因为磁盘读的时间很长，所以此后 CPU 会转去执行其他工作。</li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/4ed158cbd6bc3b547461648ceb26ecb1.png" class><ol start="2"><li>磁盘收到读命令后，将逻辑块号翻译成一个扇区地址，读取该扇区的内容，并将内容直接传送到主存，不需要经过 CPU (这称为直接内存访问(DMA))。</li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/488d9c3f16fa663d4c66dea5efee22ff.png" class><ol start="3"><li>DMA 传送完成后，即磁盘扇区的内容安全地存储在主存中后，磁盘控制器给 CPU 发送一个中断信号(Interrupt)来通知 CPU。</li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/80aaa6c0f95d1b6f78b66f070350437e.png" class><h4 id="1-3-固态硬盘"><a href="#1-3-固态硬盘" class="headerlink" title="1.3 固态硬盘"></a>1.3 <strong>固态硬盘</strong></h4><p><strong>固态硬盘 (SSD)</strong> 是一种基于闪存的存储技术。</p><p>一个固态硬盘中封装了一个<strong>闪存翻译层</strong>和多个闪存芯片。闪存翻译层是一个硬件&#x2F;固件设备，功能类似磁盘控制器，将对逻辑块的请求翻译成对底层物理设备的访问。</p><p>一个闪存由 B 个块的序列组成，每个块由 P 页组成，页的大小为 512byte~4kb。数据以页为单位进行读写。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/458f772f9bb9c637418d692c7c532c57.png" class><p>对于 SSD 来说，<strong>读比写快</strong>。因为只有在一页所属的块整个被擦除后，才能写这一页。重复写十万次后，块就会磨损，因此固态硬盘寿命较低。</p><p><strong>随机写 SSD 很慢的两个原因：</strong></p><ol><li>擦除块需要相对较长的时间。</li><li>如果写操作试图修改一个已经有数据的页，那么这个块中所有带有用数据的页都必须复制到一个新的块，然后才能向该页写数据。</li></ol><p>**SSD 相比于旋转磁盘的优点：**由半导体存储器构成，没有移动部件，所以更结实，随机访问也更快，能耗更低。</p><p><strong>缺点</strong>：更容易磨损，不过现在的 SSD 已经可以用很多年了。</p><p>基于闪存（flash memory）的存储技术</p><h4 id="1-4-存储技术趋势"><a href="#1-4-存储技术趋势" class="headerlink" title="1.4 存储技术趋势"></a>1.4 存储技术趋势</h4><p>**性能上：**SRAM &gt; DRAM &gt; SSD &gt; 旋转磁盘</p><p><strong>发展速度上</strong>：增加密度(降低成本) &gt; 降低访问时间</p><p>DRAM 和 磁盘的性能滞后于 CPU 的性能提升速度，两者之间的差距越来越大。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/7719fde4c6dafec46ee9c426c508f0a2.png" class><h2 id="2-局部性"><a href="#2-局部性" class="headerlink" title="2.局部性"></a>2.局部性</h2><p>​ 实际上弥补 CPU 和内存之间差距的关键，是程序的局部性。一个编写良好的计算机程序常常具有良好的<strong>局部性(locality)</strong>。也就是说，它们倾向于引用邻近于其他最近引用过的数据项的数据项，或者最近引用过的数据项本身。这种倾向性，被称为<strong>局部性原理(principle of locality)</strong>，是一个持久的概念，对硬件和软件系统的设计和性能都有着极大的影响。</p><p>​ 局部性是程序的一个基本属性。具有良好局部性的程序倾向于<strong>重复地访问相同的数据 (时间局部性 temporal locality)</strong>，或倾向于<strong>访问邻近的数据 (空间局部性 spatial locality)</strong>，因此运行更快。</p><p><strong>局部性有两种形式</strong>：时间局部性和空间局部性。</p><p>现代计算机系统的各个层次，从硬件到操作系统到应用程序都利用了局部性。</p><h4 id="2-1-对程序数据引用的局部性"><a href="#2-1-对程序数据引用的局部性" class="headerlink" title="2.1 对程序数据引用的局部性"></a><strong>2.1 对程序数据引用的局部性</strong></h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">for(int i=0; i&lt;N; ++i) &#123;</span><br><span class="line">sum += v[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上例中，sum 具有好的时间局部性，向量 v 具有好的空间局部性。</p><p>这里对向量 v 中元素的访问是顺序访问的，称为<strong>步长为 1 的引用模式</strong>。在空间局部性上，步长为 1 的引用模式是最好的。</p><h4 id="2-2-取指令的局部性"><a href="#2-2-取指令的局部性" class="headerlink" title="2.2 取指令的局部性"></a><strong>2.2 取指令的局部性</strong></h4><p>​ 程序指令存放在内存中，CPU 需要读这些指令，因此取指令也有局部性。比如 for 循环中的指令具有好的时间局部性和空间局部性。</p><h4 id="2-3-局部性小结"><a href="#2-3-局部性小结" class="headerlink" title="2.3 局部性小结"></a><strong>2.3 局部性小结</strong></h4><p><strong>评价局部性的简单原则：</strong></p><ol><li>重复引用相同变量的程序有好的时间局部性。</li><li>对于步长为 k 的引用模式的程序，k 越小，空间局部性越好。</li><li>对于取指令来说，循环有好的时间和空间局部性。循环体越小，循环迭代次数越多，局部性越好。</li></ol><h2 id="3-存储器层次结构"><a href="#3-存储器层次结构" class="headerlink" title="3.存储器层次结构"></a>3.存储器层次结构</h2><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/9a5f7056a064b85cd13ef10cd7b59960.png" class><h4 id="3-1-在存储器层次结构中的缓存"><a href="#3-1-在存储器层次结构中的缓存" class="headerlink" title="3.1 在存储器层次结构中的缓存"></a>3.1 在存储器层次结构中的缓存</h4><p>一般而言，高速缓存(cache)是一个小而快速的存储设备。使用高速缓存的过程称为缓存(caching)。</p><p><strong>存储器层次结构的中心思想</strong>：对于每个 k，位于 k 层的更快更小的存储设备作为位于 k+1 层的更大更慢的存储设备的缓存。换句话说，层次结构中的每一次都缓存来自较低一层的数据对象。</p><p>**缓存的具体实现：<strong>数据总是以</strong>块(block)**大小为传送单元(transfer unit)在第 k 层和第 k+1 层之间来回拷贝的。虽然在层次结构中任何一对相邻的层次之间块大小是固定的，但是其他的层次对之间可以用不同的块大小。</p><p>一般而言，层次结构较低的层(离 CPU 较远)的设备访问时间较长，因此为了补偿这些较长的访问时间，倾向于使用较大的块</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/968eef5426637935d231b7a7d020e33b.png" class><p><strong>1. 缓存命中</strong></p><p>当需要 k+1 层的某个数据对象 d 时，如果 d 恰好缓存在 k 层中，就称为缓存命中</p><p><strong>2. 缓存不命中</strong></p><p>缓存不命中时，第 k 层的缓存从 第 k+1 层缓存中取出包含 d 的块。</p><p>如果第 k 层缓存已经满了，需要根据<strong>替换策略</strong>选择一个块进行覆盖 (替换)，未满的话需要根据放置策略来选择一个块放置。</p><p><strong>3. 缓存不命中的种类</strong></p><ol><li><strong>冷不命中</strong>：一个空的缓存称为<strong>冷缓存</strong>，冷缓存必然不命中，称为冷不命中。</li><li>**冲突不命中：**常用的放置策略是将 k+1 层的某个块限制放置在 k 层块的一个小的子集中。比如 k+1 层的块 1,5,9,13 映射到 k 层的块 0。这会带来冲突不命中。</li><li><strong>容量不命中</strong>：当访问的工作集的大小超过缓存的大小时，会发生容量不命中。即缓存太小了，不能缓存整个工作集。</li></ol><p><strong>4. 缓存管理</strong></p><p>寄存器文件的缓存由编译器管理，L1,L2,L3 的缓存由内置在缓存中的硬件逻辑管理，DRAM 主存作为缓存由操作系统和 CPU 上的地址翻译硬件共同管理。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/fe4b962f01599eefb4a138a7d24ad91f.png" class><h4 id="3-2-存储器层次结构概念小结"><a href="#3-2-存储器层次结构概念小结" class="headerlink" title="3.2 存储器层次结构概念小结"></a>3.2 存储器层次结构概念小结</h4><p>存储器层次结构行之有效，因为较慢的设备比较快的设备更便宜，还因为程序偏向于展示局部性：</p><ul><li>利用时间局部性，同一数据对象可能会被多次使用</li><li>利用空间局部性，块通常包含有多个数据对象</li></ul><h2 id="4-高速缓存存储器"><a href="#4-高速缓存存储器" class="headerlink" title="4.高速缓存存储器"></a>4.高速缓存存储器</h2><p>L1 高速缓存的访问速度约为 4 个时钟周期，L2 约 10 个周期，L3 约 50 个周期。</p><p>当 CPU 执行一条读内存字 w 的指令，它首先向 L1 高速缓存请求这个字，如果 L1 没有就向 L2，依此而下。</p><h4 id="4-1-通用的高速缓存存储结构"><a href="#4-1-通用的高速缓存存储结构" class="headerlink" title="4.1 通用的高速缓存存储结构"></a>4.1 通用的高速缓存存储结构</h4><p>假设一个计算机系统中的存储器地址有 m 位，形成 M &#x3D;2^m 个不同的地址。m 个地址为划分为 <strong>t 个标记位</strong>，<strong>s 个组索引位</strong>，<strong>b 个块偏移位</strong>。</p><p>高速缓存被组织成 S&#x3D;2^s 个<strong>高速缓存组</strong>，每个组包含 E 个<strong>高速缓存行</strong>，<strong>每个行为一个数据块</strong>，包含一个<strong>有效位</strong>，t&#x3D;m-(b+s) 个<strong>标记位</strong>，和 B&#x3D;2^b 字节的<strong>数据块</strong>。高速缓存的容量 &#x3D; S _ E _ B。</p><p><strong>高速缓存可以通过简单地检查地址位来找到所请求的字。</strong></p><p>当 CPU 要从地址 A(由 m 个地址位组成) 处读一个字时：</p><ol><li>A 中的 s 个组索引位告诉我们在哪个组中</li><li>A 中的 t 个标记位告诉我们在这个组中的哪一行：当且仅当这一行设置了有效位并且标记位与 A 中的标记位匹配时，才说明这一行包含这个字。</li><li>A 中的 b 个块偏移位告诉我们在 B 个字节的数据块中的字偏移。</li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/c8140e57b956abeabe18f8c94a2d68df.png" class><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/6bee7795fe8f9ea08c72151e3e46aece.png" class><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/B4KzO5S7sC8hNeg.png" class><p><strong>理解</strong></p><p>使用高位做标记位，可以避免连续的块被映射到同一高速缓存组中。</p><p><strong>通过高速缓存从内存读字</strong></p><p>假设一个系统中只有 CPU、L1 高速缓存和主存。</p><p>当 CPU 执行一条从内存读字 w 的指令，如果 L1 有 w 的副本，就得到 L1 高速缓存命中；如果 L1 没有，就是缓存不命中。</p><p>当缓存不命中，L1 会向主存请求包含 w 的块(L1 中的块就是它的高速缓存行)的一个副本。当块从内存到达 L1，L1 将这个块存在它的一个高速缓存行里，然后从中抽取出字 w，并返回给 CPU。</p><p>高速缓存确定一个请求是否命中，然后抽取出被请求的字的过程分为三步:</p><ol><li><strong>组选择</strong></li><li><strong>行匹配</strong></li><li><strong>字抽取</strong></li></ol><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/mUPZ8qux5VJODks.png" class><p><strong>高速缓存有以下几类：</strong></p><ol><li><strong>直接映射高速缓存</strong>：每个组只有一行，即 E&#x3D;1。</li><li><strong>组相联高速缓存</strong>：每个组有多行，1</li><li><strong>全相联高速缓存</strong>：只有一个组，E&#x3D;C&#x2F;B。</li></ol><h4 id="4-2-直接映射高速缓存"><a href="#4-2-直接映射高速缓存" class="headerlink" title="4.2 直接映射高速缓存"></a>4.2 直接映射高速缓存</h4><p>每个组只有一行（E&#x3D;1）的高速缓存被称为直接映射高速缓存</p><h6 id="1-直接映射高速缓存中的组选择"><a href="#1-直接映射高速缓存中的组选择" class="headerlink" title="1. 直接映射高速缓存中的组选择"></a><strong>1. 直接映射高速缓存中的组选择</strong></h6><p>在这一步中，高速缓存从<strong>w 的地址中间抽取出 s 个组索引位</strong>。这些位被解释成一个对应于个组号的无符号整数。换句话来说，如果我们把高速缓存看成是一个关于<strong>组的一维数组</strong>，那么这些组索引位就是一个到这个数组的索引。图 6-30 展示了直接映射高速缓存的组选择是如何工作的。在这个例子中，组索引位 00001,被解释为一个选择组 1 的整数索引。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/0a1561d9733806ede8ffd1198bd7e4a1.png" class><h6 id="2-直接映射高速缓存中的行匹配"><a href="#2-直接映射高速缓存中的行匹配" class="headerlink" title="2. 直接映射高速缓存中的行匹配"></a><strong>2. 直接映射高速缓存中的行匹配</strong></h6><p>因为直接映射高速缓存每个组只有一行，只要这一行设置了有效位且标记位相匹配，就说明想要的字的副本确实存储在这一行中。</p><h6 id="3-直接映射高速缓存中的字抽取"><a href="#3-直接映射高速缓存中的字抽取" class="headerlink" title="3. 直接映射高速缓存中的字抽取"></a><strong>3. 直接映射高速缓存中的字抽取</strong></h6><p>从 w 的地址中抽取出 b 个块偏移位，块偏移位提供了所需的字的第一个字节的偏移。</p><h6 id="4-直接映射高速缓存不命中时的行替换"><a href="#4-直接映射高速缓存不命中时的行替换" class="headerlink" title="4. 直接映射高速缓存不命中时的行替换"></a><strong>4. 直接映射高速缓存不命中时的行替换</strong></h6><p>缓存不命中时需要从下一层取出被请求的块，然后将其存储在组索引位指示的组中的高速缓存行中。</p><p>因为直接映射高速缓存每个组只有一行，所以替换策略很简单：用新取出的行替换当前行。</p><h6 id="5-运行中的直接映射高速缓存"><a href="#5-运行中的直接映射高速缓存" class="headerlink" title="5. 运行中的直接映射高速缓存"></a><strong>5. 运行中的直接映射高速缓存</strong></h6><p>标记位和索引位连接起来标识了整个内存中的所有块，而高速缓存中的高速缓存组（块）是少于内存中的块数的。因此位于不同标记位，相同组索引位的块会映射到高速缓存中的同一个高速缓存组。</p><p>在一个高速缓存组中存储了哪个块，可以由标记位唯一地标识。</p><p>理解：对于主存中的整个地址空间，根据标记位不同将其分为了若干个部分，每个部分可以单独且完整地映射到高速缓存中，且刚好占满整个直接映射高速缓存。</p><h6 id="6-直接映射高速缓存中的冲突不命中"><a href="#6-直接映射高速缓存中的冲突不命中" class="headerlink" title="6. 直接映射高速缓存中的冲突不命中"></a><strong>6. 直接映射高速缓存中的冲突不命中</strong></h6><p>冲突不命中在直接映射高速缓存中很常见。因为每个组只有一行，不同标记位的块会映射到同一行，发生冲突不命中。</p><h6 id="7-综合-运行中的直接映射高速缓存"><a href="#7-综合-运行中的直接映射高速缓存" class="headerlink" title="7.综合:运行中的直接映射高速缓存"></a>7.综合:运行中的直接映射高速缓存</h6><p>​ 高速缓存用来选择组和标识行的机制极其简单。必须要这样，因为硬件必须在几个纳秒的时间内完成这些工作。不过，用这种方式来处理位对我们人来说是很令人困惑的。一个具体的例子能帮助我们解释清楚这个过程。假设我们有一个直接映射高速缓存，描述如下:<br>​ (S，E，B，m)&#x3D;(4，1，2，4)<br>​ 换句话说，高速缓存有四个组，每个组一行，每个块 2 个字节，而地址是 4 位的。我们还假设每个字都是单字节的。当然，这样一些假设完全是不现实的，但是它们能使示例保持简单。</p><p>当你初学高速缓存时，列举出整个地址空间并划分好位是很有帮助的，就像我们在图 6-32<br>中对 4 位的示例所做的那样。关于这个列举出的空间，有一些有趣的事情值得注意:</p><ul><li>**标记位和索引位连起来唯一地标识了存储器中的每个块。**例如，块 0 是由地址 0 和 1 组成的，块 1 是由地址 2 和 3 组成的，块 2 是由地址 4 和 5 组成的，依此类推。</li><li>因为有 8 个存储器块，但是只有 4 个高速缓存组，多个块映射到同一个高速缓存组<br>(即它们有相同的组索引)。例如，块 0 和 4 都映射到组 0，块 1 和 5 都映射到组 1<br>等等。</li><li>映射到同一个高速缓存组的块由标记位唯一地标识。例如，块 0 的标记位为 0，而块 4 的<br>标记位为 1，块 1 的标记位为 0，而块 5 的标记位为 1，以此类推。</li></ul><h4 id="4-3-组相联高速缓存"><a href="#4-3-组相联高速缓存" class="headerlink" title="4.3 组相联高速缓存"></a>4.3 组相联高速缓存</h4><p>​ 直接映射高速缓存中冲突不命中造成的问题是源于每一个组只有一行，组相联高速缓存（set associative cache）放松了这条限制，所以每个组都保存了有多于一行的高速缓存</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/195812beb411de0249eadb687b64ebb7.png" class><h6 id="1-组相联高速缓存中的组选择"><a href="#1-组相联高速缓存中的组选择" class="headerlink" title="1. 组相联高速缓存中的组选择"></a><strong>1. 组相联高速缓存中的组选择</strong></h6><p>与直接映射高速缓存一样，组索引位标识组。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/d645d63a307ce00384cfdd794fd18e96.png" class><h6 id="2-组相联高速缓存中的行匹配"><a href="#2-组相联高速缓存中的行匹配" class="headerlink" title="2. 组相联高速缓存中的行匹配"></a><strong>2. 组相联高速缓存中的行匹配</strong></h6><p>组相联高速缓存中的行匹配更复杂，因为要<strong>检查多个行的标记位和有效位</strong>，以确定其中是否有所请求的字。</p><p>注意：组中的任意一行都可能包含映射到这个组的内存块，因此<strong>必须搜索组中的每一行</strong>，寻找一个<strong>有效</strong>且<strong>标记位</strong>相匹配的行。</p><h6 id="3-组相联高速缓存中的字抽取"><a href="#3-组相联高速缓存中的字抽取" class="headerlink" title="3. 组相联高速缓存中的字抽取"></a><strong>3. 组相联高速缓存中的字抽取</strong></h6><p>与直接映射高速缓存一样，块偏移位标识所请求的字的第一个字节。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/5f816eb8b4fb94ceae68b929622752bd.png" class><h6 id="4-组相联高速缓存中不命中时的行替换"><a href="#4-组相联高速缓存中不命中时的行替换" class="headerlink" title="4. 组相联高速缓存中不命中时的行替换"></a><strong>4. 组相联高速缓存中不命中时的行替换</strong></h6><p><strong>几种替换策略</strong></p><ol><li>**随机替换策略：**随机选择要替换的行</li><li>**最不常使用策略：**替换在过去某个时间窗口内引用次数最少的一行。</li><li>**最近最少使用策略：**替换最后一次访问时间最久远的那一行。</li></ol><p>因为存储器层次结构中越靠下，不命中开销越大，好的替换策略越重要。</p><h4 id="4-4-全相联高速缓存"><a href="#4-4-全相联高速缓存" class="headerlink" title="4.4 全相联高速缓存"></a>4.4 全相联高速缓存</h4><p>全相联高速缓存由一个包含所有高速缓存行 (E&#x3D;C&#x2F;B) 的组组成。</p><p>因为高速缓存电路必须并行地搜索不同组已找到相匹配的标记，所以全相联高速缓存只适合做小的高速缓存。</p><p>DRAM 主存采用了全相联高速缓存，但是因为它采用了虚拟内存系统，所以在进行类似行匹配的页查找时不需要对一个个页进行遍历。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/5db3034bf836fa8a8811fdc7c33a4c1f.png" class><h6 id="1-全相联高速缓存中的组选择"><a href="#1-全相联高速缓存中的组选择" class="headerlink" title="1. 全相联高速缓存中的组选择"></a><strong>1. 全相联高速缓存中的组选择</strong></h6><p>全相联高速缓存中只有一个组，所以地址中没有组索引位，只有标记位和块偏移位。</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/310594023e2966d1704b4d3e079f5fd6.png" class><h6 id="2-全相联高速缓存中的行匹配和字抽取"><a href="#2-全相联高速缓存中的行匹配和字抽取" class="headerlink" title="2. 全相联高速缓存中的行匹配和字抽取"></a><strong>2. 全相联高速缓存中的行匹配和字抽取</strong></h6><p>与组相联高速缓存一样。与组相联高速缓存的区别在于规模大小</p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/bb212df182d4beaec000f0e0781f37e0.png" class><h4 id="4-5-有关写的问题"><a href="#4-5-有关写的问题" class="headerlink" title="4.5 有关写的问题"></a>4.5 有关写的问题</h4><p>写相比读要复杂一些。</p><p>写命中（写一个已经缓存了的字 w）的情况下，高速缓存更新了本层的 w 的副本后，如何处理低一层的副本有两种方法：</p><ol><li><strong>直写</strong>：立即将 w 的高速缓存块写回到低一层中。<ul><li>优点：简单</li><li>缺点：每次写都会占据总线流量</li></ul></li><li><strong>写回</strong>：尽可能地推迟更新，只有当替换算法要驱逐这个更新过的块时，才把它写到低一层中。<ul><li><strong>优点：利用了局部性，可以显著地减少总线流量。</strong></li><li>缺点：增加了复杂性。必须为每个高速缓存行维护一个额外的修改位，表明此行是否被修改过</li></ul></li></ol><p><strong>写不命中</strong>情况下的两种方法：</p><ol><li><p><strong>写分配</strong>：加载相应的低一层的块到本层中，然后更新这个高速缓存块。</p><ul><li><p>优点：利用写的空间局部性</p></li><li><p>缺点：每次不命中都会导致一个块从低一层传送到高速缓存</p></li></ul></li><li><p><strong>非写分配</strong>：避开高速缓存，直接把这个字写到低一层中</p></li></ol><p>直写一般与非写分配搭配，两者都更适用于存储器层次结构中的较高层。</p><p><strong>写回一般与写分配搭配，两者都更适用于存储器层次结构中的较低层，因为较低层的传送时间太长。</strong></p><p>因为硬件上复杂电路的实现越来越容易，所以现在使用写回和写分配越来越多。</p><h4 id="4-6-指令高速缓存和统一高速缓存"><a href="#4-6-指令高速缓存和统一高速缓存" class="headerlink" title="4.6 指令高速缓存和统一高速缓存"></a>4.6 指令高速缓存和统一高速缓存</h4><p><strong>三种高速缓存：</strong></p><ol><li>**i-cache：**只保存指令的高速缓存。i-cache 通常是只读的，因此比较简单。</li><li>**d-cache：**只保存程序数据的高速缓存。</li><li>**统一的高速缓存：**既保存指令又保存程序数据.</li></ol><p>现代处理器一般包括独立的 i-cache 和 d-cache，其中两个原因如下：</p><ol><li>使用两个独立的高速缓存，CPU 可以同时读一个指令字和一个数据字。</li><li>可以确保数据访问不会与指令访问形成冲突不命中（不过可能会使容量不命中增加）。</li></ol><h4 id="4-7-高速缓存参数的性能影响"><a href="#4-7-高速缓存参数的性能影响" class="headerlink" title="4.7 高速缓存参数的性能影响"></a>4.7 高速缓存参数的性能影响</h4><p><strong>高速缓存的性能指标</strong></p><ol><li>**命中率：**命中的内存引用比率。</li><li><strong>命中时间</strong>：从高速缓存传送一个字到 CPU 的时间，包括组选择、行确认和字抽取的实践。</li><li>**不命中处罚：**不命中产生的额外时间消耗。</li></ol><p><strong>几个影响因素</strong></p><ol><li>高速缓存大小：较大的高速缓存可以提高命中率，但是会运行得更慢，即增加命中时间。</li><li>块大小：较大的块更能利用空间局部性以提高命中率。但是对于给定的总容量，块越大高速缓存行就越少，不利用利用时间局部性。较大的块因为传送时间更长，所以也会增加不命中处罚。现代处理系统的高速缓存块一般为 64 字节。</li><li>相联度：参数 E 的选择的影响（每个组中高速缓存行数）。E 越高优点是：降低了高速缓存由于冲突不命中出现抖动的可能性。缺点：实现起来昂贵；增加命中时间；增加不命中处罚。</li><li>写策略：高速缓存越往下层，越可能使用写会而不是直写策略。</li></ol><h2 id="5-编写高速缓存友好的代码"><a href="#5-编写高速缓存友好的代码" class="headerlink" title="5.编写高速缓存友好的代码"></a>5.编写高速缓存友好的代码</h2><ol><li>让最常见的情况运行得快</li><li>在每个循环内部使缓存不命中数量小</li></ol><h2 id="6-综合：高速缓存对程序性能的影响"><a href="#6-综合：高速缓存对程序性能的影响" class="headerlink" title="6.综合：高速缓存对程序性能的影响"></a>6.综合：高速缓存对程序性能的影响</h2><p><strong>存储器山（memory mountain)</strong></p><img src="/2024/08/27/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-6-%E5%AD%98%E5%82%A8%E5%99%A8%E5%B1%82%E6%AC%A1%E7%BB%93%E6%9E%84/2018_1_28_1517112396218.jpg" class><h2 id="6-7-小结"><a href="#6-7-小结" class="headerlink" title="6.7 小结"></a>6.7 小结</h2><p>程序员可以通过编写有良好空间和时间局部性的程序来显著地改进程序的运行时间。利用基于 SRAM 的高速缓存存储器特别重要。</p>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSAPP_阅读笔记_5_优化</title>
      <link href="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/"/>
      <url>/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/</url>
      
        <content type="html"><![CDATA[<p>Don’t design bridges in ignorance of materials, and don’t design low-level software in ignorance of the underlying hardware.</p><p>​ 写程序最主要的目标就是使它在所有可能的情况下都正确工作.一个运行得很快但是给出错误结果的程序没有任何用处.程序员必须写出清晰简洁的代码，这样做不仅是为了程序员能够看懂代码，也是为了在检查代码和今后需要修改代码时，其他人能够读和理解代码.</p><p>编写高效程序一般需要以下理解几类活动：</p><ul><li>选择合适的算法和数据结构</li><li>编写出编译器能够有效优化以转换程高效可执行代码的源代码。（需要我们理解优化编译器的能力和局限性）</li><li>对于大型任务，将一个任务分成多份，这些部分可以在多核和多处理器的某种组合上并行地进行计算。（利用并行性）</li></ul><p>本章主要介绍第二项。具体而言:</p><ul><li>消除不必要的工作，例如: 不必要的函数调用、<strong>内存引用</strong>、条件测试。这些工作通常不依赖于执行环境的操作，有些也可以被编译器优化。</li><li>利用处理器提供的指令级并行能力，同时执行多条指令。</li><li>使用 profiler，确定程序中的关键路径并加以优化</li></ul><h2 id="1-优化编译器的能力和局限性"><a href="#1-优化编译器的能力和局限性" class="headerlink" title="1.优化编译器的能力和局限性"></a>1.优化编译器的能力和局限性</h2><p>gcc -O3 -o output_filename source_file.c</p><hr><p>编译器的优化行为需要满足一条基本的约束: “优化后的代码的行为和优化前的代码的行为相同”。</p><ul><li>大多数编译器分析局限在单个函数内<ul><li>基于整个程序的分析开销过于昂贵</li><li>新的 GCC 能够进行单个文件内部的过程间的分析</li></ul></li><li>大多数分析基于静态信息</li></ul><p>​ 编译器必须很小心地对程序只使用安全的优化，也就是说对于程序可能遇到的所有可能的情况，在 C 语言标准提供的保证之下，优化后得到的程序和未优化的版本有一样的行为。限制编译器只进行安全的优化，消除了一些造成不希望的运行时行为的可能原因，但是这也意味着程序员必须花费更大的力气写出程序使编译器能够将之转换成有效机器代码。为了理解决定一种程序转换是否安全的难度，让我们来看看下面这两个过程:</p><p>​ <img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-1.png" class><br>​</p><p>​ 乍一看，这两个过程似乎有相同的行为。它们都是将存储在由指针 yp 指示的位置处的值两次加到指针 xp 指示的位置处的值。另一方面，函数 twiddle2 效率更高一些。它只要求 3 次存储器引用(读<em>xp，读</em>yp，写<em>xp)，而 twiddle1 需要 6 次(2 次读</em>xp，2 次读<em>yp，2 次写</em>xp)。因此，如果要编译器编译过程 twiddle1，我们会认为基于 twiddle2 执行的计算能产生更有效的代码。</p><p>不过，考虑 xp 等于 yp 的情况。此时，函数 twiddle1 会执行下面的计算:<br>*Xp +&#x3D; *XP;<br>&#x2F;_[)puble value at xp _&#x2F;<br>*Xp +&#x3D; *XP;<br>&#x2F;_Double value at xp _&#x2F;<br>结果是 xp 的值会增加 4 倍。另一方面，函数 twiddle2 会执行下面的计算:<br><em>xp+&#x3D;2**xp;&#x2F;</em> Triple value at xp *&#x2F;<br>结果是 xp 的值会增加 3 倍。</p><p>​ 编译器不知道 twiddle1 会如何被调用，因此它必须假设参数 xp 和 yp 可能会相等。因此，它不能产生 twiddle2 风格的代码作为 twiddle1 的优化版本。这种两个指针可能指向同一个存储器位置的情况称为 **存储器别名使用(memory aliasing) **。在只执行安全的优化中，编译器必须假设不同的指针可能会指向存储器中同一个位置。</p><p><strong>解决</strong></p><ul><li>在内循环中使用本地变量进行 accumulate 以消除 aliasing</li><li>通过声明为<code>double *restrict a</code>，告知编译器指针参数无法重叠</li></ul><hr><p>​ 第二个妨碍优化的因素是函数调用。作为一个示例，考虑下面这两个过程：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-2-2.png" class><p>最初看上去两个过程计算的都是相同的结果，但是 func2 只调用 f1 次，而 func1 调用 f4 次。以 func1 作为源时，会很想产生 func2 风格的代码。不过，考虑一下代码：</p><p>​ <img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/e64c886d1ac1301ff44d50b03f32c702.png" class></p><p>这个函数有个副作用—它修改了全局程序状态的一部分。改变调用它的次数会改变程序的行为。特别地，假设开始时全局变量 counter 都设置为 0，对 func1 的调用会返回 0+1+2+3&#x3D;6，而对 func2 的调用会返回 4·0-0。大多数编译器不会试图判断一个函数是否没有副作用，因此任意函数都可能是优化的候选者，例如 func2 中的做法。相反，编译器会假设最糟的情况，并保持所有的函数调用不变。</p><p><strong>解决</strong></p><ul><li>使用内联函数</li><li>编程者自己进行代码移动(code motion)</li></ul><hr><h2 id="2-表示程序性能参数-Cycles-per-Element-CPE"><a href="#2-表示程序性能参数-Cycles-per-Element-CPE" class="headerlink" title="2.表示程序性能参数: Cycles per Element, CPE"></a>2.表示程序性能参数: Cycles per Element, CPE</h2><p>​ 我们引人度量标准<strong>每元素的周期数(CyclesPerElement，CPE)</strong>，作为一种表示程序性能并指导我们改进代码的方法。CPE 这种度量标准帮助我们在更详细的级别上理解迭代程序的循环性能。这样的度量标准对执行重复计算的程序来说是很适当的，例如处理图像中的像素，或是计算矩阵乘积中的元素。</p><p>​ <strong>CPE</strong>: <strong>计算&#x2F;处理单个元素所需要的时钟周期，CPE 值越小越好</strong>。当过程在一组元素上迭代时，该过程执行的时钟周期数和被处理的元素个数能够用一个线性函数来描述，这个线性函数的斜率就是 CPE。</p><p>​ 以下通过最小 2 乘估计所得到的线性函数，如图所示：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-2.png" class><h2 id="3-基础优化"><a href="#3-基础优化" class="headerlink" title="3.基础优化"></a>3.基础优化</h2><p>我们对以下函数进行分析：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/fac86485822c416f2e817bd923adb0b8.png" class><p>这些是初始的 CPE：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/b53c6237d9908d60d15b8a371f763134.png" class><h4 id="3-1-代码移动"><a href="#3-1-代码移动" class="headerlink" title="3.1 代码移动"></a>3.1 代码移动</h4><ul><li>code motion: 避免执行多次但是计算结果不变的操作，改用局部变量保存计算结果。</li><li>注意: 某些 code motion 优化是编译器能够发现的。</li></ul><p>具体而言，在循环判断语句中要避免每次都重复计算数组长度（消除循环的低效率）</p><p>​ <img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/27197b6ea0d639415ced3655d8df2b17.png" class><br>我们可以改成这样，接下来的 CPE 如图所示：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-5.png" class><p>这个优化是一类常见的优化的一个例子，称为代码移动(code motion)。这类优化包括识别要执行多次(例如在循环里)但是计算结果不会改变的计算。因而可以将计算移动到代码前面不会被多次求值的部分。在本例中，我们将对 veclength 的调用从循环内部移动到循环的前面。<br>优化编译器会试着进行代码移动。不幸的是，就像前面讨论过的那样，对于会改变在哪里调用函数或调用多少次的变换，编译器通常会非常小心。它们不能可靠地发现一个函数是否会有副<br>作用，因而假设函数会有副作用。例如，如果 veclength 有某种副作用，那么 combine1 和 combine2 可能就会有不同的行为。为了改进代码，程序员必须经常帮助编译器显式地完成代码的移动。</p><h4 id="3-2-减少过程调用"><a href="#3-2-减少过程调用" class="headerlink" title="3.2 减少过程调用"></a>3.2 减少过程调用</h4><p>注意到循环体内每次获取向量元素都要调用 get_vex_element 函数，考虑打破抽象，直接通过数组指针遍历：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/b99719a50a081c6a41f7c1065d3b9d98.png" class><p>然而结果性能没有显著增加，整数加法反而有所减小</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/cbdbefe26e79e7c381ca77aa09959342.png" class><p>这说明过程调用的开销并非性能瓶颈。</p><h4 id="3-3-消除不必要的内存引用"><a href="#3-3-消除不必要的内存引用" class="headerlink" title="3.3 消除不必要的内存引用"></a>3.3 消除不必要的内存引用</h4><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5f8a09af7ce887442c14120767bde793.png" class><p>通过分析上述汇编代码注意到循环体内每次从 dest 处读取值，同时也要写入 dest 处。考虑使用局部变量保存中间结果能够简化为:</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/ad21d529498631fa5e96635ea820ce07.png" class><p>对应的 c 代码为：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/e0546036f2ddb6f5d80187327a54987c.png" class><p>时间拥有显著的提升，如下：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-9.png" class><h4 id="3-4-其他优化"><a href="#3-4-其他优化" class="headerlink" title="3.4 其他优化"></a>3.4 其他优化</h4><h6 id="3-4-1-reduction-in-strength"><a href="#3-4-1-reduction-in-strength" class="headerlink" title="3.4.1. reduction in strength"></a>3.4.1. reduction in strength</h6><p>使用开销较小的操作替代开销较大的操作。</p><p>例如，使用 x &lt;&lt; 4 替代 16 * x</p><ul><li>Intel Nehalem: 整数乘法操作需要消耗 3 个 CPU 时钟周期，加法操作需要消耗 1 个 CPU 时钟周期</li></ul><h6 id="3-4-2-share-common-subexpressions"><a href="#3-4-2-share-common-subexpressions" class="headerlink" title="3.4.2. share common subexpressions"></a>3.4.2. share common subexpressions</h6><p>重用部分表达式，避免重复计算。例如下面通过单个局部变量的计算，减少了 3 次冗余计算</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">// version 1</span><br><span class="line">up = val[(i-1)*n + j];</span><br><span class="line">down = val[(i+1)*n + j];</span><br><span class="line">left = val[i*n + j-1];</span><br><span class="line">right = val[i*n + j+1];</span><br><span class="line">// version 2</span><br><span class="line">long inj = i*n + j;</span><br><span class="line">up = val [inj - n];</span><br><span class="line">down = val[inj + n];</span><br><span class="line">left = val[inj - 1];</span><br><span class="line">right = val[inj + 1];</span><br></pre></td></tr></table></figure><ul><li>GCC 的<strong>O1</strong>优化会优化这一点</li></ul><h2 id="4-理解现代处理器"><a href="#4-理解现代处理器" class="headerlink" title="4.理解现代处理器"></a>4.理解现代处理器</h2><p>​ 到目前为止，我们运用的优化都不依赖于目标机器的任何特性。这些优化只是简单地降低了过程调用的开销，以及消除了一些重大的“妨碍优化的因素”，这些因素会给优化编译器造成困难。随着试图进一步提高性能，我们必须考虑利用处理器微体系结构的优化，也就是处理器用来执行指令的底层系统设计。<br>​ 为了理解改进性能的方法，我们需要理解现代处理器的微体系结构。在实际的处理器中，是同时对多条指令求值，这个现象称为<strong>指令级并行</strong>。在某些设计中，可以有 100 条或更多条指令在处理中。采用一些精细的机制来确保这种并行执行的行为，正好能获得机器级程序要求的顺序语义模型的效果。</p><p>我们会发现两种 <strong>以 CPE 为单位（周期每元素）</strong> 的延迟界限描述了程序的最大性能。</p><ul><li>当一系列操作必须按照严格顺序执行时，就会遇到<strong>延迟界限(latencybound)</strong>，因为在下一条指令开始之前，这条指令必须结束。当代码中的数据相关限制了处理器利用指令级并行的能力时，延迟界限能够限定程序性能。</li><li>**吞吐量界限(throughput bound)**刻画了处理器功能单元的原始计算能力。这个界限是程序性能的终极限制</li></ul><h4 id="4-1-整体操作-乱序-超标量"><a href="#4-1-整体操作-乱序-超标量" class="headerlink" title="4.1 整体操作: 乱序 + 超标量"></a>4.1 整体操作: 乱序 + 超标量</h4><p>现代处理器一般是<strong>乱序</strong>且是<strong>超标量</strong>的。</p><ul><li><strong>超标量</strong>: 通过实现多个硬件单元，可以在每个时钟周期执行多个操作</li><li><strong>乱序</strong>: 指令执行的顺序和二进制代码中的顺序不一定相同</li></ul><p>架构如下图所示</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/e7832780740216a0dfbdcfea8e17c3ec.png" class title="中文版"><p>​</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-12.png" class title="英文版"><p>注意到，这种架构包含了两个单元</p><ul><li><p>指令控制单元</p><p>(ICU, Instruction Control Unit):</p><ul><li><p><strong>Fetch control</strong>: 包含分支预测的功能</p></li><li><p><strong>Instruction decode</strong>: 从 icache 中读取指令，然后翻译为一组微操作(x86)。例如，<code>addq %rax, %rdx</code>转换为单个微操作；<code>addq%rax, 8(%rdx)</code>转换为内存读取、加法和内存写入三个微操作。</p></li><li><p>Retirement Unit</p><p>: 退役单元控制寄存器文件，记录正在进行的处理并确保遵守顺序语义。指令译码时，和指令相关的信息入队，并一直保存在队列中，直到</p><ol><li>一条指令操作完成，且引起该指令运行的分支点也被认为预测正确，则该指令退役，对应的执行结果会更新寄存器</li><li>若某个分支点预测错误，则该指令会被 flush，执行结果被丢弃</li></ol></li></ul></li><li><p>执行单元</p><p>(EU, Execution Unit): 接收来自 ICU 的微操作，分发到各个功能单元执行，每个时钟周期一般有多个操作。</p><ul><li><strong>Load 和 Store 单元</strong>: 包含一个加法器计算地址，和 dcache 交互</li><li><strong>Branch 单元</strong>: 预测会执行的指令执行结果会保存在 EU 内的队列中，若 Branch 单元计算发现预测错误，则会丢弃保存的执行结果，并通知<strong>Fetch Control</strong>单元，之后才能获取正确的指令</li><li><strong>其它各种功能单元</strong>: 通常一个算术运算单元能够执行多种运算，例如: 整数运算、浮点乘、整数乘、分支等等</li></ul></li></ul><p>注意: 为了避免分支预测错误，任何对程序寄存器的更新都只会在指令退役时发生。</p><p>为了加快传送某个单元操作结果到另一个单元的速度，执行单元之间也可以进行数据交换。一个常用技术被称为<strong>寄存器重命名</strong>。</p><ul><li>当条更新寄存器 r 的指令译码时，产生标记，得到一个指向该操作结果的唯一标识符。条目(r，t)被加入到一张表中，该表维护着每个程序寄存器 r 与会更新该寄存器的操作的标记 t 之间的关联。</li><li>当随后以寄存器 r 作为操作数的指令译码时，发送到执行单元的操作会包含 t 作为操作数源的值。当某个执行单元完成第一个操作时，会生成一个结果(v，t)，指明标记为 t 的操作产生值 v。</li><li>所有等待 t 作为源的操作都能使用 v 作为源值，这就是一种形式的数据转发。</li></ul><p>通过这种机制，值可以直接从一个操作直接转发到另一个操作，就不需要先写入寄存器文件（<strong>隐含着需要分支判断正确，指令退役</strong>），之后再读出来。只要操作执行完成，无论预测失败与否，操作结果都可以转发并继续后续操作。注意: 该表只需维护写操作对应的寄存器，对于读操作，可以直接从寄存器文件获取这个操作数。</p><h4 id="4-2-功能单元的性能"><a href="#4-2-功能单元的性能" class="headerlink" title="4.2. 功能单元的性能"></a>4.2. 功能单元的性能</h4><p>Intel Core i7 Haswell CPU 有下面 8 个功能单元:</p><ul><li>整数运算，浮点乘法，整数和浮点除法，分支</li><li>整数运算，浮点加法，整数乘，浮点乘</li><li>load，地址计算</li><li>load，地址计算</li><li>store</li><li>整数运算</li><li>整数运算，分支</li><li>store、store 地址计算</li></ul><p>它们的性能参数分别如下</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-13.png" class><ul><li><strong>latency</strong>: 完成运算需要的总时间</li><li><strong>issue</strong>: 两个连续同类型的运算的最小发射间隔时钟周期数</li><li><strong>capacity</strong>: 能够执行该功能单元的数量</li></ul><p>从以上性能参数我们能够分析得到下面这些结论</p><ul><li>乘法和加法的 issue 时间均为 1，然而单个乘法操作以及浮点加法操作的 latency 均大于 1，这是利用了流水线技术。issue 时间为 1 的功能单元被称为<strong>完全流水线化（fully pipelined）</strong>。例如，浮点浮点加法的三个流水线级分别为<strong>处理指数</strong>，<strong>小数相加</strong>，<strong>结果舍入</strong>。</li><li>除法的 latency 和 issue 时间相同。这意味着每开始一次除法操作都需要首先完成上一次的除法操作。</li></ul><p><strong>功能单元的最大吞吐量</strong>: 对一个容量为 C，发射时间为 I 的功能单元，它的最大吞吐量为 C&#x2F;IC&#x2F;I。</p><p>对于不同的功能单元而言，它们的两个<strong>CPE 界限（即单位是周期每元素）</strong>: 延迟界限（必须顺序执行时的 CPE 值）和吞吐量界限分别为:</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-14.png" class><h4 id="4-2-处理器操作的抽象模型"><a href="#4-2-处理器操作的抽象模型" class="headerlink" title="4.2 处理器操作的抽象模型"></a>4.2 处理器操作的抽象模型</h4><p>​ 我们会使用程序的<strong>数据流(data-fow)<strong>表示，作为分析在现代处理器上执行的机器级程序性能的一个工具，这是一种图形化的表示方法，展现了不同操作之间的数据相关是如何限制它们的执行顺序的。这种限制形成了图中的</strong>关键路径(critical path)</strong>，这是执行一组机器指令所需时钟周期数的一个下界。</p><p>​ 在继续技术细节之前，检査一下对函数 combine4 所获得的 CPE 测量值是很有帮助的，到目前为止 combine4 是最快的代码:</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-15.png" class><p>注意到除了整数加法操作，其它操作的 CPE 值和延迟界限相同。事实上，此时整数加法中的<strong>数据相关</strong>构成了程序的关键路径。</p><p>可以通过数据流图研究这种相关性。对于内循环的汇编代码</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">.L25:</span><br><span class="line"> vmulsd (%rdx), %xmm0, %xmm0   ## multiply acc by data[i]</span><br><span class="line"> addq   $8, %rdx               ## increment data+i</span><br><span class="line"> cmpq   %rax, %rdx             ## %rdx - %rax</span><br><span class="line"> jne    .L25</span><br></pre></td></tr></table></figure><p>可以画出如下的数据流图</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-16.png" class><p>注意到<code>vmulsd</code>指令被翻译成两个操作: load 和 mul。</p><p>循环中使用的寄存器能够被分成四类:</p><ul><li>只读:这些寄存器只用作源值，可以作为数据，也可以用来计算存储器地址，但是在循环中它们是不会被修改的。循环 combine4 的只读寄存器是 rax 和 rbp。</li><li>只写:这些寄存器作为数据传送操作的目的。在本循环中没有这样的寄存器。</li><li>局部:这些寄存器在循环内部被修改和使用，迭代与迭代之间不相关。在这个循环中，条件码寄存器就是例子:cmp 操作会修改它们，然后 jg 操作会使用它们，不过这种相关是在单次迭代之内的。</li><li>循环:对于循环来说，这些寄存器既作为源值，又作为目的，一次选代中产生的值会在另一次迭代中用到。可以看到，%rdx 和 xmm0 是 combine4 的循环寄存器，对应于程序值 i 和 acc。</li></ul><p>接下来可以看到，<strong>循环寄存器</strong>之间的操作链决定了限制性能的数据相关。</p><p>进一步对数据流图进行优化，消除不直接影响数据流的操作（即 cmp 和 jne）以及循环寄存器后有：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-17.png" class><p>上图右侧的数据流表示的是单次迭代中进行的操作。当绘制多次迭代可以注意到</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-18.png" class><p>注意到程序存在两条数据相关链:</p><ul><li>mul 操作对%xmm0 的修改</li><li>add 操作对%rdx 的修改</li></ul><p>在单精度浮点乘法条件下，由于 mul 操作的执行需要 5 个时钟周期，而数据依赖的情况下迭代 n 次就需要 5n 个时钟周期。加法操作需要 1 个时钟周期，因此 n 次迭代整体仅需要 n 个时钟周期。所以<strong>关键路径为 mul 操作的数据依赖</strong>。</p><p><strong>接下来我们希望提高重新调整操作的结构，增强指令级并行。具体而言，我们需要对程序做变换，使得唯一的限制因素是吞吐量界限</strong>。</p><h2 id="5-程序变换：循环展开"><a href="#5-程序变换：循环展开" class="headerlink" title="5.程序变换：循环展开"></a>5.程序变换：循环展开</h2><p><strong>循环展开</strong>: 通过增加一次迭代内的处理元素数，减少迭代次数。</p><ul><li><strong>减少循环开销</strong>: 减少不直接有助于得到程序结果的操作的数量，如条件判断</li><li><strong>缩短关键路径</strong>: 提供了减少关键路径上操作数量的方法</li></ul><p>修改后 k&#x3D;2 后的代码（每次进行操作 k 次）：2x1 循环</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/98f982bce14c452f71c571fcf601884b.png" class><p>对应获得的性能为：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-20.png" class><p><strong>注意到仍没有超过延迟界限，这是因为关键路径上仍有 n 个 mul 操作，仅是将循环开销减少了一半。</strong></p><h2 id="6-提高并行性"><a href="#6-提高并行性" class="headerlink" title="6. 提高并行性"></a>6. 提高并行性</h2><p>注意到，虽然程序性能受到运算单元的延迟限制，但是加法和乘法运算单元能够完全流水线化，然而循环展开并不能利用这种能力。<strong>本质原因在于我们使用单个累计变量，仅在该变量上一个值计算完成后，才能累积计算下一个值。</strong></p><h4 id="6-1-程序变换：提高并行性"><a href="#6-1-程序变换：提高并行性" class="headerlink" title="6.1 程序变换：提高并行性"></a>6.1 程序变换：提高并行性</h4><p>对于一个可交换且可结合的合并运算，我们可以将一组合并运算分割成两个或多个部分，并在最后合并结果以提高性能。</p><p>例如使用 2x2 循环展开的代码如下：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/b15da5717ca38d4c29177d86ad88585a.png" class><p>此时可以做到两路并行乘法操作。对应的性能结果为：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-23.png" class><p>注意到几乎所有操作都改进了大约一倍。</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-24.png" class><p>由于每个关键路径只包括 1&#x2F;2 个 mul 操作，因此 CPE 值减小为原来的 1&#x2F;2。</p><p>考虑上述累计变量变换的一般形式，将循环展开为 k 次，同时并行使用 k 个循环变量。注意到当 k&#x3D;10 时，几乎能达到吞吐量界限。这是因为<strong>为了达到吞吐量界限，通常需要所有流水线都是满的，对延迟为 L，容量为 C 的操作而言，就需要循环展开因子 k &gt;&#x3D; L*C</strong>。</p><p>例如，浮点乘的 L&#x3D;5，C&#x3D;2，则 k 需要大于等于 10。而浮点加有 L&#x3D;3，C&#x3D;1，因此 k 大于等于 3 就可以达到最大吞吐量。</p><h4 id="6-2-程序变换-重新结合变换"><a href="#6-2-程序变换-重新结合变换" class="headerlink" title="6.2. 程序变换: 重新结合变换"></a>6.2. 程序变换: 重新结合变换</h4><p>重新结合变换: 变换累积变量和向量元素的合并顺序。</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/bafa7e71320a3cdec1d53bb736ebca0e.png" class><p>上面的变换被称为 2x1a unrolling。步长为 2，单个累积变量。</p><p>通过变换合并顺序，关键路径也减小了一半。</p><p>5-28 说明了 combine7 内循环的代码(对于单精度乘积的情况)是如何被译码成操作，以及得到的数据相关。我们看到，来自于 movss 和第一个 mulss 指令的 load 操作从存储器中加载向量元素 i 和 i+1，第一个 mul 操作把它们乘起来。然后，第二个 mul 操作把这个结果乘以累积值 acc。图 5-29 给出了我们如何对图 5-28 的操作进行重新排列、优化和抽象，得到表示一次迭代中数据相关的模板(见图 5-29b)。对于 combine5 和 combine7 的模板，有两个 1oad 和两个 mu1 操作，但是只有一个 mu1 操作形成了循环寄存器间的数据相关链。然后，把这个模板复制 n&#x2F;2 次，给出了 n 个向量元素相乘所执行的计算(图 5-30)，我们可以看到关键路径上只有 n&#x2F;2 个操作。每次迭代内的第一个乘法都不需要等待前一次迭代的累积值就可以执行。因此,最小可能的 CPE 减少了 2 倍。当我们增加 k 值时，每次迭代中关键路径上一直只有一个操作。</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/cb7603e5b8919c268d2a3f89713f78d4.png" class><p>得到的性能如表所示：</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-28.png" class><p>注意到和 2x2 loop unrolling 相比，2x1a 计算的 CPE 大致相同，但是 2x2 loop unrolling 能够并行利用两个 load 单元。</p><p>注: <strong>由于浮点计算的不可结合性，编译器通常不会使用这些方式对浮点运算进行优化。</strong></p><h4 id="6-3-使用-SIMD-指令"><a href="#6-3-使用-SIMD-指令" class="headerlink" title="6.3. 使用 SIMD 指令"></a>6.3. 使用 SIMD 指令</h4><p>通过使用 AVX 指令，可以进一步提高并行性。</p><img src="/2024/08/26/CSAPP/CSAPP-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-5-%E4%BC%98%E5%8C%96/5-30.png" class><h4 id="6-3-小结"><a href="#6-3-小结" class="headerlink" title="6.3. 小结"></a>6.3. 小结</h4><p>注意到，功能单元的吞吐量界限是一个极限下界。假设程序要执行 n 个操作，而硬件共有 c 个功能单元，且每个单元的发射时间为 i，则程序至少需要（n&#x2F;c）∗i 个时钟周期。</p><h2 id="7-性能优化的限制因素"><a href="#7-性能优化的限制因素" class="headerlink" title="7. 性能优化的限制因素"></a>7. 性能优化的限制因素</h2><ol><li><p>寄存器溢出</p><ul><li>循环并行性受汇编代码（通用寄存器资源）描述计算能力的限制。因此循环展开无法做到无限扩展，事实上之前的例子当从 10x10 扩展到 20x20 时，由于寄存器溢出，程序变量值会被存储在栈中，因而导致性能下降。</li></ul></li><li><p>分支预测与预测惩罚</p><ul><li><p>原则 1:</p><p>不要过分关心可预测的分支。</p><ul><li>之前的示例中将每次迭代的元素获取从<code>get_vec_element()</code>中拿出来，然而性能基本没有变化，这说明<strong>分支在高度可预测的情况下，边界检查几乎不会影响性能</strong>。</li><li>注意: 这里说的是<strong>可预测的分支</strong>！！！对于难以预测的分支，性能还是会有大的变化。</li><li>分支预测失败，有大约 20 个周期的惩罚。</li></ul></li><li><p>原则 2:</p><p>书写适合用条件传送实现的代码。</p><ul><li>使用条件传送替换传统的基于分支跳转的实现: 计算分支两个方向上的值，然后根据条件使用某一个方向上的值。</li></ul></li></ul></li></ol><p>GCC 倾向于将如下<strong>函数式</strong>风格的代码转化为使用条件传送指令，该风格倾向于用条件操作来计算值，然后用值更新程序状态。相反，<strong>命令式</strong>风格倾向于根据条件语句有选择地更新程序状态。一个例子如下:</p><p><strong>命令式</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">/*Rearrange two vectors so that for each i, bli]&gt;= a[i] */</span><br><span class="line">void minmax1(long all, long b[], long n)&#123;</span><br><span class="line">long i;</span><br><span class="line">for(i=0;i&lt;n;i++) &#123;</span><br><span class="line">if(a[i]&gt; b[i])&#123;</span><br><span class="line">long t= ali];</span><br><span class="line">a[i]= b[i];</span><br><span class="line">b[i] = t;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>函数式</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">/*Rearrange two vectors so that for each i, bli]&gt;= a[i] */</span><br><span class="line">void minmax1(long all, long b[], long n)&#123;</span><br><span class="line">long i;</span><br><span class="line">for(i=0;i&lt;n;i++) &#123;</span><br><span class="line">long min = ali]&lt; b[i]? a[i] : b[i];</span><br><span class="line">long max = ali]&lt; b[i] ? b[i] : a[i];</span><br><span class="line">a[i]= min;</span><br><span class="line">b[i]= max;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> CSAPP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> 阅读笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>组件化OS--aceros的改进：支持和优化dpdk网络协议栈</title>
      <link href="/2024/06/20/arceos-os%E8%AE%AD%E7%BB%83%E8%90%A5/%E7%BB%84%E4%BB%B6%E5%8C%96OS--aceros%E7%9A%84%E6%94%B9%E8%BF%9B%EF%BC%9A%E6%94%AF%E6%8C%81%E5%92%8C%E4%BC%98%E5%8C%96dpdk%E7%BD%91%E7%BB%9C%E5%8D%8F%E8%AE%AE%E6%A0%88/"/>
      <url>/2024/06/20/arceos-os%E8%AE%AD%E7%BB%83%E8%90%A5/%E7%BB%84%E4%BB%B6%E5%8C%96OS--aceros%E7%9A%84%E6%94%B9%E8%BF%9B%EF%BC%9A%E6%94%AF%E6%8C%81%E5%92%8C%E4%BC%98%E5%8C%96dpdk%E7%BD%91%E7%BB%9C%E5%8D%8F%E8%AE%AE%E6%A0%88/</url>
      
        <content type="html"><![CDATA[<h2 id="移植-dpdk"><a href="#移植-dpdk" class="headerlink" title="移植 dpdk"></a>移植 dpdk</h2><h3 id="dpdk概述"><a href="#dpdk概述" class="headerlink" title="dpdk概述"></a>dpdk概述</h3><p>这个是dpdk的框架，我们可以从几方面去讲述</p><p><strong>（1）核心部件库-core Libraries</strong></p><ul><li><p>通过环境抽象层（EAL）的运行环境进行初始化，包括巨页内存分配、内存&#x2F;缓冲区&#x2F;队列分配与无锁操作、CPU 亲和性绑定等。</p></li><li><p>其次，EAL 实现了对操作系统内核与底层网卡 I&#x2F;O 操作的屏蔽（I&#x2F;O 旁路了内核及其协议栈），为 DPDK 应用程序提供了一组调用接口，通过 UIO 或 VFIO 技术将 PCI 设备地址映射到用户空间，方便了应用程序的调用，避免了网络协议栈和内核切换造成的处理时延。</p></li><li><p>核心部件还包括创建适合报文处理的内存池、缓冲区分配管理、内存复制、定时器、环型缓冲区管理等。</p></li></ul><p><strong>（2）平台相关模块-platform</strong></p><p>其内部模块主要包括 KNI、能耗管理以及 IVSHMEM 接口。</p><ul><li>KNI 模块主要通过 kni.ko 模块将数据报文从用户态传递给内核态协议栈处理，以便用户进程使用传统的 Socket 接口对相关报文进行处理。</li><li>能耗管理则提供了一些 API，应用程序可以根据分组接收速率动态调整处理器频率或进入处理器的不同休眠状态。</li><li>IVSHMEM 模块提供了虚拟机与虚拟机之间，或者虚拟机与主机之间的零复制共享内存机制，当 DPDK 程序运行时，IVSHMEM 模块会调用核心部件库 API，把几个巨页映射为一个 IVSHMEM 设备池，并通过参数传递给 QEMU，这样，就实现了虚拟机之间的零复制内存共享。</li></ul><p><strong>（3）轮询模式驱动模块-PMD-natives &amp; virtual</strong></p><p>PMD 相关 API 实现了在轮询方式下进行网卡报文收发，避免了常规报文处理方法中因采用中断方式造成的响应时延，极大提升了网卡收发性能。此外，该模块还同时支持物理和虚拟化两种网络接口，从仅仅支持 Intel 网卡，发展到支持 Cisco、Broadcom、Mellanox、Chelsio 等整个行业生态系统以及基于 KVM、VMware、XEN 等虚拟化网络接口。</p><p><strong>（4）Classify 库</strong></p><p>支持精确匹配（Exact Match）、最长匹配（LPM）和通配符匹配（ACL），提供常用包处理的查表操作</p><p><strong>（5）QoS 库</strong></p><p>提供网络服务质量相关组件，如限速（Meter）和调度（Sched）</p><h3 id="DPDK-库函数"><a href="#DPDK-库函数" class="headerlink" title="DPDK 库函数"></a>DPDK 库函数</h3><p>todo()</p><h3 id="实现过程"><a href="#实现过程" class="headerlink" title="实现过程"></a>实现过程</h3><p>首先我们需要实现内核中的UIO机制，<strong>UIO 能够拦截中断，并重设中断回调行为（相当于hook了，这个功能还是要在内核实现的，因为硬件中断只能在内核处理），从而绕过内核协议栈后续的处理流程.</strong></p><p>让用户空间的程序<strong>拦截</strong>内核的中断，更改中断的handler处理函数，让用户空间的程序第一时间拿到刚从网卡接收到的“一手、热乎”数据，减少内核的数据处理流程！由于应用程序拿到的是网络链路层（也就是第二层）的数据，这就需要应用程序自己按照协议解析数据了！说个额外的：这个功能可以用来抓包**！</p>]]></content>
      
      
      
        <tags>
            
            <tag> OS </tag>
            
            <tag> net </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>2025 年春夏开源操作系统训练营三阶段总结</title>
      <link href="/2024/05/22/arceos-os%E8%AE%AD%E7%BB%83%E8%90%A5/2025%20%E5%B9%B4%E6%98%A5%E5%A4%8F%E5%BC%80%E6%BA%90%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E8%AE%AD%E7%BB%83%E8%90%A5%E4%B8%89%E9%98%B6%E6%AE%B5%E6%80%BB%E7%BB%93/"/>
      <url>/2024/05/22/arceos-os%E8%AE%AD%E7%BB%83%E8%90%A5/2025%20%E5%B9%B4%E6%98%A5%E5%A4%8F%E5%BC%80%E6%BA%90%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E8%AE%AD%E7%BB%83%E8%90%A5%E4%B8%89%E9%98%B6%E6%AE%B5%E6%80%BB%E7%BB%93/</url>
      
        <content type="html"><![CDATA[<h1 id="2025-年春夏开源操作系统训练营三阶段总结"><a href="#2025-年春夏开源操作系统训练营三阶段总结" class="headerlink" title="2025 年春夏开源操作系统训练营三阶段总结"></a>2025 年春夏开源操作系统训练营三阶段总结</h1><h2 id="第一阶段总结"><a href="#第一阶段总结" class="headerlink" title="第一阶段总结"></a>第一阶段总结</h2><p>学习 rust 的语法，发现很多都是现代 c++的写法，遂上手很快，但与 c++不同的是，其拥有所有权机制，人为限制了 c++中不安全的写法，对内存更加安全，改正了自己之前写 c++时候的乱用指针，引用的不安全的写法，同时也学会了一门新的语言。</p><h2 id="第二阶段总结"><a href="#第二阶段总结" class="headerlink" title="第二阶段总结"></a>第二阶段总结</h2><h3 id="lab1"><a href="#lab1" class="headerlink" title="lab1"></a>lab1</h3><p>为了实现 <code>sys_trace</code> 系统调用（ID 为 410），我们查看了任务系统的机构，在其中人为的加上了我们所需要的参数，使得我们对 TCB 的结构熟悉和了解</p><h3 id="lab2"><a href="#lab2" class="headerlink" title="lab2"></a>lab2</h3><p>我们实现了 mmap syscall ID：222 和 munmap syscall ID：215 的系统调用，其中，我们了解到每个任务的 mm，也就是 TCB 中 memory_set,中的结构有了更加深刻的了解。我们只需要在其中的 area 完成插入和删除。</p><h3 id="lab3"><a href="#lab3" class="headerlink" title="lab3"></a>lab3</h3><p>这里要求我们实在 spawn 进程，因此，我们可以根据将其类似为 fork + exec ，其中，不必像 fork 一样复制父进程的地址空间。我们只需要新建一个地址空间，其中，我们需要新建一些虚拟区域，然后就可以完成，整体类似于与 fork + exec 的逻辑，其中，只是没有复制其地址空间。</p><p>然后，要我们完成 stride 调度算法，我们可以直接参考 manager 中的 fifo 的实现来进行更改，其中，manager 增加 BIG_STRIDE 这个成员，然后 tcb 中的 inner 加入 priority 和 Stride 这两个成员（因为是可变的）。这个时候，我们需要每次调度的时候寻找最小的 Stride，linux 中是采用红黑树查询的，但是这里比较简单，我们就直接使用轮询的方式。</p><h3 id="lab4"><a href="#lab4" class="headerlink" title="lab4"></a>lab4</h3><p>要求我们去完成<code>pub fn sys_linkat(_old_name: *const u8, _new_name: *const u8) -&gt; isize</code>这样的调用接口，仿照上面的 sys_open()我们可以也写一个类似于 open_file 的函数，其形式为：<code>pub fn __sys_linkat(_old_name: &amp;str, _new_name: &amp;str) -&gt; isize</code>我们在__sys_linkat 中就可以调用 ROOTINODE 中的 linkat 方法。对于 linkat 方法，我们可以参照上面 create 方法，我们只需要注意以下：<code>pub fn linkat(&amp;self, _old_name: &amp;str, _new_name: &amp;str) -&gt; isize</code></p><ul><li>判断 oldname 的文件不为空，为空直接返回负一</li><li>由 oldname 中寻找 inode_id,而不是像 create 中直接 alloc_inode 得到</li></ul><p><strong>unlinkat</strong>:</p><p> 前面于 linkat 相同，当到了 ROOTINODE 中的 unlink 方法<code>pub fn unlink(&amp;self, name: &amp;str) -&gt; isize</code>接下来，其思路和 linkat 完全相反，由于这里不要求我们删除，我们便不管</p><p><strong>fstat</strong>:</p><p> 参考文档，我们只需要获取 ino 和 nlink。其中 ino 我们写一个 osnode 中的<code>get_inode_id</code>,其中，类型转化我们写一个 any 的 trait，将其转换，nlink 我们写一个 get_link_num 的方法，数有几个 DirEntry。再将其数据写入_st 中，我们便完成的 fstat 的写入</p><h3 id="lab5"><a href="#lab5" class="headerlink" title="lab5"></a>lab5</h3><p>根据要求，我们，需要 Available ，为含有 m（资源数量） 个元素的一维数组，我们可以把他放到 process 中，其中他检测每个资源，根据题意要求，我们可以分别给 mutex 和 semaphore 设计一个。同时在线程中(task)，我们需要添加相应的 Allocation 和 Need，其中长度为 mutex 和 semaphore 的数量，我们在修改相应的，创建，lock，unlock 函数的逻辑，按照作业中要求我们使用的逻辑和流程去完成编写。于此同时，我们需要修改 task 的创建，进程的初始化，fork 函数的逻辑，这样，我们就可以通过了。</p><h2 id="第三阶段总结"><a href="#第三阶段总结" class="headerlink" title="第三阶段总结"></a>第三阶段总结</h2><h3 id="Unikernel"><a href="#Unikernel" class="headerlink" title="Unikernel"></a>Unikernel</h3><p>这个阶段，我们需要完成以下内容：</p><ol><li>带颜色的输出</li><li>手写一个 HashMap</li><li>实现 bump 内存分配算法</li><li>在 axfs_ramfs 下实现 rename 功能</li></ol><p>print_with_color 很简单，我们只需要理解一下 axstd 模块，然后对照着对 println 这个宏进行修改</p><p>support_hash_map 比较困难，这里我偷了个懒，使用了 hashbrown**::**HashMap，这个过程中我们需要注意编译时候的条件限制，我们需要这样去进行条件编译</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">#[cfg(feature = &quot;alloc&quot;)]</span><br><span class="line">pub mod collections;</span><br></pre></td></tr></table></figure><p>alt_alloc 很简单，我们按照 ppt 的提示，我们完成其 byte 和 page 的 api 的编写，只需要实<code>earlyAllocator</code>trait 就可以接入内核中了</p><p>ramfs_rename 也不是很难,要把 cargo 里的包换成 arceos 下面的,找到对 trait 去实现 rename 函数，我们可以参照上面的创建和删除的 api，我们只需要修改就是修改两个 DirNode 下的<code>BTreeMap&lt;String, VfsNodeRef&gt;</code></p><h3 id="Monolithic-Kernel"><a href="#Monolithic-Kernel" class="headerlink" title="Monolithic Kernel"></a>Monolithic Kernel</h3><p>接下来我们从 Unikernel 到了宏内核，我们需要完成以下内容：</p><ol><li>实现 sys_mmap</li></ol><p>sys_map 我们可以按照提示，参考 m1_0，我们需要在 current 的<code>task_ext</code>提供的接口去使用，我们可以使用 man 工具具体的查看每个参数的作用，根据其为 NULL 的时候，我们需要找片空地，在翻 aspeace 提供的 api 中，我们可以使用<code>find_free_area</code>，在按照其逻辑，如果有文件，读文件，写入，就是文件映射，没有，就直接返回所寻找的虚拟地址。等实际使用爆出页错误再进行处理。</p><h3 id="Hypervisor"><a href="#Hypervisor" class="headerlink" title="Hypervisor"></a>Hypervisor</h3><p>在 Unikernel 的基础上引入 RISC-V 的 H 扩展，获得了虚拟化的能力。我们需要虚拟化 cpu，内存和进程。我们需要完成以下内容：</p><ol><li>支持 simple_hv</li></ol><p>simple_hv，我们只要在 <code>vmexit_handler</code>中处理异常，然后给 a0，a1 赋值，我们就可以完成我们的任务。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>相比 rcore，arceos 复杂了许多，但是 arceos 中的很多思想思路都出现在 rcore 中，所以我们学习，上手的就很同时我们了解了组件化内核，其可以到 Monolithic Kernel 和 Hypervisor 中，我收获到了很多。同时遇上了很多的困难，在与伙伴聊天中收获到了很多的思路，看到了很多优秀的人。</p>]]></content>
      
      
      
        <tags>
            
            <tag> OS </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
